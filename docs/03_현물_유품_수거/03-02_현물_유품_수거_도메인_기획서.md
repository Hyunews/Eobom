# 📦 03-02. 현물 유품 수거 — 도메인 상위 기획서

> **문서 성격**: 상위 기획서 · **이 도메인의 첫 기획 문서**. `[Claude:Opus]` 작성(2026-08-14).
> **계기**: `00-14` §4-2에 대한 개발자 확정 —
> > *"현물 유품 관련 **Partner로 운영**. 다른 Partner와 마찬가지 **별도의 Domain으로 운영**하는 것이
> > 좋다고 판단됨."*
>
> **이 확정으로 `03-01` §3(계정 형태 보류)이 해소되고, Domain 03의 기획 보류가 풀립니다.**
> **표기**: ✅ 확정 사실 / 🟡 권고 / 🔴 확정 필요(주체를 함께 표기)

> ⚠️ **이 문서는 법률 자문이 아닙니다.** §6의 인허가·책임 관련 서술은 **검토 대상**이며,
> 실제 업체 모집 전에 확인이 필요합니다(§8-2).

---

## 📌 1. 확정된 것 — 그리고 확정되지 **않은** 것

`03-01` §3은 세 선택지를 놓고 보류 중이었습니다. 개발자가 첫 번째를 골랐습니다.

| 선택지 | 결과 |
| :--- | :--- |
| 신규 계정 유형 신설 | ❌ |
| **기존 `Partner` 확장** | ✅ **채택** |
| 외주 제휴사로 처리(계정 없음) | ❌ |

### 1.1 ✅ 이 선택이 옳은 이유 — 우연이 아니라 구조가 같습니다

| | Domain 01 장사시설 | **Domain 03 (이 문서)** |
| :--- | :--- | :--- |
| 사업자가 하는 일 | 유족의 문의를 받아 **오프라인에서 수행** | 동일 |
| 이어봄이 하는 일 | **리드 전달 + 수수료 정산** | 동일 |
| 필요한 계정 요소 | 사업자등록번호·상호·정산 계좌·승인 상태 | **동일** |
| 이어봄의 개입 깊이 | 매칭까지. 서비스 품질은 사업자 책임 | 동일 |

**`Partner` 모델의 필드를 하나도 버리지 않고 그대로 씁니다**(`schema.prisma:98`).
`Expert`(02)를 따로 만든 것과 대조됩니다 — 전문가는 **자격증 검증·직역·변호사법 가드레일**이라는
전혀 다른 요구가 있었지만, 유품 수거 업체는 **장사시설과 요구가 같습니다.**

### 1.2 🔴 확정되지 않은 것 — "계정을 어떻게 만드나"와 "사업을 할 것인가"는 다릅니다

> **이 문서는 계정·데이터·화면 설계까지만 답합니다.** 아래 3가지는 여전히 사장님 결정입니다.

| 미확정 | 왜 이 문서가 못 정하나 |
| :--- | :--- |
| **업체 모집을 실제로 할 것인가 · 언제** | 영업 자원 배분 문제 |
| **수수료 기준·요율** | `00-14` §3-4(01 수수료 기준)와 같은 성격. 01이 정해지면 따라갑니다 |
| **특수청소(고독사 현장) 취급 여부** | §6.3 — 리스크 성격이 완전히 다릅니다 |

---

## 🧭 2. 도메인 정의와 경계

### 2.1 ✅ 하는 일 / 하지 않는 일

| 하는 일 | 하지 않는 일 |
| :--- | :--- |
| 유품 정리·수거 업체 **탐색과 연결** | 이어봄이 **직접 수거·정리** |
| 견적 요청 **리드 전달** | **견적 금액 보증·중재** |
| 업체 등록 심사(사업자등록증·인허가 확인) | **작업 품질·분실 사고에 대한 책임 인수** |
| 유품 중 **디지털 자산이 나오면 04로**, **상속 재산이면 02로** 연결 | 유품의 **가치 평가·감정·매입** |

> **`01-01`과 같은 태도입니다** — 이어봄은 **연결자이지 수행자가 아닙니다.**
> 이 경계를 화면 문구에서 흐리면 `04-01` §1이 지적한 *"목업이 약속한 것을 실제로는 할 수 없는"*
> 문제가 03에서 반복됩니다.

### 2.2 인접 도메인 경계

| 인접 | 겹치는 지점 | 정리 |
| :--- | :--- | :--- |
| **01** 장사시설 | 같은 `Partner` 계정 유형 | ✅ **계정은 공유, 도메인은 분리**(§3.1). 한 사업자가 양쪽을 다 할 수도 있습니다 |
| **04** 디지털 계정 정리 | *"유품"* 이라는 단어 | ✅ **현물 ↔ 디지털**. 04는 계정, 03은 물건. 구 Domain 03에서 갈라진 이유(`00-12` §7) |
| **02** 전문가 | 유품 중 고가 물품 = 상속재산 | 🟡 **분할 협의가 안 끝난 유품은 처분하면 안 됩니다** → §6.2 |
| **07** 상중·행정 | 체크리스트에 *"유품 정리"* 항목 | 🟡 **07은 체크만, 실행은 03**. `07-01` §2.2가 01에 적용한 규칙과 동일 |

### 2.3 ✅ 유가족 전용입니다

`03-01`이 2026-08-12에 확정했습니다 — **생전(WellDying) 축이 없습니다.**
따라서 `PreDeathDirective`(`00-12` §2.4)의 대상이 아니고, `00-12` §7.2의 승계 논의에도 들어가지
않습니다. **이 도메인은 사후에만 존재합니다.**

> 🟡 **예외를 하나 열어둘 여지는 있습니다** — *"내 물건은 이렇게 정리해달라"* 는 의사는
> **엔딩노트(06)에 적히는 메시지**이지 03의 기능이 아닙니다. 06이 유일한 전달 통로라는
> `00-12` §7.2 원칙을 03도 그대로 따릅니다.

---

## 🔑 3. 계정 설계 — `Partner` 확장

### 3.1 🔴 현행 `Partner`는 "장사시설 사업자"를 암묵적으로 전제하고 있습니다

`schema.prisma:98`의 `Partner`에는 **업종을 구분하는 필드가 없습니다.** 지금까지 파트너가
장사시설뿐이었기 때문입니다. 그리고 관계 필드 3개가 **01 전용**입니다.

```prisma
facilities  Facility[]        // 01 전용 — 수거 업체는 시설을 갖지 않음
claims      FacilityClaim[]   // 01 전용 — 클레임할 공공데이터 시설이 없음
leads       Lead[]            // ✅ 공용 가능
settlements Settlement[]      // ✅ 공용 가능
```

**→ 최소 변경으로 업종 축을 추가합니다.**

```prisma
model Partner {
  // ... 기존 필드 전부 유지

  partnerType String @default("FUNERAL_FACILITY")
  // FUNERAL_FACILITY(01 장사시설) | ESTATE_CLEANUP(03 유품 수거)
  // 기본값을 01로 두어 기존 데이터·기존 흐름이 그대로 유효합니다(마이그레이션 무해)

  serviceAreas String[]  // ESTATE_CLEANUP 전용 — 출동 가능 시·군·구 코드
  serviceTags  String[]  // ESTATE_CLEANUP 전용 — 일반정리 | 특수청소 | 폐기물운반 | 기부연계 ...
  licenseNo    String?   // ESTATE_CLEANUP 전용 — 폐기물처리업 허가·신고 번호(§6.1)
}
```

> ✅ **`contextType`(`02-04` §5) · `partnerType` 둘 다 "기본값으로 무해하게 축을 하나 더한다"는
> 같은 패턴입니다.** 이 프로젝트에서 두 번째이므로 **패턴으로 굳혀도 좋습니다** —
> 열거형 컬럼 + 안전한 기본값 = 기존 데이터 무영향.

### 3.2 🟡 업체 목록은 **어디서 오나** — 01과 결정적으로 다른 점

**이것이 03에서 가장 과소평가되기 쉬운 문제입니다.**

| | Domain 01 | **Domain 03** |
| :--- | :--- | :--- |
| 초기 목록 | **공공데이터 1,552건**(e하늘 등) | **0건** |
| 제휴 전 상태 | 목록은 이미 있고, 사업자가 **자기 시설을 클레임** | **아무것도 없음** |
| 사용자가 보는 것 | 전국 시설 + 제휴사 강조 | **제휴 업체만** |

**01은 "먼저 지도를 그려놓고 사업자를 불러들이는" 구조였는데, 03에는 그릴 지도가 없습니다.**
유품정리업은 공공 데이터셋이 없습니다(§6.1 — 법정 업종이 아니기 때문).

> 🔴 **그래서 `FacilityClaim`에 해당하는 절차가 03에는 없고, 대신 "직접 등록 심사"가 그 자리를
> 대신합니다.** 업체가 스스로 가입 신청 → 운영자가 서류를 보고 승인. `Partner.status`
> (`PENDING → APPROVED`)를 그대로 씁니다.
>
> ⚠️ **부작용**: 제휴 업체가 0건인 동안 **화면에 보여줄 것이 없습니다.**
> `00-14` §2-5가 지적한 *"제휴 업체 10곳 목업 + 별점"* 문제가 여기서 나온 것이며,
> **업체를 모으기 전에 화면을 열면 같은 문제가 반복됩니다.** → §7.2

---

## 🔄 4. 리드·정산 — 01 인프라를 그대로 재사용합니다

**새로 만들 것이 거의 없습니다.** `01-05`가 만든 것이 01 전용이 아니라 **파트너 공용**이기 때문입니다.

| 요소 | 재사용 여부 | 비고 |
| :--- | :---: | :--- |
| `Lead` (리드 로그) | ⭕ | ✅ `facilityId` **nullable 전환 완료**(08-18) → §4.1 |
| `leadNo` 발번(`EB-YYMMDD-NNNN`) | ⭕ | 그대로 |
| `CommissionPolicy` (요율 정책) | ⭕ | **`partnerType`별 기본 요율**을 둘 수 있음 |
| `Settlement` (월 정산) | ⭕ | 그대로 |
| 제3자 제공 동의 | ⭕ | **문구만 03용으로**(수거 업체에 연락처가 넘어감) |
| 사업자 가입·심사 화면(SCR-008·009) | 🟡 | **탭 또는 업종 선택 추가** |

### 4.1 ✅ `Lead`가 시설을 **필수로** 전제하고 있었습니다 — **2026-08-18 해소**

> ## 🔵 2026-08-18 — **마이그레이션 완료. 0단계가 닫혔습니다.**
> `[Claude:Sonnet]` 구현(walkthrough **(25)** ✅통과 / 마이그레이션 `20260818004442_lead_facility_nullable_partner_type`).
>
> | 항목 | 결과 |
> | :--- | :--- |
> | `Lead.facilityId` | ✅ `String` → **`String?`** |
> | `Lead.facility` 관계 | ✅ `Facility` → **`Facility?`** (FK는 **`ON DELETE SET NULL`** 로 자동 조정) |
> | `Partner.partnerType` | ✅ **신설** — `String @default("FUNERAL_FACILITY")` |
> | 기존 데이터 영향 | ✅ **없음(실측)** — `Partner` 2건 전부 기본값으로 채워짐, `Lead` 5건 전부 `facilityId` 유지. **제약만 완화됐고 어떤 값도 null이 되지 않았습니다** |
> | 검증 | 백엔드·프론트 `tsc --noEmit` 0건 · 서버 기동 · `psql` 직접 조회. 적용 전 `pg_dump` 백업(`security.md` §6) |
>
> **🟡 아래 권고의 "앱 레벨 강제"는 절반만 섰습니다** — 아래 원문의 진단은 그대로 유효하므로 남겨둡니다.
>
> | 방향 | 상태 |
> | :--- | :--- |
> | `FUNERAL_FACILITY` 리드 → `facilityId` **필수** | ✅ **코드 추가 없이 이미 서 있음.** `leadService.ts`의 `createLead`가 리드 생성의 **유일한 경로**이고(`.lead.create` 호출부 전수 확인), `CreateLeadInput.facilityId: string`(필수 타입) + `facility.findUnique` 실패 시 `FacilityNotFoundError`가 이미 강제 |
> | `ESTATE_CLEANUP` 리드 → `facilityId` **null 허용** | 🔴 **미착수 — 강제할 코드가 아직 없습니다.** 03 리드 생성 경로 자체가 없기 때문입니다. **§5 `CleanupRequest` 구현 시 분기 규칙을 반드시 신설할 것** |
>
> > ⚠️ **이것이 `00-15` §3이 회귀 테스트의 근거로 든 바로 그 형태입니다** — **DB 제약을 앱 로직으로
> > 옮긴 변경**이라, 강제 코드가 빠지면 **조용히** 깨집니다. `CleanupRequest` 착수 시 이 칸을 먼저 볼 것.
>
> 🟡 **§3.1의 03 전용 필드 3개(`serviceAreas`·`serviceTags`·`licenseNo`)는 이번에 넣지 않았습니다** —
> 읽거나 쓰는 코드가 없는 상태로 넣으면 `00-14` §2.2류(필드는 있는데 안 채워지는) 문제를 새로
> 만들 뿐이라는 판단이며, **범위 해석이지 스펙 이탈이 아닙니다.** → §7.1 단계 1에 그대로 남습니다.

**실제 스키마를 확인했습니다**(당시 `schema.prisma:162` — 아래는 **변경 전** 원문입니다).

```prisma
facilityId String                 // ❌ 필수. null 불가
facility   Facility @relation(fields: [facilityId], references: [id])
```

**03의 리드에는 시설이 없습니다.** 유족이 업체에 직접 요청하며, 붙일 `Facility` 레코드가
존재하지 않습니다(§3.2 — 03에는 공공데이터 목록 자체가 없음).

> 🔴 **따라서 `Lead.facilityId`를 `String?`으로 전환하는 마이그레이션이 03 착수의 0단계입니다.**
> 관계도 `Facility?`로 함께 완화해야 합니다.
>
> ⚠️ **이건 01의 무결성을 낮추는 변경이므로 그냥 풀면 안 됩니다.** 🟡 **권고**:
> 컬럼은 nullable로 열되, **애플리케이션 레벨에서 `partnerType`별로 강제**합니다 —
> `FUNERAL_FACILITY` 리드는 `facilityId` 필수, `ESTATE_CLEANUP` 리드는 null 허용.
> `04-01` §3.1의 `lastVerifiedAt`을 API 레벨에서 강제한 것과 같은 방식입니다.

**`Lead.type`도 함께 봐야 합니다** — 현재 `QUOTE | CONSULT | CALL`(`schema.prisma:160`)이고
03의 수거 견적은 `QUOTE`에 얹으면 되지만, **집계할 때 01·03이 섞입니다.**
🟡 `partnerType`으로 갈라 집계하는 것으로 충분하며, **새 `type` 값을 만들지 않는 편**을 권고합니다.

### 4.2 🟡 수수료 기준은 01을 따릅니다

`00-14` §3-4(리드 건당 / **성사 건당(권고)** / 월 정액)가 정해지면 **같은 기준을 03에 적용**하고,
요율만 다르게 둡니다. `CommissionPolicy` 레코드 입력으로 끝나므로 코드 수정이 필요 없습니다
(`01-05` §12 정책 계층).

> **03이 01보다 성사 건당에 더 잘 맞습니다** — 유품 정리는 단가가 크고 건수가 적어,
> 리드 건당 과금은 업체 저항이 큽니다.

---

## 🗄️ 5. 데이터 모델 — 신규는 최소한으로

`Partner` 확장(§3.1) 외에 **신규 모델은 1개**입니다.

```prisma
// 유품 수거 요청 — 유족이 남기는 견적 요청. Lead로 이어짐
model CleanupRequest {
  id         String   @id @default(uuid())
  userId     String              // FK → User (03-01 §2 원칙)
  deceasedId String?             // FK → Deceased (00-12 §2.1) — 확정 후 부착
  partnerId  String?             // 지정 요청이면 값, 지역 일괄 요청이면 null

  areaCode     String            // 작업 지역 시·군·구
  propertyType String            // APARTMENT | HOUSE | VILLA | OFFICE ...
  sizePyeong   Int?              // 평수 — 견적의 핵심 변수
  serviceTags  String[]          // 요청 항목(§3.1 serviceTags와 같은 어휘)
  preferredAt  DateTime?         // 희망 일정
  memo         String?

  status    String   @default("REQUESTED")  // REQUESTED | MATCHED | DONE | CANCELED
  createdAt DateTime @default(now())

  @@index([userId])
  @@index([areaCode, status])
}
```

> **`04-01` `DigitalCleanupItem` · `07-01` `AdminTaskProgress`와 같은 형태**입니다 —
> **유족 개인의 요청 1건 = 레코드 1건**. 플랫폼 전체가 같은 모양을 반복하고 있고, 그게 맞습니다.

> 🟡 **사진 첨부는 1차에서 넣지 않습니다.** 견적 정확도는 올라가지만 **고인의 주거 내부 사진**은
> 민감도가 높고, `00-11` 오브젝트 스토리지 전환 전에는 저장할 곳도 마땅치 않습니다
> (`05-01` §2.6 추모 사진과 같은 제약).

---

## ⚠️ 6. 이 도메인 고유의 위험 — 01·02와 다릅니다

### 6.1 🔴 "유품정리업"은 법정 업종이 아닙니다

확인 결과, **유품정리업·특수청소업이라는 이름의 허가·등록 제도는 없습니다.**
업체들이 실제로 하는 일을 쪼개 보면 각각 다른 법에 걸립니다.

| 실제 행위 | 관련 규제 | 이어봄이 확인할 것 |
| :--- | :--- | :--- |
| 폐기물 수집·운반을 **업으로** 수행 | **폐기물관리법 §25** 폐기물처리업 허가 / 신고 | 🔴 **허가·신고 번호**(`licenseNo`) |
| 재활용품·중고물품 매입·판매 | 지자체 신고 등 | 🟡 해당 시 확인 |
| 청소·정리 용역만 수행(배출은 유족 명의) | 별도 규제 없음 | 사업자등록증 |

> 🔴 **그래서 등록 심사 기준을 이어봄이 직접 정해야 합니다.** 01은 공공데이터에 등재된 시설이라
> 존재 자체가 검증이었지만, **03은 "우리가 승인한 것"이 유일한 검증**입니다.
> **무허가 업체를 매칭해 불법 투기가 발생하면 플랫폼이 지목됩니다.**
>
> 🟡 **권고**: 사업자등록증 + **폐기물 관련 허가·신고증(해당 시)** 을 필수 제출로 두고,
> 해당 없음을 주장하는 업체는 **"폐기물은 유족 명의로 배출"** 임을 서면 확인받습니다.
> ⚠️ 지자체별 운용이 다를 수 있어 **모집 전 행정사·지자체 확인**이 필요합니다(§8-2).

#### 🔵 2026-08-19 개발자 확정 — **업체 심사를 하지 않습니다. 이 절은 보류합니다.**

**시장 실측 결과**: *"폐기물 처리 서비스 · 재활용센터 · 청소 전문업체"* 라는 **기존 업종 이름으로
사업이 이미 활성화**돼 있음을 확인했습니다. **즉 "유품정리업"이라는 업종이 없을 뿐, 그 일을 하는
업체는 각자 다른 간판으로 실재합니다** — 위 표의 3분류가 시장에서 그대로 관측된 셈입니다.

> 🔵 **확정: 이어봄은 업체를 심사하지 않습니다.** 따라서 **위 🟡 권고(허가·신고증 필수 제출,
> 서면 확인)는 시행하지 않으며, §8-2(행정사·지자체 확인)도 지금은 진행하지 않습니다.**
> ✅ **지금 시점에서는 실질 위험이 0입니다** — 제휴 업체가 **1건도 없고**, 화면은 전부
> 더미 데이터이기 때문입니다(§7.2).

🔴 **다만 두 가지가 이 결정에 딸려옵니다 — 지금 지킬 것 1개, 나중에 다시 열 것 1개.**

| | 내용 |
| :--- | :--- |
| **지금 지킬 것** | 🔴 **심사하지 않으면 "추천·검증·엄선"이라고 쓸 수 없습니다.** 화면 문구는 **"연결"·"안내"** 까지만 허용됩니다 — *"이어봄이 검증한 업체"* 류의 표현은 **심사를 하지 않는 한 그 자체로 허위**입니다(`00-14` §2.2 계열). 더미 단계에서 쓴 문구가 그대로 실서비스로 넘어가는 일이 이 프로젝트에서 이미 여러 번 있었으므로, **목업 문구부터 이 선을 지킵니다.** |
| **다시 열 것** | 🔴 **실제 업체 모집을 시작하는 시점**(§7.1 4단계)에 이 절이 **자동으로 다시 열립니다.** 심사를 안 하더라도 **폐기물관리법 §25 무허가 업체를 매칭했을 때 플랫폼이 지목되는 구조 자체는 사라지지 않습니다.** 그때의 선택지는 ⓐ심사 도입 ⓑ *"업체 정보는 업체가 제공한 것"* 임을 고지하고 책임 분리 — 둘 중 하나입니다. |

> ⚠️ **`01-05`(장사시설 사업자 심사)와 03이 이제 다른 규칙을 씁니다.** 01은 공공데이터 + 클레임
> 심사, 03은 **무심사**입니다. **같은 파트너 테이블(`Partner`)을 쓰므로**(§3.1) 심사 로직을
> `partnerType`으로 분기해야 하며, **01의 심사 화면을 03에 그대로 재사용하면 안 됩니다.**

### 6.2 🔴 유품은 상속재산입니다 — 이게 이 도메인의 가장 큰 함정

**분할 협의가 끝나기 전에 유품을 처분하면 상속인 간 분쟁이 됩니다.**
게다가 상속재산 처분은 **단순승인으로 간주될 수 있습니다**(민법 §1026① — `07-02` §2가 다루는
상속포기·한정승인의 전제를 깨뜨립니다).

> ⚠️ **즉 03의 서비스가 유족의 상속포기 권리를 날려버릴 수 있습니다.**
> `07-02`가 *"3개월을 놓치면 빚을 상속한다"* 를 경고해놓고, 03이 그 3개월 안에
> *"유품 정리해드립니다"* 를 팔면 **플랫폼이 스스로 모순**입니다.

🟡 **권고 — 요청 화면에 게이트 1개**:

> *"상속포기·한정승인을 고려 중이시라면, 유품 처분 전에 확인이 필요합니다."*
> → `07-02` 체크리스트 해당 항목 + Domain 02 상담 연결

**막지는 않되 반드시 보여줍니다.** `07-01` §4.3(기준일은 법률 판단이라 확정 불가)과 같은 태도이며,
**이 연결이 `07-01` §6이 말한 "07이 02의 유입구"와 같은 구조를 03에도 만듭니다.**

#### 🔵 2026-08-19 개발자 확정 — **유언·상속 도메인과 함께 설계합니다. 지금은 넘깁니다.**

> 🔵 **확정**: 상속 게이트는 **03 단독으로 문구만 얹을 일이 아니라, 추후 개발할 유언·상속 기능과
> 연동될 것**이므로 **그 설계 시점으로 넘깁니다.**

✅ **이 판단이 맞습니다 — 그리고 이 문서의 권고보다 낫습니다.** 위 🟡 권고는 *"게이트 문구 1개"*
였는데, 그건 **유족이 상속 상황을 스스로 판단해 읽어내야** 성립합니다. 유언·상속 기능이 붙으면
**플랫폼이 이미 알고 있는 상태**(상속포기 검토 중인지, 분할 협의가 끝났는지)로 게이트를 걸 수
있으므로, **문구가 아니라 조건으로** 동작합니다. **먼저 만들면 두 번 만들게 됩니다.**

> 🔴 **다만 §7.1 4단계(유족 화면 오픈)보다 이 연동이 늦으면, 그 사이 기간은 게이트가 **비어 있는
> 상태**가 됩니다.** 민법 §1026①(처분 = 단순승인 간주) 위험은 유언·상속 기능과 무관하게
> **유족 화면이 열리는 순간부터** 존재합니다.
> 🟡 **그때의 최소 조치**: 위 🟡 권고의 **문구 1줄만 임시로** 얹으십시오. 조건부 게이트가
> 완성될 때까지의 **공백 메우기**이지 최종 설계가 아닙니다.

> 🔵 **참고 — 사장님 논의 결과(2026-08-19), 유언·상속 기능의 방향**:
> *"이용자에게 관련 가족 등의 **이름과 연락처**를 받고, 이를 통해 진행하는 것으로 기획 예정."*
> **→ 이 방향은 03보다 `00-12` §7.3(유족 "발견" 문제)에 훨씬 크게 작용합니다** — 그쪽에 등재했습니다.
> ⚠️ **03 관점에서 중요한 것 하나**: 가족 연락처가 확보되면 **§6.2의 "상속인이 여럿일 때 분쟁"
> 문제에 처음으로 손댈 수 있게 됩니다**(지금은 요청자 1명만 보이므로 다른 상속인의 존재 자체를
> 플랫폼이 모릅니다). **연동 설계 시 이 절을 함께 여십시오.**

### 6.3 🔴 특수청소(고독사·유품 훼손 현장)는 별개 판단입니다

| | 일반 유품 정리 | **특수청소** |
| :--- | :--- | :--- |
| 작업 성격 | 이사와 유사 | **감염·악취·현장 훼손** |
| 단가 | 수십~수백만원 | 훨씬 높고 **분쟁 빈발** |
| 이용자 상태 | 정리 모드 | **충격 직후** |
| 플랫폼 위험 | 보통 | **높음** — 과다청구·2차 피해 보도 표적 |

🔴 **취급 여부는 사장님 판단입니다**(§1.2). 다만 **취급한다면 `serviceTags`로 분리하고 별도
심사 기준**을 두는 것을 권고합니다 — 같은 목록에 섞으면 안 됩니다.

### 6.4 🟡 분실·파손 클레임

유품에는 **금전 가치보다 감정 가치가 큰 물건**이 섞입니다. *"어머니 반지가 없어졌다"* 는
민형사 문제로 갑니다. **이어봄은 당사자가 아니지만 "여기서 소개받았다"의 대상**은 됩니다.

🟡 **권고**: 등록 심사에 **배상책임보험 가입 여부**를 항목으로 넣고, **가입 업체를 화면에
표시**합니다. 강제하지 않되 드러내면 시장이 정리됩니다.

---

## 🎯 7. 착수 순서와 비목표

### 7.1 단계

| 단계 | 내용 | 선행 조건 |
| :---: | :--- | :--- |
| **0** | ✅ **완료(08-18)** — `Lead.facilityId` nullable 전환 + `Partner.partnerType` 신설(§4.1) | — |
| **1** | 03 전용 필드 마이그레이션 `serviceAreas`·`serviceTags`·`licenseNo`(§3.1) — **`partnerType`은 0에서 이미 나감** | 0 ✅ |
| **2** | 사업자 가입·심사 화면에 업종 축 추가(SCR-008·009) | 1 |
| **3** | `CleanupRequest` + 리드 연결(§5) | 1 |
| **4** | 유족 화면(업체 목록·요청 폼) | **제휴 업체 ≥ 1건**(§7.2) — 🔄 **목업은 선행 조건 없이 이미 노출 중**(`/pickup`, 더미 데이터 + 고지 배너) |
| **5** | `deceasedId` 부착 | `00-12` §2.1 `Deceased` 확정 |

### 7.2 🔄 비목표 — **거짓말은 하지 않되, 목업은 만듭니다** (2026-08-19 개정)

`00-14` §2-5가 잡아낸 문제(존재하지 않는 제휴 업체 10곳 + 창작 별점)의 **재발 방지 규칙**입니다.

> ⛔ ~~**제휴 업체가 1건이라도 실제로 등록되기 전에는 `/digital-estate`의 현물 유품 탭을
> "준비 중"으로 둡니다.** 목록을 채워 보여주는 것은 그 자체로 거짓말입니다.~~
> **→ 2026-08-19 개정. 아래가 유효합니다.**

#### 🔵 개정 사유 — 개발자 확정(2026-08-19)

> 🔵 *"**더미 회사로 우선 진행할 예정.** 아직 서비스 단계가 아니기에 **목업을 꼼꼼히 만드는 데 집중.**"*

**구 규칙의 진짜 표적은 "목록의 존재"가 아니라 "진짜인 척"이었습니다.** `00-14` §2-5가 문제 삼은
것은 존재하지 않는 업체 10곳을 **실재하는 제휴사처럼** 띄우고 **별점까지 창작**한 것이었지,
화면을 그린 행위가 아니었습니다. **해법은 이미 `00-14` §2.2가 확정해 뒀습니다** —
*"지우면 회의에서 보여줄 화면이 없어지고, 두면 거짓말입니다. **목업임을 화면에 명시**하면
둘 다 해결됩니다."* **03에도 같은 기준을 적용합니다.**

#### ✅ 개정 규칙 — 3가지를 지키면 노출합니다

| # | 조건 | 현재 이행 |
| :---: | :--- | :--- |
| **1** | 🔴 **화면 상단에 예시 데이터 고지를 고정 노출** — *"이 페이지는 화면 구성을 보여드리기 위한 **예시 데이터**로 채워져 있습니다. 아래 업체·평점은 실존하지 않으며, 실제 제휴 업체는 아직 없습니다."* | ✅ `PickupPage.tsx` 상단 배너 |
| **2** | 🔴 **상태 배지는 `미리보기`** — `진입`으로 올리지 않습니다(`00-23` §5.1 3단계 표기) | ✅ `modeNav.ts` `status: 'preview'` |
| **3** | 🔴 **더미에 실존 상호·실존 연락처·실존 주소를 넣지 않습니다** | ✅ **08-19 실측 확인**(아래) |

> 🔴 **3번이 이번 개정에서 가장 위험한 자리입니다 — 다만 위험의 성격이 갈립니다.**
>
> | 항목 | 배너가 막아주나 | 이유 |
> | :--- | :---: | :--- |
> | **창작 별점·후기** | ✅ **막아줍니다** | 피해가 **이용자**에게 가는데, 배너가 *"평점은 실존하지 않는다"* 를 이미 고지합니다. **목업의 목적(화면이 어떻게 보일지)에는 별점이 필요**하므로 허용합니다 |
> | **실존 상호·연락처·주소** | ❌ **못 막습니다** | 피해가 **제3자(그 업체)** 에게 갑니다. **정보주체가 배너를 볼 일이 없으므로 고지가 성립하지 않습니다** — 실존 상호를 예시로 쓰면 그 업체에 대한 **허위 표시**이고, 실존 번호를 쓰면 **그 번호로 실제 전화가 갑니다** |
>
> ⚠️ **`00-14` §2-5의 원래 지적(*"업체 10곳 + 창작 별점"*)과 결론이 달라 보이지만 같은 기준입니다.**
> 그때 문제였던 것은 **별점이 창작이라는 사실이 화면 어디에도 없었다**는 점입니다. 배너가 그 결함을
> 메웁니다. **배너를 떼는 순간 §2-5 상태로 정확히 되돌아갑니다.**

✅ **2026-08-19 실측 — 현재 `/pickup` 더미 10건은 3번 조건을 지키고 있습니다.**
`digitalEstate.json`의 `vendors` 10건은 **지역명 + 일반 명사 조합**(*"평안 유품 정리 & 특수청소"*,
*"호남 안심 유품 수거 케어"* 등)이고, **`name`·`region`·`rating`·`tags` 4개 필드뿐 — 전화번호·주소
필드 자체가 없습니다.** `rating`은 4.7~5.0 창작값이지만 위 표에 따라 **배너로 고지되므로 허용**입니다.

> 🔴 **다음에 더미를 늘리거나 상세 화면을 만들 때 주의할 것**: **전화번호 필드를 추가하는 순간**
> 3번이 실제로 걸립니다. 넣어야 한다면 **`000-0000-0000` 형태의 명백한 자리표시자**를 쓰고,
> **`tel:` 링크로 걸지 마십시오**(01의 직통전화 패턴을 그대로 복사하면 사고가 납니다).

#### 🔴 실서비스 전환 시 반드시 되돌릴 것

| 시점 | 할 일 |
| :--- | :--- |
| **제휴 업체 1건 등록** | 더미 데이터 **전량 삭제**(부분 혼합 금지 — 진짜와 가짜가 한 목록에 섞이는 것이 가장 나쁩니다) · 배너 제거 · 배지 `미리보기`→`진입` |
| **그 전에 절대 금지** | 🔴 **배너만 떼는 것.** 배너는 §7.2가 노출을 허용하는 **유일한 근거**입니다(`00-26` §7.3) |

**`00-14` §2.3(`/digital-estate` 한 페이지가 03·04·05 세 도메인을 뭉치고 있음)의 분할**은
✅ **2026-08-19 완료**됐습니다 — `/pickup`(`SCR-014`)·`/memorial`(`SCR-015`)로 분리
(`00-06` §1, `00-26` §7.1). 이 절이 예상한 *"어차피 탭을 건드려야 한다"* 가 그대로 실현됐습니다.

> ✅ **`00-26` §6 #8(이 절 ↔ `/pickup` 노출 충돌)은 이 개정으로 닫힙니다.**

---

## ❓ 8. 미결 쟁점

| # | 쟁점 | 권고 | 주체 |
| :---: | :--- | :--- | :--- |
| 1 | **업체 모집 개시 여부·시점**(§1.2) | — | 🔴 사장님 |
| ~~2~~ | ~~**폐기물 인허가 확인 기준**(§6.1)~~ | 🔵 **2026-08-19 보류** — **업체 심사를 하지 않기로 확정**. 시장에 *"폐기물 처리·재활용센터·청소 전문업체"* 이름으로 실재함을 확인. 행정사·지자체 확인도 지금은 안 함. 🔴 **§7.1 4단계(실제 모집)에서 자동 재개** | ~~외부 확인~~ → **보류** |
| 3 | **특수청소 취급 여부**(§6.3) | 🟡 취급 시 별도 심사 기준 | 🔴 사장님 |
| 4 | **수수료 기준·요율**(§4.2) | 01(`00-14` §3-4) 확정을 따름 | 🔴 사장님 |
| 5 | **배상책임보험 표시**(§6.4) | 🟡 표시(강제 아님) | 개발자 |
| ~~6~~ | ~~**`Lead.facilityId` nullable**(§4.1)~~ | ✅ **2026-08-18 해소** — 마이그레이션 완료. 다만 **`ESTATE_CLEANUP` 앱 레벨 강제는 §5 구현 시 신설**해야 함 | ~~`[Claude:Sonnet]`~~ → **닫힘** |
| ~~7~~ | ~~**상속 게이트 문구**(§6.2)~~ | 🔵 **2026-08-19 이관** — **유언·상속 도메인 설계와 함께** 조건부 게이트로. 문구 1줄로 먼저 만들면 두 번 만들게 됨 | ~~개발자~~ → **유언·상속 사이클** |
| 8 | `deceasedId` 도입(§5) | `00-12` §2.1 확정 후 | — |
| **9** | 🟡 **더미 상호·연락처 선정**(§7.2) | 🔴 **실존 상호·번호·주소 금지**(배너가 제3자에겐 고지가 안 됨) · 창작 별점은 허용 | 🔵 **08-19 신규** — ✅ **현재 10건은 조건 충족**. `[Sonnet]`이 **전화번호 필드를 추가할 때** 재확인 |
| **10** | 🔴 **화면 문구에서 "검증·추천·엄선" 금지**(§6.1) | 🔴 **"연결"·"안내"까지만** — 심사를 안 하므로 | 🔴 **08-19 신규 — 이미 위반 1건 발견**(아래) · `[Sonnet]` |

> **#1·#3·#4만 사장님 결정이고, 나머지는 착수를 막지 않습니다.**
> `01-05`가 수수료율 미확정 상태로 0~3단계를 진행한 것과 같은 구조입니다.
>
> 🔵 **2026-08-19 — #2·#7이 닫히고 #9·#10이 열렸습니다.** 무게가 **"모집 전 확인"에서
> "목업 작성 규율"로** 옮겨갔습니다. **지금 단계에서 실제로 사고가 날 수 있는 자리는 #9·#10뿐**입니다.

#### 🔴 #10 실측 — **현재 화면에 위반 1건이 있습니다**

| 위치 | 현재 문구 | 문제 |
| :--- | :--- | :--- |
| `PickupPage.tsx:38` | *"지역 기반 현물 유품 정리 전문 업체 **추천**"* | 🔴 **심사를 하지 않는데 "추천"** — §6.1 확정과 어긋납니다 |

> 🟡 **`[Sonnet]` 조치**: *"추천"* → **"연결"** 또는 **"안내"**.
> (같은 페이지의 배지 *"지역 기반 현물 유품 정리 **매칭**"* 과 소개문 *"…업체와 **연결**해 드립니다"*
> 는 문제없습니다 — **매칭·연결은 우리가 실제로 하는 일**이고, 추천은 아닙니다.)
>
> ⚠️ **02와 혼동하지 마십시오** — `domainSlides.tsx:78`의 *"분야별 **검증된** 변호사·세무사"* 는
> **02(전문가 매칭)이고 02에는 실제 승인 심사가 있습니다**(`SCR-010` 운영자 승인 대시보드).
> **03만 무심사**이므로, 이 금지는 **03 화면에만** 적용됩니다.

---

## 🔗 9. 관련 문서

| 문서 | 관계 |
| :--- | :--- |
| `03-01` | **이 문서의 전신.** §3(계정 형태 보류)이 이 문서로 해소됨. §2 `userId` FK 원칙은 §5에 반영 |
| `01-05` | **재사용 원본** — `Partner`·`Lead`·`CommissionPolicy`·`Settlement`. §4가 전부 여기서 옴 |
| `04-01` | 형제 도메인(디지털 ↔ 현물). "안내·연결이지 대행이 아니다"라는 태도를 승계 |
| `07-02` | §2 상속포기·한정승인 — **§6.2 게이트가 연결되는 곳** |
| `00-12` | `Deceased`(§5), 도메인 재편(§7), 유가족 전용 확정 |
| `00-14` | §4-2가 이 문서의 계기. §2-5(목업 노출)의 재발 방지는 §7.2 |
| `00-06` | SCR-003(`/digital-estate`) 현물 탭 · SCR-008·009 파트너 화면 — §7.1 반영 대상 |
