# 📱 00-38. 적응형 모바일 UX 개편 — 명세서

> **문서 성격**: 명세서 · **값과 규칙**을 정한다. 경위·원인 분석은 `00-29`(검토서)가 정본이고,
> 이 문서는 그 뒤에 오는 **실행 스펙**이다. `[Claude:Opus]` 작성(2026-09-08).
> **계기**: 사용자 지적 — *"모바일에서 좌우 여백이 부족해 서비스 전반이 답답하다. 데스크톱 페이지를
> 그대로 반응형 축소만 하고 있어 가독성이 떨어진다."*
> **선행 검증**: Gemini 시뮬레이터 2건(`reports/mobile_360px_preview.html` ·
> `reports/text_heavy_whitespace_solutions.html`) + Sonnet 도메인 목업 2건(엔딩노트·상중행정가이드)으로
> **사용자 확인 완료**. 5대 원칙(§3)은 **재검토 대상이 아니다.**
> **표기**: ✅ 확정 / 🟡 권고 / 🔴 확정 필요·주의 / 🔵 개발자 확정 / ⏸ 보류

---

## 📌 1. 결론 먼저

| 질문 | 답 |
| :--- | :--- |
| **무엇이 문제였나** | 🔴 **`00-29` §12 조치 A(모바일 루트 폰트 18px)가 여백까지 12.5% 키웠다.** `rem`으로 쓴 `padding`/`margin`/`gap`이 **869곳**이라, 글자를 키운 대가로 **360px에서 좌우 여백을 잃었다.** "답답하다"의 정체가 이것이다 |
| **무엇을 바꾸나** | ✅ **루트를 16px로 되돌리고, 여백을 `px` 절대값으로 못박는다**(§4). 타입은 토큰으로만 쓰고 하한을 올린다 |
| **왜 반응형이 아니라 적응형인가** | ✅ 데스크톱 레이아웃을 좁혀서는 §3의 원칙 3·4·5를 만들 수 없다. **같은 데이터·다른 화면**이 필요하다(§6) |
| **URL은 나뉘나** | 🔴 **아니다.** `/farewell`·`/ending-note` 등 경로는 **그대로**다. 라우팅·SEO·공유링크(`00-13`)에 영향 0 |
| **가장 큰 위험은** | 🔴 **소형 `rem` 리터럴 441곳**(§4.5). 루트만 16px로 되돌리면 이것들이 11% 더 작아져 **가독성이 즉시 후퇴한다.** 같은 커밋에서 치환해야 한다 |
| **얼마나 드나** | Phase 1(기반) 1회 + 페이지 13개. **한 번에 하지 않는다**(§10) |

---

## 🧭 2. 정본 경계 — 이 문서가 덮어쓰는 것

**같은 값을 두 곳에 적지 않는다.** 이 문서가 새로 정하는 것과, 기존 문서에 남기는 것을 먼저 가른다.

| 대상 | 정본 |
| :--- | :--- |
| **모바일 여백·타입 스케일·행간 값** | 🆕 **이 문서 §4** — `00-09` §2.3·§2.4·§6.2의 해당 값을 **개정**한다 |
| **`useIsMobile` 훅 · 적응형 뷰 분리 패턴** | 🆕 **이 문서 §5·§6** |
| **도메인별 모바일 화면 구성** | 🆕 **이 문서 §8** — 단, 도메인 고유 규칙(무엇을 보여줄지)은 해당 도메인 문서가 계속 정본 |
| 색·브랜드·컴포넌트 토큰 | `00-09` §2.1·§6.2-1·§6.2-2·§6.3 — **손대지 않는다** |
| 원인 분석·실측 경위 | `00-29` §10~§15 — **덮어쓰지 않는다.** §12 조치 A만 철회(§7) |
| 화면 ID·와이어프레임 | `00-06` — **화면 ID 신설 없음**(적응형은 같은 화면의 두 표현) |
| 네비게이션·모드 | `00-26` · 홈 진입구조 `00-23` |

> 🔴 **`00-29`는 폐기하지 않는다.** §10~§13의 실측(375px `scrollWidth`·`19,698px` 문서 높이·`rem` 전수)은
> 이 문서의 근거이고, 다시 재려면 같은 비용이 든다.

---

## 📐 3. 5대 원칙 — ✅ 확정 (재검토 없음)

사용자 확인이 끝난 항목이다. **각 원칙에 측정 가능한 값을 붙여** DoD(§11)로 이어붙인다.

### 3.1 원칙 1 — 360px 최저선 · **헤더와 본문의 여백을 분리한다**

| | 값 | 근거 |
| :--- | :---: | :--- |
| 최저 기준폭 | **360px** | `00-29` §8 #3이 정한 375px에서 **하향**. Galaxy S 계열·안드로이드 보급기의 실폭 |
| 본문 좌우(`.container`) | **28px** | 360 − 56 = **본문 폭 304px** ≈ 한글 16px에서 **줄당 19자** — 국문 가독 적정대(18~25자) 안 |
| 헤더·크롬 좌우 | **16px** | 헤더는 읽는 면이 아니라 **잡는 면**이다. 28px를 주면 로고+모드+프로필이 328px에 안 들어간다 |
| ≤359px 안전판 | 본문 **20px** | 320px(구형 SE)은 지원 대상이 아니나(`00-29` §8 #3) **깨지지는 않게** 둔다 |

> 🔴 **"헤더 ≠ 본문"이 이 원칙의 핵심이다.** 지금은 `.container` 하나가 둘 다 맡고 있어,
> 본문을 넓히면 헤더가 깨지고 헤더를 맞추면 본문이 좁아진다. **토큰을 둘로 나눈다**(§4.1).

### 3.2 원칙 2 — 🔴 **모바일 18px 기준선 폐기 → 본문 16px 표준**

`00-09` §2.3이 *"협상 대상이 아님"* 으로 못박은 `--base-font-size: 18px`를 **폐기한다.**

| | |
| :--- | :--- |
| **폐기하는 것** | `@media (max-width:768px) { html { font-size: 18px } }` — `00-29` §12 **조치 A** (`index.css:274~278`) |
| **폐기 이유** | 🔴 **값이 아니라 전파 방식이 틀렸다.** 루트를 올리면 `fontSize` 377곳뿐 아니라 **`padding`/`margin`/`gap` 869곳이 같이 12.5% 커진다**(`00-29` §12.2 실측). 360px에서 이 12.5%가 곧 좌우 여백이다 — **글자를 키우려다 여백을 잃었다** |
| **대신 하는 것** | 루트는 **16px 고정**. 본문은 `--fs-body: 1rem`(=**16px**)로 **절대값을 못박는다**. 여백은 `px` 토큰(§4.1) |
| **행간** | 본문 **1.8** · 몰입 리더 **1.9** (`00-09` §2.3의 `1.7`을 개정) |

> 🔴 **시니어 접근성이 후퇴하는가 — 정직하게 적는다.**
> `00-29` §11이 실측한 대로, **18px 기준선은 화면에 한 번도 적용된 적이 없다.** 조치 A 이전 실제 본문은
> **12~13.6px**였고, 조치 A 이후(2026-08-25~) 약 17.1px다. **16px는 프로젝트 전 기간 대비 개선이고,
> 최근 2주 대비로는 1.1px 후퇴다.** 그 1.1px과 맞바꾸는 것이 **좌우 28px 여백 · 행간 1.8 · 1열 몰입 리더**다.
> 🔵 **글자를 더 키우고 싶은 사용자의 경로는 막히지 않는다** — 타입이 전부 `rem`이라 브라우저 확대와
> OS 글꼴 크기 설정이 정상 동작한다. 루트를 고정한다는 것은 **우리가 임의로 올리지 않는다**는 뜻이지
> 사용자가 못 올린다는 뜻이 아니다.
> 🔴 **`--min-touch-target: 56px`는 유지한다** — §2.3의 3개 토큰 중 이것만 남는다.

### 3.3 원칙 3 — 탈박스화 · 위계는 **간격으로만**

| 🔴 금지 | ✅ 대신 |
| :--- | :--- |
| 회색 테두리 박스 안의 박스(**박스-in-박스**) | 섹션 사이 **≥40px** 마진 + 필요하면 **1px 구분선** |
| 그림자 표면의 중첩 | 그림자는 **한 화면에 1단계만**(`00-09` §6.3 #3 재확인) |
| 카테고리 박스 > 항목 박스 3중 중첩 | 카테고리는 **텍스트 헤더 또는 칩**, 항목은 **행(row)** |

### 3.4 원칙 4 — 상단 크롬 감량

- 🔴 **배지·설명·경고배너가 4~5단으로 쌓이지 않는다.** 첫 화면에서 **콘텐츠 첫 줄까지 ≤1스크롤**.
- ✅ **법적 고지·면책은 최하단 또는 모달**로 내린다. *"차단 배너"* 형태를 쓰지 않는다.
- 🟡 예외: **되돌릴 수 없음 경고**(`07-04` §4.3 상단 고정 배너)는 **1개까지** 상단에 남는다.

### 3.5 원칙 5 — 2단계 노출 (목록 → 몰입 리더)

```
[목록]  날짜 + 한 줄 제목 …… 그게 전부다
   └ 탭 → [전체화면 몰입형 리더]  본문·행간 1.9·좌우 28px·닫기만
```

- ✅ 적용 대상: **읽을 본문이 있는 것**(엔딩노트 섹션·유족 메시지·법적 고지).
- 🔴 **적용하지 않는 것**: `CareGuidePage` 체크리스트 — `07-04` §8-8이 **펼칠 상세를 이미 없앴다.**
  카드 2줄이 곧 전체다. **감출 것이 없는 목록에 2단계를 씌우면 클릭만 늘어난다**(§9).

---

## 🎛️ 4. 전역 CSS 토큰 — 개정본

🔴 **단위 규칙은 `00-09` §6.2를 그대로 계승한다: 타입은 `rem`, 간격은 `px`.**
이번 개정은 그 규칙을 **루트까지 밀어붙인 것**이다.

### 4.1 🆕 여백 토큰 — 신설 5개

```css
:root {
  /* 간격 — 00-09 §6.2의 6칸을 유지하고 2칸을 잇는다. 규칙: --sp-N = N × 4px */
  --sp-10: 40px;   /* 🆕 섹션 사이 최소 간격 (원칙 3) */
  --sp-14: 56px;   /* 🆕 화면 단위 구분 · 리더 상하 여백 */

  /* 🆕 적응형 거터 — 헤더와 본문을 분리한다 (원칙 1) */
  --gutter-page:   28px;  /* 본문 .container 좌우 (모바일) */
  --gutter-chrome: 16px;  /* 헤더·드로어·바텀시트 손잡이 좌우 */
  --gutter-page-sm: 20px; /* ≤359px 안전판 */
}
```

**`.container` 개정** — 현재 `index.css:251~270`을 대체한다.

| 구간 | 현재 | ✅ 개정 |
| :--- | :--- | :--- |
| 기본(데스크톱) | `padding: 120px` | **변경 없음** |
| ≤768px | `32px 24px` | **`var(--sp-10) var(--gutter-page)`** = `40px 28px` |
| ≤480px | `28px 20px` | *(구간 삭제 — 위 구간이 흡수)* |
| ≤359px | — | 🆕 `padding-left/right: var(--gutter-page-sm)` |

🔴 **헤더·`Sidebar`·`.mobile-drawer-panel`·바텀시트는 `--gutter-chrome`(16px)을 쓴다.**
`.container`의 거터를 상속받지 않게 **명시**한다 — 이 분리가 원칙 1의 전부다.

### 4.2 ✅ 타입 스케일 — `00-09` §6.2 개정 (루트 16px 기준)

```css
:root {
  --fs-caption: 0.875rem;                      /* 14px — 보조 하한. 이보다 작게 쓰지 않는다 */
  --fs-body:    1rem;                          /* 16px — 본문 표준 */
  --fs-lead:    1.125rem;                      /* 18px — 리드문·리더 본문 */
  --fs-section: 1.25rem;                       /* 20px — 섹션 제목 */
  --fs-title:   clamp(1.375rem, 4.5vw, 1.75rem);  /* 22 → 28px */
  --fs-display: clamp(1.625rem, 6vw, 2.25rem);    /* 26 → 36px */
}
```

| 토큰 | 기존(루트 18px) | 개정(루트 16px) | 360px 실효 | 판정 |
| :--- | :---: | :---: | :---: | :---: |
| `--fs-caption` | 0.85rem = 15.3px | **0.875rem** | **14px** | 🟡 −1.3px (보조 텍스트만) |
| `--fs-body` | 0.95rem = 17.1px | **1rem** | **16px** | 🟡 −1.1px (§3.2에서 감수) |
| `--fs-lead` | 1.05rem = 18.9px | **1.125rem** | **18px** | ≈ |
| `--fs-section` | 1.3rem = 23.4px | **1.25rem** | **20px** | 🟡 −3.4px |
| `--fs-title` | 27~34px | clamp | **22 → 28px** | ✅ 의도 — 원칙 4(크롬 감량) |
| `--fs-display` | 34~47px | clamp | **26 → 36px** | ✅ 의도 |

### 4.3 🆕 행간 토큰 — 신설 3개

```css
:root {
  --lh-tight:  1.35;  /* 제목 */
  --lh-body:   1.8;   /* 본문 기본 — 00-09 §2.3의 1.7을 개정 */
  --lh-reader: 1.9;   /* 몰입 리더·긴 글 (원칙 5) */
}
body { line-height: var(--lh-body); }
```

### 4.4 🔴 폐기·정정 목록 — `index.css`

| # | 대상 | 조치 |
| :---: | :--- | :--- |
| 1 | `--base-font-size: 18px` (`:22`) | 🔴 **폐기** — 선언 삭제 |
| 2 | `@media (max-width:768px){ html{font-size:var(--base-font-size)} }` (`:274~278`) | 🔴 **블록 삭제** = `00-29` §12 조치 A 철회 |
| 3 | `body { font-size: var(--base-font-size) }` (`:103`) | → `font-size: var(--fs-body)` |
| 4 | `body { line-height: 1.7 }` (`:104`) | → `var(--lh-body)` |
| 5 | `--min-touch-target: 56px` (`:22`) | ✅ **유지** |
| 6 | `.container` ≤480px 구간 (`:265~270`) | 🔴 삭제 (§4.1이 흡수) |

### 4.5 🔴 차단 리스크 — 소형 `rem` 리터럴 **441곳**

루트를 16px로 되돌리면 코드에 남은 `0.85rem` 이하 리터럴이 **전부 11% 더 작아진다.**
`0.85rem` → **13.6px**, `0.8rem` → **12.8px**. `00-29` §11이 잡아낸 문제가 **그대로 재발한다.**

🔴 **따라서 §4.4 #2(조치 A 철회)와 아래 치환은 같은 커밋이어야 한다.** 나눠서 배포하지 않는다.

#### ✅ 치환 규칙 — **기계적이고, 어떤 값도 작아지지 않는다**

| 기존 리터럴 | → | 실효 변화 |
| :--- | :--- | :--- |
| `0.85rem` · `0.8rem` | **`var(--fs-body)`** | 15.3 / 14.4 → **16px** (↑) |
| `0.78rem` · `0.75rem` · `0.7rem` | **`var(--fs-caption)`** | 14.0 / 13.5 / 12.6 → **14px** (=·↑) |

🔴 **이 표는 §4.5-4가 구간 규칙으로 대체했다(2026-09-09).** 열거된 7개 값은 결과가 같지만,
**열거 형식 자체가 `0.82`·`0.88`·`0.92`·`0.98rem` 등 45곳을 흘렸다.** 새 작업은 §4.5-4를 본다.

- **판단이 필요 없다** — 값만 보고 바꾼다. 모든 결과가 기존 이상이므로 화면별 검수 부담이 낮다.
- 🔴 **제외**: `AdminPage.tsx`(80) · `BizDashboard.tsx`(37) · `PartnerPortalPage.tsx`(6) = **123곳**.
  `00-09` §2.5 밀도 우선 예외이며, **조치 A가 ≤768px에만 걸려 있었으므로 이 세 화면은 영향이 없다.**
- 대상은 나머지 **약 318곳 / 38개 파일**.
- 🟡 `1.1rem`·`1.3rem` 등 큰 값은 이번에 건드리지 않는다 — 11% 줄어도 `--fs-section`(20px) 위다.

#### 🔴 4.5-1 정정 (2026-09-08, wt173 후) — **`index.css` 자신이 대상에서 빠져 있었다**

§4.5가 치환 대상을 *"인라인 리터럴"* 로만 적어 Sonnet이 `.tsx` 34개만 처리했다(wt173). **맞는 해석이다 —
빠뜨린 것은 스펙 쪽이다.** `index.css`에도 같은 리터럴이 19곳 남아 있고, 루트가 16px가 되면서
**클래스로 쓴 것들이 그대로 DoD #2를 위반한다.**

| 위치 | 선택자 | 현재 | 실효 | → |
| :--- | :--- | :---: | :---: | :--- |
| `:794` | `.stat-row__label` | `0.85rem` | **13.6px** ❌ | `var(--fs-caption)` |
| `:803` | `.stat-row__label` (≤480px) | `0.7rem` | **11.2px** ❌ | 🔴 **규칙째 삭제** (아래) |
| `:973` | `.header-outline-btn` | `0.85rem` | **13.6px** ❌ | `var(--fs-caption)` |
| `:1596` | `.farewell-message-badge` | `0.78rem` | **12.5px** ❌ | `var(--fs-caption)` |
| `:1781` | `.farewell-rail-label` | `0.8rem` | **12.8px** ❌ | `var(--fs-body)` |
| `:1834` | `.farewell-result-label` | `0.8rem` | **12.8px** ❌ | `var(--fs-body)` |
| `:1948` | `.farewell-board-recipient-sub` | `0.78rem` | **12.5px** ❌ | `var(--fs-caption)` |

🔵 **나머지 12곳은 `gap`·`padding`·`margin`이라 DoD와 무관**하다. 단위 규칙(간격은 `px`) 위반이지만
**이번에 건드리지 않는다** — `00-09` §6.4 P-3의 몫이다.

> 🔴 **`.stat-row__label`의 ≤480px `0.7rem` 축소 규칙은 폐기한다** — `00-09` §3.2 표 정정.
> **11.2px는 6070 페르소나에게 글자가 아니다.** 이 축소는 3칸 카운터를 좁은 폭에 우겨넣으려던 것인데,
> §8.2가 이미 **다른 답**을 정했다 — *"4자리 이상이면 2×2 또는 세로 스택"*. **글자를 줄여 칸을 지키지 않고,
> 칸을 바꾼다.** 14px로 올린 뒤 3칸이 좁아 보이면 그것이 Phase 3 `MyPage` 분리의 근거가 된다.
>
> 🔴 **3곳이 `.farewell-*`** — Phase 2 ①이 바로 그 페이지다. **Phase 2 착수 전에 닫는다**(§10 선행 A).
> ✅ **wt174에서 처리 완료.**

#### 🔴 4.5-2 정정 (2026-09-08, wt174 후) — **`0.9rem`·`0.95rem` 140곳. DoD #2가 아직 통과하지 않았다**

§4.5가 치환 대상을 **`0.85rem` 이하**로 정의했다. 근거는 `00-29` §12 **조치 B**가 손댔던 티어가
`0.7~0.8rem`이었기 때문이다. 🔴 **그 위 티어를 보지 않았다.**

| 리터럴 | 루트 18px(조치 A 시절) | **루트 16px(지금)** | DoD #2 |
| :--- | :---: | :---: | :---: |
| `0.95rem` | 17.1px ✅ | **15.2px** | ❌ |
| `0.9rem` | 16.2px ✅ | **14.4px** | ❌ |

**이 값들은 조치 A 아래에서는 기준을 만족했고, 루트를 되돌리면서 처음으로 미달이 됐다.**
🔴 **§4.5가 *"어떤 값도 작아지지 않는다"* 고 쓴 것은 <ins>치환 대상 안에서만</ins> 참이었다.**
대상 밖의 값은 그냥 11% 작아졌다 — **그 문장이 실제보다 넓게 읽히도록 쓰여 있었다.**

- **실측 140곳 / 38개 파일.** 운영자 3화면(`AdminPage` 8 · `BizDashboard` 7 · `PartnerPortalPage` 3
  = 18곳) 제외 → **소비자 화면 약 122곳.**
🔴 **§4.5-4가 대체함(2026-09-09)** — `0.92rem` 9곳·`0.98rem` 6곳이 이 열거를 빠져나갔다.
- ✅ **치환**: `0.9rem` · `0.95rem` → **`var(--fs-body)`** (16px). 판단 불요, 전부 증가(14.4/15.2 → 16).
  🔵 조치 A 시절 실효(16.2/17.1px)와 사실상 같은 자리로 돌아온다.
- 🔴 **`index.css` 10곳도 포함**한다(§4.5-1과 같은 실수를 반복하지 않는다).

> 🔴 **따라서 DoD #2("본문 ≥16px 위반 0")는 아직 통과하지 않았다.** wt173의 360px 실측은
> `/facility`에서 `body`가 16px인 것을 확인했을 뿐 **요소 전수 검사가 아니었다.**
> **Phase 2 ② 착수 전에 닫는다**(§10 Phase 1.6).

#### 🟡 4.5-3 정정 — **타입 토큰이 간격에 쓰인 곳 132군데**

§4.5의 치환 규칙이 *"`0.85rem` → `var(--fs-body)`"* 라고만 적고 **`font-size`에 한정하지 않았다.**
그래서 `padding: '0.85rem 1rem'` 같은 것도 **`padding: 'var(--fs-body) 1rem'`** 이 됐다.
🔴 **간격이 타입 스케일을 따라가게 된 것이고, 이는 `00-09` §6.2 단위 규칙(*타입은 `rem`, 간격은 `px`*)
— 이번 개정 전체가 딛고 선 바로 그 규칙 — 을 뒷문으로 되돌린다.** `--fs-body`를 건드리면
**여백 132곳이 같이 움직인다.** §3.2가 폐기한 "rem 폭주"와 같은 구조다.

| 현재 | → | 값 변화 |
| :--- | :--- | :--- |
| 간격 속성의 `var(--fs-body)` (63곳) | **`var(--sp-4)`** | 16px → 16px · **변화 0** |
| 간격 속성의 `var(--fs-caption)` (69곳) | **`var(--sp-3)`** | 14px → 12px · 🔵 **원래대로 돌아가는 것** — 이 자리들의 원값은 `0.7~0.78rem`(12.6~14px)이었고 Phase 1이 14px로 **부풀렸다** |

- 대상 속성: `padding`·`margin`·`gap`·`top/left/right/bottom`·`width/height`.
  🔴 **`fontSize`·`lineHeight`는 건드리지 않는다.**
- 🟡 **지금 화면이 깨져 있지는 않다** — 잠복 결합이다. 다만 `00-09` §6.4 **P-5 드리프트 훅이
  거부할 132곳**이므로, 훅을 만들기 전에 없앤다.

---

#### 🔴 4.5-4 정정 (2026-09-09, Phase 1.6 후) — **열거식 표가 구멍을 만든다. 구간 규칙으로 대체한다**

§4.5와 §4.5-2는 리터럴을 **열거**했다 — `0.7`·`0.75`·`0.78`·`0.8`·`0.85`(§4.5), `0.9`·`0.95`(§4.5-2).
**그 사이의 값은 전부 샜다.** Phase 1.6이 "잔여 0"으로 끝난 직후에도 **45곳**이 그대로 남아 있다.

| 샌 값 | 곳 | 루트 16px 실효 | DoD #2 |
| :--- | :--- | :--- | :--- |
| `0.98rem` | 6 | **15.7px** | ❌ 본문 미달 |
| `0.92rem` | 9 (tsx 6 · `index.css` 3) | **14.7px** | ❌ |
| `0.88rem` | 16 | **14.1px** | ❌ |
| `0.87`·`0.86rem` | 2 | 13.9 / 13.8px | ❌ |
| `0.85rem` | 2 | **13.6px** | ❌ *(§4.5 표에 있으나 wt176 신설 파일이라 스윕 대상이 아니었음)* |
| `0.82rem` | 9 | **13.1px** | ❌ |
| `0.78rem` | 1 | 12.5px | ❌ 보조 미달 |
| **합계** | **45** | | **DoD #2 미통과** |

🔴 **원인은 값을 못 찾은 게 아니라 규칙의 형식이다.** 열거를 유지하는 한 새 파일이 생길 때마다
같은 구멍이 다시 난다 — 실제로 `FooterMobile.tsx`(wt176 신설)가 `0.78`·`0.82`·`0.85`를 들고 태어났다.

##### ✅ 판정 — 열거를 **구간**으로 바꾼다 (§4.5 치환 규칙 표를 이것으로 대체)

| `font-size` 리터럴 구간 | → | 실효 |
| :--- | :--- | :--- |
| **`< 0.8rem`** | **`var(--fs-caption)`** | → 14px |
| **`0.8rem` 이상 `1rem` 미만** | **`var(--fs-body)`** | → 16px |
| `1rem` 이상 | 건드리지 않는다 | §4.5 마지막 항목 그대로 |

- 🔵 **새로운 판단이 아니다.** 경계 `0.8`은 §4.5 표가 이미 그어 둔 선이고(`0.8`→본문 / `0.78`→보조),
  구간 규칙을 기존 7개 리터럴에 적용하면 **결과가 한 곳도 달라지지 않는다.** 표현 형식만 바꾼 것이다.
- **"판단 불요"는 그대로 유지된다** — 값만 보고 바꾼다. 화면별 검수 부담이 늘지 않는다.
- 🔴 **적용 대상은 `font-size` / `fontSize` 뿐이다.** 간격 속성은 §4.5-3이 정본이며 여기 규칙을 적용하지 않는다.
- 🔴 **제외 3화면**(`AdminPage`·`BizDashboard`·`PartnerPortalPage`)은 §4.5 그대로 유지한다.

##### ⓐ 단 하나의 예외 — 푸터 카피라이트 2곳

| 위치 | 현재 | → |
| :--- | :--- | :--- |
| `components/Footer.tsx:190` | `0.82rem` | **`var(--fs-caption)`** |
| `components/FooterMobile.tsx:97` | `0.78rem` | **`var(--fs-caption)`** |

**같은 문구(`Copyright © 2026 …`)에 두 파일이 서로 다른 값을 쓰고 있다.** 구간 규칙을 그대로 먹이면
데스크톱은 16px, 모바일은 14px로 **갈라진다.** 저작권 표기는 §4.2 정의상 명백한 보조 텍스트이고
**14px는 DoD #2의 보조 하한을 만족**하므로, 둘 다 `--fs-caption`으로 통일한다.

##### 🔵 확인했고 예외가 아닌 것 — 기록해 둔다

- **`FooterMobile` 펼침 패널**(`:78`, `0.85rem` → 16px): `index.css:575` `.footer-mobile-panel`의
  `max-height: 170px`를 넘지 않는다 — 3줄 × `line-height 1.8` × 16px ≈ 86px + 여백 ≈ **100px**.
  🔵 다만 wt176이 13.6px 기준으로 잡은 값이므로 **실기동에서 함께 확인**한다.
- **`height: 36px` 버튼 3곳**(`MemorialPage:250`·`MyObituaryListPage:199`·`:210`, `0.82rem` → 16px):
  세로로 터지지 않는다(16px × 1.2 ≈ 19px < 36px). 가로 폭만 늘어난다 → 구간 규칙 그대로 적용.
  🔴 **다만 이 3곳은 별건의 문제를 안고 있다** — 아래 §4.5-5.

---

#### 🔴 4.5-5 신규 발견 (2026-09-09) — **터치 타깃 36px, 토큰은 56px**

`index.css:22`가 `--min-touch-target: 56px`로 서 있는데, 아래 3곳은 `height: '36px'` 하드코딩이다.

| 위치 | 현재 |
| :--- | :--- |
| `pages/MemorialPage.tsx:250` | `height: '36px'` |
| `pages/MyObituaryListPage.tsx:199` | `height: '36px'` |
| `pages/MyObituaryListPage.tsx:210` | `height: '36px'` |

**§4.5-4와 같은 커밋에 넣지 않는다.** 폰트 치환은 값 대 값이라 판단이 없지만, 버튼 높이를
56px로 올리면 그 줄의 레이아웃이 바뀌어 **DoD #8(데스크톱 회귀 0) 판정이 흐려진다.**
🟡 **Phase 3에서 `00-38` §3.1 터치 타깃 항목과 함께 처리**한다.

---

## 🪝 5. `useIsMobile` 훅 명세

### 5.1 시그니처

```ts
// eobomDev/frontend/src/hooks/useIsMobile.ts
export function useIsMobile(maxWidth: number = 768): boolean;
```

| 항목 | 스펙 |
| :--- | :--- |
| **인자** | `maxWidth` — **모바일로 간주할 최대 폭(inclusive)**. 기본 **768** |
| **질의** | `window.matchMedia(\`(max-width: ${maxWidth}px)\`)` |
| **왜 inclusive 768인가** | `index.css`의 지배적 브레이크포인트가 `@media (max-width: 768px)`다. 여기에 맞춰야 **768px 정확히에서 JS와 CSS가 어긋나지 않는다** |
| **초기값** | 🔴 **lazy initializer로 즉시 평가**한다(`useState(() => mql.matches)`). `useEffect`에서 처음 계산하면 **데스크톱 뷰가 한 프레임 그려졌다 바뀐다** |
| **SSR** | 없다(Vite SPA). `typeof window` 가드는 넣지 않는다 — 죽은 코드다 |
| **구독** | `addEventListener('change', …)` + cleanup. 🟡 **`addListener` 폴백을 둔다** — 주 페르소나가 6070이라 구형 iOS Safari(≤13) 비중을 0으로 볼 수 없다 |
| **의존성** | `[maxWidth]`. 🔴 호출부는 **리터럴만** 넘긴다 — 계산식을 넘기면 매 렌더 재구독된다 |

### 5.2 🔴 기존 `matchMedia` 호출부 — 훅으로 통일하되 **값은 바꾸지 않는다**

| 위치 | 현재 | 조치 |
| :--- | :--- | :--- |
| `components/home/EntryBoxes.tsx:198·252` | `(max-width: 767px)` | → `useIsMobile(767)` — **767 유지** |
| `pages/HomePage.tsx:24` | `(max-width: 640px)` | → `useIsMobile(640)` |
| `pages/HomePage.tsx:231` · `DomainOverviewPage.tsx:71` | `(pointer: coarse)` | 🔴 **건드리지 않는다** — 폭이 아니라 **입력 방식** 판정이다. 다른 관심사 |

> 🔴 **값을 768로 통일하지 않는다.** 이 호출부들은 각자 이유가 있는 값이고(`EntryBoxes` 페이지당 항목 수,
> `HomePage` 히어로 레이아웃), 바꾸면 이번 작업과 무관한 회귀가 섞인다. **통일하는 것은 API지 값이 아니다.**

### 5.3 🔴 함정 — **effect 클로저가 훅 값을 굳힌다** (2026-09-08 wt173에서 실제로 걸림)

훅은 **렌더 스냅샷**을 준다. 그 값을 **마운트 시 1회만 도는 `useEffect`** 안에서 읽으면
클로저에 캡처돼 **리사이즈 이후에도 옛 값이 남는다.** `matchMedia`를 직접 구독하던 코드에는
없던 함정이라, 훅으로 바꾸는 것만으로 조용히 회귀가 생긴다.

```tsx
// ❌ useEffect(..., [sections.length]) 안에서 isMobile을 직접 읽으면 값이 굳는다
// ✅ ref로 미러링하고 핸들러는 ref를 읽는다
const isMobile = useIsMobile(640);
const isMobileRef = useRef(isMobile);
useEffect(() => { isMobileRef.current = isMobile; }, [isMobile]);
```

- ✅ **`HomePage.tsx`가 이 방식으로 처리됐다**(wt173 편차) — **판정: 통과, 스펙갱신 불요.**
  동작은 기존과 동일하고 §5.2의 *"값은 그대로"* 취지도 지켰다.
- 🔴 **Phase 2~4에서 같은 함정이 재발한다.** 스크롤·휠·키보드 핸들러를 마운트 1회 effect로 다는
  화면(`HomePage`·`DomainOverviewPage`·모달류)에서 훅 값을 쓸 때는 **반드시 ref 미러링**한다.
- 🔵 **JSX 안에서 쓸 때는 문제없다** — 매 렌더 최신값이다. 함정은 **effect 클로저**뿐이다.

---

## 🧩 6. 적응형 뷰 분리 패턴

### 6.1 구조

```
pages/XxxPage.tsx  ← 라우트가 가리키는 곳. URL 그대로.
 ├── 데이터 로딩 · API 호출 · 상태(useState) · 핸들러 …… 🔴 여기 1벌만
 ├── const isMobile = useIsMobile();
 └── return isMobile ? <XxxMobileView {...vm} /> : <XxxDesktopView {...vm} />

components/<domain>/XxxDesktopView.tsx   ← 표현만. 상태 없음.
components/<domain>/XxxMobileView.tsx    ← 표현만. 상태 없음.
```

- **파일 위치**: `components/<domain>/` — `00-35` §4.1이 세운 관례(`components/endingNote/`·`facility/`·`home/`)를 따른다. **barrel(`index.ts`) 만들지 않는다.**
- **`vm`(뷰모델)**: 두 뷰가 **같은 props 타입**을 받는다. 타입은 `components/<domain>/types.ts`에 둔다.

### 6.2 🔴 지켜야 할 것 — 4개

| # | 규칙 | 왜 |
| :---: | :--- | :--- |
| **1** | 🔴 **상태·입력값은 부모에만 둔다. 뷰는 controlled.** | 조건부 렌더는 **형제 교체**라 폭이 바뀌면 자식이 언마운트된다. 뷰가 값을 들고 있으면 **회전·창 크기 변경에 입력이 사라진다** — `00-35` §9 C와 같은 위험 |
| **2** | 🔴 **`fetch`/`useEffect`를 뷰에 두지 않는다.** | 두 벌이 되면 전환 시 **재요청**이 뜬다 |
| **3** | 🔴 **뷰 컴포넌트를 부모 함수 안에 정의하지 않는다.** | `00-35` §6.1 기존 규칙. 매 렌더 새 타입 = 전체 재마운트 |
| **4** | 🟡 **`XxxDesktopView`는 기존 JSX를 그대로 옮긴다.** | 데스크톱 회귀 0이 DoD다(§11 #8). 옮기면서 고치지 않는다 |

### 6.3 ❌ 하지 않는 것

- ❌ **URL 분리** (`/m/...` 없음) — 라우팅·`00-13` 공유링크·SEO에 영향 0이어야 한다.
- ❌ **UA 스니핑** — 폭으로만 판정한다.
- ❌ **전 페이지 일괄 분리** — 분리는 **비용**이다. §8이 지정한 페이지만 나눈다.
- ❌ **모바일 전용 동선·기능** — `00-29` §3 ②의 판단(하지 않음)을 유지한다. **같은 데이터·다른 배치**까지다.

### 6.4 ✅ 허용되는 가로 스크롤 — `00-29`의 "가로 스크롤 0"과 충돌하지 않는다

`06-04` §6.1-1 ③이 *"탭 9~10개는 가로 스크롤이라 `00-29`를 정면으로 깬다"* 고 적었다. **둘은 다른 것이다.**

| | 판정 |
| :--- | :--- |
| 🔴 **문서(`body`) 가로 오버플로** | **금지.** DoD #1. `00-29` §10.1이 확보한 것이 이것이다 |
| ✅ **명시적 `overflow-x:auto` 트랙** (카테고리 칩 등) | **허용.** 아래 3조건 전부 충족 시 |

1. 트랙 우측에서 **다음 항목이 잘려 보이거나** 페이드가 걸려 *"더 있다"* 가 드러날 것.
2. 🔴 **그 트랙이 유일한 도달 경로가 아닐 것** — 칩은 **필터·점프**이고, 스크롤만으로도 전체에 닿는다.
3. 트랙 자신이 화면 폭을 넘기지 않을 것(`width:100%; overflow-x:auto`).

> 🔵 **탭이 금지된 이유는 가로 스크롤이 아니라 조건 2였다** — 탭은 *"안 열어본 탭"* 을 만든다.
> 칩은 목록을 감추지 않으므로 `06-04` §6.1-1의 ①(빠짐없이)을 깨지 않는다.

#### 6.4-1 🆕 **세 번째 유형 — 데스크톱 최소폭 하한 + `.main-wrapper` 스크롤 패널** (2026-09-09)

> 🔴 **사람이 직접 지시하고 `/facility` 실기동에서 확인한 확정 사항이다**(wt179). 이 절은 그것을
> 뒤늦게 옮겨 적는 기록이며 **판정 대상이 아니다.** 경위: 데스크톱에서 창을 좁히면 `.container`가
> 끝없이 줄어들며 폼·버튼 줄바꿈이 반복적으로 깨져 **수동 CSS 땜질이 계속 필요했다.**

**769px 이상에서는 `.container`를 일정 폭 아래로 더 줄이지 않고, 넘치는 폭은 `.main-wrapper`가 가로 스크롤로 받는다.**

| | 값 (`index.css:299~340`) |
| :--- | :--- |
| 적용 구간 | `@media (min-width: 769px)` — 🔴 **모바일(≤768px) 규칙은 건드리지 않는다** |
| **하한** | `.main-wrapper .container { min-width: 1200px }` — 🔄 최초 1024 → **1200px**(실기동 후 사람이 *"걸리는 지점을 조금 더 크게"* 로 상향) |
| **스크롤 패널** | `.main-wrapper { height: calc(100vh - var(--header-h)); overflow-x: auto; overflow-y: auto; min-width: 0 }` |
| 전파 차단 | `.main-wrapper > main { min-width: 0 }` |
| 🔴 **제외** | **운영자 3화면**(`Admin`·`BizDashboard`·`PartnerPortalPage`)은 `.main-wrapper` 없이 렌더돼 **자동으로 범위 밖**(`App.tsx`). 운영자 화면에 하한을 걸 이유가 없고, 거기까지 스크롤 패널로 바꾸면 회귀 표면만 늘어난다 |

##### 🔴 함정 2개 — 둘 다 실기동에서 걸렸고, 둘 다 "스크롤이 아예 안 생긴다"로 보였다

| # | 증상 | 원인 | 조치 |
| :---: | :--- | :--- | :--- |
| **1** | 스크롤이 안 생기거나 페이지 전체가 밀린다 | `body>#root>App루트>.main-wrapper>main`이 전부 `display:flex(column)`이고, **플렉스 아이템의 자동 최소 크기**(`min-width:auto` 기본값)가 내용물의 `min-content`를 물려받는다 → `.container`의 `min-width:1200px`가 **조상으로 계속 전파**돼 `body`까지 늘어났다 | `main`·`.main-wrapper` 양쪽에 **`min-width:0`** 으로 전파를 끊는다 |
| **2** | `overflow-x:auto`를 줬는데도 콘텐츠가 그냥 잘려 보인다(`assets/scroll.png`, `/facility` 861px) | `overflow-x:auto` 요소의 가로 스크롤바는 **그 요소의 바닥**에 붙는다. `.main-wrapper`는 `min-height`만 있어 페이지 길이만큼 자라므로 바닥이 **수천 px 아래**에 있었다 | `min-height` → **`height: calc(100vh - var(--header-h))`** 로 캡. 패널 자신이 "헤더 아래 화면 전체"가 되어 세로·가로 스크롤바가 **둘 다 화면 안**에 온다 |

> 🔵 **`sticky`는 깨지지 않는다.** `position:sticky`는 가장 가까운 스크롤 조상 기준으로 붙는데,
> 이 패널이 정확히 *"헤더 아래 화면 전체"* 라 `EndingNote` 목차 등 기존 동작과 **시각적으로 동일**하다.
> 🔴 **§6.4 표의 "`body` 가로 오버플로 금지"와 충돌하지 않는다** — 초과폭은 `.main-wrapper` 안에
> 갇히고(함정 1이 바로 그것을 못 하고 있었던 상태다), **DoD #1은 360px 기준이라 769px↑ 규칙과 만나지 않는다.**
> 🟡 §6.4의 3조건은 **트랙**(칩) 규칙이고 이건 **패널** 규칙이다 — 같은 잣대로 재지 않는다.

### 6.5 🔴 분리 판정 기준 — **"모든 페이지를 나누는 것 아닌가"에 대한 답** (2026-09-08, 파일럿 근거)

**아니다. 13개 중 5개만 나눈다**(§8 표). 파일럿(wt174)이 그 근거를 실측으로 줬다.

#### ⓐ 실측 — 분리의 값

| | 분리 전 | 분리 후 |
| :--- | :---: | :---: |
| `FarewellMessagePage` 계층 | **217줄 / 1파일** | **434줄 / 5파일** (page 131 + Desktop 131 + Mobile 124 + Notice 31 + types 17) |

🔴 **페이지 계층이 정확히 2배가 됐다.** 이것이 분리 1건의 값이고, 그 뒤로 **영원히 두 벌을 같이 고쳐야**
한다. DoD #8(데스크톱 회귀) 표면과 #9(입력 소실) 표면도 나눌 때마다 늘어난다.

#### ⓑ 🔵 결정적 반례 — **701줄짜리 컴포넌트가 CSS만으로 모바일 대응됐다**

같은 커밋에서 `FarewellMessageCard`(**701줄**)는 **쪼개지 않았다.** 리더 전체화면화·레일 접기·
프리뷰 숨김·`100dvh` 전부 **CSS + 로컬 토글 하나**로 끝났다.

> 🔴 **그러므로 "페이지가 크니까 나눈다"는 틀렸다.** 크기는 분리의 근거가 아니다.
> `FarewellMessagePage`(217줄)는 나눴고 `FarewellMessageCard`(701줄)는 안 나눴다 — **3배 큰 쪽을 안 나눴다.**

#### ⓒ ✅ 기준 — **화면의 수 또는 상태 기계가 달라지는가**

| 모바일에서 달라지는 것 | 판정 | 예 |
| :--- | :---: | :--- |
| **배치만** — 2단→1단, 그리드→1열 스트림, 폭·여백·글자 | ❌ **CSS로 한다** | 그룹 3 전부 · `MyObituaryListPage` · 그룹 4 |
| **한 요소의 표현** — 모달→바텀시트, 펼침→접힘, 가로→세로 | ❌ **CSS + 로컬 토글** | `FarewellMessageCard` · `MyPageFamilyDesignation` |
| 🔴 **화면(스텝)의 수** — 1화면이 2화면이 됨 | ✅ **분리** | `FarewellMessagePage`(사이드바+상세 → 마스터·디테일 2화면) · `EndingNotePage`(아코디언 스택 → 목차 + 리더) |
| 🔴 **상태 기계** — 데스크톱에만 있는 상태가 모바일에서 사라짐 | ✅ **분리** | `CareGuidePage`(구간 접기 상태가 모바일엔 없음 + 칩 필터가 데스크톱엔 없음) |

> 🔵 **한 문장으로**: **정보 구조가 같으면 CSS, 동선이 달라지면 분리.**
> ❌ 줄 수 · 인라인 스타일 개수 · "복잡해 보임"은 근거가 아니다.

#### ⓓ 🟡 Phase 3 ✅ 2건은 착수 시점에 재판정한다

§8.2가 `ObituaryPage`·`MyPage`를 ✅로 잡았으나, **그때는 파일럿 데이터가 없었고 줄 수·인라인 개수가
판단에 섞여 있었다.** ⓒ 기준으로 다시 보면 둘 다 **배치 변경에 가깝다**(2단→1열 폼, 3칸→2×2 카운터).

🔴 **지금 뒤집지 않는다** — Phase 3은 멀고, 그 사이 `00-36`(마이페이지 개편)·`00-37`이 화면을 바꾼다.
**Phase 3 착수 시 ⓒ 기준으로 재판정**하며, **❌로 내려갈 가능성이 높다는 것을 미리 적어 둔다.**
✅ `EndingNotePage`(③)는 ⓒ의 "화면 수" 항목에 명확히 걸리므로 **재판정 대상이 아니다.**

> 🔵 **2026-09-09 — `ObituaryPage` 관리 모드는 이미 CSS만으로 끝났다**(wt179, 사람 지시). 좌(미리보기)·
> 우(공유) 2열을 `flex-wrap`으로 짜서 **768px 이하에서 자동 줄바꿈**된다(§8.2 갱신분). 🔴 **분리 없이 됐다.**
> ⓓ의 예측이 **관리 모드에 관해서는 실증됐다** — 남은 재판정 대상은 **개설 폼 본체**뿐이다.

---

## 🔗 7. 정합성 판정 — 기존 문서 5건

**조용히 다르게 구현하지 않는다**(`roles.md` §2-1). 어긋나는 곳을 먼저 적는다.

| 문서·절 | 현재 내용 | 판정 | 조치 |
| :--- | :--- | :---: | :--- |
| **`00-09` §2.3** | `--base-font-size: 18px` *"협상 대상이 아님"* · `line-height 1.7` | 🔴 **개정** | 18px 폐기(§3.2) · 행간 1.8(§4.3). `--min-touch-target: 56px`만 존치 |
| **`00-09` §6.2** | `--fs-*` 6칸 (루트 18px 전제) | 🔴 **개정** | §4.2 값으로 대체. **간격·색·모서리·그림자·모션 토큰은 그대로** |
| **`00-09` §3.2** | 반응형 프리미티브 4종 | ✅ **유지** | 적응형 뷰는 프리미티브를 **대체하지 않는다.** 분리하지 않는 페이지는 계속 이 4개를 쓴다 |
| **`00-29` §12 조치 A** | 모바일 루트 18px | 🔴 **철회** | 사유·경위는 `00-29`에 남기고, 값은 이 문서가 가져간다. §11·§13 실측은 유효 |
| **`00-29` §8 #3** | 최저 기준폭 375px | 🟡 **하향** | **360px**(§3.1) |
| **`00-29` §8 #5** | 루트 상향 범위 = 모바일만 | ⛔ **무효** | 상향 자체를 하지 않는다 |
| **`06-04` §6.1-1** | *"탭이 아니라 아코디언"* | 🟡 **모바일만 개정** | **데스크톱은 아코디언 유지.** 모바일은 **목차 리스트 + 포커스 리더**(§8.1-2). ①빠짐없이 ②시니어 인지 ③세로 스택 — **세 근거 모두 목차에서도 성립**한다 |
| **`06-04` §6.1-1 ⏸** | 완료 표시는 저장이 붙은 뒤 | ✅ **해제** | `Entry`+`Grant` 배선 완료(2026-08-27, `context.md`). **완료/미작성 표시 가능** |
| **`06-04` §6.1-1 ⑨** | 유언장 초안은 별도 블록 | ✅ **일치** | 목업도 리스트 밖 별도 카드 |
| **`00-35` §5.2** | `sectionBodies: Record<string, ReactNode>` | ✅ **그대로 쓴다** | 🔵 **모바일 리더가 공짜로 얻어진다** — 모달이 `sectionBodies[code]`를 렌더하면 끝. §10.4(본문 8개 분리) **재개 불필요** |
| **`07-04` §8-8-2** | 카드 안 펼치지 않음 · 접기는 구간 5개에만 | 🟡 **모바일만 개정** | 모바일은 **접기 없이 시간축 구분선 + 카테고리 칩**(§8.1-3). 데스크톱은 구간 접기 유지 |
| **`07-04` §5.1** | CRITICAL = ⭐ + 글자 배지 + 좌측 굵은 테두리 | 🔴 **정정** | 목업의 *"좌측 3px 색선 + 별 하나"* 는 **글자 배지를 뺐다.** `00-23` §8.7(색만으로 구분 금지)에 걸린다 → **⭐ + 좌측 3px 색선 + `되돌릴 수 없음` 글자**를 **한 줄에** 둔다(§9.3) |
| **`07-04` §8-9** | id 23 구간 1 이동 · id 12 `conditional`+제목 | ✅ **선행** | 데이터·라벨 변경이므로 **적응형 분리보다 먼저** 끝낸다(§10) |
| **`07-02` §2** | `deadlineShort` 필드 신설 필요 | ✅ **확정(09-09)** | 🔄 **⏸ 해제.** `07-02` **§2-1**에 23건 전수 + 축약 규칙 5개가 섰다. 🔵 옵셔널 필드라 이 문서의 Phase와 순서로 얽히지 않는다 |

---

## 📂 8. 도메인별 적용 가이드 — 13개 페이지

🔴 **"분리"는 비용이다** — 파일럿에서 페이지 계층이 **2배**가 됐다(§6.5 ⓐ).
**분리 열이 ❌면 적응형 뷰를 만들지 않고 CSS·프리미티브로 끝낸다.** 판정 기준은 **§6.5 ⓒ**다 —
🔴 **줄 수·인라인 개수는 근거가 아니다**(701줄 카드가 CSS만으로 됐다).

| 그룹 | 페이지 | 줄수/인라인 | 분리 | Phase |
| :---: | :--- | :---: | :---: | :---: |
| **1** | `FarewellMessagePage` | 217 / 19 | ✅ **완료(wt174)** | 2 |
| **1** | `CareGuidePage` | 347 / 36 | ✅ (상태 기계) | 2 |
| **1** | `EndingNotePage` | 901 / 59 | ✅ (화면 수) | 2 |
| **2** | `ObituaryPage` | 743 / 87 | 🟡 **재판정**(§6.5 ⓓ) — 🔵 **관리 모드는 CSS로 완료**(wt179) | 3 |
| **2** | `MyPage` | 358 / 40 | 🟡 **재판정**(§6.5 ⓓ) | 3 |
| **2** | `MyObituaryListPage` | 307 / 32 | ❌ | 3 |
| **2** | `MyPageFamilyDesignation`(모달) | — | ❌ | 3 |
| **3** | `FacilityPage` | 570 / 51 | ❌ | 3 |
| **3** | `CounselingPage` | 231 / 29 | ❌ | 3 |
| **3** | `DigitalEstatePage` | 198 / 41 | ❌ | 3 |
| **4** | `HomePage` | 558 / 28 | ❌ | 4 |
| **4** | `ObituaryLandingPage` | 236 / 37 | ❌ | 4 |
| **4** | `MemorialLandingPage` | 319 / 33 | ❌ | 4 |

### 8.1 그룹 1 — 텍스트·감성형 (원칙 5가 여기서 산다)

#### 8.1-1 `FarewellMessagePage` — 🔵 **파일럿** (2026-09-08 상세 확정)

페이지 자체는 217줄로 가장 작고, 원칙 5의 형태가 가장 분명하다. **여기서 패턴을 세우고 나머지가 베낀다.**
🟡 시안: `reports/farewell_messages_redesign.html`.

##### ⓐ 🔴 실측 — 360px에서 깨지는 것 3개

wt164~172로 *"사이드바 + 상세"* 구조가 들어왔다(사용자 확정). **데스크톱에서는 옳고, 360px에서는 성립하지 않는다.**

| # | 위치 | 값 | 360px에서 |
| :---: | :--- | :--- | :--- |
| **1** | `.farewell-board-layout` (`index.css:1887`) | `grid-template-columns: 220px 1fr` · `gap:1.75rem` | 🔴 본문 폭 304 − 220 − 28 = **56px**. 상세 칸이 사실상 사라진다 |
| **2** | `.farewell-composer-body` (`:1767`) | `grid-template-columns: 200px 1fr` | 🔴 같은 이유로 붕괴 |
| **3** | `.farewell-board-shell` (`:1880`) | `padding: 1.5rem` **안에** `.container` 28px | 🔴 **박스-in-박스**(원칙 3). 좌우 합 52px → 본문 **256px** |

##### ⓑ ✅ Mobile — 마스터·디테일 2화면 + 리더

원칙 5를 **그대로** 쓴다. 사이드바가 곧 1단계 목록이 된다.

```
[1단계] 받는 분 목록          [2단계] 그 사람의 편지          [리더]
 ─────────────────            ← 어머니                        ← 닫기
 어머니      배우자 · 3통  →   ─────────────────               ─────────────
 아버지      배우자 · 0통  →   09.02  건강 잘 챙기세요  →       (본문)
 …                             08.17  손주 이야기      →       --fs-lead
                               [＋ 편지 남기기]                --lh-reader
```

| | 스펙 |
| :--- | :--- |
| **1단계 행** | 아바타 + 이름 + `관계 · N통`. 🔴 히트 영역 **≥56px**. `.farewell-board-sidebar`를 그대로 세로 목록으로 쓴다 |
| **2단계** | 상단 **← 이름** 한 줄(뒤로) + 편지 목록 = **[날짜 + 한 줄 제목]** 만. 🔴 **`.farewell-message-preview` 제거**(원칙 5 — 목록에 본문 조각을 흘리지 않는다) |
| **리더** | 기존 `.farewell-message-overlay`/`-panel`을 **전체화면**으로. 본문 `--fs-lead`·`--lh-reader`·좌우 `--gutter-page`. 🔴 `max-height:100dvh`(`vh` 아님) |
| **컴포저** | ⓐ#2 레일(`.farewell-composer-rail`)을 **본문 위로 접어 올린다**(1열). 🔴 레일의 힌트 목록은 **접힌 상태 기본** |
| **탈박스화** | 🔴 `.farewell-board-shell`의 카드 표면을 **모바일에서 해제** — 배경·그림자·`padding` 0. `.container`가 유일한 좌우 여백이 된다(원칙 3) |
| **뒤로가기** | 🔴 **`history.pushState`를 쓰지 않는다**(§6.3 URL 분리 금지). 화면 내 상태로만 두고 **← 버튼을 좌상단에 명시**한다. 🟡 안드로이드 물리 뒤로가기가 페이지를 떠나는 것은 **알려진 대가**로 받는다 |

##### ⓒ 🔴 상단 크롬 감량 (원칙 4) — 4단 → 1단

현재 `:108~129`가 **배지 + h1 + 설명문 + 안내박스** 4단이다. 360px에서 **편지가 첫 화면에 없다.**

| 요소 | 조치 |
| :--- | :--- |
| 배지 *"하고 싶은 말을 그대로"* (`:109`) | 🔴 **모바일 제거** — h1이 같은 말을 한다 |
| `h1` + 아이콘 32px (`:112`) | ✅ 유지, 아이콘 **24px** |
| 설명문 (`:115`) | 🟡 유지 (1줄) |
| **전달 고지 박스** (`:124`) | 🔴 **컴포저 바로 위로 이동** — 아래 |

> 🔵 **`06-05` §4.3의 "상시 노출"과 충돌하지 않는다.** *상시 노출*은 **그 화면에 항상 있다**는 뜻이지
> **상단에 있다**는 뜻이 아니다. 이 고지는 *"재산 분배·상속에 관한 내용은 남기지 마세요"* 이므로
> 🔴 **쓰기 직전에 읽혀야 뜻이 있다.** 목록 위가 아니라 **컴포저 바로 위**가 제자리다.
> 🟡 현재 코드(`:127`)는 *"전달됩니다"* 절반만 있고 **"남기지 마세요" 절반이 빠져 있다** — `06-05` §4.3
> 원문과 대조해 **복원한다**(09-08 톤 조정은 색·아이콘에 대한 것이지 문구 삭제 지시가 아니었다).

##### ⓓ ✅ Desktop — 변경 없음

`.farewell-board-layout` 2단, `.farewell-composer-body` 2단, `.farewell-board-shell` 카드 표면
**전부 그대로**다. DoD #8(데스크톱 회귀 0)의 첫 시험대다.

##### ⓔ 🔴 분리 경계 — 부모에 남는 것

`FarewellMessagePage.tsx`의 **상태 5개**(`loading`·`recipients`·`messages`·`exporting`·
`selectedRecipientId`)와 **핸들러 3개**(`handleExport`·`fetchMessages`·`useEffect` 로딩),
그리고 `onOpenFamilyDesignation` 분기(`:17`·`:141`)는 **전부 부모에 남는다**(§6.2 #1·#2).
🔵 `selectedRecipientId`가 **모바일에서는 곧 "2단계에 있는가"** 를 겸한다 — 상태를 새로 만들지 않는다.
🔴 `FarewellMessageCard`(684줄)는 **이번에 쪼개지 않는다.** 두 뷰가 그대로 재사용한다.

##### ⓕ 🔴 자동선택 effect — **모바일에서 1단계를 건너뛴다**

`:30~36`이 `recipients`가 오면 **무조건 `recipients[0]`을 고른다.** 데스크톱에서는 옳다(상세 칸이
비면 안 되니까). 🔴 **모바일에서는 목록을 보여주기도 전에 2단계로 떨어진다.**

```tsx
// 🔴 지금: recipients가 오면 항상 첫 사람 선택
useEffect(() => { setSelectedRecipientId(prev => …recipients[0].id); }, [recipients]);

// ✅ 데스크톱에서만 자동선택. 🔴 deps에 isMobile을 넣어야 폭 전환 때도 맞는다
useEffect(() => {
  if (isMobile) return;                       // 모바일은 사용자가 고를 때까지 null
  setSelectedRecipientId(prev => …);
}, [recipients, isMobile]);
```

| 폭 전환 | 결과 |
| :--- | :--- |
| 데스크톱 → 모바일 | 이미 고른 사람이 있으므로 **2단계로 진입.** ✅ 의도한 동작(보던 것을 유지) |
| 모바일(1단계, `null`) → 데스크톱 | `isMobile` 변화로 effect 재실행 → **첫 사람 자동선택.** 🔴 `deps`에 `isMobile`이 없으면 **상세 칸이 빈 채로 남는다** |

> 🔴 **이것이 DoD #9(뷰 전환 시 소실 0)를 이 페이지에서 재는 지점**이다. 767↔769 리사이즈로 확인한다.

##### ⓖ ✅ wt174 편차 판정 (2026-09-08) — **3건 전부 통과**

| # | 편차 | 판정 |
| :---: | :--- | :--- |
| **1** | 고지 문구 복원을 **Desktop에도** 적용 | ✅ **통과. 판단이 옳다.** 문구 복원의 근거는 `06-05` §4.3(도메인 정본)이지 §8.1-1 ⓒ가 아니다. **모바일 사정과 무관하게 양쪽 다 틀려 있던 것**이고, 한쪽만 고치면 두 뷰가 서로 다른 고지를 내보낸다 — 그게 더 나쁘다. 🔵 **위치는 뷰마다 다르게(Desktop 상단 유지 / Mobile 컴포저 위) 가져간 것도 맞다** |
| **2** | `railOpen`을 `FarewellMessageCard` **로컬 state**로 | ✅ **통과. 판단이 옳다.** §6.2 #1이 막는 것은 **페이지 상태**(서버 데이터·사용자 입력값)이고, 이유는 *뷰 교체 시 언마운트로 소실*이다. 접기 토글은 **소실돼도 잃을 것이 없다** — 오히려 폭이 바뀌면 초기화되는 편이 맞다. 🔵 데스크톱에서 토글을 숨기고 내용을 항상 펼치게 한 것(`:560`)도 정확하다 — DoD #8 영향 0 |
| **3** | 도달 불가능해진 `≤680px` 죽은 CSS 삭제 | ✅ **통과.** 자기가 유발한 결과를 같은 커밋에서 정리한 것이다 |

> ✅ **DoD #8(데스크톱 회귀 0)의 뜻을 여기서 못박는다 — §11 #8 문구 개정.**
> **"레이아웃·구조의 동일성"이지 "픽셀 동결"이 아니다.** 문구 오류 수정·접근성 수정·죽은 코드 제거처럼
> **모바일 작업과 무관하게 옳은 변경**은 막지 않는다. 🔴 다만 **의도한 차이임을 walkthrough에 적어야**
> 하고(wt174가 그렇게 했다), 스크린샷 대조 때 **그 항목을 미리 알고 봐야** 한다.

> 🟡 **함께 정리 — `FarewellNotice.tsx:15`의 `fontSize: '0.9rem'`.** 옛 코드에서 그대로 옮겨온 값인데
> 루트 16px에서 **14.4px**라 §4.5-2의 140곳 중 하나다. 고지문은 본문 성격이므로 `var(--fs-body)`.
> **Phase 1.6에서 함께 닫힌다** — 새로 만든 파일이라 눈에 띌 뿐, 이 커밋이 만든 문제는 아니다.

#### 8.1-2 `EndingNotePage` — ✅ 목업 방향 **확정**

| 뷰 | 구성 |
| :--- | :--- |
| **Mobile** | 🔴 **8개 아코디언 동시 스택 → 목차 리스트.** 행마다 **제목 + 완료/미작성**만. **아이콘·프리뷰 텍스트 제거.** 탭 → **포커스 리더 모달**(그 섹션 하나만, 저장 버튼 하단 고정) |
| | ⑨ 유언장 초안 → **리스트 밖 별도 카드.** 🔴 *"유언장이 아님"* 고지(`NOT_A_WILL_NOTICE`)는 **상단 차단 배너에서 그 카드 안으로 이동**(원칙 4) |
| **Desktop** | 아코디언 유지 (`06-04` §6.1-1) + 🟡 좌측 섹션 목차 고정 |
| ✅ 공짜 | `00-35` §5.2 `sectionBodies[code]`를 모달에 넣으면 끝. **본문 8개 분리(§10.4) 재개 불필요** |
| 🔴 주의 | 입력 상태 22개는 **전부 부모에**(§6.2 #1). `sectionPayloads`를 `useMemo`로 감싸지 않는다(`00-35` §5.3) |

#### 8.1-3 `CareGuidePage` — ✅ 목업 방향 **확정** (§9 인터랙션 포함)

| 뷰 | 구성 |
| :--- | :--- |
| **Mobile** | 🔴 **구간 박스 > 카테고리 박스 > 항목 3중 중첩 제거.** ①상단: **가로 스크롤 카테고리 칩**(§6.4 3조건 준수 — 필터일 뿐 유일 경로 아님) ②**시간축 섹션 구분선**(접기 없음 — 구간 제목 + 1px 선) ③**여유 있는 체크리스트 행**(§9) |
| **Desktop** | `07-04` §8-7-3 **구간 접기 + 지금 구간만 펼침** 유지 |
| ✅ 선행 | `07-04` §8-9(id 23 이동 · id 12 `conditional`+제목)는 **wt163에서 구현 완료.** 🟡 단 *"id 12 `conditional`이 화면에 표시되지 않는다"* 는 편차가 Opus 판단 대기로 남아 있다(`context.md`) — **분리 전에 닫는다** |
| 🔴 상단 배너 | `07-04` §4.3 배너 **1개만** 남긴다(원칙 4). §8-8-3 ②(되돌릴 수 없는 것 요약 띠)는 칩 트랙과 **합친다** — 둘 다 상단에 두면 크롬이 다시 2단이 된다 |

### 8.2 그룹 2 — 폼·입력형

| 페이지 | 지침 |
| :--- | :--- |
| **`ObituaryPage`** (743줄·인라인 87 — 🔴 최다) | 🟡 **재판정**(§6.5 ⓓ). 모바일은 **1열 수직 폼 · 섹션 사이 40px · 필드 라벨 상단 배치**. 🔵 **관리 모드는 §8.2-1로 이관 — 2026-09-09 완료**. `00-29` §15가 **미검증으로 남긴 화면** — 실기동 검증 필수 |
| **`MyPage`** | ✅ 분리. 🔴 `.stat-row` 3칸 카운터가 `00-29` §15의 미검증 1순위다. 모바일은 **3칸 유지 + `tabular-nums`**(`00-09` §6.3 #4), 4자리 이상이면 **2×2 또는 세로 스택**. `width:'84px'` 고정 프로필 카드(`00-29` §2.2) 제거 |
| **`MyObituaryListPage`** | ❌ 분리하지 않음. **1열 카드 스트림 + 카드 내부 탈박스화**로 충분 |
| **`MyPageFamilyDesignation`** | ❌ 분리하지 않음. 🔴 **모달 → 바텀시트 전환**: ≤768px에서 `position:fixed; bottom:0; border-radius:16px 16px 0 0; max-height:88dvh; overflow-y:auto`. 좌우 `--gutter-chrome`. 🔴 **`dvh`를 쓴다** — `vh`는 모바일 주소창 때문에 잘린다. 🟡 이 전환은 **모달 공통 규칙**으로 만들어 `LoginModal`·`InquiryModal`·`SummaryModal`·`AddressSearchModal`이 함께 쓴다 |

#### 8.2-1 🆕 `ObituaryPage` 관리 모드 — ✅ **완료** (2026-09-09 wt179, 사람 지시)

> 🔴 **사람이 UX 판단으로 직접 지시한 확정 사항이다.** 스펙이 먼저 서지 않았을 뿐 편차가 아니다 —
> 이 절은 구현된 결과를 정본에 옮겨 적는 것이고, **다음 세션이 여기를 근거로 삼는다.**

부고장이 이미 있으면(`obituaryRef`) **미리보기·공유가 먼저** 보이고, 개설 폼은 뒤로 물러난다.

```
[데스크톱 ≥769px]                          [≤768px — 자동 줄바꿈]
┌────────────┐ ┌────────────┐              ┌────────────┐
│ 카톡 카드   │ │  공유 패널  │              │ 카톡 카드   │
│ 미리보기    │ │  420px     │      →       └────────────┘
│  ⊕수정 칩  │ │            │              ┌────────────┐
│ flex 432px │ │            │              │  공유 패널  │
└────────────┘ └────────────┘              └────────────┘
      ↓ 클릭 = 모달(560px)
```

| | 스펙 |
| :--- | :--- |
| **레이아웃** | 🔴 **`.auto-grid`가 아니라 `flex-wrap`** — 좌 `flex: 0 1 432px` + 우 `420px`, `gap: 1rem`, `justifyContent: center`, `alignItems: flex-start` |
| **왜 `.auto-grid`를 버렸나** | `.auto-grid`는 컬럼을 `1fr`로 **늘려 채운다**(`00-29` §6.1). §6.4-1의 `min-width:1200px` 이후 각 컬럼이 콘텐츠(432/420px)보다 훨씬 넓어졌고, 그 안에서 `margin:0 auto`로 중앙 정렬해 둔 탓에 **두 콘텐츠 사이에 (컬럼 여유폭 + gap)만큼 빈 띠**가 생겼다. 🔴 **gap을 줄여도 안 좁아진다** — 여백이 컬럼 안쪽에 있었기 때문 |
| 🔵 **모바일 대응** | 두 섹션 폭 합(432+420+16=**868px**)이 768px를 넘으므로 **줄바꿈이 공짜로 온다.** 미디어쿼리도, 분리도 필요 없다 → §6.5 ⓒ의 *"배치만 = CSS"* 사례 |
| **수정 진입** | 🔴 카드 아래 별도 버튼 → **카드 우상단 아이콘 칩**(`.obituary-edit-chip`, 사람이 시안 5개 중 선택). 호버·`:focus-visible`에 **40px → 104px 가로 확장** + `수정하기` 라벨 노출. `width:auto`는 보간이 안 되므로 **고정폭으로 전환**한다 |
| **수정 폼** | 인라인 → **모달**(`maxWidth:560px`·`maxHeight:90vh`·`overflowY:auto`·배경 `rgba(0,0,0,.65)`+`blur(4px)`·`zIndex:3000`) |
| **카톡 카드 미리보기** | 🔴 **실사진 대조로 정정**(`assets/obituary_card.png`) — 배경 `#DCE3E8`(쿨톤 대안·사람 확정) · 최대폭 **360px** · **왼쪽 정렬**(카톡 수신 메시지 형태) · 카드 폭 20%↑(67→80%) |

🆕 **2026-09-09 — 세 번째 섹션은 만들지 않는다.** 조문객 화면 미리보기는 **공유 패널의 버튼 + 모달**로
간다(정본 = `07-03` §6.4). 🔴 **3열이 폭에 안 맞는다** — 432 + 360 + 420 + gap ≈ **1284px** 로 §6.4-1의
`.container` 하한 1200px을 넘어 줄바꿈되고, **공유 패널이 아래로 떨어져 이 화면의 목적이 밀린다.**
🔵 **모달이라 이 문서가 판정할 것이 없다** — 레이아웃이 안 바뀌므로 관리 모드는 **2섹션 그대로**다.

🟡 **`--min-touch-target`(56px)와의 관계**: 칩은 기본 **40px**이다. `ObituaryPage`는 Phase 3이므로
§4.5-5의 터치타깃 3건과 **함께** 다룬다(Phase 3 착수 시). 🔴 지금 단독으로 고치지 않는다 —
관리 모드는 **상주 본인만 보는 화면**이라 조문객 동선의 터치 리스크와 급도가 다르다.

### 8.3 그룹 3 — 매칭·탐색형

세 페이지 모두 ❌ **분리하지 않는다.** `.auto-grid` 프리미티브(`00-09` §3.2)가 이미 1열로 접는다.

| 페이지 | 지침 |
| :--- | :--- |
| 공통 | **그리드 → 1열 카드 스트림.** 카드 내부는 원칙 3(탈박스화): 테두리 1겹, 그림자 1단계 |
| **`FacilityPage`** | 🔴 `00-29` §13 미이행 2건을 **여기서 닫는다**: ①모바일 1페이지 **30 → 15건** ②이미지 없는 시설의 *"등록된 이미지 없음"* 플레이스홀더 **≤480px 숨김**(카드 640px 중 150px). 목표: 1페이지 19,698px → **≤10,000px** |
| **`CounselingPage`** | `minmax`·`repeat` 각 1곳 → `.auto-grid`. 🟡 `TaxSimulatorModal`은 §8.2 바텀시트 공통 규칙 적용 |
| **`DigitalEstatePage`** | 198줄에 인라인 41 — **밀도 최고**. §4.5 치환 효과가 가장 큰 화면 |

### 8.4 그룹 4 — 홈·랜딩형

| 페이지 | 지침 |
| :--- | :--- |
| **`HomePage`** | ❌ 분리하지 않음. 🔴 **`00-23` §5.4-1(`body` 네이티브 스크롤)·§8.3·§8.5가 정본이다 — 이 문서가 홈 레이아웃을 다시 정하지 않는다.** 적용하는 것은 §4 토큰과 거터뿐. `EntryBoxes`는 `useIsMobile(767)`로 API만 통일(§5.2) |
| **`ObituaryLandingPage`** | ❌ 분리하지 않음. ✅ **이미 모바일 우선으로 잘 돼 있다**(`00-29` §2.3, `maxWidth:360~480` 중앙 정렬). 🔴 **거터만 맞추고 구조를 건드리지 않는다** — 조문객 100%가 모바일로 보는 유일한 화면이라 회귀 비용이 가장 크다 |
| **`MemorialLandingPage`** | ❌ 분리하지 않음. 토큰·거터 적용. 🟡 `MemorialPage` 본체는 **범위 밖**(`00-29` §8 #4 — 슬러그 미정, 추모관 재구현과 함께) |

---

## ✅ 9. 상중행정가이드 체크리스트 — 인터랙션 모델 **확정**

> 요청: *"체크박스를 유지할지, 탭=완료이고 상세는 별도 진입인지 등 대안 검토. 목업의 임시 수정은
> 최소 패치일 뿐 최종 패턴 판단 필요."*

### 9.1 대안 4개와 판정

| 안 | 내용 | 판정 |
| :---: | :--- | :--- |
| A | 체크박스 유지 · 체크박스만 히트 영역 (**현행**) | ❌ **15px 히트 영역**은 `--min-touch-target`(56px) 정면 위반. 목업의 15px 축소는 폭 확보가 목적이었으나 **터치 정확도를 대가로 냈다** |
| B | **탭 = 완료** · 상세는 별도 진입(원칙 5 적용) | ❌ 🔴 **감출 상세가 없다.** `07-04` §8-8이 아코디언을 없애 카드는 이미 2줄이고, 11개는 펼쳐도 빈 서랍이었다. **2단계를 다시 씌우면 §8-8이 되돌아온다** |
| C | 체크박스 없이 **탭 = 완료**, 완료는 색·취소선으로 | ❌ `00-23` §8.7 **색만으로 구분 금지**. 완료 상태의 시각 어포던스가 사라져 *"눌리는 것인지"* 를 6070이 판단할 근거가 없다 |
| **D** | ✅ **체크박스 유지 · 히트 영역을 행 전체로** · 액션은 별도 버튼 | ✅ **채택** |

### 9.2 ✅ 채택안 D — 스펙

```
┌─────────────────────────────────────────┐
│ ☐  한정승인 또는 상속포기   [3개월] ⭐되돌릴 수 없음  │  ← 행 전체가 토글 (≥56px)
│    지나면 빚을 그대로 물려받습니다                │
│                            [전문가 상담 →]     │  ← 별도 히트 영역
└─────────────────────────────────────────┘
 ▎← 좌측 3px 색선 (CRITICAL만)
```

| 항목 | 값 |
| :--- | :--- |
| 체크박스 **시각** 크기 | **20px** (목업 15px에서 **정정**) |
| **히트 영역** | 🔴 **행 전체**, 최소 높이 `var(--min-touch-target)` = **56px** |
| 행 탭 동작 | **완료 토글** |
| 액션 링크(6개 항목) | 🔴 **별도 `<button>`**, `stopPropagation`. 자체 히트 영역 ≥44px |
| 행 사이 | 구분선 1px, 상하 패딩 `var(--sp-3)`(12px) |
| 항목 좌우 | `var(--gutter-page)`(28px) — 체크박스 포함 |
| 텍스트 폭 | 360 − 28×2 − (20 + 12) = **272px** |
| 중복 화살표 | 🔴 **제거**(목업 결정 유지) — 행 자체가 탭 대상이므로 화살표는 거짓 신호다 |

> 🔵 **목업의 15px를 20px로 되돌려도 텍스트 폭은 잃지 않는다.** 폭 손실의 원인은 체크박스 크기가 아니라
> **중복 화살표와 3중 박스 패딩**이었고, 그 둘이 사라진다. 20 vs 15px의 차이는 272px 중 **5px(1.8%)** 다.
> 대신 얻는 것이 **히트 영역 15px → 56px**이다. 🔴 **작은 것을 지키려고 큰 것을 내주지 않는다.**

### 9.3 🔴 CRITICAL 표기 — `07-04` §5.1 정정

목업의 *"좌측 3px 색선 + 별 하나"* 는 **글자 배지를 뺐다.** `00-23` §8.7과 `07-04` §5.1이
*"색만으로 구분하지 않는다 — ⭐ + 글자 + 테두리 3중"* 을 이미 정해 두었다.

✅ **모바일 확정형**: **⭐ + `되돌릴 수 없음` 글자 + 좌측 3px 색선** — 셋 다 남기되,
글자 배지를 **박스형 배지에서 인라인 텍스트로** 바꿔 기한 배지와 **한 줄에** 둔다(원칙 3 탈박스화).
🔵 *"굵은 테두리 → 3px 색선"* 은 채택한다. 줄인 것은 **테두리 굵기이지 정보가 아니다.**

### 9.4 ⏸ 저장(F단계)과의 관계

체크 상태 저장(`07-04` §3.4·§8-7-4 F단계)은 **`00-19` 제4조 확정에 막혀 있다.**
🔵 **이 인터랙션 모델은 F단계와 독립이다** — 프론트 전용이므로 **먼저 가도 된다**(`07-04` §8-8-5).
🔴 다만 저장이 붙기 전까지는 *"체크했는데 없어졌다"* 가 남는다는 사실이 바뀌지 않는다.
**완료 개수를 성과처럼 강조하지 않는다**(§8-8-3 ③ 분모 변경은 F단계 뒤로).

---

## 🚀 10. 실행 순서 — Phase 1~4

🔴 **한 Phase = 한 세션 = 한 커밋 초안.** 페이지를 섞지 않는다.

### Phase 1 — 기반 (1회, 화면 구조 변경 없음)

| # | 작업 | 파일 |
| :---: | :--- | :--- |
| 1 | 토큰 신설·개정 (§4.1·§4.2·§4.3) | `index.css` `:root` |
| 2 | 🔴 조치 A 철회 + `body` 정정 (§4.4 #1~#4·#6) | `index.css` |
| 3 | 🔴 **소형 `rem` 리터럴 318곳 치환** (§4.5) | 38개 파일 |
| 4 | `.container` 거터 분리 + 헤더·드로어 `--gutter-chrome` | `index.css` · `Header.tsx` · `Sidebar.tsx` |
| 5 | `useIsMobile` 훅 신설 + 기존 호출부 3곳 통일 (§5) | `hooks/useIsMobile.ts` |

> 🔴 **#2와 #3은 반드시 같은 커밋이다**(§4.5). 나누면 그 사이 배포본의 본문이 12.8px가 된다.
> 🔵 **여기까지가 `00-09` §6.4의 P-1·P-2와 같은 성격**이다 — 화면을 건드리지 않고 값만 정렬한다.

### Phase 1.5 — 🔴 선행 마감 2건 (Phase 1의 남은 꼬리. 각 10분)

Phase 1(wt173)이 넘긴 것들이다. **Phase 2 ①·② 착수 전에 닫는다.**

| | 작업 | 근거 |
| :---: | :--- | :--- |
| **A** | ✅ **완료(wt174)** — `index.css` `font-size` 7곳 + `.stat-row__label` `0.7rem` 규칙 삭제 | §4.5-1 |
| **B** | ✅ **완료(wt174)** — `CareGuidePage.tsx:231` `items[0].conditional` 삭제 | `07-04` §8-9-2-1 |

### Phase 1.6 — 🔴 기계적 치환 2건 (Phase 2 ② 착수 전)

§4.5의 치환 규칙이 좁게·넓게 각각 어긋난 것을 닫는다. **판단이 필요 없는 값 치환뿐이고, 화면 구조를
건드리지 않는다.** 🔴 **C가 DoD #2 미달을 남기고 있어 차단급이다.**

| | 작업 | 규모 | 근거 |
| :---: | :--- | :---: | :--- |
| **C** | 🔴 `0.9rem`·`0.95rem` **fontSize** → `var(--fs-body)` | **140곳 / 38파일** (운영자 3화면 18곳 제외 → 122곳). `index.css` 10곳 **포함** | §4.5-2. **DoD #2가 아직 통과하지 않았다** |
| **D** | 🟡 **간격 속성**의 `var(--fs-body)`→`var(--sp-4)` · `var(--fs-caption)`→`var(--sp-3)` | **132곳 / 37파일** | §4.5-3. P-5 훅이 거부할 자리 |

> 🔵 **C와 D를 같은 커밋에 넣어도 된다** — 서로 겹치지 않는다(C는 `fontSize`만, D는 간격 속성만).
> 🔴 **D를 먼저 하면 안 된다** — C가 `fontSize`의 `--fs-body`를 늘리는데, D가 `--fs-body`를 지우는
> 작업이라 순서를 바꾸면 서로를 덮는다. **C → D.**

### Phase 1.7 — 🔴 §4.5-4 구간 치환 (Phase 2 ② 착수 전, C·D와 같은 성격)

Phase 1.6이 `잔여 0`으로 끝났는데도 **DoD #2가 여전히 미통과**다. C·D가 틀린 게 아니라 §4.5·§4.5-2의
**열거식 표가 그 사이 값을 안 잡았다**(→ §4.5-4). 판단은 없다 — 구간 규칙 하나로 닫는다.

| | 작업 | 규모 | 근거 |
| :---: | :--- | :---: | :--- |
| **E** | 🔴 `fontSize` **`0.8rem` 이상 `1rem` 미만** → `var(--fs-body)` | **44곳** (tsx 41 + `index.css` 3 = `0.82`~`0.98rem` 8종) | §4.5-4 |
| **F** | 🔴 `fontSize` **`0.8rem` 미만** → `var(--fs-caption)` | **1곳** (`FooterMobile.tsx:97` `0.78rem`) | §4.5-4 |
| **G** | 🟡 푸터 카피라이트 예외 → `var(--fs-caption)` | **1곳** (`Footer.tsx:190`. `FooterMobile.tsx:97`은 F가 이미 처리) | §4.5-4 ⓐ **E보다 뒤** |

> 🔴 **순서 E → F → G.** G가 E의 결과를 되돌리는 자리(`Footer.tsx:190`의 `0.82rem`)를 하나 포함한다.
> **G를 먼저 하면 E가 다시 16px로 덮는다.** E·F·G를 합치면 **45곳 = §4.5-4가 센 전량**이다(G는 E의 1곳을 덮는다).
> 🔵 **재현 grep**(운영자 3화면 제외):
> `grep -rnE "fontSize:\s*['\"]0\.(7[0-9]?|8[0-9]?|9[1-9])rem['\"]" eobomDev/frontend/src --include=*.tsx`
> `grep -nE "font-size:\s*0\.(7[0-9]?|8[0-9]?|9[1-9])rem" eobomDev/frontend/src/index.css`
> **완료 기준 = 두 grep 모두 잔여 0**(운영자 3화면 제외분만 남는다).

🔴 **터치 타깃 36px 3곳(§4.5-5)은 여기 넣지 않는다** — Phase 3.

> ✅ **2026-09-09 — Phase 1.6·1.7 완료**(wt177·178, 커밋 `879407c`·`24e551a`). C·D·E·F·G 전량,
> 위 재현 grep **둘 다 잔여 0** · 빌드 통과. 🟡 남은 것은 **실기동 확인 3건**(360px · 1280px 회귀 ·
> `FooterMobile` 패널 높이) 중 **`FooterMobile` 패널 높이는 사람이 완료**(09-09).

### Phase 1.8 — 🔵 **계획 밖에서 들어온 것** (2026-09-09 wt179, 사람 지시)

Phase 순서에 없던 작업이 사람 지시로 먼저 들어왔다. **되돌리지 않는다** — 둘 다 §6.5 ⓒ 기준으로 옳다.

| | 작업 | 정본 |
| :---: | :--- | :--- |
| **H** | 데스크톱 최소폭 하한(`min-width:1200px`) + `.main-wrapper` 가로 스크롤 패널 | **§6.4-1** |
| **I** | `ObituaryPage` 관리 모드 2열(미리보기+수정 칩→모달 / 공유) · 카톡 카드 실사진 대조 | **§8.2-1** |

> 🔵 **I가 Phase 3(④)을 앞당긴 것은 아니다** — ④는 **개설 폼 본체**의 적응형 분리이고, I는 관리 모드
> 배치다. 🔴 다만 ④ 착수 시 **§6.5 ⓓ 재판정의 재료가 하나 늘었다**(관리 모드는 분리 없이 됐다).

### Phase 2 — 그룹 1 (텍스트·감성형) · **적응형 분리 3건**

```
① FarewellMessagePage   ← ✅ 완료(wt174). 상세 스펙 §8.1-1 ⓐ~ⓕ. 선행 A
② CareGuidePage          ← ▶ 다음. 선행 B
③ EndingNotePage         ← 가장 큼(901줄). 파일럿·②가 선 뒤에
```

### Phase 3 — 그룹 2·3

```
④ ObituaryPage (분리 — 🟡 §6.5 ⓓ 재판정. 관리 모드는 §8.2-1로 이미 완료)  ⑤ MyPage (분리)
⑥ 모달 → 바텀시트 공통 규칙 (MyPageFamilyDesignation 기준, 모달 5종에 적용)
⑦ MyObituaryListPage · FacilityPage · CounselingPage · DigitalEstatePage (분리 없음)
   └ FacilityPage는 00-29 §13 미이행 2건 동반
```

### Phase 4 — 그룹 4 + 재발 방지

```
⑧ HomePage · ObituaryLandingPage · MemorialLandingPage (토큰·거터만)
⑨ 🔴 드리프트 차단 훅 — 00-09 §6.4 P-5
   거부 대상: 하드코딩 HEX · 스케일 밖 padding/gap · fontSize에 raw rem 리터럴
```

> 🔴 **P-5가 없으면 Phase 1~4는 되돌아온다.** `00-09` §2.4의 *"되도록"* 이 그렇게 무너졌고,
> 인라인이 982 → 1,304개로 늘었다. **이 문서도 예외가 아니다.**

### 10.1 🔴 순서를 바꾸면 안 되는 것

| 반드시 먼저 | 이유 |
| :--- | :--- |
| Phase 1 #2 → #3 (**같은 커밋**) | §4.5 |
| Phase 1 전체 → Phase 2 | 토큰 없이 뷰를 나누면 두 벌 다 고쳐야 한다 |
| `07-04` §8-9 편차 종결 → `CareGuidePage` 분리 | §8-9 본체는 wt163에서 구현됐으나 **id 12 `conditional` 무표시 편차**가 열려 있다. 데이터·라벨이 흔들리는 중에 뷰를 나누면 충돌 |
| `FarewellMessagePage` → `EndingNotePage` | 리더 모달 패턴을 작은 화면에서 먼저 세운다 |

---

## 🧪 11. 완료 검증 기준 (DoD)

🔴 **`.harness/done.md`를 통과하는 것과 별개로, 아래 9개를 전부 만족해야 "완료"다.**
측정은 **360px · 768px · 1280px** 세 폭에서 한다.

| # | 기준 | 측정 |
| :---: | :--- | :--- |
| **1** | 🔴 문서 가로 오버플로 **0** | 360px에서 `document.documentElement.scrollWidth ≤ 360`. 🔵 `overflow-x:auto` 트랙(§6.4)과 `.mobile-drawer-panel`은 제외 |
| **2** | 🔴 본문 텍스트 **≥16px** · 보조 **≥14px** · **위반 0** | 360px 렌더에서 `getComputedStyle(el).fontSize` 전수. 운영자 3화면 제외 |
| **3** | 본문 좌우 **28px** · 헤더 좌우 **16px** | `getBoundingClientRect` 실측 |
| **4** | 본문 블록 행간 **≥1.8** | `getComputedStyle(el).lineHeight / fontSize` |
| **5** | 🔴 터치 타깃 **≥56px** (행·주요 버튼) · 보조 액션 **≥44px** | 실측 |
| **6** | 섹션 사이 세로 간격 **≥40px** | 실측 |
| **7** | 🔴 **중첩 표면 0** — 그림자/테두리 있는 면 안에 같은 것이 없음 | `00-09` §6.3 #3 |
| **8** | 🔴 **데스크톱 회귀 0** — 1280px에서 **레이아웃·구조가 작업 전후 동일** | 페이지별 육안 대조. 🔵 **픽셀 동결이 아니다**(§8.1-1 ⓖ) — 문구 오류·접근성 수정, 죽은 코드 제거처럼 **모바일과 무관하게 옳은 변경**은 허용하되 **walkthrough에 미리 적는다** |
| **9** | 🔴 **뷰 전환 시 입력값 소실 0** | 폼 페이지에서 입력 중 **폭을 767↔769로 리사이즈** 후 값 유지 |

> 🔴 **#9는 적응형 분리에만 있는 새 결함 유형**이다. §6.2 #1을 어기면 여기서만 잡힌다.
> 🔴 **로그인 후 화면은 에이전트가 닫을 수 없다**(`00-29` §15). `MyPage`·`ObituaryPage`·`EndingNotePage`의
> #1~#7은 **사람이 실기기로 로그인해** 확인한다. `00-26` §6 #9(375px 헤더)도 그때 함께 닫는다.

---

## ❓ 12. 확정이 필요한 것 / 하지 않는 것

### 12.1 🔴 확정 필요 — 3건 (전부 **비차단**, Phase 1 착수 가능)

| # | 항목 | 권고 |
| :---: | :--- | :--- |
| 1 | **본문 16px가 6070 페르소나에 충분한가** — `00-09` §2.3을 뒤집는 결정이다 | 🟡 **16px로 간다.** 실제로 나가던 것은 12~13.6px였고(§3.2), 행간 1.8·좌우 28px·1열 리더가 크기보다 크게 작동한다. **사람이 실기기로 본 뒤 한 번 되짚을 것** |
| 2 | **PWA를 이 개편에 붙이나** (`00-29` §14가 *"조치 A·B 배포 뒤 반나절"* 로 권고) | 🟡 **Phase 4 뒤 별건.** 이번 범위에 넣지 않는다 — 원인이 다르고(§14 자인), 섞으면 #8 회귀 판정이 흐려진다 |
| 3 | **`MemorialPage` 본체** | ⏸ **범위 밖 유지**(`00-29` §8 #4) — 추모관 재구현과 함께 |

### 12.2 ❌ 이 문서가 하지 않는 것

- ❌ **URL·라우팅 변경** · ❌ **모바일 전용 기능·동선**(`00-29` §3 ②)
- ❌ **색·브랜드 토큰 변경** — `00-09` §6.2-1·§6.2-2 그대로
- ❌ **운영자 3화면**(`AdminPage`·`BizDashboard`·`PartnerPortalPage`) — `00-09` §2.5 밀도 우선.
  🔵 **§6.4-1의 최소폭 하한도 여기서 자동으로 빠진다**(`.main-wrapper` 미사용)
- ❌ **백엔드·데이터 모델** — `07-04` §8-9(데이터)는 이 문서 밖의 선행 작업이다
- ❌ **성능·번들** — 별건

---

## 🔗 13. 관련 문서

| 문서 | 관계 |
| :--- | :--- |
| `00-09` §2.3·§6.2 | 🔴 **이 문서가 개정**(§7) — 색·간격·모서리·그림자·모션 토큰은 그대로 |
| `00-09` §3.2 · §6.4 · §6.5 | 프리미티브 4종 유지 · P-1~P-5 이관 계획과 Phase가 맞물림 |
| `00-29` | 🔵 **원인·실측의 정본.** §12 조치 A만 철회 |
| `00-35` §5.2 · §6 | `sectionBodies` 재사용(§8.1-2) · 프론트 전역 규칙 |
| `06-04` §6.1-1 | 🟡 모바일만 개정(§7) |
| `07-04` §4.3·§5.1·§8-8·§8-9 | 🟡 모바일 형태·CRITICAL 표기 정정(§7·§9.3) |
| `00-23` §5.4-1·§8.3·§8.5·§8.7 | 홈 레이아웃·색만으로 구분 금지 — **정본 그대로** |
| `00-26` §6 #9 | 375px 헤더 미검증 — DoD #2에서 함께 닫음 |
| `00-06` | 화면 ID 신설 없음 |
| `reports/mobile_360px_preview.html` · `text_heavy_whitespace_solutions.html` · `farewell_messages_redesign.html` | 시안 (읽기 전용) |
