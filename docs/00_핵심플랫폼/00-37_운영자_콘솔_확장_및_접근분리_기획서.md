# 00-37. 운영자 콘솔 확장 및 접근 분리 기획서

> **[구현 기획서 · 2026-09-07]** 개발자 지시 — *"admin 계정의 대시보드가 지금 부실한데 추가할 기능 및 기획 필요.
> admin 페이지에서 서비스와 관련된 거의 모든 것을 다룰 수 있었으면 좋겠음.
> 추후에 보안 관련 `/admin` 주소가 아닌 별개의 페이지를 만드는 것도 검토."*
>
> 화면 ID **`SCR-010`**(`00-06` §3). 짝 문서 = **`00-36`**(마이페이지).
> 🔴 **결론: "거의 모든 것"으로 넓히기 전에 먼저 그어야 할 선이 3개 있습니다**(§3). 그 선 없이 화면만 늘리면 **개인정보 노출면이 화면 수만큼 커집니다.**

---

## 📌 1. 결론 먼저

| | |
| :--- | :--- |
| **점검 방법** | `AdminPage.tsx`(875줄) ↔ `adminRoutes.ts`(20개 엔드포인트) ↔ `Admin` 모델 3중 대조 (2026-09-07 실측) |
| **부실한 이유 ①** | 🔴 **이미 만든 API 4개에 화면이 없습니다** — 추모관 심사·디지털 플랫폼 카탈로그·상담 신청 조회·회원 상세. **서버는 됐는데 못 씁니다** |
| **부실한 이유 ②** | 로그인하면 곧바로 *"사업자 심사"* 탭입니다. **오늘 처리할 일이 몇 건인지 알려주는 홈이 없습니다** |
| **부실한 이유 ③** | 다루는 범위가 **심사·승인 5종**뿐입니다. 회원·부고장·정산·감사로그는 화면 자체가 없습니다 |
| 🔴 **먼저 할 일** | 기능 추가가 아니라 **①열람 경계 ②감사로그 ③권한 등급**(§3). 지금은 셋 다 없습니다 |
| **접근 분리 답** | 🟡 **경로 변경(`/admin`→다른 주소)은 효과가 거의 없습니다.** 실효는 **별도 서브도메인 + 별도 빌드**(§5 B안). 다만 **인프라 확정 대기**에 종속 |

---

## 🔍 2. 현황 실측

### 2.1 화면 — 탭 5개

`AdminPage.tsx` `QueueTab = 'PARTNERS' | 'EXPERTS' | 'CLAIMS' | 'FACILITIES' | 'FAREWELL_PURGE'`

| 탭 | 하는 일 |
| :--- | :--- |
| 사업자 심사 | 목록 · 승인/거절 · 정보 수정 |
| 전문가 심사 | 목록 · 승인/거절 · 공개 전환 · 정보 수정 |
| 시설 연동(클레임) | 목록 · 승인/거절 |
| 전체 시설 조회 | 조회만 |
| 유족메시지 파기 🆕 | 만료 목록 · 건별 선택 실행 · 아카이브 2단계 (`06-05` §5.6-8-3) |

### 2.2 🔴 API는 있는데 화면이 없는 것 — 4건

| 엔드포인트 | 무엇 | 화면 |
| :--- | :--- | :---: |
| `GET /admin/memorials` · `PATCH /:id/review` · `PATCH /:id/guestbook/:gid/hide` | **추모관 신고 심사 · 방명록 숨김** | ❌ |
| `GET /admin/digital-platforms` · `POST` · `PATCH /:id` | **디지털 플랫폼 카탈로그 관리**(04 도메인 마스터 데이터) | ❌ |
| `GET /admin/consult-requests` | 상담 신청 전체 조회 | ❌ |
| `GET /admin/users/:id` | **회원 상세**(개인정보 열람) | ❌ |

> 🔴 **특히 추모관 신고 심사입니다.** 공개 추모관(`/m/:slug`)에 신고 버튼이 이미 붙어 있고
> (`MemorialLandingPage.tsx`), 신고가 들어오면 **처리할 화면이 없습니다.**
> 오픈 후 첫 악성 방명록이 들어오는 순간 바로 드러납니다.

### 2.3 🔴 계정·보안 실측

| 항목 | 현재 | 판정 |
| :--- | :--- | :--- |
| `Admin` 모델 필드 | `email`·`passwordHash`·`refreshTokenHash`·`name`뿐 | 🔴 **권한 등급 없음** — 계정이 곧 최고권한 |
| 계정 생성 | `prisma/seed-admin.ts`로만 (공개 가입 없음) | ✅ 적절 |
| 인증 가드 | 🔴 **라우터 미들웨어가 아니라 핸들러마다 `verifyAdminBearerToken(req)` 직접 호출** | 🔴 **구조적 위험** — 새 엔드포인트에서 한 줄 빠지면 그대로 공개 |
| 서명 키 | 🔴 **사용자 토큰과 같은 `JWT_SECRET`**, `aud: 'admin'` 클레임으로만 구분 | 🟡 검증은 `aud`를 확인하므로 지금은 안전. 다만 키 하나가 새면 양쪽이 함께 무너짐 |
| 토큰 수명 | access `2h` / refresh **`30d`** | 🟡 운영자에 30일은 깁니다 |
| 로그인 보호 | 🔴 실패 횟수 제한·잠금·2단계 인증 **없음** | 🔴 |
| 감사 로그 | **`FarewellPurgeAuditLog` 하나뿐** — 파기 외에는 누가 무엇을 승인·거절·조회했는지 **기록 0건** | 🔴 |
| 화면 노출 | 어디에도 링크 없음, 직접 URL만 | 🟡 §5 참조 — **숨김은 보안이 아닙니다** |

---

## 🔴 3. 넓히기 전에 그어야 할 3개의 선

> *"거의 모든 것을 다룬다"* 는 요구는 옳습니다. 다만 **무엇을 다루는가**를 먼저 정의해야 합니다.

### 3.1 🔴 첫 번째 선 — **상태는 다루고, 내용은 보지 않는다**

이어봄이 보관하는 것 중 **운영자도 봐서는 안 되는 것**이 있습니다.

| 자산 | 운영자가 볼 수 있는 것 | 🔴 볼 수 없는 것 | 근거 |
| :--- | :--- | :--- | :--- |
| 유족 메시지(편지) | 존재 여부 · 건수 · 만료일 · 삭제 상태 | 🔴 **본문**(`bodyEnc`) | `06-05` §8 Phase B |
| 음성 | 존재 여부 · 용량 · 만료일 | 🔴 **재생·다운로드** | `06-05` §5.6-1 |
| 엔딩노트 | 섹션 완료 수 · 상태 | 🔴 **항목 내용** | `06-03` §7 `select` 명시 규칙 |
| 회원 개인정보 | 🟡 **필요할 때만**, 기록을 남기고 | — | §3.2 |

- 🔴 **이 선은 "안 보여주기"로 지키지 않습니다.** 화면에서 감춰도 API가 내려주면 새는 것입니다.
  `06-03` §7의 **`select` 명시 규칙**(응답에 담을 필드를 일일이 적는다)을 **운영자 API 전체에 적용**합니다.
- 🔵 이 선이 있어야 *"거의 모든 것"* 이 안전하게 성립합니다. 운영자 콘솔은 **내용 열람 도구가 아니라
  상태 관리 도구**입니다.

### 3.2 🔴 두 번째 선 — **조회도 기록한다** (`AdminAuditLog` 신설)

`GET /admin/users/:id`는 **회원 1명의 개인정보를 여는 API**인데 **누가 언제 누구를 열었는지 기록이 없습니다.** 화면이 없어서 지금까지 문제가 안 됐을 뿐이고, §4에서 회원 관리 화면을 만들면 그날부터 실제 열람이 시작됩니다.

**남길 것**: 운영자 ID · 시각 · 행위(`VIEW`/`APPROVE`/`REJECT`/`SUSPEND`/`PURGE`) · 대상 종류 · 대상 ID · 사유(선택)
🔴 **대상의 내용은 로그에 남기지 않습니다** — 로그가 두 번째 유출 경로가 됩니다.

> 🔵 **모델을 새로 만들지 않아도 됩니다.** `FarewellPurgeAuditLog`(`06-05` §6.5)가 이미 같은 목적으로
> 서 있습니다. **그것을 일반화할지, 나란히 둘지**는 §7 확정 #2.

### 3.3 🔴 세 번째 선 — **권한 등급 2단계**

지금은 운영자 계정이 하나의 등급이고, 그 계정으로 **파기 실행**까지 됩니다. 파기는 되돌릴 수 없습니다.

| 등급 | 할 수 있는 것 |
| :--- | :--- |
| `OPERATOR` (운영) | 심사(사업자·전문가·클레임) · 신고 처리 · 조회 |
| `SUPERADMIN` (최고) | 위 전부 + **파기 실행** · **회원 정지/탈퇴 처리** · **정산·수수료 값 변경** · 운영자 계정 관리 |

🔵 계정이 몇 개 없더라도 등급은 지금 넣는 편이 쌉니다 — 나중에 넣으면 **모든 엔드포인트를 다시 훑어야** 합니다.

---

## 🗺️ 4. 운영자 콘솔 기능 지도 — "거의 모든 것"의 정의

> 🔵 **표의 "현재" 칸이 이 문서의 답입니다.** ⬜ = 신설 · 🔶 = API만 있고 화면 없음 · ✅ = 있음

| # | 영역 | 화면(탭) | 하는 일 | 현재 |
| :---: | :--- | :--- | :--- | :---: |
| **0** | **홈 대시보드** | 홈 | 🔴 오늘 처리할 큐 **건수**(심사 대기·신고 접수·파기 만료) · 최근 가입 · 최근 오류 | ⬜ |
| 1 | 회원 | 회원 | 목록·검색 · 상세 · **정지/해제** · 탈퇴 요청 처리 | 🔶 상세 API만 |
| 2 | 부고장 | 부고장 | 목록 · 신고 처리 · **강제 종료** · 공유/조회 집계 | ⬜ |
| 3 | 추모관 | 추모관 | **신고 심사** · 방명록 숨김 · 보존기간(동결·연장) | 🔶 API 3개 |
| 4 | 06 엔딩노트·보관함 | (파기) | 🔴 **건수·상태만**(§3.1) · 파기 실행 | ✅ 파기 탭 |
| 5 | 01 시설 | 시설 | 전체 조회 ✅ · 클레임 심사 ✅ · **리드·정산** | ✅ / ⬜ 정산 |
| 6 | 02 전문가 | 전문가 | 심사 ✅ · 공개 전환 ✅ · **상담 신청 처리** | ✅ / 🔶 |
| 7 | 04 디지털 | 카탈로그 | **플랫폼 카탈로그 CRUD**(마스터 데이터) | 🔶 API 3개 |
| 8 | 03 유품수거 | — | 목업이라 관리 대상 없음 | — |
| 9 | 정산·수수료 | 정산 | `CommissionPolicy` 값 · `Settlement` 처리 | ⬜ 🔒 |
| 10 | **감사로그** | 로그 | §3.2 기록 열람·검색 | ⬜ |
| 11 | 운영자 계정 | 설정 | 계정 목록 · 등급(§3.3) · 비밀번호 변경 | ⬜ |

🔒 **#9는 착수 불가** — 수수료 산정 기준 4건이 `pending-approvals.md` 대기 중입니다(`01-05` §10). 값이 정해져야 화면이 섭니다.

---

## 🔐 5. 접근 분리 검토 — *"`/admin` 아닌 별개의 페이지"*

### 5.1 🔴 먼저: 지금 방식의 한계

현재는 *"어디에도 링크하지 않고 직접 URL로만"* 입니다. 이것은 **은폐이지 차단이 아닙니다.**

🔴 **더 중요한 사실** — `AdminPage.tsx`는 **일반 사용자와 같은 프론트 번들에 함께 빌드됩니다.**
경로를 바꿔도 **번들 안에 운영자 화면 코드와 API 경로 문자열이 그대로 들어 있습니다.**
누구나 받아볼 수 있습니다. 그러니 **경로 변경만으로는 얻는 것이 거의 없습니다.**

### 5.2 선택지 3개

| 안 | 무엇 | 얻는 것 | 비용 | 판정 |
| :---: | :--- | :--- | :--- | :--- |
| **A** | 경로만 변경(`/admin` → 추측하기 어려운 주소) | 🟡 자동 스캐너 노이즈 감소 | 낮음 | 🟡 **단독으로는 권장 안 함**(§5.1) |
| **B** | **별도 서브도메인 + 별도 빌드**(`admin.eobom.kr`, 운영자 프론트를 다른 번들로) | ✅ 사용자 번들에서 운영자 코드가 **완전히 빠짐** · 쿠키/스토리지 출처 분리 · CSP·CORS를 따로 조일 수 있음 | 중간 (빌드 2개, 배포 2개) | ✅ **권장** |
| **C** | B + **네트워크 앞단 차단**(Cloudflare Access 등 IP·SSO 게이트) | ✅ **로그인 화면 자체가 외부에 안 뜸** — 앱 코드가 아니라 앞단에서 막음 | 중간 (이미 Cloudflare 사용 중) | ✅ **최종형** |

### 5.3 🔴 순서 — 지금 할 수 있는 것과 없는 것

**B·C는 인프라 확정 대기에 종속됩니다.** 도메인·배포 위치가 `pending-approvals.md`에서 대기 중이고, 백엔드는 Render `oregon`↔DB 서울로 갈려 있어 **이전이 예정된 상태**입니다. 지금 서브도메인을 붙이면 **이전 때 두 번 일합니다.**

```
지금 (인프라와 무관)                          인프라 확정 후
─────────────────────────────                ─────────────────────
① 라우터 가드 미들웨어화 (§2.3)               ④ B안 — 서브도메인 + 별도 빌드
② AdminAuditLog (§3.2)                       ⑤ C안 — 앞단 차단
③ 권한 등급 (§3.3)                            ⑥ (선택) 경로 변경은 이때 같이
```

🔵 **①②③이 오히려 실질 보안입니다.** ④⑤는 노출면을 줄이지만, 가드 누락 한 줄과 기록 없는 열람은
서브도메인을 나눠도 그대로 남습니다.

---

## 🛠️ 6. 구현 단계

> ✅ **순서 확정(2026-09-07 개발자) — A-1을 먼저 합니다.** 화면부터 붙이는 A-2 선행안은
> 채택하지 않았습니다. 🔵 근거: A-2 #8(회원 관리)이 **개인정보 열람 화면**이라, 기록 장치(§3.2)와
> 권한 등급(§3.3)이 서기 전에 그 화면을 열면 **열람이 시작된 뒤에 로그를 붙이게** 됩니다.

### A-1 — 기반 (🔴 선행, 기능 추가 아님)

1. 🔴 **`adminRoutes.ts`에 라우터 레벨 인증 미들웨어** — `router.use()`로 한 번에 걸고, 각 핸들러의 개별 호출을 정리. 🔴 `/login`·`/refresh`는 제외
2. 운영자 API 응답 전수에 **`select` 명시**(§3.1) — 특히 회원·추모관
3. 🔴 **`AdminAuditLog` 신설**(§3.2) — 스키마 변경 = `db-safety.md` 백업 + **CONFIRM**
4. 🔴 **`Admin.role` 추가**(§3.3) — 기존 계정은 `SUPERADMIN`으로 채움

### A-2 — 이미 만든 API에 화면 붙이기 (서버 변경 거의 없음)

5. **추모관 탭** — 신고 심사 · 방명록 숨김 (🔴 오픈 전 필수, §2.2)
6. **디지털 플랫폼 카탈로그 탭** — CRUD
7. **상담 신청 탭** — 조회
8. **회원 탭** — 🔴 목록 API는 신설 필요(`GET /admin/users`), 상세는 기존 재사용. **열람 시 감사로그 기록**

### A-3 — 홈 대시보드

9. `GET /admin/dashboard` — 큐 건수 집계(심사 대기·신고 접수·파기 만료·최근 가입). 🔴 **건수만**, 내용 미포함
10. 로그인 후 첫 화면을 홈으로

### A-4 — 부고장 관리 + 감사로그 열람

11. 부고장 목록·신고·강제 종료 · 12. 감사로그 화면

### A-5 — 접근 분리 (⏸ 인프라 확정 후)

13. B안(서브도메인 + 별도 빌드) → 14. C안(앞단 차단)

### A-6 — 정산 (🔒 승인 대기)

15. `pending-approvals.md` 수수료 4건 확정 후

---

## 🔴 7. 확정이 필요한 것

| # | 무엇 | 권장안 | 막는 단계 |
| :---: | :--- | :--- | :---: |
| **1** | **운영자가 회원 개인정보를 열람할 수 있는 범위** | 🟡 **연락처까지, 사유 입력 필수, 전건 기록.** 🔴 처리방침(`00-19`)에 *"운영자 접근 통제"* 문구가 있는지 대조 필요 | A-2 #8 |
| **2** | `AdminAuditLog`를 **신설할지 `FarewellPurgeAuditLog`를 일반화할지** | 🟡 **신설 후 파기로그는 그대로 둠.** 파기로그는 `06-05` §6.5가 필드를 특화해 뒀고, 합치면 그 문서까지 흔들립니다 | A-1 #3 |
| **3** | **권한 등급 2단계로 충분한가** | 🟡 충분. 계정 수가 한 자리인 동안 3단계는 관리비용만 늘어납니다 | A-1 #4 |
| **4** | **접근 분리 시점** | 🟡 **인프라 확정 후 B+C 한 번에.** 지금 A안(경로 변경)만 하면 효과 없이 북마크·문서만 흔듭니다 | A-5 |
| **5** | **운영자 refresh 30일** | 🟡 **단축 권장**(예: 12시간~1일). 운영자 세션이 한 달 살아 있을 이유가 없습니다 | A-1 |
| ~~**6**~~ | ~~운영 `JWT_SECRET` 환경변수 설정 여부~~ | ✅ **해소(2026-09-11, 사람 확인)** — Render 환경변수에 설정되어 있음. 기본값 서명 아님 | ~~A-1~~ |

---

## 🔗 8. 관련 문서

- `00-06` §3 `SCR-010` — 화면 ID 정본 · 탭 구성
- `06-05` §5.6-8-3 · §6.5 — 파기 탭 · 파기 감사로그(§3.2의 선례)
- `06-03` §7 — **`select` 명시 규칙**(§3.1의 근거)
- `01-05` §6.4 · §10 — 운영자 계정 생성 · **수수료 확정 대기**(A-6)
- `00-11` §8 — 인프라 확정(A-5가 종속) · `pending-approvals.md`
- `00-19` — 개인정보 처리방침(§7 확정 #1 대조 대상)
- `00-36` — **마이페이지** (짝 문서)
