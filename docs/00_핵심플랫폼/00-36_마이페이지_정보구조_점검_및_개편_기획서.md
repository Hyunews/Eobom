# 00-36. 마이페이지 정보구조 점검 및 개편 기획서

> **[구현 기획서 · 2026-09-07]** 개발자 지시 — *"마이페이지에 현재 우리 기능을 충분히 담았는가 확인 및 기획."*
> 화면 ID **`SCR-012`**(`00-06` §1). 짝 문서 = **`00-37`**(운영자 콘솔).
> 🔴 이 문서는 **점검 결과가 본론**입니다. 결론부터: **담지 못했습니다. 구현이 끝난 기능 4개가 마이페이지에서 보이지 않습니다.**

---

## 📌 1. 결론 먼저

| | |
| :--- | :--- |
| **점검 방법** | 화면(`MyPage.tsx`) ↔ 라우트(`App.tsx`) ↔ 사용자 소유 모델(`schema.prisma`) **3중 대조** (2026-09-07 코드 실측) |
| **판정** | 🔴 **미달**. 마이페이지가 노출하는 사용자 자산은 **11개 중 4개**뿐입니다 |
| **가장 큰 구멍** | **유족 메시지 보관함** — `06-05` D-1~D-11이 전부 구현됐는데(음성·반출까지) **마이페이지에 입구가 없습니다** |
| **두 번째** | **숫자는 보여주는데 열 수 없습니다** — "문의 내역 3"을 눌러도 아무 일이 일어나지 않습니다 🔄 *(부고장 칸은 wt144로 해소, 문의·상담 2칸 남음)* |
| **법적 구멍** | 🔴 **회원 탈퇴 창구가 없습니다.** `06-05` §5.4-2가 30일 유예 탈퇴를 확정해 뒀는데 **화면·API 0건** |
| **성격** | 신규 개발이 아니라 **이미 만든 것을 잇는 작업**입니다. 1단계는 서버 변경 0건 |

---

## 🔍 2. 현황 실측 — 지금 마이페이지에 있는 것

`MyPage.tsx`(2026-09-07 기준)

| 구역 | 내용 | 상태 |
| :--- | :--- | :--- |
| 프로필 카드 | 이름 · 사진(`Camera`) · **계정 연동** 버튼 | ✅ 실동작 |
| 통계 3칸 | 문의 내역 · 상담 내역 · 내 부고장 (`GET /api/me/summary`) | 🔄 **부고장만 링크**(wt144) · 문의·상담은 갈 곳 없어 클릭 불가 |
| 카드 ① | **내 정보** (연락처·주소·연락 가능 시간대) → 모달 | ✅ 실동작 (`00-28`) |
| 카드 ② | **가족 지정** → 모달 | ✅ 실동작 (`00-27`) |
| 카드 ③ | **나의 예약 현황** | ⬜ `comingSoon` 배지 · 클릭 불가 → 🔄 **"문의 내역"으로 교체 확정**(§6 #1) |
| 카드 ④ | **상담 신청 내역** | ✅ 실동작 |
| 카드 ⑤ | **디지털 엔딩노트** | 🔶 `preview` 배지 |

> 🔄 **2026-09-07 wt144 반영** — 헤더의 *"내 부고장"* 직행 메뉴를 없애고 경로를
> **`/my-obituaries-memorials`**(부고장·추모관 반반 화면)로 바꿨습니다. 개발자 지시로
> **마이페이지가 그 화면의 유일한 통로**가 됐습니다(화면 스펙 정본 = `00-06` §8, 같은 날 갱신).
> → 통계 "내 부고장" 칸이 링크가 됐으므로 **§3.2가 지적한 막다른 길은 문의·상담 2칸만 남았습니다.**

---

## 🔴 3. 갭 분석 — 사용자가 가진 것 ↔ 마이페이지가 보여주는 것

**사용자 1명이 이 서비스에서 실제로 소유하는 레코드**를 모델 기준으로 전수 나열하고, 마이페이지 노출 여부를 대조했습니다.

| # | 사용자 자산 | 모델 · API | 화면 실체 | 마이페이지 | 판정 |
| :---: | :--- | :--- | :--- | :---: | :--- |
| 1 | 프로필(연락처·주소) | `User` · `/api/me/profile` | 모달 | ✅ | — |
| 2 | 소셜 계정 연동 | `SocialAccount` | 모달 | ✅ | — |
| 3 | 가족 지정 | `FamilyDesignation` | 모달 | ✅ | — |
| 4 | 상담 신청 | `ConsultRequest` | 인라인 목록 | ✅ | — |
| 5 | **엔딩노트** | `EndingNote`·`EndingNoteEntry` | `/ending-note` | 🔶 `preview` 배지 | 🟡 배지 재판정 필요(백엔드 배선됨) |
| 6 | 🔴 **유족 메시지 보관함** | `FarewellMessage`(+음성 `mediaKey`) | `/farewell-messages` | ❌ **없음** | 🔴 **최우선** |
| 7 | **내 부고장·추모관** | `Obituary`·`Memorial` · `/api/me/obituaries`·`/api/me/memorials` | `/my-obituaries-memorials` · `/memorial` | 🔄 **숫자만 링크**(wt144) | 🟡 카드 필요 |
| 8 | 🔴 **디지털 자산 정리** | `DigitalCleanupItem` · `/api/me/cleanup-items` | `/digital-estate` | ❌ 없음 | 🔴 |
| 9 | **문의 내역** | `Lead` | ❌ **목록 API 없음** | 🟡 숫자만 | 🔴 숫자가 막다른 길 |
| 10 | 작성한 시설 리뷰 | `FacilityReview` | ❌ 조회 경로 없음 | ❌ | 🟡 비차단 |
| 11 | 유품 수거 | *(레코드 없음 — 목업)* | `/pickup` | ❌ | ✅ **안 넣는 게 맞음** |
| **12** 🆕 | **내가 남긴 방명록** | `MemorialGuestbook`(`userId`) · ❌ 목록 API 없음 | 추모관 안에만 | ❌ | 🔴 **넣음**(2026-09-21 사람 결정) |
| **13** 🆕 | 내가 남긴 헌화 | `MemorialTribute`(`userId`) | 〃 | ❌ | ✅ **안 넣음**(2026-09-21 사람 결정 — 기록으로 남기지 않음) |
| **14** 🆕 | **나에게 공유된 엔딩노트** | `FamilyDesignation.acceptedUserId` + `EndingNoteGrant` · ✅ `GET /api/ending-note/family-view` **구현됨** | ❌ 화면 없음 | ❌ | 🔴 **넣음** — §4.6 |

> 🔵 **#14는 성격이 다릅니다** — 내가 **소유한** 것이 아니라 **받은** 것입니다. 그래서 B구역이
> 아니라 **C구역(나에게 공유된 것)** 을 새로 만듭니다(§4.1).
> 🔵 **#12·#13이 갈린 이유**(2026-09-21 사람 결정): 방명록은 **내가 쓴 글**이라 되찾아볼 값이
> 있지만, 헌화는 **누른 행위**여서 모아 보여줄 내용이 없습니다. 🔴 둘 다 **회원 글만** 해당합니다
> (`userId`가 `null`인 비회원 행은 애초에 조회되지 않습니다).

### 3.1 🔴 #6·#7이 왜 심각한가

**`06-05`(유족 메시지 보관함)는 2026-09-07 기준 D-1~D-11이 전부 구현됐습니다** — 편지 저장·음성 녹음·R2 업로드·듣기·소프트 삭제·반출(zip)·단건 반출까지. 그런데 **사이드바에만 있고 마이페이지에는 입구가 없습니다.**

마이페이지는 *"내가 이 서비스에 무엇을 남겨 뒀는가"* 를 확인하는 자리입니다. 이어봄에서 **사용자가 남기는 것 중 가장 되돌리기 어려운 것**(사후에만 열리는 편지·음성)이 바로 그 자리에서 빠져 있습니다.

같은 논리로 **부고장·추모관**도 카드가 없습니다. 🔄 다만 통계 *"내 부고장"* 칸이 **wt144에서 클릭 가능해져** `/my-obituaries-memorials`로 연결됐고, 동시에 **헤더 메뉴가 삭제돼 마이페이지가 그 화면의 유일한 통로**가 됐습니다(`00-06` §8.2-1).

> 🔴 **그래서 M-1의 성격이 바뀌었습니다.** 통계 칸 하나가 **화면 하나를 통째로 물고 있습니다.**
> B구역 카드(§4.1)는 이제 *"있으면 좋은 것"* 이 아니라 **그 단일 진입점을 이중화하는 안전장치**입니다.
> 🔴 구역을 재배치할 때 **통계 칸의 링크를 떨어뜨리면 화면 전체가 닫힙니다.**

### 3.2 🔴 #9 — 세기만 하고 못 여는 숫자

`summaryController.ts`는 `lead.count`·`consultRequest.count`·`obituary.count` 3개를 셉니다. 이 중 **`Lead`만 목록 API가 없습니다.** 숫자는 보이는데 내역을 볼 방법이 서비스 전체에 없습니다.

> 🔵 **6070 대상 서비스에서 "눌러도 안 되는 숫자"는 고장으로 읽힙니다**(`00-24`·`00-29`의 일관된 지적).
> 셋 다 링크가 되거나, 링크가 안 되는 것은 숫자를 빼는 편이 낫습니다.

### 3.3 🔴 회원 탈퇴 — 화면도 API도 없다

| 근거 | 요구 | 현재 |
| :--- | :--- | :---: |
| `06-05` §5.4-2 · §5.4-2-1 | **30일 유예 후 파기**(소프트 삭제), 그 사실을 이용자에게 알림 | ❌ |
| `00-19` 제4조 | 탈퇴 시 회원 정보 지체 없이 파기 | ❌ |
| `00-19` 제9조 | 열람·정정·삭제·처리정지 요구권 | 🟡 **"서면·전자우편"** 으로만 규정 |

스펙은 확정돼 있는데 **구현이 0건**이고, 마이페이지에 **자리 자체가 없습니다.** 처리방침이 이메일 창구를 규정하고 있어 *게시* 는 막지 않지만, 탈퇴 버튼이 없는 서비스는 오픈 후 민원이 곧바로 들어옵니다.

---

## 🧱 4. 개편 설계 — 마이페이지를 "내 자산 허브"로

### 4.1 3구역 구조

지금은 카드 5장이 **성격 구분 없이** 한 줄로 늘어서 있습니다(내 정보 → 가족 지정 → 예약 → 상담 → 엔딩노트). 자산이 11개로 늘면 이 배열은 읽히지 않습니다.

```
┌─ 프로필 카드 (이름·사진·계정 연동)          ← 현행 유지
├─ 통계 2칸             상담 · 부고장                        ← 🔄 "문의" 칸 제거(§6 #5)
├─ A. 나                내 정보 · 가족 지정
├─ B. 내가 남긴 것       엔딩노트 · 유족 메시지 보관함 · 내 부고장·추모관 ·
│                       디지털 정산 · 내가 남긴 방명록          ← 🆕 방명록
├─ C. 나에게 공유된 것    나를 가족으로 지정한 분                 ← 🆕 2026-09-21 (§4.6)
└─ D. 내 활동과 계정     내 상담 내역 · 카카오톡으로 문의하기 ·
                        개인정보·동의 · 내 데이터 반출 · 회원 탈퇴 · 로그아웃
```

- 🔴 **B구역이 이 개편의 핵심입니다.** *"내가 남긴 것"* 이라는 이름 자체가 이어봄의 값어치이고,
  지금 그 구역이 통째로 비어 있습니다.
- 통계 3칸은 **없애지 않되 링크를 겁니다.** 각 카드에도 **건수 배지**를 답니다(편지 3통 · 부고장 1건).
🔄 **2026-09-21 갱신 — 구역이 3개에서 4개가 됐습니다.** 바뀐 것만 적습니다.

| 무엇 | 전 | 후 | 근거 |
| :--- | :--- | :--- | :--- |
| 통계 | 3칸(상담·문의·부고장) | **2칸**(상담·부고장) | 문의가 우리 DB에 없는 채널이 됨 → §6 #5 |
| "문의 내역" 행 | `comingSoon` 배지 | **"카카오톡으로 문의하기"**(외부 링크·숫자 없음) | 〃 |
| "상담 신청 내역" 행 | 신청 화면(`counseling`)으로 감 | **내 상담 내역 목록**(§4.4) | 지금은 *내역* 이라 적고 신청 화면을 여는 **오연결** |
| "디지털 자산 정리" | — | **"디지털 정산"** 으로 개명 | 2026-09-21 사람 지시 |
| C구역 | 없음 | **나에게 공유된 것** 신설 | 자산표 #14 |
| 로그아웃 | 헤더·사이드바에만 | **D구역 맨 아래에도** | 🔴 `00-39` §6 규칙 3이 사이드바를 없애면 **모바일에 진입점이 사라집니다** — §3.1의 *"단일 진입점 이중화"* 와 같은 논리 |
| 프로필 사진 배지 | 금색 카메라 *"(개발중)"* | ✅ **제거**(2026-09-21) | 서버에 `profileImage`를 **바꾸는 경로가 없습니다** — `profileController`는 읽기만 합니다. `00-23` §2.2의 *"곧 나올 기능"* 전제를 채우지 못합니다 → `00-28` §1.1 |
| 금색 액센트 | 히어로·배지 | ✅ **제거**(2026-09-21) | 🔴 `00-39` §3 색 팔레트에 **금색이 없습니다.** 시안 v3에서 빠진 것과 같게 맞춘 것이 맞습니다 |

- ✅ **"나의 예약 현황" 카드는 "문의 내역"으로 교체**(2026-09-07 개발자 확정) — C구역으로 내려갑니다.
  🔵 카드를 없애지 않고 **자리를 옮겨 쓰는 것**이라, `00-23` §2.2의 *"준비 중 명시"* 배지도
  **다시 정당해집니다** — 예약과 달리 문의 목록은 **M-2에서 실제로 나오는 기능**이기 때문입니다.

### 4.2 🔴 각 카드가 지켜야 할 것 — 건수는 세되 내용은 안 보여준다

| 카드 | 배지에 쓸 수 있는 것 | 🔴 쓰면 안 되는 것 |
| :--- | :--- | :--- |
| 유족 메시지 보관함 | 편지 **수**, 수신자 **수** | 🔴 편지 본문·수신자 실명 미리보기 |
| 엔딩노트 | 작성한 섹션 **수** | 🔴 항목 내용 |
| 내 부고장·추모관 | 건수, 진행/종료 상태 | — |

> 근거: 편지 본문(`bodyEnc`)·엔딩노트 항목은 **암호화 저장**이며 목록 API가 본문을 안 내려줍니다
> (`06-05` §8 Phase B 완료 판정). 카드 배지 때문에 목록 API에 본문을 얹지 않습니다.

### 4.3 회원 탈퇴 — C구역 맨 아래, 별도 확인 흐름

`06-05` §5.4-2의 확정 스펙을 화면으로 옮긴 것입니다.

```
[회원 탈퇴]  ← 눈에 띄되 맨 아래
   ↓
① 무엇이 지워지는지 목록으로 보여준다 (편지 n통 · 음성 n건 · 부고장 n건 · 추모관 n건)
② 🔴 "반출부터 하시겠습니까" — 내 데이터 반출(zip)로 유도. 되돌릴 수 없기 때문
③ 30일 유예 고지 — 🔄 **"30일 안에 다시 로그인해 *계속 이용* 을 선택하시면 취소됩니다"**(2026-09-21 정정)
④ 확인 → 삭제대기 상태 전환 (🔴 이 시점에 실제로 지우지 않는다)
```

> 🔄 **2026-09-21 문구 정정** — 원안은 *"로그인만 하면 되돌릴 수 있습니다"* 였는데 M-3이
> *"**계속 이용**을 눌러야 비운다"* 로 정했습니다. **두 곳이 어긋나 있었고, M-3이 맞습니다** —
> 모르고 로그인했다가 탈퇴가 **조용히 취소되면 안 됩니다.** 화면도 M-3을 따른 것이 맞습니다.

- 🔴 **런타임에서 파일을 지우지 않습니다**(`06-05` §5.4-2-1) — 실삭제는 승인 배치(`SCR-010` 파기 탭)만.
- 🔴 **추모관은 함께 지워지지 않습니다.** 추모관 보존은 `00-20`(13개월 동결·3년 파기)이 따로 정하고
  있고 **조문객의 방명록이 남아 있습니다.** 탈퇴자 1명의 의사로 타인의 기록을 지울 수 없습니다
  → **§6 확정 #2.**

---

### 4.4 🆕 내 상담 내역 — **두 테이블을 한 목록으로** (2026-09-21)

`Lead`(업체 상담)와 `ConsultRequest`(전문가 상담)는 **사용자에게 같은 것**입니다 — 2026-09-10
사람 정정이 통계를 `상담 = consultCount + leadCount`로 합친 것이 그 근거입니다. 목록도 합칩니다.

| 표시 | `Lead` | `ConsultRequest` |
| :--- | :--- | :--- |
| 접수번호 | `leadNo`(`EB-`) | `requestNo`(`EC-`) |
| 어디에 | 시설명(없으면 *"비제휴 업체"*) | 전문가명 + `categorySnapshot` |
| 어떻게 | `type` 견적/상담/전화 | `channel` 알림톡/전화/화상/방문 |
| 언제 | `createdAt` | `createdAt` |
| 상태 | 아래 4단계로 접어서 | 〃 |

🔴 **내부 상태를 그대로 보여주지 않습니다.** `CONVERTED`·`LOST`·`billable`은 **정산용**이고
사용자에겐 뜻이 통하지 않습니다(`01-05` 영역). 네 단계로 접습니다.

| 보이는 것 | `Lead` | `ConsultRequest` |
| :--- | :--- | :--- |
| **접수됨** | `REQUESTED` `NOTIFIED` | `REQUESTED` |
| **진행 중** | `RESPONDED` | `ACCEPTED` |
| **완료** | `CONVERTED` | `COMPLETED` |
| **종료** | `LOST` `INVALID` | `CANCELLED` `INVALID` |

🔴 **응답 화이트리스트** — `billable`·`billedAmount`·`commissionPolicyId`·`settlementId`·
`disputeReason`·`partnerId`·`applicantPhone`·`payload` **원문**을 내리지 않습니다.
🔵 `payload`를 보여주려면 **서버가 `type`별 한 줄 요약**으로 만들어 내립니다.
🔴 `Lead`는 **비회원 문의도 담는 테이블**이라 `where: { userId }` 가 빠지면 남의 문의가 보입니다.
🔵 마스킹된 건(`maskedAt != null`)도 **본인 목록에는 남습니다** — 애초에 스냅샷 필드를 안 내리므로
표시가 달라지지 않습니다.
빈 목록이면 *"아직 신청하신 상담이 없습니다"* + 상담 신청 버튼.

#### 4.4-1 🔴 **`CALL`은 목록에 넣지 않습니다** — 상담이 아니라 클릭입니다 (2026-09-21 정정)

§4.4가 `type`을 그대로 열거한 탓에 **전화 버튼 클릭이 *"접수됨"* 으로 떴습니다.** 잘못입니다.

`01-05` §4.1은 `CALL`을 **"❌ 익명 이벤트 · 기본 비청구"** 로, §9는 *"클릭했다고 통화한 것이
아니고 통화했다고 계약한 것도 아니다"* 로 규정합니다. **접수된 상담이 아닙니다.**

🔴 **게다가 구현이 문서와 다릅니다** — `leadController.createCallEvent`가 `userId`를 붙여
저장합니다(로그인 상태면). 문서는 **익명**이라고 했습니다. **청구에도 안 쓰는 이벤트에 개인
식별자를 남길 이유가 없습니다**(`00-19` 최소수집).

| 고칠 것 | 어떻게 |
| :--- | :--- |
| ① 적재 | 🔴 `createCallEvent`에서 **`userId`를 넣지 않습니다**(항상 `null`) → `01-05` §4.1과 일치 |
| ② 조회 | 🔴 `GET /api/me/leads`에 **`type: { not: 'CALL' }`** — ①을 고쳐도 **이미 쌓인 행**이 남아 있습니다 |
| ③ 기존 행 | 🔵 **지우지 않습니다.** `updateMany`는 DB 쓰기라 백업+CONFIRM 대상인데, ②로 화면에서 사라지므로 **급할 이유가 없습니다** |

##### 🟡 그러면 카카오톡 상담은 남겨야 하나 — **지금은 남기지 않습니다**

🔴 **같은 이유로 걸립니다.** 우리가 아는 것은 **버튼을 눌렀다**뿐이고, 대화를 했는지·해결됐는지는
모릅니다. `CALL`을 뺀 근거가 그대로 적용됩니다.
🔴 **`Lead`에 얹지 않습니다.** `Lead`는 **수수료 청구의 증거** 테이블(`01-05` §4)인데 카톡 채널은
시설·전문가 **어디에도 귀속되지 않는 전사 채널**이라 넣을 자리가 없습니다. 비청구 이벤트를 더
얹으면 정산 테이블이 지표 테이블로 흐려집니다.
✅ **정말로 세야 하면 §6 #6(상담톡 API)으로 제대로 셉니다.** 클릭 수는 그 대체재가 아닙니다 —
*"몇 명이 눌렀나"* 와 *"몇 건이 상담으로 이어졌나"* 는 다른 숫자입니다.
🔵 **클릭 지표가 필요하면** 도메인 테이블이 아니라 **웹 분석 이벤트**(별도 지표 계층)로 셉니다.

### 4.7 🆕 내가 남긴 방명록 — `SCR-021` (2026-09-21 사람 지시)

API만 만들고 화면을 안 만들면 **행을 눌러도 갈 곳이 없습니다.** 화면을 정의합니다.

| 열 | 내용 |
| :--- | :--- |
| 어디에 | 추모관(고인) 성함 — 누르면 `/m/:slug` 열기 |
| 무엇을 | **내가 쓴 글 한 줄**(말줄임) |
| 언제 | `createdAt` |

🔵 **본문을 보여도 됩니다.** §4.2의 *"내용은 안 보여준다"* 는 **암호화 저장된 사적 기록**(편지·
엔딩노트)에 대한 규칙입니다. 방명록은 **애초에 공개 글**이라 해당하지 않습니다.
🔴 숨겨진 글은 목록에서도 뺍니다 — `deletedByOwnerAt`(상주 삭제)·`hiddenAt`(운영자) 둘 다.
🔴 **비회원 글은 애초에 조회되지 않습니다**(`userId`가 `null`).
빈 목록이면 *"아직 남기신 글이 없습니다"* 한 줄. 🔴 권유 문구를 붙이지 않습니다.

#### 4.7-1 ✅ 내 글 삭제 — 허용하되 **소프트 삭제**, 시점은 M-3과 함께 (§6 #8 확정)

자기가 쓴 글을 거둘 수 없으면 표현의 철회가 막힙니다. **허용합니다.** 다만 방식이 정해져 있습니다.

- 🔴 **소프트 삭제**입니다 — 이 테이블은 이미 `deletedByOwnerAt`·`hiddenAt` **둘 다 원문을
  보존**합니다. 본인 삭제만 하드 삭제로 두면 혼자 다른 규칙이 됩니다.
- 🔴 **`deletedByAuthorAt` 를 새로 만듭니다.** `deletedByOwnerAt`을 재사용하면 *"상주가 지웠다"*
  로 기록이 오염됩니다.
- 🔴 **개설자용 `DELETE /:id/guestbook/:gid`를 쓰지 않습니다**(§6 #8) — 권한 판정이 섞입니다.
  **`DELETE /api/me/guestbook-entries/:id`** 를 따로 둡니다.
- 🔵 **스키마 변경이므로 M-3 마이그레이션에 묶습니다** — 백업·CONFIRM을 두 번 하지 않습니다.
  **`SCR-021` 1차는 읽기 전용**으로 내고, 삭제 버튼은 그때 켭니다.


### 4.5 🆕 개인정보·동의 — **역할별이 아니라 성격별 3층** (2026-09-21)

> 사람 요청은 *"역할별 on/off"* 였는데, 코드를 전수 확인한 결과 **끌 수 있는 동의는 하나뿐**이고
> 전문가·사업자 계정에는 **동의 필드가 아예 없습니다**. 그래서 축을 바꿔 정리합니다.

| 층 | 항목 | 화면에서 |
| :--- | :--- | :--- |
| **계정 필수** | `termsAgreedAt`(약관) · `privacyAgreedAt`(개인정보 수집·이용) | 🔴 **토글 없음.** 동의한 **날짜만** 표시 + *"동의를 거두시려면 회원 탈퇴가 필요합니다"* → 탈퇴 행으로 |
| **선택** | `marketingAgreedAt` | ✅ **토글 1개.** 🔴 "내 정보" 모달의 체크박스를 **여기로 옮깁니다**(복제 금지 — 같은 값을 두 곳에서 고치게 됩니다) |
| **건별** | `thirdPartyConsentAt`(상담·문의 건마다) | 🔴 **토글 대상이 아닙니다.** 이미 제3자에게 **제공된 사실**이라 끌 수 없습니다. §4.4 상세에 **동의 시각만** 표시 |

- 위치: **D구역 "개인정보·동의" 행 → 모달**(`00-39` §6.8-1 규칙 21).
  🔵 "내 정보"는 **연락처·주소를 고치는 곳**이고 동의는 성격이 달라 한 모달에 섞지 않습니다.
- 🔴 **전문가·사업자 계정은 범위 밖**입니다 — `Partner`·`Expert`에 동의 필드가 없어 켜고 끌 대상이
  없습니다. 필요해지면 `02-02`·`00-37`에서 별건으로 봅니다.

### 4.6 🆕 나에게 공유된 것 — C구역 (2026-09-21)

**행은 하나입니다** — *"나를 가족으로 지정한 분"*. ⑧(공유된 엔딩노트)과 ⑨(수락한 가족 지정)은
**같은 `FamilyDesignation` 한 줄에서 나오므로**, 두 행으로 나누면 같은 것을 두 번 보여줍니다.

#### 4.6-1 화면 (`SCR-020`)

지정자별 카드 — **성함 · 관계 · `scope`(주 연락자/열람자) · 수락한 날짜 · 지금 볼 수 있는 섹션**.
섹션을 누르면 본문을 엽니다. 데이터는 `GET /api/ending-note/family-view`(**이미 구현됨**).

| 🔴 보이지 않아야 하는 것 | 이유 |
| :--- | :--- |
| 지정자 **연락처·이메일** | `00-27` §3 불변식 2 — 어떤 응답에도 평문을 넣지 않습니다 |
| 같은 노트의 **다른 수락자** | 가족 구성을 서로 알려줄 이유가 없습니다 |
| `POSTMORTEM` 섹션의 **존재 자체** | `06-04` §7.4 *"잠긴 섹션은 제목도 보이지 않는다"* — API가 이미 그렇게 내려줍니다 |

🔴 **과장 금지**(`00-27` §9.1-4 경고와 같은 것) — *"○○님의 엔딩노트를 볼 수 있습니다"* 는
사실이 아닙니다. 실제로 열리는 것은 **`IMMEDIATE`로 부여된 섹션뿐**입니다.
🔴 **빈 상태에 권유를 붙이지 않습니다** — *"아직 공유받은 것이 없습니다"* 한 줄. 누가 나를
지정했기를 **기대하게 만드는 자리가 아닙니다.**

#### 4.6-1-1 🔄 서버 응답 확장 — `scope`·`acceptedAt`을 더합니다 (2026-09-21 확인)

M-1.5 구현에서 드러났습니다 — §4.6-1이 요구한 **`scope`(주 연락자/열람자)와 수락한 날짜**가
`GET /api/ending-note/family-view` 응답에 **없습니다**(지금은 `designationId`·`ownerName`·
`relationship`·`relationshipEtc`·`entries`). ✅ **확장을 승인합니다** — M-2에서 두 필드를 더합니다.

| 추가 | 내려도 되는 근거 |
| :--- | :--- |
| `scope` | `00-27` §9.1-6 b가 **수락 화면 표시 항목**으로 확정한 셋(성함·관계·`scope`) 중 하나입니다 |
| `acceptedAt` | §2.1이 정의한 **본인의 동의 시각**입니다. 자기가 언제 동의했는지는 본인에게 보이는 것이 맞습니다 |

🔴 **그 외에는 늘리지 않습니다** — 연락처·이메일은 불변식 2, `priority`는 불변식 4(연락 순서일
뿐 상속순위가 아니라 표시하지 않습니다).

##### 🔴 문구 — *"지정 관계 · 자녀"* 는 **방향이 빠진 말입니다**

`relationship`은 **지정자가 적은 값**이라 *"자녀"* 는 **내가 그분의 자녀**라는 뜻입니다. 그런데
*"지정 관계 · 자녀"* 만 보면 **그분이 내 자녀인가**로 읽힙니다.
🔵 초대 수락 화면은 *"○○○님이 당신을 가족으로 지정했습니다 / 관계: 자녀"* 처럼 **문장이 방향을
잡아 줍니다.** 그 문장 없이 값만 목록으로 옮기면서 방향이 사라졌습니다.

| 자리 | 🔴 확정 문구 |
| :--- | :--- |
| 구간 제목 | `홍길동 님` (그대로) |
| 제목 줄 오른쪽 | **`나를 자녀로 지정 · 주 연락자`** (관계 + `scope`) |
| 카드 맨 아래 흐린 줄 | **`2026-09-14 수락함`** |

🔵 **초대 화면과 같은 말을 씁니다.** 두 화면이 다르게 말하면 같은 관계가 다른 것으로 보입니다.

#### 4.6-2 ✅ 수락 철회 — 허용합니다 (스키마 변경 없음)

수락 시각은 `00-27` §2.1이 **정보주체의 동의 시각**이라고 정의합니다. **동의에는 철회가 따라야**
합니다. 다만 새 상태를 만들지 않고 **기존 `DECLINED` 경로를 그대로 씁니다.**

1. `POST /api/family-designations/accepted/:id/withdraw`(본인만)
2. 한 트랜잭션에서 — `status = DECLINED` · `declinedAt` 스탬프 · `acceptedUserId = null` ·
   **그 `designationId`의 `EndingNoteGrant` 전부 `revokedAt`**
3. 지정자 화면에는 `acceptedAt != null && status == DECLINED` 조합으로 **"수락 후 거두심"** 으로
   구분해 표시합니다(새 필드 불필요). 🔴 **사유는 받지 않습니다**(`00-27` §9.1-6 c와 같은 규칙)

🔵 지정자 쪽 되돌리기는 이미 있습니다 — 불변식 5(본인은 언제든 하드 삭제).

---

## 🛠️ 5. 구현 단계

> 🔵 **M-1은 서버 변경 0건입니다.** 이미 있는 라우트·API를 잇기만 하면 됩니다.

### M-1 — 잇기 (프론트만 · 🔴 우선)

1. B구역 신설 + 카드 3장 추가 — **유족 메시지 보관함**(`/farewell-messages`) · **내 부고장·추모관**(`/my-obituaries-memorials`) · **디지털 자산 정리**(`/digital-estate`)
2. ✅ **완료(wt144)** — 통계 "내 부고장" → `/my-obituaries-memorials` 링크. 문의·상담은 갈 곳이 없어 숫자만 유지(§6 #3 권장안대로)
3. 카드 5장을 A/B/C 구역으로 재배치 + 구역 소제목
4. **"나의 예약 현황" → "문의 내역"으로 교체**(§6 #1 확정) · C구역으로 이동 · 배지는 `comingSoon` 유지(M-2에서 해제)
5. 엔딩노트 `preview` 배지 재판정 — 백엔드(`Entry`·`Grant`)가 배선된 뒤로 *"미리보기"* 가 아니다

> **완료 판정**: 로그인 사용자가 **마이페이지 한 화면에서** 자기가 만든 모든 것에 도달할 수 있다.
> 눌러서 아무 일도 일어나지 않는 요소가 **0개**.

### M-1.5 — 🆕 잇기 2차 (프론트만 · 2026-09-21 신설)

> 🔵 **M-1과 같은 성격**입니다 — 서버 변경 0건. 이미 있는 API·링크를 잇고 죽은 자리를 치웁니다.

5-1. 통계 **"문의" 칸 제거**(2칸으로) · C구역 *"문의 내역"* → **"카카오톡으로 문의하기"**
     (푸터와 **같은 URL·같은 `MessageCircle` 아이콘**, 새 창, 배지·숫자 없음)
5-2. **C구역 "나에게 공유된 것" 신설** + `SCR-020` 화면 — `GET /api/ending-note/family-view`는
     **이미 있습니다**(§4.6)
5-3. **D구역 맨 아래 로그아웃 행** — 사이드바 폐지(`00-39` §6 규칙 3) 대비
5-4. 아이콘 정정 — **상담 = `Send` · 문의 = `MessageCircle`**(시안 v3 기준. 현재 코드는 반대)

### M-2 — 건수·목록 (서버 소폭)

6. 🔄 **정정(2026-09-21)** — `GET /api/me/leads` **+ `GET /api/me/consult-requests`** 신설,
   **두 결과를 합쳐 "내 상담 내역" 한 목록**으로 보여줍니다(§4.4). 🔴 둘 다 `where: { userId }`
   필수 — `Lead`는 **비회원 문의도 담는 테이블**입니다. `SCR-019`
7. 🔄 **정정** — *"문의 내역"* 은 이제 `Lead`가 아닙니다(§6 #5). 대신 **"상담 신청 내역" 행의
   오연결을 고칩니다** — 지금은 *내역* 이라 적고 **신청 화면**(`counseling`)을 엽니다.
   🟡 **M-2가 미뤄지면** 그동안은 라벨을 목적지에 맞춰 *"상담 신청하기"* 로 바꿉니다 — 이름과
   목적지가 어긋난 채로 오래 두지 않습니다(§3.2가 말한 *"고장으로 읽힌다"* 와 같은 자리)
7-2. 🆕 **`family-view` 응답에 `scope`·`acceptedAt` 추가**(§4.6-1-1) + 제목 줄 문구 교체.
     🔴 **같이 고칩니다 — 이 라우트는 왕복이 너무 많습니다.** 지정 n건이면 **`1 + 3n`번**
     질의합니다(노트·권한·항목을 지정마다 따로). Render(오리건)↔DB(서울) 왕복이 회당 **~1.3초**라
     **지정 2건이면 ~9초**입니다. `summaryController`가 쓴 `$transaction` 묶음이나 `in` 질의로
     **왕복 수를 지정 건수와 무관하게** 만듭니다.
7-1. 🔄 **내가 남긴 방명록** — `GET /api/me/guestbook-entries`(회원 글만) **+ `SCR-021` 화면
     + B구역 행**(§4.7). 🔴 **API만 내고 화면을 빼지 않습니다** — 누를 곳 없는 행이 됩니다.
     🔵 삭제는 §4.7-1대로 **M-3 마이그레이션과 함께**(1차는 읽기 전용)
8. `GET /api/me/summary` 확장 — 편지 수·수신자 수·엔딩노트 섹션 수·추모관 수. 🔴 **본문 필드 금지**(§4.2)
9. 카드 건수 배지 배선

### M-3 — 회원 탈퇴 (🔴 스키마 변경 · `db-safety.md` 백업 + CONFIRM 선행)

✅ **착수 승인 — 2026-09-21 사람 지시.** `pending-approvals.md`에 올리지 않습니다. 그 파일은
**결정이 없어서 못 움직이는 것**을 담는 곳인데, 여기는 §4.3·M-3과 `06-05` §5.4-2로 **스펙이
이미 확정**돼 있고 사람이 착수를 지시했습니다.

🔴 **착수 승인 ≠ DB 실행 승인.** `AGENTS.md` §1의 CONFIRM은 **마이그레이션 직전에 따로** 받습니다.

| 순서 | 무엇 |
| :---: | :--- |
| 1 | `backup-db.ps1` 실행 → 🔴 **파일이 생겼는지 확인까지** (`db-safety.md`) |
| 2 | 사람에게 **CONFIRM** |
| 3 | `prisma migrate` |
| 4 | `node .harness/tools/generate-db-doc.js` (`done.md` 체크리스트) |

🔵 **필드 권고** — `User`에 `deletionRequestedAt`·`deletionScheduledAt`(요청 + 30일) **두 개.**
🔴 상태 문자열을 새로 만들지 않습니다 — `06-05` §5.4-2-1의 소프트 삭제가 **시각 필드 방식**이라
#10이 말한 *"새 방식을 만들지 않는다"* 를 그대로 지킵니다.
🔴 **되살리기는 자동이 아닙니다** — 유예 중 로그인하면 **복구 안내를 먼저** 띄우고, 사용자가
*"계속 이용"* 을 눌러야 두 필드를 비웁니다. 모르고 로그인해서 되살아나면 안 됩니다.
🔵 API 3개 — `POST /api/me/deletion-request` · `DELETE`(취소) · `GET /api/me/deletion-preview`
(①"무엇이 지워지는가" **건수만**, 🔴 본문 금지 — §4.2와 같은 규칙).
🔵 **지워지는 것**(건수): 편지 · 음성 · 엔딩노트 섹션 · 부고장 · **방명록** · 시설 후기 · 가족 지정.
🔴 **남는 것을 반드시 고지**: **추모관**(`00-20` 보존정책 + 타인의 방명록 — §6 #2) ·
**이미 접수된 상담/문의**(계약·정산 증거. `Lead.userId`는 `SetNull`이라 연결만 끊기고 건은 남습니다)
🔵 화면은 §4.3 4단계 그대로 + `00-39` — 동의 체크는 **규칙 18**, 진행 표시는 **규칙 19**,
모달은 **규칙 21**.

#### 4.3-1 🆕 유예 중 계정 — **서버도 막습니다** (2026-09-21 신설)

M-1.5~M-3 구현에서 드러났습니다 — 복구 안내를 **프런트에서만** 막고 있어 **API는 그대로
쓸 수 있습니다.** 스펙에 없었던 것이 맞고, 여기서 정합니다.

| | 유예 중(`deletionRequestedAt != null`) |
| :--- | :--- |
| ✅ 허용 | **조회 전부** · 탈퇴 **취소** · 데이터 **반출** · 로그아웃 |
| 🔴 차단(403) | **새 자산 생성** — 부고장 · 추모관 · 엔딩노트 작성 · 상담/문의 신청 · 방명록 작성 · 가족 지정 |

🔴 **왜 생성만 막나.** 30일 뒤 파기될 계정으로 **타인이 보는 것**(부고장·추모관·방명록)을 새로
만들면, 파기 시점에 **조문객 쪽이 깨집니다.** 반대로 조회·반출까지 막으면 *"되돌릴 수 있다"* 고
해놓고 자기 것을 못 보게 하는 셈이라 §4.3 ③과 어긋납니다.
🔵 `GET /api/auth/me` 응답에 `deletionRequestedAt`·`deletionScheduledAt`을 실어 프런트가 같은
근거로 판단하게 합니다(프런트가 따로 추측하지 않습니다).

10. `User`에 삭제대기 상태 + 유예 만료 시각 — 🔴 `06-05` §5.4-2-1의 **소프트 삭제와 같은 방식**을 쓴다(새 방식을 만들지 않는다)
11. `POST /api/me/deletion-request` · 취소(재로그인) 경로
12. §4.3 확인 흐름 UI
13-1. 🔴 **파기 배치는 아직 없습니다(2026-09-21 확인) — 다음 작업으로 승인합니다.**
   화면은 *"30일 뒤 삭제됩니다"* 라고 쓰는데 **삭제하는 코드가 없습니다.** `06-05` §5.4-2가
   *"쓴 대로 동작해야 한다"* 고 못박은 바로 그 자리입니다. 🔵 별도 실행 스크립트라 M-3 범위
   밖으로 본 판단은 맞습니다. 🔴 **실서비스 오픈 블로커**로 둡니다 — 유예 30일이라 지금 당장
   사고는 아니지만, 오픈하면 첫 30일 뒤부터 거짓이 됩니다.
13. ✅ **`06-05` §5.6-8 갱신 완료(2026-09-21)** — *"세 가지 만료"* 가 **네 가지**가 됐고 ④가 회원 탈퇴 유예 만료다. 구현은 그 표를 그대로 따른다. 🔴 **런타임에서 지우지 않는다** — `SCR-010` 파기 탭의 운영자 승인으로만(§5.4-2-1)

### M-4 — 계정 단위 반출 (후순위)

14. 지금은 보관함 zip만 있다(`GET /api/farewell-messages/export`). 계정 전체(프로필·부고장·엔딩노트 포함)로 넓힐지는 **탈퇴 흐름이 선 뒤에** 판단

---

## 🔴 6. 확정이 필요한 것

| # | 무엇 | 결정 | 막는 단계 |
| :---: | :--- | :--- | :---: |
| **1** | **"나의 예약 현황" 카드 존치 여부** | ✅ **확정(2026-09-07 개발자) — 문구 교체.** 카드를 지우지 않고 **"문의 내역"으로 바꿔 `Lead` 목록(M-2) 자리로 전용**합니다. 🔵 제거안(권장)보다 나은 점: 카드 수를 유지하면서 **죽은 자리를 살아 있는 기능에 넘겨주고**, `comingSoon` 배지도 *"곧 나올 기능"* 이라는 `00-23` §2.2 전제를 **다시 충족**합니다 | ✅ 해제 |
| **2** | **탈퇴 시 추모관·부고장 처리** | 🟡 **함께 지우지 않음.** 추모관은 `00-20` 보존정책을 따르고 방명록에 타인의 글이 있습니다. 탈퇴 화면에서 *"추모관은 남습니다"* 를 고지하고 **닫기는 따로 하게** 합니다 | M-3 |
| **3** | **문의(`Lead`) 숫자를 M-1에서 어떻게 할지** | ✅ **해결(wt144)** — 숫자를 남기고 링크만 걸지 않았습니다. #1과 짝을 이뤄 **M-2에서 카드·숫자가 함께 살아납니다** | ✅ 해제 |
| **4** | **정보주체 권리 행사 창구** | 🟡 마이페이지 하단에 `00-19` 제9조 안내 + 이메일 링크. **비차단** — 처리방침이 이미 이메일로 규정 | M-4 |
| **5** 🆕 | 🔄 **#1 재확정 — "문의"가 무엇인가** | ✅ **확정(2026-09-21 사람)** — 문의 = **카카오톡 채널**(푸터와 같은 링크)이고 `Lead`가 아닙니다. `Lead`는 **상담**에 속합니다(§4.4). 🔴 #1·#3과 M-2 #6·#7의 *"문의 = Lead"* 정의는 **폐기**합니다 | ✅ 해제 |
| **6** 🆕 | **카카오 상담톡 API 도입 여부** | 🟡 **미정 · 비차단.** 카톡 채널은 **외부 링크뿐**이라 우리 서버는 누가 말을 걸었는지 알 수 없습니다. 세려면 **비즈니스 채널 계약·심사·웹훅 수신**이 필요합니다(알림톡 발신번호와 같은 계열). 도입 전까지는 **숫자 없이 링크 행**입니다 | — |
| **7** 🆕 | **수락한 가족 지정의 철회** | ✅ **확정(2026-09-21)** — 허용합니다. 기존 `DECLINED`를 재사용해 **스키마 변경이 없습니다**(§4.6-2) | ✅ 해제 |
| **8** 🆕 | **내 방명록 글을 내가 지울 수 있나** | ✅ **확정(2026-09-21) — 허용.** 🔴 단 **소프트 삭제**(`deletedByAuthorAt` 신설)이고 **전용 엔드포인트**를 씁니다. 스키마 변경이라 **M-3과 함께** — §4.7-1 | ✅ 해제 |
| **9** 🆕 | **`SCR-019` 구현 중 스펙에 없어 구현자가 정한 것 3건** | ✅ **전부 추인(2026-09-21)** — ⓐ 응답 필드명 `no`(접수번호. `EB-`/`EC-` 접두어로 출처가 구분되므로 별도 필드 불필요) ⓑ 최신순 **100건 상한** ⓒ 페이지네이션 없음. 🔴 **단 ⓑ에 조건 하나** — 100건을 넘으면 목록 끝에 **잘렸다는 한 줄**을 답니다. 6070 대상에서 조용히 잘리면 *"내 기록이 사라졌다"* 가 됩니다 | ✅ 해제 |

> ✅ **M-1을 막는 확정은 없습니다** — #1·#3이 닫혔으므로 바로 착수할 수 있습니다.

---

## 🔗 7. 관련 문서

- `00-06` §1 `SCR-012` — 화면 ID 정본 · `SCR-018`(내 부고장·추모관)
- `00-27`(가족 지정) · `00-28`(회원 프로필) — 현행 카드 ①②의 근거
- `06-05` §5.4-2·§5.4-2-1·§5.6-8 — **탈퇴 소프트 삭제·파기 배치 정본** (M-3은 이 문서를 갱신해야 함)
- `00-19` 제4조·제9조 — 탈퇴·정보주체 권리
- `00-20` — 추모관 보존기간(§6 확정 #2의 근거)
- `00-37` — **운영자 콘솔** (짝 문서)
