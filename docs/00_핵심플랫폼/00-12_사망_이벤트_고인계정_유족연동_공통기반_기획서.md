# ⚰️ 00-12. 사망 이벤트 · 고인 계정 · 유족 연동 — 공통 기반 기획서

> **문서 성격**: 플랫폼 공통 기반 기획서. `[Claude:Opus]` 작성(2026-08-12).
> **왜 00 레벨인가**: 이 문제는 특정 도메인 것이 아닙니다. 03~07(신 번호 기준)이
> **전부 같은 "사망"이라는 사건을 다루는데, 각자 다르게 다루고 있어서** 서로 연결되지 않습니다.
> **계기**: 개발자 지적 — *"추모하는 대상 계정과 이용자(가족)의 연동 문제. 사후 계정을 계속
> 남겨두는 게 옳은가."* (2026-08-12)
> **표기**: ✅ 이 문서에서 확정 / 🟡 권고안 / 🔴 확정 필요(주체는 §8 표기)

---

## 📌 1. 이 문서가 푸는 문제

이어봄은 **사별을 다루는 플랫폼인데, "사망"이라는 사건을 시스템이 표현하는 방법이 정의된 적이 없습니다.**

지금 상태를 나란히 놓으면 모순이 바로 보입니다.

| 도메인 | "고인"을 무엇으로 다루나 | "사망 확인"을 어떻게 하나 |
| :--- | :--- | :--- |
| 추모관(`05-01`) | **문자열** (`Memorial.deceasedName`) | **안 함** — 자기신고 |
| 계정 정산(`04-01`) | 다루지 않음 (유족 본인 관점) | **안 함** — 증빙서류 미수집(§3.3) |
| 엔딩노트(`06-01`) | 다루지 않음 (작성자 = 본인) | **"사망진단서 확인"을 화면에서 이미 약속** |
| `User` 모델 | **개념 없음** — 살아있음이 암묵 전제 | — |

**엔딩노트는 이미 사용자에게 약속했습니다**: *"생전에는 철저히 비밀이 보장되며, 사후 사망진단서
확인 및 지정 유족의 이중 동의 완료 시에만 안전하게 복호화되어 전송됩니다."*(`EndingNotePage.tsx`,
`HomePage.tsx` 섹션 5 — `06-01` §1 인용)

그런데 **"사망진단서 확인"도 "지정 유족"도 데이터 모델이 없습니다.** 약속만 화면에 걸려 있습니다.

### 1.1 이걸 안 풀면 생기는 일

1. 생전에 엔딩노트를 써둔 회원이 사망 → **받을 유족을 시스템이 모릅니다.** 기능이 영원히 미완성.
2. 유족이 추모관을 만들어도 → **고인의 이어봄 계정과 연결되지 않습니다.** 별개의 데이터로 남습니다.
3. 고인의 계정이 → **살아있는 채로 방치됩니다.** 로그인 가능, 마케팅 발송 대상, 보유기간 무기한.
4. 개발자가 제안한 **생전 정산 신청**(본인이 미리 처리 방식 선택)을 **얹을 자리가 없습니다.**

---

## 🧩 2. ✅ 핵심 개념 4개

### 2.1 `Deceased` — 고인은 문자열이 아니라 엔티티다

**이것이 3-1 문제의 답입니다.** 고인을 독립 엔티티로 만들고, **이어봄 회원이었으면 그 계정과 연결**합니다.

두 경우를 하나의 모델로 다뤄야 합니다.

| 케이스 | 상황 | `Deceased.userId` |
| :--- | :--- | :---: |
| **A. 회원이었던 고인** | 생전에 엔딩노트를 쓰거나 정산 계획을 신청해둔 사람 | 값 있음 |
| **B. 비회원 고인** | 유족이 추모관만 개설한 경우 (대다수 예상) | `null` |

`Deceased`가 있으면 추모관·정산·엔딩노트가 **전부 같은 고인을 가리킬 수 있습니다.** 지금은 각자
따로 놉니다. B 케이스도 자연스럽게 담깁니다 — 계정 없는 고인도 고인입니다.

### 2.2 `DeathVerification` — 사망 확인은 "있다/없다"가 아니라 강도다

`04-01` §3.3은 증빙서류를 안 받기로 했고, `06-01`은 사망진단서를 약속했습니다. **이 둘은 충돌이
아니라 층위 차이입니다** — 행위마다 필요한 확인 강도가 다릅니다. §4에서 사다리로 정리합니다.

### 2.3 `BereavedRelation` — 유족에는 두 종류가 있다

| 종류 | 누가 정하나 | 신뢰도 | 권한 |
| :--- | :--- | :---: | :--- |
| **지정 유족** | **본인이 생전에 지정** | 높음 | 엔딩노트 개봉 동의, 정산 실행 요청 |
| **자칭 유족** | 사후에 스스로 주장 | 낮음 | 추모관 개설·관리 정도 |

**이 구분이 없으면 §4의 검증 사다리를 만들 수 없습니다.** 지금은 전부 "로그인한 아무나"입니다.

### 2.4 `PreDeathDirective` — 생전 위임 (개발자 제안의 자리)

> 개발자 지시(2026-08-12): *"죽기 전의 고인이 정산에 대한 계획을 미리 신청해놓고, 처리 방식을 미리
> 선택하는 선택지를 주는 게 옳지 않나. 1)유족처리 2)행정사처리 3)운영자가 대신처리 를 모두 제공."*
>
> ⚠️ **개발자 본인의 구상이며 사장님 확답은 아직 없습니다**(2026-08-14 확인). ③은 신규 수익 모델
> 이자 법률 검토 대상이라 **사장님 판단이 반드시 필요합니다**(§8-1).

**이 제안이 `04-01`의 가장 큰 제약을 풀어버립니다.** 제가 "이어봄은 대행할 수 없다"고 단정한 근거는
*"고인의 위임 의사를 사후에 확인할 방법이 없다"* 였는데, **본인이 생전에 위임 의사를 명시해두면
전제가 달라집니다.** 유족이 사후에 주장하는 위임과 본인이 생전에 남긴 위임은 법적 무게가 다릅니다.

| 처리 방식 | 이어봄 역할 | 수익 | 법적 근거 |
| :--- | :--- | :--- | :--- |
| **① 유족 처리** | 안내·진행 추적 | 없음(무료 훅) | 낮음 — 이미 구현됨 |
| **② 행정사 처리** | 연결·중개 | 중개 수수료 | `02-01` 기존 검토 활용 |
| **③ 운영자 대행** | **직접 처리** | **대행 수수료** | 🔴 **신규 법률 검토 필수** |

③은 **생전 위임이 있어도 자동으로 합법이 되진 않습니다.** 각 플랫폼의 약관이 제3자 대리를 금지할
수 있고, 유상 대행이 행정사법 저촉 소지가 있는지 별도 확인이 필요합니다. 다만 **"검토할 가치가
생겼다"는 점이 중요합니다** — 사후 위임만 있을 때는 검토 자체가 무의미했습니다.

---

## 🗄️ 3. 🟡 데이터 모델 초안

```prisma
// 고인 — 회원이었을 수도, 아닐 수도 있다(§2.1)
model Deceased {
  id           String    @id @default(uuid())
  userId       String?   @unique   // 이어봄 회원이었으면 연결. 비회원 고인이면 null
  name         String              // 고인 성명
  birthDate    DateTime?
  deathDate    DateTime?
  registeredBy String              // 이 고인 정보를 등록한 User (자칭 유족일 수 있음)

  verifications DeathVerification[]
  relations     BereavedRelation[]
  // memorials, cleanupPlans 등 각 도메인이 여기 붙는다
}

// 사망 확인 — 강도별로 여러 건이 쌓일 수 있다(§4)
model DeathVerification {
  id         String    @id @default(uuid())
  deceasedId String
  level      String    // SELF_DECLARED | OPERATOR_REVIEWED | DOCUMENT_VERIFIED
  reviewedBy String?   // 운영자 확인 시
  verifiedAt DateTime?
  // ⚠️ 서류 파일 자체를 저장하지 않는다 — 파일 경로·이미지·주민등록번호 컬럼 금지(→ 00-16 §4.3)
}

// 유족 관계 — 지정(생전) vs 자칭(사후)(§2.3)
model BereavedRelation {
  id           String  @id @default(uuid())
  deceasedId   String
  userId       String
  kind         String  // DESIGNATED(본인이 생전 지정) | SELF_CLAIMED(사후 자칭)
  relationship String? // 자녀·배우자 등
  designatedAt DateTime?  // DESIGNATED일 때, 본인이 지정한 시각
  @@unique([deceasedId, userId])
}

// 생전 위임 — 본인이 살아있을 때 작성(§2.4)
model PreDeathDirective {
  id         String @id @default(uuid())
  userId     String @unique          // 작성자(생전 본인)
  handlerType String                 // FAMILY | AGENT | OPERATOR (①②③)
  // 대상 플랫폼별 처리 지시는 하위 테이블로
  createdAt  DateTime @default(now())
}
```

> 확정 전 초안입니다. §8 미결이 정해지면 정식 스펙으로 옮깁니다.

---

## 🪜 4. ✅ 검증 강도 사다리 — 이것이 "검증 체계"의 정본

개발자가 지적한 *"검증이 부족하다"* 의 답입니다. **검증은 도메인별이 아니라 행위의 위험도별**입니다.

| 단계 | 확인 수준 | 어떤 행위에 요구되나 | 근거 |
| :---: | :--- | :--- | :--- |
| **L0** | **없음** | 안내 카탈로그 열람, 추모관 조문(헌화·방명록) | 정보 제공·조의 표현일 뿐 |
| **L1** | **자기신고 + 로그인** | 추모관 개설(`LINK`), 정리 체크리스트 사용 | 확산 제한적, 되돌릴 수 있음 |
| **L2** | **운영자 검토** | 추모관 `PUBLIC` 전환, 유족 관계 자칭 등록 | 색인·공개 노출은 위험도가 다른 층 |
| **L3** | **서류 기반 사망 확인** | **운영자 대행 신청, 엔딩노트 개봉** | 되돌릴 수 없음. 재산·비밀에 접근 |
| **L4** | **L3 + 지정 유족 이중 동의** | 엔딩노트 중 민감 항목 | `06-01`이 화면에서 이미 약속한 수준 |

### 4.1 이 사다리가 해결하는 것

- **`04-01` §3.3(서류 미수집)과 `06-01`(사망진단서 확인)이 더 이상 충돌하지 않습니다.** 전자는
  L0~L1 행위였고 후자는 L3~L4 행위입니다. **층이 다른 얘기였습니다.**
- Domain 05(추모관)에만 게이트가 없다는 `05-02` §6.1 지적도 여기서 풀립니다 — 추모관은 L1이 적정이고,
  `PUBLIC`만 L2로 올리면 됩니다.
- **운영자 대행(③)이 왜 어려운지가 분명해집니다** — L3가 필요하고, L3는 서류를 봐야 하며,
  서류를 보려면 §5 문제가 따라옵니다.

### 4.2 ✅ L3의 서류를 저장할 것인가 → **`00-16`으로 해소(2026-08-18)**

`04-01` §3.3은 *"안 받는 쪽을 기본값으로"* 라고 했고 그 판단은 지금도 유효합니다. 하지만 L3
행위(대행·엔딩노트 개봉)에는 확인이 필요합니다. 절충안:

- **운영자가 화면에서 확인하되 서버에 파일을 남기지 않는다** — 확인 사실(`verifiedAt`, `reviewedBy`)만 기록
- 또는 **단기 보관 후 자동 파기**(확인 완료 시점에 즉시 삭제)

> 🔵 **2026-08-18 스펙갱신 — 권고는 그대로(화면 확인 후 미저장)이고 근거가 바뀌었습니다.**
> **개인정보보호법 §24조의2**는 법령 근거 없이는 **주민등록번호를 처리할 수 없다**고 정하며,
> **이것은 동의로도 풀리지 않는 예외**입니다. §2.1 서류에는 주민등록번호가 인쇄돼 있고
> 이어봄에는 처리 근거 법령이 없습니다. **즉 이 항목은 "판단"이 아니라 "제약"에 가깝습니다** —
> 사장님·전문가 확인 대상에서 내립니다. 상세와 구현 절차는 **`00-16`이 정본**입니다.
>
> 🔴 **다만 `00-16` §2.3이 이 절의 빠진 부분을 하나 찾았습니다** — L3는 **①사망 사실**만 말하는데
> **②신청인이 유족인지**는 다른 서류(가족관계증명서)의 문제입니다. **한 서류로 둘 다 안 됩니다.**
> 해법은 이미 이 문서 안에 있습니다: **생전 지정(`BereavedRelation.DESIGNATED`)이 있으면
> ②는 서류 없이 성립**하므로, `00-16` §4.2가 그 경로를 **기본값**으로 권고합니다.

---

## 🔐 5. ✅ 고인 계정 처리 정책 — "계속 남겨두는 게 옳은가"

**결론: 그냥 두면 안 되고, 그냥 지워서도 안 됩니다. 새로운 상태가 필요합니다.**

### 5.1 왜 방치하면 안 되나

- 로그인이 살아있습니다 — 비밀번호를 아는 사람이면 누구나 고인 행세를 할 수 있습니다
- 마케팅·알림 발송 대상에 남습니다 (유족에게 2차 상처)
- 개인정보 보유기간이 무기한이 됩니다

### 5.2 왜 삭제해서도 안 되나

- **엔딩노트가 그 계정 안에 있습니다** — 삭제하면 유족에게 전달할 대상이 사라집니다
- 상속·분쟁 대비 기록 보존 필요
- `Lead`·`ConsultRequest` 등 거래 이력이 FK로 물려 있습니다

### 5.3 ✅ 권고 — `MEMORIALIZED`(사망 처리) 상태 신설

```
User.status:  ACTIVE  →  MEMORIALIZED  (사망 확인 L3 완료 시)
```

| | `MEMORIALIZED` 계정의 동작 |
| :--- | :--- |
| 로그인 | **차단** |
| 마케팅·알림 | **전면 중단** |
| 데이터 | **보존** (삭제 아님) |
| 접근 | **지정 유족만**, 사다리 L3~L4를 통과한 행위에 한해 |
| 추모관 | `Deceased.userId`로 연결되어 표시 |

소셜 플랫폼들의 "추모 계정 전환"과 같은 개념이고, `04-01` §4.1의 `actionType: MEMORIALIZE`와도
용어가 맞습니다 — **우리가 남에게 안내하는 처리를 우리 플랫폼에도 적용하는 셈**입니다.

> 🔴 **보유기간은 미정**입니다. `05-02` §9(추모관 보존기간)와 함께 정해야 하며, `00-11` 인프라
> 결정(스토리지 비용)이 나와야 근거가 생깁니다.

---

## 🧭 6. 🟡 사용자 여정과 계정 모드 — 개발자 제안

> 개발자 지시(2026-08-12): *"사용자 여정 순으로 재배치와 함께… 이것을 사용자와 엮어서 따로
> 보여주거나 하는 방식은 어떤가? 뭔가 계정별로 나눌 수 있으면 좋을 것 같기도."*

### 6.1 이 플랫폼 사용자는 두 상태로 들어온다

| | **생전 준비 모드** | **유족 모드** |
| :--- | :--- | :--- |
| 주체 | **나 자신** | **고인** |
| 심리 | 여유·신중·미룰 수 있음 | 긴급·경황 없음·되돌릴 수 없음 |
| 기능 | 엔딩노트, 생전 정산 위임, 지정 유족 설정 | 장사시설, 상중·행정, 추모관, 현물 수거, 정산 실행 |
| 시간축 | 사망 이전, 무기한 | D+0 ~ 수개월 |

**같은 메뉴판을 둘 다에게 보여주는 건 양쪽 모두에게 나쁩니다.** 사별 직후 유족에게 "엔딩노트
작성하기"는 잔인할 수 있고, 생전 준비 중인 사람에게 "장례식장 찾기"는 불필요하게 무겁습니다.

`00_DOCS_INDEX`도 이미 이 구분을 인식하고 있습니다 — *"01~04는 유족이 고인을 위해 쓰는 사후
기능이고, 05만 본인이 자신을 위해 미리 쓰는 생전 기능"*(구 번호 기준). **인식은 했는데 화면에

### 6.2 구현 방식 비교

| 안 | 방식 | 평가 |
| :--- | :--- | :--- |
| A | **계정 유형 분리** (생전용/유족용 별개 가입) | ❌ 한 사람이 둘 다 필요합니다. 내 엔딩노트도 쓰고 아버지 상도 치릅니다 |
| B | **수동 모드 전환** (상단에서 직접 전환) | 🟡 단순하지만 전환 존재를 모르면 무용 |
| C | **컨텍스트 자동 + 수동 전환** | ✅ **권고** |

**C 권고 이유**: `Deceased`를 등록하는 순간(= 추모관 개설, 정산 실행 신청) **"이 사람은 지금 유족
이다"가 데이터로 확정됩니다.** 별도로 물어볼 필요가 없습니다. 반대로 엔딩노트를 시작하면 생전
준비 모드입니다. 자동으로 맞춰주되 언제든 수동 전환 가능하게 둡니다.

**§2.1의 `Deceased` 엔티티가 모드 판정의 근거가 됩니다** — 이 기획의 두 축이 여기서 만납니다.

### 6.3 🟡 그래서 메뉴는

```
[내 준비]            ← 생전 준비 모드 (기본: 신규 가입자)
  · 엔딩노트
  · 디지털 자산 정산 계획   ← 생전 위임(§2.4)
  · 지정 유족 설정

[○○○님 보내드리기]   ← 유족 모드 (Deceased 등록 시 자동 활성)
  · 장사시설 찾기
  · 상중 · 행정 케어
  · 온라인 추모관
  · 현물 유품 수거
  · 디지털 자산 정산 실행
  · 상속 · 법률 상담
```

유족 모드 헤더에 **고인 이름이 들어가는 것**이 중요합니다 — 기능 목록이 아니라 "누구를 위한
일인지"가 먼저 보여야 합니다.

---

## 🔢 7. ✅ 도메인 번호 — **2026-08-12 개발자 확정 · 실행 완료**

"번호 = 사이드바 순서"라는 기존 규칙(`00_DOCS_INDEX`)은 **메뉴가 한 줄일 때만 성립합니다.** §6.3처럼
두 모드로 갈라지면 규칙을 바꿔야 합니다.

**✅ 확정: 번호는 "사용자 여정 순서"로 하고, 메뉴 순서와의 1:1 대응은 포기합니다.**

| 신 번호 | 도메인 | 모드 | 기존 |
| :---: | :--- | :---: | :--- |
| 01 | 장사시설 매칭 | 유족 | 01 (유지) |
| 02 | 전문가 매칭(상속·법률) | **양쪽** | 02 (유지) |
| 03 | **현물 유품 수거** | 유족 | 03에서 분리 |
| 04 | **디지털 자산·계정 정산** | **양쪽** | 03에서 분리 |
| 05 | **디지털 추모관** | 유족 | 03에서 분리 |
| 06 | **엔딩노트·유언** | 생전 | 05 → 06 |
| 07 | **상중·행정 케어** | 유족 | 04 → 07 |
| **08** | 비즈니스 분석(공통) | — | 06 → **08** |

- **01·02는 번호가 안 바뀝니다** → 코드 주석 28건 무영향 (**실측 확인됨**)
- **`docs 03-02` 참조 14건만** 04(정산) 또는 05(추모관)로 갈라졌습니다 (**전건 갱신 완료**)
- 08은 원래 09로 제안했으나 **개발자 지시로 08**(번호 공백 없음)

### 7.1 ✅ "양쪽"은 두 가지 뜻이었습니다 — 개발자 지적으로 드러난 정정

초안은 **04만 "양쪽"** 으로 표시했는데, 개발자의 진입 분류(WellDying / 유가족)를 대보니
**02도 양쪽**입니다. 그런데 **같은 라벨이 전혀 다른 구조를 가리키고 있었습니다.**

| | "양쪽"의 의미 | 승계 |
| :--- | :--- | :---: |
| **02** 전문가 매칭 | 같은 기능을 **두 주체가 각자** 쓴다 | ❌ 없음 |
| **04** 디지털 정산 | **한 주체가 시작해 다른 주체가 끝낸다** | ⭕ 있음 |

### 7.2 ✅ 승계 원칙 — 통로는 06 하나

개발자 지적(2026-08-12): *"굳이 전문가가 연결되어야 하나 싶음. 유언 등을 남길 것이었다면 06과
이어지는 게 자연스러움."* → **채택.**

| 생전 도메인 | 고인이 남기는 것 | 유족 연결 | 근거 |
| :--- | :--- | :---: | :--- |
| **02** 전문가 | 상담 이력 | ❌ | 거래 이력이지 유산이 아님. 넘기면 **유족 간 분쟁 소지 + 전문가 비밀 위반**. 유족은 본인 자격으로 새로 신청 |
| **04** 정산 | `PreDeathDirective` | ⭕ | 유족이 **집행 주체**. 연결 안 되면 지시가 소멸 |
| **06** 엔딩노트 | 유언·메시지 | ⭕ | **이 도메인의 존재 목적 그 자체**(§1.1-1) |

> **✅ 원칙: 고인이 유족에게 무언가를 남기는 통로는 06 하나입니다.** 다른 도메인이 각자 승계 경로를
> 만들지 않습니다 — 경로가 하나여야 §4의 **L4 게이트도 하나로 족합니다.** 도메인마다 게이트를 만들면
> **가장 약한 게이트가 플랫폼 전체의 보안 수준**이 됩니다.
>
> **04만 예외인 이유**: 04는 "전달"이 아니라 **"위임"**이고, 수신자가 유족이 아닐 수도 있어
> (②행정사·③운영자) 06을 경유할 수 없습니다.
>
> 고인이 담당 전문가를 알리고 싶으면 **엔딩노트에 적어서 보냅니다** — 02가 직접 연결되는 것이
> 아니라 06이 매개합니다.

### 7.3 🔴 아직 안 풀린 것 — 연결의 "발견" 단계

승계 **대상**은 정해졌지만, 유족이 *"고인이 이어봄 회원이었다"* 는 사실을 **어떻게 아는지**는
미해결입니다. 순환 구조가 있습니다:

> 이어봄이 지정 유족에게 알리려면 → **사망을 알아야 함** → 누군가 신고해야 함 →
> 신고할 사람은 유족인데 → **유족은 계정 존재를 모름**

🟡 **권고(사장님 상의 대기)**: **생전 지정 시점에 지정된 유족에게 즉시 통지**하면 순환이 끊깁니다.
덤으로 §2.3 `BereavedRelation`에 없는 **"지정당한 사람의 수락"** 절차도 생깁니다 — 지금은 본인
동의 없이 남을 지정 유족으로 박아넣을 수 있는 상태입니다.

**제약**: 비회원 고인이 다수(§2.1 케이스 B)이므로 **연결 기능이 기본 동선에 끼어들면 안 됩니다.**
매칭 실패와 "요청자가 지정 유족이 아님"을 **구분 없이 무반응** 처리해야 합니다(`05-01` §4.1의
404 원칙과 같은 사상).

---

## ❓ 8. 미결 쟁점

| # | 쟁점 | 권고 | 막는 것 |
| :---: | :--- | :--- | :--- |
| 1 | **운영자 대행(③) 법률 검토** | 🔵 **2026-08-14 → `04-02`로 이관.** 유료 검토 **전에** 무료 확인(플랫폼 제3자 대리 채널 유무)이 0번으로 앞섬 — 결과가 NO면 검토 불필요 | 04 ③ 상품화만 |
| 2 | ~~**L3 서류 저장 여부**(§4.2)~~ | ✅ **2026-08-18 → `00-16`으로 해소.** 화면 확인 후 미저장 — **개인정보보호법 §24조의2(주민등록번호)가 근거**이므로 확정 대기 항목이 아님 | ~~04·06 착수~~ |
| 3 | **`MEMORIALIZED` 보유기간**(§5.3) | `00-11`·`05-02` §9와 함께 | 오픈 전 |
| 4 | **도메인 번호 확정**(§7) | 위 표, 상중·행정 위치만 판단 | 문서 재편 |
| 5 | **모드 전환 UX**(§6.2) | C안(자동+수동) | 프론트 전반 |
| 6 | 사망 확인 주체·비용 | 운영자 수동 심사 전제 | §1 결정과 연동 |

---

## 🔗 9. 관련 문서

| 문서 | 관계 |
| :--- | :--- |
| `05-01` | 고인=문자열 전제 → **§2.1로 대체 필요**(구 `03-02` 분할본) |
| `05-02` | §6.1 개설 게이트 → **§4 사다리 L1/L2로 해소**(구 `03-03`) |
| `06-01` | "사망진단서 확인 + 지정 유족 이중동의" 약속 → **§4 L3·L4가 그 구현 근거**(구 `05-01`) |
| **`00-16`** | **§4.2 L3 상세의 정본** — 서류 4종·시간축(D+0에는 기본증명서가 안 됨)·주민등록번호 제약·구현 절차 |
| `00-11` | 스토리지 비용 → §5.3 보유기간의 전제 |
| `00-06` | 모드 분리 시 화면 대장 재정리 필요 |
