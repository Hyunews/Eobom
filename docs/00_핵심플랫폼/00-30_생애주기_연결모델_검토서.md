# 🔄 00-30. 3단계 생애주기 연결 모델 — 전 도메인 검토서

> **문서 성격**: 검토서 · **사장님 제공안에 대한 전 도메인 대조**. `[Claude:Opus]` 작성(2026-08-25).
> **계기**: 사장님 제공 자료 3종 — **3단계 생애주기별 연결 모델** · **사용자 여정(UX Flow)** ·
> **기획자 추천 운영 정책 2건**. 개발자 전달 시 *"전체적인 흐름에 대한 정보인 거 같아"* 라는
> 단서가 붙었습니다 — **특정 도메인의 기능 요구가 아니라 플랫폼 전체의 연결 구조**입니다.
>
> **정본 관계 — 이 문서는 "연결"만 다룹니다.**
> 각 단계의 구현 스펙은 도메인 문서가 정본입니다: 1단계 → `00-27` · 2단계 → `00-12` §4·`00-16` ·
> 3단계 → `07-03` · 열려야 할 데이터 → `06-04`(엔딩노트)·`04-01`(디지털 유품).
> **파생되는 사람의 판단은 `00-14` §11**이 정본입니다. 여기서 다시 열거하지 않습니다.
> **표기**: ✅ 확정·구현됨 / 🟡 권고 / 🔴 확정 필요·위험 / 🔵 개발자·사장님 확정 / ⏸ 선행 대기

> 🔴 **작성 경위 정정(2026-08-25)**: 이 자료는 처음에 `06-04`(엔딩노트 실구현 기획서) 안에서
> 함께 다뤄졌으나, **엔딩노트 스펙과 플랫폼 연결 구조는 별개**이므로 이 문서로 분리했습니다.
> `06-04`는 **06 도메인 구현 스펙**만 남기고, 단계 대조·충돌·도메인 횡단 판단은 여기가 정본입니다.

---

## 📌 1. 결론 먼저

| 질문 | 답 |
| :--- | :--- |
| **사장님 모델이 지금 설계와 충돌하나?** | ❌ **거의 일치합니다.** 3단계 중 **1·3단계는 이미 구현돼 동작 중**이고, 이 프로젝트가 `00-12`(2026-08-12)에서 도달한 결론과 같은 구조입니다(§3) |
| **그럼 새로 만들 것은?** | **2단계(사후 승계)뿐입니다.** 그리고 그 2단계는 **사망 확인(L3)** 하나에 전부 걸려 있습니다(§4) |
| **가장 큰 발견은?** | 🔵 **이 모델의 가치는 기능이 아니라 "순서"입니다** — 흩어져 있던 `00-27`·`00-12`·`07-03`·`06-04`·`04-01`이 **하나의 시간축 위에 놓입니다**(§4.1) |
| **충돌하는 것은?** | 🔴 **3건**(§5) — ①알림톡 발송 주체(08-21 확정과 반대) ②정부24 연계(확인 전) ③Zero-Knowledge(이미 기각된 개념) |
| **가장 약한 고리는?** | 🔴 **2단계의 "확인"을 할 사람과 시간이 없습니다**(§9). 모델 전체가 이 한 칸에 걸려 있는데 그 칸이 비어 있습니다 |

---

## 📄 2. 사장님 제공 자료 — 원문 보존

> **판단의 근거가 되는 원문은 요약하지 않고 남깁니다.** 이후 절의 대조는 전부 이 표를 기준으로 합니다.

### 2.1 3단계 생애주기별 연결 모델

| 단계 | 이름 | 내용 |
| :---: | :--- | :--- |
| **1** | **생전 초대 모델**(Pre-Mortem Link) | 회원이 디지털 엔딩노트 작성 시 **지정 유족(보호자)의 이름과 연락처(휴대폰/이메일)** 입력 → 유족에게 *"[이어봄] 000님이 회원님을 안심 엔딩 수신자로 지정하셨습니다"* **카카오 알림톡/문자 발송** → 유족이 해당 링크로 가입하면 **즉시 가족 관계 매핑 완료**, 생전에는 민감 정보 **잠금(Lock)** 유지 |
| **2** | **사후 승계 및 조회 모델**(Post-Mortem Verification) | 회원 사망 시 유족이 웹에서 **'사후 승계 요청'** → **사망진단서 또는 행정망(정부24 연계) 확인** 후, 잠겨 있던 **엔딩노트·디지털 유품 리스트·사전 장례 의향서**의 열람 권한이 **유족 계정으로 활성화** |
| **3** | **상중 긴급 연결 모델**(Emergency Onboarding) | 사전에 회원이 가입하지 않았더라도, **상주가 긴급 장례·행정 지원을 신청하며 가입**할 때 **고인의 기본 정보를 등록**하여 사후 유족 계정으로 즉시 생성·연결 |

### 2.2 사용자 여정(UX Flow)

| 주체 | 흐름 |
| :--- | :--- |
| **본인(시니어)** | 엔딩노트/상속 메뉴 → **[수신 가족 지정]** → 자녀 이름·휴대폰 입력 및 **열람 범위 설정**(*"장례 방식은 즉시 공유, 자산·비밀번호는 사후 공유"*) → **초대장 공유** |
| **유족(자녀)** | 알림톡 수신 → 초대 링크 접속 → **간편 회원가입** → 가입 즉시 **자동으로 부모님 계정과 연결 완료 화면** → 대시보드에 *"ooo님의 안심 보호자로 등록되었습니다"* **배너 + 현재 상태(보호 중)** |

### 2.3 기획자 추천 운영 정책

| # | 정책 | 원문 요지 |
| :---: | :--- | :--- |
| **1** | **보안 분리(Zero-Knowledge)** | 유족으로 연결됐더라도 **생전에는 자산 정보·비밀 유언을 볼 수 없도록 UI와 API 레벨에서 완벽히 격리** — 그래야 회원이 안심하고 기록 |
| **2** | **다중 유족 지정** | 자녀가 여럿일 경우 **1:N 매핑** 지원 + **대표 상주 1인 지정 옵션** |

---

## 🔍 3. 단계별 대조 — 이미 있는 것과 없는 것

### 3.1 1단계 · 생전 초대 — ✅ **구현 완료. 새로 만들 것은 진입점뿐**

| 사장님 안 | 현재 | 근거 |
| :--- | :--- | :--- |
| 유족 이름·연락처(휴대폰/이메일) 입력 | ✅ `FamilyDesignation` — 성함·`phoneEnc`·`emailEnc`·관계·`scope`·`priority` | `00-27` §5 |
| 초대 링크로 가입 시 즉시 매핑 | ✅ `/invite/:token` → 소셜 로그인 → `acceptedUserId` 연결 | `00-27` §9.1 (Phase 2 완료, `fa865a4`) |
| 생전 잠금(Lock) | ✅ **개념 확정** — `ACCEPTED`여도 **권한 0**(불변식 1) | `00-27` §3 |
| 알림톡/문자 발송 | 🔴 **이어봄은 발송 주체가 되지 않기로 확정**(2026-08-21) | §5.1 |
| **엔딩노트 화면에서 가족 지정** | 🔴 **없음** — 현재 진입점은 마이페이지 한 곳 | `06-04` Phase 2 |

### 3.2 2단계 · 사후 승계 — ❌ **여기만 실체가 없습니다**

| 사장님 안 | 현재 | 근거 |
| :--- | :--- | :--- |
| 유족의 **'사후 승계 요청'** | ❌ 없음 | — |
| **사망진단서 확인** | 🟡 **설계는 있음, 구현 0** — 화면 확인 후 **미저장**이 확정 | `00-12` §4 L3 · `00-16` |
| **행정망(정부24) 확인** | 🔴 **확인 전** | §5.2 |
| **엔딩노트** 열람 활성화 | 🟡 설계됨·미구현(백엔드 0건) | `06-04` |
| **디지털 유품 리스트** 활성화 | 🟡 모델은 있음(`DigitalCleanupItem`) — **승계 경로가 없음** | `04-01` §4.3 |
| **사전 장례 의향서** 활성화 | 🟡 엔딩노트 섹션으로 설계됨 | `06-04` §6.1 ② |
| 사망 확인 모델 | ❌ **`DeathVerification` 없음** — `Deceased`는 부고장용 최소 범위(사망 *등록*, 확인 아님) | `07-03` §4.1 |

### 3.3 3단계 · 긴급 온보딩 — ✅ **이미 동작 중. 이름이 없었을 뿐**

| 사장님 안 | 현재 | 근거 |
| :--- | :--- | :--- |
| 미가입 상주가 지원 신청하며 가입 | ✅ **부고장 개설이 정확히 이 흐름** | `07-03` |
| 고인 기본 정보 등록 | ✅ `Deceased`(비회원 고인은 `userId = null`) | `schema.prisma:534` |
| 유족 계정으로 즉시 생성·연결 | ✅ `Deceased.registeredBy` · `Obituary.createdByUserId` | 동상 |
| — | 🔴 **검증이 없습니다** — 이 경로는 `00-12` §4 **L1(자기신고)** 수준 | §4.2 |

---

## 🧩 4. 이 모델이 실제로 만드는 것 — **연결**

### 4.1 🔵 가장 큰 기여는 기능이 아니라 **순서**입니다

지금까지 이 프로젝트의 문서들은 **각자 옳지만 서로 이어져 있지 않았습니다.**

```
[가입]───[00-27 가족지정]───[?????]───[06 엔딩노트]
                              │        [04 디지털유품]
                              │        [07 부고·행정]
                        여기가 비어 있었다
```

사장님 모델은 **그 빈칸의 이름이 "사망 확인"이고, 그 앞뒤에 무엇이 와야 하는지**를 말합니다.
**`00-12`가 2026-08-12에 이미 같은 결론에 도달했지만**(사다리 L0~L4 · *"통로는 06 하나"*),
**그것을 사용자가 지나가는 순서로 다시 쓴 것은 이 자료가 처음입니다.**

### 4.2 🔴 그래서 드러나는 것 — **"유족 계정"이 두 종류입니다**

| 경로 | 검증 수준 | 할 수 있는 것 | 할 수 없는 것 |
| :--- | :---: | :--- | :--- |
| **1→2단계**(생전 지정 후 사망 확인) | **L3~L4** | 부고·행정 + **엔딩노트 개봉 · 디지털 유품 승계** | — |
| **3단계**(긴급 온보딩) | **L1** | 부고장·장례·행정 체크리스트 | 🔴 **엔딩노트를 열 수 없습니다** |

> 🔴 **이 구분을 화면에서 미리 말하지 않으면 그대로 CS가 됩니다** — *"가입했는데 왜 안 열리냐."*
> 3단계로 들어온 상주는 **자기가 유족이라고 신고했을 뿐**이고, 고인이 생전에 그를 지정한 적이
> 없습니다. **같은 "유족 계정"이라는 말이 두 가지를 가리키고 있습니다.**
>
> 🟡 **권고: 3단계 온보딩 화면에서 *"생전에 지정받으신 분은 추가 절차 없이 열람하실 수 있습니다"*
> 를 안내**해, 1단계의 존재를 **사후에 알게 되는 경로**를 만듭니다. 이건 마케팅이 아니라
> **1단계 가입 동기가 가장 강해지는 순간**입니다.

### 4.3 데이터가 흐르는 방향

| 시점 | 무엇이 어디로 | 정본 |
| :--- | :--- | :--- |
| 1단계 | 가족 연락처 → `FamilyDesignation` | `00-27` |
| 생전 | 의향·메시지 → `EndingNote*`(암호화·봉인) | `06-04` §5 |
| 2단계 | 사망 확인 → `DeathVerification` → 권한 활성화 | `00-12` §4 |
| 2단계 직후 | **장례 의향 → 07 부고장 프리필** · **디지털 계정 의향 → 04 체크리스트** | `06-04` §8.4 |
| 3단계 | 고인 기본정보 → `Deceased` → 부고장·추모관 | `07-03` |

> ✅ **마지막 줄이 이 모델의 실익입니다.** 엔딩노트를 *"열어서 읽는 것"* 으로 끝내지 않고
> **유족이 바로 다음에 해야 할 일에 값을 흘려보내는 것** — 그게 이 기능이 플랫폼 안에 있는
> 이유이고, 단독 앱과 다른 점입니다.

---

## ⚠️ 5. 충돌·정정 3건

### 5.1 🔴 알림톡 발송 — **2026-08-21 확정과 정반대입니다**

`00-27` §9.1은 **이어봄이 발송 주체가 되지 않는다**를 개발자 확정으로 못박았습니다
(*"가족 등록 관련 공유버튼 추가 + 해당링크로 회원가입시 연결"*). 근거는 **비용 0 · 발신번호
사전등록 불필요**였고, `07-03` §2.2(카톡 부고장)와 같은 사상입니다.

**두 판단은 트리거를 가르면 양립합니다.**

| 트리거 | 발송 주체 | 판정 |
| :--- | :--- | :--- |
| **ⓐ 본인이 살아서 가족을 지정할 때** | **본인**(공유 버튼) | 🟡 **현행 유지 권고** — 모르는 번호로 온 *"[이어봄]"* 문자보다 본인이 보낸 링크의 수락률이 높습니다 |
| **ⓑ 사망 확인 후 유족에게 알릴 때** | 🔴 **이어봄일 수밖에 없음** | 본인이 죽었으므로 보낼 사람이 없습니다. `00-27` §11 #2도 *"Phase 3에는 발송 수단이 반드시 필요"* 라고 이미 남겨 뒀습니다 |

> 🔴 **판단은 `00-14` §11-1·§11-2.** ⓑ는 어차피 필요하므로 **그때 한 번에 도입**하는 것이
> 권고이며, **발신번호 실번호 확보(§11-2)가 그보다 먼저**입니다.

### 5.2 🔴 정부24 연계 — **확인 전에는 설계에 넣지 않습니다**

`00-27` §10 Phase 3이 이미 *"정부24 연계는 그 위에 L4로 얹습니다(개발자 지시로 후순위)"* 라고
적어 두었습니다. 여기서 더 나가지 않는 이유:

- 민간 사업자가 **타인의 사망 사실을 행정망에서 조회**할 수 있는지는 제도로 정해지는 문제이고,
  **확인되지 않았습니다.**
- 안 될 경우 되돌릴 곳이 많아집니다(화면·모델·약관·처리방침).

> ✅ **정본 경로는 L3(서류)입니다.** 정부24가 성립하면 **L3를 대체하는 것이 아니라 자동화**하는
> 것이므로, **L3를 먼저 만들면 나중에 얹는 형태**가 됩니다. 순서상 손해가 없습니다.
> 🔴 **확인 전까지 화면에 *"행정망 연동"* 문구를 쓰지 않습니다** — `00-14` §2.5(*"KISA 기준 최고
> 등급 보안"*)와 정확히 같은 종류의 사고가 됩니다.

### 5.3 ⚠️ Zero-Knowledge — **요구는 채택, 용어는 정정**

| | 뜻 | 판정 |
| :--- | :--- | :--- |
| **암호학적 ZK(E2E)** | 서버도 복호화 못 함 | ❌ **`06-03` §1.1이 이미 기각** — 키를 아는 사람이 **죽고 없어** 사후 개봉이 원리적으로 불가능. 6070 시니어에게 키 관리를 넘길 수도 없음 |
| **권한 격리**(사장님 의도) | 유족 계정으로는 생전에 접근 경로 자체가 없음 | ✅ **채택** — API·쿼리·UI 3층 구현(`06-04` §7.4) |

> 🟡 **대외 용어는 「생전 봉인(Sealed)」으로 통일 권고.** *"Zero-Knowledge"* 는 **서버도 못 본다는
> 뜻으로 읽히는데 사실이 아니고**, 그 상태로 화면에 쓰면 `06-03` §1이 경계한
> *"보증의 어투로 말하는 약속"* 이 됩니다. 정직한 표현은 `06-03` §5가 이미 확정해 두었습니다.

---

## 🧭 6. 사용자 여정(§2.2) 검토

| 사장님 흐름 | 판정 | 비고 |
| :--- | :---: | :--- |
| 엔딩노트/상속 메뉴 → **[수신 가족 지정]** | 🟡 **추가 필요** | 지금은 마이페이지에만 있음. **컴포넌트는 재사용**(`MyPageFamilyDesignation`) |
| 자녀 이름·휴대폰 입력 | ✅ 있음 | `00-27` §5 |
| **열람 범위 설정**(즉시/사후) | 🟡 **설계됨·미구현** | `06-04` §7.1 매트릭스. 🔴 **자산·메시지는 "즉시" 선택 불가로 고정** |
| 초대장 공유 | ✅ 있음 | `navigator.share` + 링크 복사 |
| 알림톡 수신 | 🔴 §5.1 | |
| 초대 링크 → **간편 회원가입** | ✅ 있음 | `/invite/:token` — 🔴 **만 14세 게이트가 여기에도 걸립니다**(`00-19` §9-2-1) |
| 가입 즉시 **연결 완료 화면** | ✅ 있음 | walkthrough 56 |
| 대시보드 **"안심 보호자로 등록되었습니다" 배너 + 상태(보호 중)** | 🟡 **없음 — 추가 권고** | **가장 값싸고 효과가 큰 항목**(§6.1) |

### 6.1 🟡 "보호 중" 배너를 권고하는 이유

**지정을 수락한 유족은 그 뒤로 이어봄에 올 이유가 없습니다.** 사망은 수십 년 뒤일 수 있고,
그 사이 **연락처가 바뀌거나 앱의 존재를 잊습니다**(`00-27` §7이 *"번호가 틀린 채 20년 묵으면
권한이 엉뚱한 사람에게 간다"* 고 지적한 문제).

> 🟡 **배너는 장식이 아니라 `lastConfirmedAt` 재확인의 자연스러운 자리**입니다.
> *"ooo님의 안심 보호자로 등록되어 있습니다 · 연락처가 바뀌셨나요?"* 한 줄이
> **연 1회 재확인**(`00-27` §7)을 **별도 통지 없이** 수행합니다.

---

## 🔐 7. 운영 정책 2건 검토

### 7.1 정책 1 · 보안 분리 → ✅ 채택 (§5.3의 용어 정정 조건부)

구현 위치는 `06-04` §7.4가 정본입니다. **이 문서에서 덧붙일 것은 하나**입니다 —
🔴 **잠긴 섹션은 제목도 보이지 않아야 합니다.** *"자산 정보(잠김)"* 라고 표시하면
**자산이 있다는 사실 자체**가 이미 새어 나갑니다.

### 7.2 정책 2 · 다중 유족 → ✅ **이미 지원. 열려 있던 결정 하나를 닫습니다**

| 사장님 정책 | 현재 |
| :--- | :--- |
| 1:N 매핑 | ✅ 지원(상한 10명) — `00-27` §4.2 |
| **대표 상주 1인 지정 옵션** | ✅ `scope = PRIMARY`가 그것입니다 — `00-27` §6 |

> ✅ **`00-27` §11 #3(`PRIMARY` 복수 허용 여부)이 확정 대기였는데, 사장님 문구가 답이 됩니다.**
> 🟡 **권고 확정안: 기본 1명 · 복수 허용 · 강제하지 않음.** *"옵션"* 이라는 표현이 강제가 아님을
> 이미 말하고 있고, **1명 강제는 그가 먼저 사망·연락두절이면 되돌릴 방법이 없습니다.**

---

## 🗺️ 8. 도메인별 영향

| 도메인 | 이 모델이 요구하는 것 | 상태 |
| :--- | :--- | :---: |
| **00-12**(사망 이벤트 공통기반) | `DeathVerification` 도입 — **2단계의 심장** | ⏸ 미착수 |
| **00-27**(생전 가족지정) | 1단계 본체. §7.2 결정 반영 | ✅ 구현 |
| **06**(엔딩노트) | 잠길 데이터 본체 + 열람 범위 | 🔴 백엔드 0건 |
| **04**(디지털 자산) | *"디지털 유품 리스트"* 승계 경로 — **모델은 있으나 사후 이관 설계가 없음** | 🔴 미설계 |
| **07**(상중 행정) | 3단계 온보딩의 실제 입구 + 장례 의향 프리필 | ✅ 부분 |
| **05**(추모관) | 사후 권한 활성화 대상에 포함될지 미정 | 🔴 미정 |
| **01·02**(매칭) | 3단계 유입이 리드로 이어지는 지점 — **수수료 기준 미정**(`00-14` §3-4) | ⏸ |

> 🔴 **04가 조용한 구멍입니다.** 사장님 2단계는 *"디지털 유품 리스트"* 를 명시했는데,
> `04-01`은 **본인이 생전에 정리하는 것**까지만 설계돼 있고 **사후에 유족이 이어받는 경로가
> 없습니다.** `00-12` §7.2가 *"통로는 06 하나"* 로 정한 뒤 **04 쪽 후속 설계가 비어 있습니다.**

---

## 🧱 9. 선행 조건 — **2단계는 무엇이 있어야 서나**

```
2단계(사후 승계)
  ├─ ① DeathVerification 모델            ⏸ 00-12 확정 대기
  ├─ ② 사망 확인을 "사람이" 처리          🔴 00-14 §11-3 (SLA·인력 미정)
  ├─ ③ 열릴 데이터가 존재                 🔴 06 백엔드 0건
  ├─ ④ 유족에게 알릴 발송 수단            🔴 00-14 §11-1·11-2
  └─ ⑤ 권한 활성화 후 도메인 연결          🔴 04 사후 이관 미설계
```

> 🔴 **다섯 칸 중 서 있는 것이 하나도 없습니다.** 그리고 ②는 **기술이 아니라 사람 문제**라
> 개발로 메울 수 없습니다 — **모델 전체가 "누가 언제 확인하는가"에 걸려 있는데 그 칸이
> 비어 있는 것**이 이 검토에서 가장 중요한 발견입니다.
>
> 🟡 **착수 순서 권고: ③ → ① → ② → ④ → ⑤.**
> ③(엔딩노트 본체)은 **다른 넷과 독립적으로 지금 만들 수 있는 유일한 칸**이고,
> 만들어 두면 ①②가 정해지는 즉시 붙습니다. 반대로 ③ 없이 ①을 만들면 **열 것이 없습니다.**

---

## ❓ 10. 판단이 필요한 것 → **`00-14` §11이 정본**

이 문서에서 파생된 사람의 판단은 **전부 `00-14` §11**에 모았습니다. 여기서는 목록만 둡니다.

| 분류 | 항목 |
| :--- | :--- |
| 💰 경영 | 11-1 발송 주체 · 11-2 발신번호 · 11-8 보유기간 · 11-10 국내 리전 · 11-11 06 수익 지점 |
| 🏢 운영 | **11-3 사망 확인 처리 SLA**(§9 ②) · 11-4 2인 승인 실재 |
| 🔧 기술 | 11-5 유족 1명일 때 L4 · 11-6 `PRIMARY` 복수(→ §7.2에서 권고 확정안 제시) · 11-9 06 전용 키 |
| ⚖️ 법·문구 | 11-7 형제 이견 시 위치 · 11-12 *"Zero-Knowledge"* 표기 |
| 🔍 사실 확인(비용 0) | 정부24 민간 조회 · 알림톡 요건 · 안심상속 원스톱 · 등록기관/상담창구 |

> 🟡 **이 문서가 새로 추가한 판단 후보 1건** — **05(추모관)를 사후 권한 활성화 대상에 넣는가**(§8).
> 사장님 자료는 엔딩노트·디지털 유품·장례 의향 3개만 열거했고 추모관은 언급이 없습니다.
> `00-13` §4.5 링크 모델과 겹치므로 **여기서 정하지 않고 남깁니다.**

---

## 🔗 11. 관련 문서

| 문서 | 관계 |
| :--- | :--- |
| **`00-12` §4** | 사다리 L0~L4 — **§4.2 "유족 계정 두 종류"의 근거** |
| **`00-27`** | 1단계 정본. §7.2가 §11 #3을 닫음 |
| **`06-04`** | 2단계에서 열릴 데이터의 구현 스펙. **이 문서에서 분리됨** |
| `00-16` | L3 서류 상세·시간축(D+0에는 기본증명서가 안 됨) |
| `07-03` | 3단계의 실제 입구(부고장 개설) |
| `04-01` | 🔴 **사후 이관 경로 미설계**(§8) |
| **`00-14` §11** | **파생 판단 12건의 정본** |
| `00-13` §4.5 | §10의 추모관 쟁점 |
| `00-19`·`00-21` | 2단계 도입 시 처리방침·약관 개정 대상 |
