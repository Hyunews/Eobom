# 🗄️ 05. DB 요구사항 및 테이블 사전 (Prisma Schema)

> **문서 목적**: 이어봄 (Eobom) PostgreSQL 데이터베이스(`eobom_db`)의 핵심 테이블 스키마 컬럼 및 관계(Relationship) 사전을 정의합니다.
>
> **관리 방식(2026-08-10 개편)**: 이 문서는 두 구간으로 나뉜다.
> 1. **자동 생성 구간**(아래 `AUTO-GENERATED` 마커 사이) — `eobom/backend/prisma/schema.prisma`의 한글 주석에서
>    `node .harness/tools/generate-db-doc.js`로 추출한다. 스키마가 바뀌면 이 구간을 직접 고치지 말고,
>    **schema.prisma의 주석을 고친 뒤 스크립트를 재실행**할 것 — 두 곳이 갈라지는 걸(drift) 막기 위함.
> 2. **보충 설명 구간**(그 아래) — JSON 필드의 실제 형태, 상태값 전이, 테이블 간 업무 흐름처럼 한 줄 주석으로
>    담기 어려운 서술은 여기 사람이 직접 쓴다. 스크립트는 이 구간을 절대 건드리지 않는다.

---

## 📋 자동 생성: 테이블·컬럼 사전

<!-- AUTO-GENERATED:BEGIN (generate-db-doc.js — 직접 수정 금지, schema.prisma를 고치고 스크립트를 재실행할 것) -->

### `User`
> 유저 마스터 (B2C 일반 회원) — Partner/Expert/Admin과는 완전히 분리된 별도 인증 체계

**최초 생성**: 2026-08-04 (`20260804054755_init`) · **최근 수정**: 2026-08-04 (`20260804173245_social_account_split`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `email` | `String?` | UNIQUE, NULL | 유저 대표 이메일 (유니크) |
| `name` | `String` | NOT NULL | 유저 이름 / 닉네임 |
| `profileImage` | `String?` | NULL | 대표 프로필 이미지 URL |
| `role` | `String` | NOT NULL | 'USER', 'EXPERT', 'ADMIN' |
| `refreshToken` | `String?` | NULL | JWT Refresh Token |
| `createdAt` | `DateTime` | NOT NULL | 가입 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `accounts` | `SocialAccount[]` | 연동된 소셜 계정 목록 |
| `facilityReviews` | `FacilityReview[]` | 이 유저가 작성한 시설 리뷰 목록 |
| `leads` | `Lead[]` | docs/01_.../01-05 — 이 유저가 남긴 문의/견적요청 리드 |
| `consultRequests` | `ConsultRequest[]` | docs/02_.../02-03 — 이 유저가 남긴 전문가 상담 신청 |
| `digitalCleanupItems` | `DigitalCleanupItem[]` | docs/03_.../03-02 — 디지털 정리 진행 항목 |
| `memorials` | `Memorial[]` | docs/03_.../03-02 — 개설한 추모관 |
| `memorialTributes` | `MemorialTribute[]` | 남긴 헌화 |
| `memorialGuestbooks` | `MemorialGuestbook[]` | 남긴 방명록 |
| `memorialPhotos` | `MemorialPhoto[]` | 업로드한 추모 사진 |

### `SocialAccount`
> 소셜 로그인 계정 (User 1:N) — 카카오/네이버/구글 연동 정보, 1인 다계정 연동 지원

**최초 생성**: 2026-08-04 (`20260804173245_social_account_split`) · **최근 수정**: 2026-08-05 (`20260805033209_social_account_soft_delete_unlink`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `userId` | `String` | FK → User.id, NOT NULL | 이 소셜 계정이 연동된 User |
| `provider` | `String` | UNIQUE(복합), NOT NULL | 'KAKAO', 'NAVER', 'GOOGLE' |
| `providerId` | `String` | UNIQUE(복합), NOT NULL | 각 소셜 제공자의 고유 식별자 ID |
| `email` | `String?` | NULL | 소셜 프로필상의 이메일 |
| `createdAt` | `DateTime` | NOT NULL | 최초 연동 일시 |
| `unlinkedAt` | `DateTime?` | NULL | null이면 연동 중. 값이 있으면 사용자가 연동 해제한 상태(소프트 삭제) — 재로그인 시 이 provider+providerId를 다시 인식해 같은 계정으로 복구하기 위해 하드 삭제하지 않음 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `user` | `User` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@unique([provider, providerId])`

### `Facility`
> 시설 마스터 (장례식장 / 묘지·수목장)

**최초 생성**: 2026-08-05 (`20260805053339_facility_domain`) · **최근 수정**: 2026-08-12 (`20260812024232_drop_facility_deprecated_columns`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자 — 소스별 규칙 있음(CSV: bongan_NNN/jayeon_NNN, 카카오: kakao_<placeId>, 수기 시드: f_*) |
| `name` | `String` | NOT NULL | 시설명 |
| `type` | `String` | NOT NULL | '장례식장' \| '묘지/수목장' |
| `location` | `String` | NOT NULL | 도로명 주소 (화면 표시용 — 전남광주통합특별시 등 병합 표기는 utils/address.ts 정규화 거침) |
| `lat` | `Float` | NOT NULL | 위도 |
| `lng` | `Float` | NOT NULL | 경도 |
| `phone` | `String?` | NULL | 실데이터 수집(카카오/공공 CSV) 시 채워지는 연락처 |
| `kakaoPlaceId` | `String?` | UNIQUE, NULL | 카카오 로컬 API로 수집한 장례식장의 장소ID (재동기화 dedup용) |
| `price` | `String` | NOT NULL | 대표 가격 표시 문자열 (예: "950만원~") |
| `rating` | `Float` | NOT NULL | 초기 시딩값(공시 만족도 기준) — 리뷰 5건 미만일 때 노출값으로 사용 |
| `religion` | `String` | NOT NULL | 수용 가능 종교 (예: '전체 종교') |
| `guests` | `String` | NOT NULL | 수용 가능 하객 규모 (예: '100~300명') |
| `isPartner` | `Boolean` | NOT NULL | 파생 캐시 컬럼 — 정본은 partnerId. 클레임 승인/해지 트랜잭션 안에서만 갱신한다(docs 01-05 §3.4). — 제휴(클레임 승인된) 시설 여부 |
| `partnerId` | `String?` | FK → Partner.id, NULL | 클레임 승인된 사업자 (없으면 비제휴) |
| `tags` | `String[]` | NOT NULL | 표시용 태그 배열 — 분류 가능한 값(공설/사설 등)만 frontend TAG_CATALOG로 필터링됨(docs 01-06) |
| `amenities` | `String[]` | NOT NULL | 부대시설 목록 (예: 식당/매점/주차장/유족대기실/장애인편의시설) |
| `images` | `String[]` | NOT NULL | 파트너가 BizDashboard에서 업로드한 시설 사진 URL 목록 (2026-08-10) |
| `createdAt` | `DateTime` | NOT NULL | 레코드 생성 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `partner` | `Partner?` |  |
| `reviews` | `FacilityReview[]` | 이 시설에 달린 리뷰 목록 |
| `claims` | `FacilityClaim[]` | 이 시설에 대한 소유권 클레임 이력 |
| `leads` | `Lead[]` | 이 시설로 들어온 리드(문의/견적요청/답사예약) 목록 |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([partnerId])`

### `FacilityReview`
> 시설 리뷰

**최초 생성**: 2026-08-05 (`20260805053339_facility_domain`) · **최근 수정**: 2026-08-05 (`20260805053339_facility_domain`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `facilityId` | `String` | FK → Facility.id, UNIQUE(복합), NOT NULL | 리뷰 대상 시설 |
| `userId` | `String` | FK → User.id, UNIQUE(복합), NOT NULL | 리뷰 작성자 |
| `rating` | `Float` | NOT NULL | 별점 (1~5) |
| `content` | `String` | NOT NULL | 리뷰 본문 |
| `createdAt` | `DateTime` | NOT NULL | 작성 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `facility` | `Facility` |  |
| `user` | `User` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@unique([facilityId, userId])` — 1인 1리뷰 (실제 답사완료 인증 체계 없어 로그인 유저 단위로 단순화)

### `Partner`
> 사업자 (법인격 단위) — B2C User와 완전 분리 (§3.1)

**최초 생성**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`) · **최근 수정**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `email` | `String` | UNIQUE, NOT NULL | 로그인 ID |
| `passwordHash` | `String` | NOT NULL | bcrypt 해시 (평문 저장 금지) |
| `refreshTokenHash` | `String?` | NULL | 발급된 refresh token의 sha256 해시 (회전·재사용 탐지용, §6.1) |
| `bizRegNo` | `String` | UNIQUE, NOT NULL | 사업자등록번호 (하이픈 제거 10자리) |
| `companyName` | `String` | NOT NULL | 상호 |
| `ownerName` | `String` | NOT NULL | 대표자명 |
| `contactName` | `String` | NOT NULL | 담당자명 |
| `contactPhone` | `String` | NOT NULL | 담당자 연락처 (숫자만 정규화 저장, utils/phone.ts) |
| `bizLicenseUrl` | `String?` | NULL | 사업자등록증 사본 (비공개 스토리지, §7.4) |
| `status` | `String` | NOT NULL | PENDING \| APPROVED \| REJECTED \| SUSPENDED (§3.2) |
| `rejectReason` | `String?` | NULL | 반려 사유 (status=REJECTED일 때) |
| `approvedAt` | `DateTime?` | NULL | 운영자 승인 일시 |
| `settlementBank` | `String?` | NULL | 정산 계좌 은행명 |
| `settlementAccount` | `String?` | NULL | 암호화 저장 (평문 금지, §7.4) |
| `createdAt` | `DateTime` | NOT NULL | 가입 신청 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `facilities` | `Facility[]` | 클레임 승인으로 연동된 시설 목록 |
| `claims` | `FacilityClaim[]` | 이 파트너가 신청한 시설 클레임 이력 |
| `leads` | `Lead[]` | 이 파트너에게 귀속된 리드 목록 |
| `settlements` | `Settlement[]` | 월 정산 이력 |
| `commissionPolicies` | `CommissionPolicy[]` | 이 파트너 전용 특약 요율 (없으면 기본 요율 적용) |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([status])`

### `FacilityClaim`
> 시설 소유권 클레임 (§3.3) — 신청/심사 API는 이번 범위(0~3단계) 밖, 스키마만 선반영

**최초 생성**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`) · **최근 수정**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `partnerId` | `String` | FK → Partner.id, UNIQUE(복합), NOT NULL | 클레임을 신청한 사업자 |
| `facilityId` | `String` | FK → Facility.id, UNIQUE(복합), NOT NULL | 클레임 대상 시설 |
| `status` | `String` | NOT NULL | PENDING \| APPROVED \| REJECTED |
| `evidenceUrl` | `String?` | NULL | 시설 인허가증 등 소유 증빙 |
| `reviewNote` | `String?` | NULL | 운영자 심사 메모 |
| `reviewedAt` | `DateTime?` | NULL | 운영자 심사 일시 |
| `createdAt` | `DateTime` | NOT NULL | 클레임 신청 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `partner` | `Partner` |  |
| `facility` | `Facility` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@unique([partnerId, facilityId])` — 같은 사업자의 중복 신청 방지
- `@@index([status])`

### `LeadNumberCounter`
> 견적요청 번호(leadNo) 발번용 일자별 원자적 카운터. UPDATE ... SET seq = seq + 1 형태(Prisma upsert increment)가 Postgres 행 잠금으로 동시 요청에서도 안전하다 — leadNo @unique와 함께 이중 방어(§4.2).

**최초 생성**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`) · **최근 수정**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `dateKey` | `String` | PK, NOT NULL | 'YYMMDD' |
| `seq` | `Int` | NOT NULL | 해당 날짜의 다음 발번 시퀀스 (Postgres 행 잠금 기반 원자적 증가) |

### `Lead`
> 리드 (문의 · 견적요청 · 답사예약) — 수수료 청구의 증거 (§4)

**최초 생성**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`) · **최근 수정**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `leadNo` | `String` | UNIQUE, NOT NULL | EB-260810-0001 (표시·대조용, 불변, §4.2) |
| `type` | `String` | NOT NULL | QUOTE \| CONSULT \| CALL (§4.1 — BOOKING은 2026-08-11 FacilityBooking 폐기와 함께 제거됨) |
| `facilityId` | `String` | FK → Facility.id, NOT NULL | 리드가 발생한 시설 |
| `partnerId` | `String?` | FK → Partner.id, NULL | 발생 시점 귀속 파트너 (없으면 비제휴) |
| `userId` | `String?` | FK → User.id, NULL | 로그인 유저면 참조 (탈퇴 시 null) |
| `applicantName` | `String?` | NULL | 신청자 스냅샷 (§4.4) — 정산 확정 후 마스킹 — 신청 시점 이름 스냅샷 |
| `applicantPhone` | `String?` | NULL | 신청 시점 연락처 스냅샷 |
| `maskedAt` | `DateTime?` | NULL | 마스킹 처리 일시 (null이면 아직 원본 유지) |
| `payload` | `Json` | NOT NULL | type별 요청 내용 (희망일시/하객수/종교/예산 등) |
| `thirdPartyConsentAt` | `DateTime?` | NULL | 개인정보 제3자 제공 동의 시각 (§7.1) |
| `consentSnapshot` | `Json?` | NULL | 동의 시점의 고지 문구 버전 — 분쟁 대비 |
| `status` | `String` | NOT NULL | REQUESTED\|NOTIFIED\|RESPONDED\|CONVERTED\|LOST\|INVALID (§4.3) |
| `statusHistory` | `Json` | NOT NULL | [{status, at, by, note}] |
| `billable` | `Boolean` | NOT NULL | 청구 대상 여부 (운영자 확정) |
| `billedAmount` | `Int?` | NULL | 청구 금액 스냅샷 (§8.2) |
| `commissionPolicyId` | `String?` | FK → CommissionPolicy.id, NULL | 어느 요율로 계산했는지 (분쟁 시 유일한 증거) |
| `settlementId` | `String?` | FK → Settlement.id, NULL | 정산에 포함된 이후 채워짐 |
| `disputeReason` | `String?` | NULL | 정산 이의제기 사유 |
| `createdAt` | `DateTime` | NOT NULL | 리드 발생 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `facility` | `Facility` |  |
| `partner` | `Partner?` |  |
| `user` | `User?` |  |
| `commissionPolicy` | `CommissionPolicy?` |  |
| `settlement` | `Settlement?` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([partnerId, status])`
- `@@index([facilityId, createdAt])`
- `@@index([createdAt])`

### `CommissionPolicy`
> 수수료 정책 — 값이 아니라 "유효기간이 붙은 레코드"로 관리 (§8.2, §12.2)

**최초 생성**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`) · **최근 수정**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `basis` | `String` | NOT NULL | PER_CONVERSION \| PER_LEAD \| MONTHLY_FLAT |
| `amount` | `Int?` | NULL | 정액(원) |
| `rate` | `Decimal?` | NULL | 정률 (0.0500 = 5%) |
| `currency` | `String` | NOT NULL | 통화 단위 (기본 원화) |
| `partnerId` | `String?` | FK → Partner.id, NULL | null = 기본 요율 / 값 = 해당 파트너 특약 |
| `effectiveFrom` | `DateTime` | NOT NULL | 적용 시작 시점 |
| `effectiveTo` | `DateTime?` | NULL | null이면 현재 유효 |
| `memo` | `String?` | NULL | 이 요율인 이유 (특약 사유 등) |
| `createdBy` | `String?` | NULL | 변경한 운영자 User.id |
| `createdAt` | `DateTime` | NOT NULL | 정책 등록 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `partner` | `Partner?` |  |
| `leads` | `Lead[]` | 이 요율이 적용된 리드 목록 |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([partnerId, effectiveFrom])`

### `Settlement`
> 월 정산 (§8.1) — 발행/이의제기 API는 이번 범위 밖, 스키마만 선반영

**최초 생성**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`) · **최근 수정**: 2026-08-10 (`20260810020410_partner_lead_commission_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `partnerId` | `String` | FK → Partner.id, UNIQUE(복합), NOT NULL | 정산 대상 사업자 |
| `periodYm` | `String` | UNIQUE(복합), NOT NULL | '2026-08' |
| `leadCount` | `Int` | NOT NULL | 정산 대상 리드 건수 |
| `amount` | `Int` | NOT NULL | 정산 총액(원) |
| `status` | `String` | NOT NULL | DRAFT \| ISSUED \| DISPUTED \| PAID |
| `issuedAt` | `DateTime?` | NULL | 정산서 발행 일시 |
| `paidAt` | `DateTime?` | NULL | 실제 지급 완료 일시 |
| `createdAt` | `DateTime` | NOT NULL | 레코드 생성 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `partner` | `Partner` |  |
| `leads` | `Lead[]` | 이 정산에 포함된 리드 목록 |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@unique([partnerId, periodYm])` — 같은 달 중복 발행 방지

### `Expert`
> 전문가(변호사·세무사·행정사·장례지도사) 계정 체계 — Partner(장사시설)와 완전 분리. docs/02_전문가_매칭/02-02_전문가_계정_체계_구현_메모.md 참고. 의도적으로 Lead/CommissionPolicy 관계를 만들지 않는다 — 변호사법 제34조 때문에 직역별 수수료 구조가 갈라져 있어(같은 문서 §2), Partner의 리드 구조를 그대로 재사용하면 변호사만 예외 처리해야 해서 스키마가 오염된다.

**최초 생성**: 2026-08-10 (`20260810134356_expert_account`) · **최근 수정**: 2026-08-11 (`20260811072855_expert_office_address`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `email` | `String` | UNIQUE, NOT NULL | 로그인 ID |
| `passwordHash` | `String` | NOT NULL | bcrypt 해시 (평문 저장 금지) |
| `refreshTokenHash` | `String?` | NULL | Partner와 동일한 회전 방식(sha256 해시) |
| `category` | `String` | UNIQUE(복합), NOT NULL | LAWYER \| TAX_ACCOUNTANT \| ADMINISTRATIVE_SCRIVENER \| FUNERAL_DIRECTOR (docs 02-01 §2) |
| `name` | `String` | NOT NULL | 전문가 본인 성명 |
| `licenseNo` | `String` | UNIQUE(복합), NOT NULL | 자격증·등록번호 (사업자등록번호 아님 — 변호사/세무사 등록번호 등) |
| `licenseOrg` | `String?` | NULL | 등록 기관 (대한변호사협회, 한국세무사회 등) |
| `licenseDocUrl` | `String?` | NULL | 자격증 사본 (비공개 스토리지) |
| `contactPhone` | `String` | NOT NULL | 연락처 (숫자만 정규화 저장, utils/phone.ts) |
| `officeAddress` | `String?` | NULL | 사무실 주소 (전문직 신뢰도·안내용, 선택 입력) |
| `bio` | `String?` | NULL | 소개·경력 |
| `specialties` | `String[]` | NOT NULL | 세부 전문 분야 |
| `status` | `String` | NOT NULL | PENDING \| APPROVED \| REJECTED \| SUSPENDED — 자동승인 없음(Partner와 동일 원칙) |
| `rejectReason` | `String?` | NULL | 반려 사유 (status=REJECTED일 때) |
| `approvedAt` | `DateTime?` | NULL | 운영자 승인 일시 isPublished: 승인(status=APPROVED)과 공개 노출을 분리하는 독립 플래그 — 승인은 됐지만 |
| `isPublished` | `Boolean` | NOT NULL | 프로필이 미비해 아직 노출하고 싶지 않은 경우가 있다(docs 02-03 §4.3). 운영자가 직접 켠다. — 소비자 목록(GET /api/experts)에 노출할지 여부 |
| `settlementBank` | `String?` | NULL | 정산 계좌 은행명 |
| `settlementAccount` | `String?` | NULL | 암호화 저장. 변호사 카테고리는 건별 정산 자체가 없음(Tier3 월정액 광고만) |
| `createdAt` | `DateTime` | NOT NULL | 가입 신청 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `consultRequests` | `ConsultRequest[]` | 이 전문가에게 들어온 상담 신청 목록 (docs 02-03) |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@unique([category, licenseNo])` — 같은 자격증 번호 중복 가입 방지
- `@@index([status])`
- `@@index([category])`

### `ConsultRequest`
> 전문가 상담 신청 (Stage 1 — 무과금 루프). docs/02_전문가_매칭/02-03_전문가_공개노출_및_상담신청_명세서.md Lead(장사시설)와 의도적으로 분리한다 — 대상 주체가 다르고(Expert vs Partner), 변호사법 제34조 때문에 과금 가능 여부가 직역별로 갈린다(§3). 그래서 이 테이블엔 과금 컬럼을 아예 두지 않는다 — billable/billedAmount/commissionPolicyId 같은 필드가 없으면 실수로도 못 채운다(§3.2, docs 02-02 §2가 Lead 재사용을 거부한 논리와 동일).

**최초 생성**: 2026-08-11 (`20260811011506_expert_consult_request_infra`) · **최근 수정**: 2026-08-11 (`20260811011506_expert_consult_request_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `requestNo` | `String` | UNIQUE, NOT NULL | EC-YYMMDD-NNNN (표시·대조용 불변 번호, Lead의 EB-와 접두어로 구분) |
| `expertId` | `String` | FK → Expert.id, NOT NULL | 신청 대상 전문가 |
| `categorySnapshot` | `String` | NOT NULL | 신청 시점의 전문가 직역 — 전문가가 나중에 카테고리를 바꿔도 당시 기준 보존 |
| `userId` | `String?` | FK → User.id, NULL | 로그인 유저면 참조 (탈퇴 시 null) |
| `applicantName` | `String?` | NULL | 신청자 스냅샷 — 정산 확정 후가 아니라 일정 기간 경과 후 마스킹(§8, Lead §4.4와 동일 원칙) — 신청 시점 이름 스냅샷 |
| `applicantPhone` | `String?` | NULL | 신청 시점 연락처 스냅샷 |
| `maskedAt` | `DateTime?` | NULL | 마스킹 처리 일시 (null이면 아직 원본 유지) |
| `channel` | `String` | NOT NULL | ALIMTALK \| PHONE \| VIDEO \| VISIT — 희망 상담 방식 |
| `preferredAt` | `DateTime?` | NULL | 희망 상담 일시 (선택) |
| `content` | `String` | NOT NULL | 상담 희망 내용 |
| `thirdPartyConsentAt` | `DateTime?` | NULL | 개인정보 제3자 제공 동의 시각 — 없으면 접수 거부(§5.2) |
| `consentSnapshot` | `Json?` | NULL | 동의 시점의 고지 문구 버전 — 분쟁 대비 |
| `status` | `String` | NOT NULL | REQUESTED \| ACCEPTED \| COMPLETED \| CANCELLED \| INVALID |
| `statusHistory` | `Json` | NOT NULL | [{status, at, by, note}] |
| `createdAt` | `DateTime` | NOT NULL | 신청 접수 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `expert` | `Expert` |  |
| `user` | `User?` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([expertId, status])`
- `@@index([createdAt])`

### `ConsultNumberCounter`
> 상담 신청 번호(requestNo) 발번용 일자별 원자적 카운터. LeadNumberCounter와 별도 테이블 — EB-(리드)와 EC-(상담)의 일련번호가 서로를 건너뛰면 대조가 어려워진다(docs 02-03 §4.2).

**최초 생성**: 2026-08-11 (`20260811011506_expert_consult_request_infra`) · **최근 수정**: 2026-08-11 (`20260811011506_expert_consult_request_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `dateKey` | `String` | PK, NOT NULL | 'YYMMDD' |
| `seq` | `Int` | NOT NULL | 해당 날짜의 다음 발번 시퀀스 (Postgres 행 잠금 기반 원자적 증가) |

### `DigitalPlatform`
> 디지털 플랫폼 안내 카탈로그(§3.1) — 운영자가 관리하는 마스터 데이터, 유저 데이터 아님. 금융·가상자산 플랫폼은 이 테이블에 절대 넣지 않는다(§3.2 C분류, §1.1 배제 근거).

**최초 생성**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`) · **최근 수정**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `name` | `String` | NOT NULL | 표시명 (예: '네이버') |
| `category` | `String` | NOT NULL | EMAIL \| SNS \| CLOUD \| SUBSCRIPTION \| ETC |
| `actionType` | `String` | NOT NULL | DELETE(삭제) \| MEMORIALIZE(추모 전환) \| CANCEL(해지) |
| `officialUrl` | `String?` | NULL | 공식 유족 절차 안내 페이지 |
| `requiredDocs` | `String[]` | NOT NULL | 필요 서류 체크리스트 — 파일 자체는 받지 않는다(§3.3) |
| `guideSummary` | `String` | NOT NULL | 절차 요약 — 유족이 읽는 본문 |
| `estimatedDays` | `Int?` | NULL | 예상 소요일 (참고값) |
| `needsAgentHelp` | `Boolean` | NOT NULL | true면 행정사 상담(Domain 02) 유도(§3.4) |
| `isPublished` | `Boolean` | NOT NULL | 확인 완료 전에는 노출 안 함 |
| `lastVerifiedAt` | `DateTime?` | NULL | 최종 확인일 — 화면에 함께 노출(§3.1). 오래되면 운영자 화면에서 경고 |
| `sortOrder` | `Int` | NOT NULL | 노출 순서 |
| `createdAt` | `DateTime` | NOT NULL | 등록 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `cleanupItems` | `DigitalCleanupItem[]` | 이 플랫폼을 대상으로 한 유족들의 진행 항목 |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([category, isPublished])`

### `DigitalCleanupItem`
> 유족 개인의 디지털 정리 진행 추적(§3). 증빙 파일 컬럼은 없다 — 없는 필드는 실수로도 채워질 수 없다(§3.3, §9.1-1, docs 02-03 §3.2와 동일 논리).

**최초 생성**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`) · **최근 수정**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `userId` | `String` | FK → User.id, NOT NULL | FK → User (03-01 §2 원칙, §5.5) |
| `platformId` | `String?` | FK → DigitalPlatform.id, NULL | FK → DigitalPlatform (직접 입력 항목이면 null) |
| `customName` | `String?` | NULL | 카탈로그에 없는 서비스를 유족이 직접 추가한 경우 |
| `status` | `String` | NOT NULL | TODO \| PREPARING \| SUBMITTED \| DONE \| BLOCKED |
| `statusHistory` | `Json` | NOT NULL | [{status, at, note}] |
| `memo` | `String?` | NULL | 유족 개인 메모 (접수번호 등을 본인이 적어두는 칸) |
| `createdAt` | `DateTime` | NOT NULL | 항목 추가 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `user` | `User` |  |
| `platform` | `DigitalPlatform?` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([userId, status])`

### `Memorial`
> 온라인 추모관(§4) — 고인 1명당 1개, 유족이 개설(§4.1). 공개범위 기본값은 PUBLIC이 아니라 LINK다(§4.2) — 사망 사실+유족 구성이 공개 색인되면 부고 사칭 보이스피싱의 표적 정보가 된다.

**최초 생성**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`) · **최근 수정**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `slug` | `String` | UNIQUE, NOT NULL | 공개 URL용 추측 불가 토큰(§4.2, §9.1-2) — 순번 금지 |
| `createdByUserId` | `String` | FK → User.id, NOT NULL | FK → User — 개설자(상주) |
| `deceasedName` | `String` | NOT NULL | 고인 성명 |
| `deceasedBirthDate` | `DateTime?` | NULL | 생년월일 — 공개 응답에서 제외(§4.2, §6.1) |
| `deceasedDeathDate` | `DateTime?` | NULL | 사망일 |
| `portraitUrl` | `String?` | NULL | 영정 사진 |
| `epitaph` | `String?` | NULL | 추모 문구 |
| `visibility` | `String` | NOT NULL | PRIVATE \| LINK \| PUBLIC (§4.2) |
| `falseReportAgreedAt` | `DateTime` | NOT NULL | 허위 개설 시 법적 책임 고지 동의 시각(§4.3-4) |
| `reportedAt` | `DateTime?` | NULL | 신고 접수 시각 — 값이 있으면 자동 PRIVATE 전환(§4.3-3, §9.1-5) |
| `reviewedAt` | `DateTime?` | NULL | 운영자 확인 시각 |
| `closedAt` | `DateTime?` | NULL | 개설자가 닫음 (소프트 삭제) |
| `createdAt` | `DateTime` | NOT NULL | 개설 일시 |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `createdByUser` | `User` |  |
| `tributes` | `MemorialTribute[]` | 헌화 목록 |
| `guestbook` | `MemorialGuestbook[]` | 방명록 목록 |
| `photos` | `MemorialPhoto[]` | 사진 앨범 |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([createdByUserId])`
- `@@index([visibility])`

### `MemorialTribute`
> 헌화(§4.4) — 비회원 허용, 로그인 사용자는 1인 1회 보장. IP는 원문 저장 금지, 해시만(§9.1-6).

**최초 생성**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`) · **최근 수정**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `memorialId` | `String` | FK → Memorial.id, UNIQUE(복합), NOT NULL | 헌화 대상 추모관 |
| `userId` | `String?` | FK → User.id, UNIQUE(복합), NULL | FK → User (비회원이면 null) |
| `visitorHash` | `String` | NOT NULL | 비회원 중복 억제용 해시(브라우저 토큰+IP 조합) — IP 원문 저장 금지(§4.4) |
| `createdAt` | `DateTime` | NOT NULL | 헌화 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `memorial` | `Memorial` |  |
| `user` | `User?` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@unique([memorialId, userId])` — 로그인 사용자 1인 1회 보장 (userId=null인 비회원 행은 유니크 제약 미적용)

### `MemorialGuestbook`
> 방명록(§4.5) — 비회원 작성 허용, 작성자명은 스냅샷(01-05 §4.4 원칙 승계, §5.5).

**최초 생성**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`) · **최근 수정**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `memorialId` | `String` | FK → Memorial.id, NOT NULL | 방명록 대상 추모관 |
| `userId` | `String?` | FK → User.id, NULL | FK → User (비회원이면 null) |
| `authorName` | `String` | NOT NULL | 작성자명 스냅샷 |
| `relationToDeceased` | `String?` | NULL | 고인과의 관계 (예: '친구') |
| `message` | `String` | NOT NULL | 본문 |
| `deletedByOwnerAt` | `DateTime?` | NULL | 상주가 삭제(§4.5) — 소프트 삭제 |
| `hiddenAt` | `DateTime?` | NULL | 운영자 강제 비공개 — 소프트 삭제로 원문 보존 |
| `createdAt` | `DateTime` | NOT NULL | 작성 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `memorial` | `Memorial` |  |
| `user` | `User?` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([memorialId, createdAt])`

### `MemorialPhoto`
> 추모 사진(§4.6) — 업로드는 구현하되 실서비스 오픈은 오브젝트 스토리지 전환 이후로 묶는다 (systems.md §5, §9.1-7). 업로드 상한 등 수치는 policy.ts에 둔다(01-05 §12).

**최초 생성**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`) · **최근 수정**: 2026-08-12 (`20260812050713_digital_estate_memorial_infra`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `memorialId` | `String` | FK → Memorial.id, NOT NULL | 사진이 속한 추모관 |
| `uploadedByUserId` | `String` | FK → User.id, NOT NULL | FK → User — 업로드 권한은 개설자로 제한(초기) |
| `url` | `String` | NOT NULL | 이미지 URL |
| `title` | `String` | NOT NULL | 사진 제목 |
| `sortOrder` | `Int` | NOT NULL | 대표 사진은 sortOrder 최소값 (Facility.images[0] 규칙과 동일 사상) |
| `createdAt` | `DateTime` | NOT NULL | 등록 일시 |

**관계 필드** (물리 컬럼 아님 — Prisma가 FK를 통해 연결해 보여주는 뷰. 제약조건은 위 표의 FK 스칼라 컬럼 참고)

| 필드 | 대상 모델 | 설명 |
|---|---|---|
| `memorial` | `Memorial` |  |
| `uploadedByUser` | `User` |  |

**복합 제약조건 / 인덱스 원문** (위 표의 UNIQUE(복합) 태그가 정확히 어떤 컬럼 조합인지 여기서 확인)
- `@@index([memorialId, sortOrder])`

### `Admin`
> 운영자(내부 직원) 계정 — Partner/Expert와 인증 패턴은 같지만 **공개 가입 API가 없다.** 계정 생성은 시드 스크립트(prisma/seed-admin.ts)로만 한다 — 가입 폼을 공개하면 그 자체가 보안 구멍이 된다. 2026-08-10 신설 — 장사시설/전문가 가입 승인 + 시설 클레임 승인용.

**최초 생성**: 2026-08-10 (`20260810141549_admin_account`) · **최근 수정**: 2026-08-10 (`20260810141549_admin_account`)

| 컬럼 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | `String` | PK, NOT NULL | 고유 식별자(UUID) |
| `email` | `String` | UNIQUE, NOT NULL | 로그인 ID |
| `passwordHash` | `String` | NOT NULL | bcrypt 해시 (평문 저장 금지) |
| `refreshTokenHash` | `String?` | NULL | 발급된 refresh token의 sha256 해시 (회전·재사용 탐지용) |
| `name` | `String` | NOT NULL | 운영자 이름 |
| `createdAt` | `DateTime` | NOT NULL | 계정 생성 일시 (seed-admin.ts로만 생성됨) |
| `updatedAt` | `DateTime` | NOT NULL | 최종 수정 일시 |

<!-- AUTO-GENERATED:END -->

---

## 📝 보충 설명 (수동 관리 — 자동 생성 스크립트가 건드리지 않음)

_아래에 JSON 필드 실제 형태, 상태값 전이도, 테이블 간 업무 흐름 등을 채워나갈 것._
