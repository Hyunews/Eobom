# 🎨 00-39. 디자인 기준 재정립 — 명세서

> **문서 성격**: 명세서 · **값과 규칙의 새 정본**. `[Claude:Opus]` 작성(2026-09-18).
> **계기**: 사람 지적 — *"디자인 관련 작업이 굉장히 불만족스럽고 불필요한 작업의 반복이라
> 방법 자체를 다시 잡고 가야 한다고 판단."* · *"규칙처럼 명시된 것들에 영향받지 않게 했으면 한다.
> 새롭게 작업을 해보려 해도 기존의 규칙 등에 의해 계속 막히는 느낌을 받음. 심지어 그 규칙들을
> 일종의 하드코딩 식으로 주먹구구식이기에 더욱 엉망."*
> **방법**: 기준 화면 하나(`/care-guide` 상중 행정 가이드)를 **백지에서 다시 그려** 규칙을 뽑았다.
> 시안·확정 과정 → Claude Design 캔버스(사람 소유 아티팩트, 2026-09-18 버전 7).

---

## 📌 1. 결론 먼저

**A · 문서형으로 확정**했다(2026-09-18, 사람 선택). 세 방향(A 문서형 / B 시간축 / C 지금 할 일
하나)을 웹·모바일 한 쌍씩 그려 비교한 결과다.

🔴 **이 문서가 `00-09`(디자인 시스템)·`00-29`(모바일 전략)·`00-38`(적응형 모바일 UX)의 디자인
규정을 대체한다.** 세 문서는 2026-09-18부로 구속력이 해제됐고(각 문서 머리말 참조), **근거와
경위를 남기는 기록**으로만 유지한다.

🔵 **폐기하지 않는 것**: `00-38` §11.1·§11.2의 360px 실측 기록과 `00-29`의 계측값은 *"그때 이랬다"*
는 **사실**이므로 그대로 둔다. 새 화면을 재측정할 때 비교 대상이 된다.

---

## 🧭 2. 왜 다시 잡았나 — 반복의 원인

**증상**: 2026-09-16~17 이틀 동안 *"측정 → 위반 목록 → 개별 수정 → 재측정"* 을 반복했고,
그 결과물은 토큰 치환·`44px` 맞추기·클래스 추출이었다. 실제로 화면이 나아진 것은 **사람이 직접
*"줄글 나열이 지저분하다"* 고 지적했을 때뿐**이었다(→ `walkthrough.md` 09-16 CounselingPage 4차 후속).

**원인 1 — 수치를 품질로 착각했다.** `00-38` §11의 DoD 9개(44px·1.8배·16px)는 **접근성 최저선**이지
디자인 품질 기준이 아니다. 전부 통과해도 답답하거나 촌스러울 수 있다.

**원인 2 — 규칙이 코드에 흩어져 있었다.** 2026-09-18 실측 기준 **인라인 스타일 1,426개**
(`.tsx` 53개 중 49개 파일). `00-38` §10 Phase 4 주석이 *"인라인이 982 → 1,304개로 늘었다"* 고
경고했는데 그 뒤로 더 늘었다. 화면 규칙이 공통 CSS가 아니라 컴포넌트마다 숫자로 박혀 있으니,
행간 하나 고치는 데 14곳을 찾아야 하고 다음 사람이 또 인라인으로 짠다.

**원인 3 — 규칙이 새 시도를 막았다.** `token-guard.js`(PreToolUse 훅)가 허용하는 글자 크기가
**`0.85`·`0.95`·`1.05`·`1.3rem` 네 개**뿐이고 HEX 색은 전부 위반으로 잡았다. `00-09` §2.3은
*"협상 대상이 아님"* 으로 `18px`·`56px`·`1.7`을 못박았고, §6.3은 *"세련됨은 이 6개"* 라며 미감까지
규정했다. **새 디자인을 하는 게 아니라 기존 틀 안에서 조립하는 상태**였다.
→ 훅은 2026-09-18 사람이 주석 처리했고, 2026-09-21 `settings.json`에서 제거했다(JSON 주석이 설정 전체를 깨뜨림).
재가동 방법·조건은 `.harness/tools/token-guard.js` 머리 주석에 있다.

---

## 🖌️ 3. 색 — 확정값

🔵 기존 브랜드 색에서 **미세하게 조정**했다(폐기가 아니라 톤 정리). 웜 뉴트럴 계열을 유지하되
본문 대비를 올리고, 경고색을 붉은 쪽으로 통일했다.

| 용도 | 값 | 비고 |
| :--- | :--- | :--- |
| 페이지 배경 | `#FDFCFA` | 기존 `#FBF9F5`보다 살짝 밝다 |
| 본문 글자 | `#22303F` | 기존 `#1A2B4C`(남색)에서 중성 쪽으로 |
| 보조 글자 | `#5C6773` | 설명문·부연 |
| 흐린 글자 | `#8A9199` | 기한·건수 |
| 더 흐린 글자 | `#A29B90` | 라벨(`기한`·`근거`·`기관`) |
| 구분선 | `#EFEBE4` | 항목 사이 |
| 구분선(강) | `#ECE7DF` · `#E7E2DA` | 영역 사이 |
| 강조 / 긴급 | `#C0392B` | *되돌릴 수 없음* · 기한 경고 |
| 포인트 | `#5B7065` | 링크·로그인·진행바. 기존 값 유지 |
| 선택 배경 | `#F5F2EC` | 목차 현재 항목 |
| 입력 테두리 | `#B9B3AA` | 체크박스 |
| 버튼 테두리 | `#D8D2C8` | |

🔴 **그림자를 쓰지 않는다**(모달 제외). 기존 `--box-shadow: 0 8px 30px rgba(26,43,76,.06)`는
카드 구조와 함께 폐기했다. 모달만 `0 24px 70px rgba(34,48,63,.26)`.

---

## 🔤 4. 글자 — 확정값

**서체**: 제목 **명조**(`KoPub World Batang`, 캔버스에서는 `Noto Serif KR`로 대체) ·
본문 **산세리프**(`Pretendard`). 🔵 두 서체의 대비가 이 디자인의 인상을 만든다.

| 자리 | 웹 | 모바일 | 서체 |
| :--- | :---: | :---: | :--- |
| 페이지 제목 | **40px** / 600 | **27px** / 600 | 명조 |
| 구간 제목 | **24px** / 600 | — | 명조 |
| 모달 제목 | **20px** / 600 | **20px** / 600 | 명조 |
| 항목 제목 | **19px** / 700 | **17px** / 700 | 산세 |
| 본문·설명 | **16~17px** / 400 | (목록에 두지 않음) | 산세 |
| 보조·기한 | **15px** / 400 | **14px** / 400 | 산세 |
| 라벨 | **12~13px** / 500~700 | **12px** / 700 | 산세 |

**행간**: 본문 **1.8** · 제목 **1.3~1.5** · 짧은 보조 텍스트는 지정하지 않는다.
🔴 **`letter-spacing`은 큰 제목에만**(`-0.3` ~ `-0.8px`). 본문에는 주지 않는다.

🔴 **스케일 밖 값을 금지하지 않는다.** 위 표는 *지금 쓰는 값*이지 허용 목록이 아니다.
새 화면에서 다른 크기가 필요하면 쓰고, 자리 잡으면 이 표에 추가한다 — `token-guard`가 네 개
값으로 막던 방식으로 돌아가지 않는다.

---

## 📐 5. 레이아웃 — 확정값

| | 웹 | 모바일 |
| :--- | :---: | :---: |
| 헤더 높이 | **76px** | 50px |
| 좌우 여백 | **40px** | **20px** |
| 좌측 목차 | **236px** | 없음(가로 탭) |
| 본문 읽기 폭 | 🔴 **764px 고정** | 320px(360 − 여백) |
| 항목 상하 패딩 | **22~24px** | **18~20px** |

🔴 **본문은 넓은 화면을 채우지 않는다.** 1404px 화면에서도 읽기 폭 764px을 유지하고 남는 공간은
비운다. 기존 화면이 `1077px` 2단 그리드를 쓰면서 **오른쪽 열이 거의 비어 있던 것**이 이 결정의 계기다.

### 5-1 🔴 예외 — 쓰는 화면의 `입력 | 미리보기` 2단은 **1048px** (2026-09-21 등재)

기준 화면 **`obituary`**. 폼은 **넣은 값이 어떻게 보이는지**를 옆에서 같이 봐야 하는 화면이라
읽기 폭 하나로 부족하다. 🔵 **새 숫자가 아니라 §5 확정값의 합**이다 —
`236`(좌측 목차) + `764`(읽기 폭) + `48`(사이 간격) = **1048px**. 제목 줄(`.v2-page-head`)이
이미 같은 폭이라 **제목과 본문의 양 끝이 맞는다.**

| | 값 | 비고 |
| :--- | :---: | :--- |
| 전체 폭 | **1048px** | `.v2-form-shell` · `max-width:100%` · 가운데 정렬 |
| 입력 열 | **560px** | 입력 칸 한 줄이 다 보이는 폭. 폼 모달(§6.8 규칙 21)과 같은 값 |
| 미리보기 열 | 나머지 | `sticky`(위 **24px**) — 스크롤을 따라온다 |
| 두 열 간격 | **48px** | |
| 🔴 767px 이하 | **1단** | 미리보기는 옆 열이 아니라 **폼 안**으로 들어간다(규칙 20) |

🔵 **이것도 "채우지 않는다"의 편이다.** 1404px 화면에서 1048px을 쓰고 남는 곳은 그대로 비운다.
🔴 **예외를 여기서 늘리지 않는다.** `입력 | 미리보기`가 **실제로 있는** 화면만 해당하고,
미리보기 없이 폼만 있는 화면은 **읽기 폭 764px 그대로**다.

---

## 🧩 6. 규칙

각 규칙은 기준 화면에서 실제로 정해진 것이고, 괄호 안은 그렇게 정한 이유다.
🔵 **번호를 제목에 세지 않는다** — §9.3대로 화면을 하나씩 구현하면서 **여기 계속 추가**되기 때문이다.
지운 번호는 비워 둔다(다른 문서가 `§6-14`처럼 참조한다).

🔴 **규칙을 읽기 전에 화면 종류부터 고른다** (2026-09-18 신설 — 근거는 아래 🔵).
**목록이라고 다 같은 목록이 아니다.** 아래 셋 중 어디에 속하는지가 규칙 1·8의 적용을 가른다.

| 종류 | 하는 일 | 기준 화면 | 생김새 |
| :--- | :--- | :--- | :--- |
| **훑는 목록**(체크리스트·문서형) | 위에서 아래로 **훑고 체크**한다. 고르지 않는다 | `care-guide` | 카드 없음. **1px 구분선 행** |
| **고르는 목록**(선택형) | 여럿을 **비교해서 하나를 고른다** | 🔴 **`facility`** | **카드 허용**(규칙 1-1) |
| **쓰는 화면**(폼·입력) | 값을 **넣는다** | 🔴 **`obituary`** | **§6.8**(2026-09-21 확장) |

🔵 **왜 갈랐나** — 규칙 1·8은 `care-guide` **한 화면에서** 나왔는데, 문자 그대로 읽으면 사람이
*"현재 만족스럽다"* 고 확정한 `facility`(카드 + 그림자 + 카드 전체 클릭)가 **규칙 위반이 된다**
(2026-09-18). 규칙이 틀린 게 아니라 **적용 범위를 안 적었던 것**이다.

### 6.1 구조

1. 🔴 **훑는 목록에는 카드와 그림자를 쓰지 않는다.** 구분은 **1px 선과 여백**으로 한다.
   (카드 안에 카드, 그 안에 칩 트랙 — 상자가 3겹이었다)
   🔴 **적용 범위는 훑는 목록·문서형 화면까지다**(2026-09-18 정정). 고르는 목록은 규칙 1-1.

1-1. 🔴 **고르는 목록은 카드를 써도 된다** (2026-09-18 사람 확정). 기준 예시는 **`facility`** —
   썸네일·거리·평점·`[지도] [상담]`이 한 면에 모이고 **카드형↔리스트형 토글**까지 있는 화면이며,
   사람이 *"현재 만족스러움, 고칠 필요 없음"* 으로 확정했다. 🔴 **`facility`는 손대지 않는다.**
   - 🔴 **표면 스타일(그림자·호버 떠오름)은 규칙으로 정하지 않는다** — 화면마다 고른다
     (2026-09-18 사람 선택). 지금: `facility`=그림자 `0 8px 30px`+호버 4px / `pickup`=**테두리만**.
     🔵 *한쪽으로 통일하자*는 안은 기각됐다 — `pickup` 카드는 사람이 캔버스 시안 A에서 직접
     그림자를 빼고 저장한 형태이고, `facility`는 그대로 두라고 했다. **둘 다 의도된 결과다.**
   - 🔴 **다만 중첩 표면은 0** — 그림자나 테두리가 있는 면 **안에 같은 것을 또 두지 않는다**
     (`00-38` §11 DoD #7). 카드를 허용한다고 상자 3겹이 허용되는 게 아니다.
   - 카드 면에 무엇을 얹을지는 화면이 정한다. `pickup`은 **제목·지역·CTA뿐**이고 평점·태그는
     모달로 보냈다(2026-09-18 시안 A).
2. 🔴 **본문 읽기 폭을 고정**하고 넓은 화면을 채우지 않는다. (§5)
3. 🔴 **사이드바 대신 헤더 드롭다운.** 좌측 72px 아이콘 바를 없애고, 헤더의 모드 버튼
   (`생전 준비` / `임종 및 사후 정리`)에 호버하면 하위 메뉴가 열린다. 메뉴 구성은
   `modeNav.ts`가 정본이고 `status: 'preview'`인 항목에는 **"준비 중"** 배지를 단다.

### 6.2 활자와 위계

4. 🔴 **제목은 명조, 본문은 산세리프.** (§4)
5. 🔴 **긴급도를 색 배지로 만들지 않는다.** *되돌릴 수 없음* 을 **12px 붉은 글자 한 줄**로
   제목 위에 둔다. (회색 알약 배지가 모든 항목에 붙어 위계가 사라져 있었다)
6. 🔴 **기한은 오른쪽 끝 열에 회색으로 정렬**한다. 제목 옆에 붙이지 않는다.
   (제목 길이에 따라 배지가 다음 줄로 밀려 레이아웃이 흔들렸다)

6-1. 🔴 **목록에는 설명문을 두지 않는다 — 웹·모바일 공통** (2026-09-18 사람 결정).
   한 줄은 **제목 + 기한**이 전부다. 설명이 필요하면 **제목을 눌러 모달**로 본다(규칙 9).
   🔵 처음에는 *"넓은 화면은 읽을 여유가 있으니 웹에는 남기자"* 고 했으나, **같은 문장이 모달에서는
   빠지고 목록에는 남는** 상태가 되어 규칙 11과 어긋났다. 목록은 **훑는 곳**이지 읽는 곳이 아니다.

### 6.3 조작

7. 🔴 **버튼은 밑줄이 아니라 테두리**로 만든다. (`내용 보기` · `전문가 상담` · `장사시설 찾기`)
8. 🔴 **체크는 체크박스를 눌러야만 된다.** 행 전체를 클릭 영역으로 삼지 않는다.
   🔴 **훑는 목록에만 해당**(2026-09-18 정정) — 체크와 열람이 **다른 동작**이라 가른 것이다.
8-1. 🔴 **고르는 목록은 행·카드 전체가 클릭 영역이다.** 동작이 *"고른다"* 하나뿐이라 가를 게 없다.
   (구현: `counseling` 행 `role="button"` · `facility` 카드 · `pickup` 카드 제목)
   🔵 카드 면의 CTA(`견적 신청`)처럼 **모달을 건너뛰는 지름길 버튼**은 둘 수 있다 — 대신
   `stopPropagation`으로 행 클릭과 겹치지 않게 한다(`00-38` §8.2와 같은 처리).
9. 🔴 **항목 제목을 누르면 안내가 열린다.** 체크와 안내는 다른 동작이다.

### 6.4 안내(모달)

10. 🔴 **모달은 세 줄뿐이다** — **제목 · 기한 · 근거**. 그 이상 넣지 않는다.
    🔵 *근거*와 *기관*을 따로 두지 않는다 — **근거 줄 자체가 링크**다(§6-14). 기관이 특정되는
    3건만 그 자리에 기관명이 오고, 나머지는 법령명과 조문이 온다.
11. 🔴 **해설하거나 조언하는 문장을 넣지 않는다.** 모달은 **사실만** 준다.
    (2026-09-18 사람 결정 — *"화장 후에는 부검·재확인이 불가능합니다"*, *"'언제까지'가 아니라
    '언제 이후에' 가능한지의 문제입니다"* 같은 문구를 뺐다. 해석은 사용자 몫이고, 더 필요하면
    전문가 상담으로 보낸다.)
    🔵 **모달에 완료 버튼을 두지 않는다** — 체크 경로가 둘이 되면 규칙 8과 어긋난다.

### 6.5 모바일

12. 🔴 **간소화하고 줄글을 최소로 한다.** (2026-09-18 사람 지시)
    🔵 목록에서 설명문을 빼는 것은 이제 **웹도 같다**(규칙 6-1). 아래는 **모바일에만** 해당한다.
    - *"가장 급한 것"* 같은 **상단 요약 블록을 두지 않는다**
    - 부제를 두지 않는다 (제목이 이미 말하고 있다)
    - 각 행 오른쪽에 **꺾쇠(›)** 로 "누르면 더 있다"를 표시한다
    - 모달은 **바텀시트**로 열고, 닫기는 X 아이콘이 아니라 **하단 버튼**으로 둔다
    - 모달 안에서도 더 줄인다 — 근거를 `장사법 제6조`까지만(웹은 `감염병 등 예외`도 표시)

### 6.6 공통

13. 🔴 **줄바꿈을 코드에 박지 않는다.** `<br>` 대신 `text-wrap: pretty`로 브라우저가 나누게 한다.
    (`사망 후 24시간 경과 전<br>화장·매장 금지`는 그 항목 하나에만 맞는 줄바꿈이었다)

14. 🔴 **기관을 못 찾으면 법령으로 보낸다** (2026-09-18 사람 결정 — *"따로 검색되지 않은 기관명은
    관련 법령을 따라갈 수 있도록 법령정보 사이트로 연결한다"*). 모달의 `기관` 줄은 아래 순서로 정한다.

| 순위 | 조건 | 링크 |
| :---: | :--- | :--- |
| **1** | 기관·서비스가 특정됨 | 그 사이트로. 지금 해당하는 것은 **3건** — `정부24`(id 7) · `e하늘 장사정보시스템`(id 4) · `안심상속 원스톱 서비스`(id 9·18, 정부24 안의 서비스) |
| **2** | `legalBasis`에 **법령**이 있음 | 🔴 **국가법령정보센터**로. **13건** 해당 |
| **3** | 둘 다 아님 | **`기관` 줄을 생략한다.** 빈 링크를 만들지 않는다. **5건** — *"확인 필요"*(id 17·22) · *"제도별 상이"*(id 21) · *"실무: …"*(id 1) · `-`(id 3·19) |

#### 🔗 국가법령정보센터 URL — **실측으로 확인한 규칙** (2026-09-18)

```
https://www.law.go.kr/법령/{법령명}/{제N조}
```

🔵 **세 가지를 실제로 열어보고 확인했다** — 추측이 아니다.

| 확인한 것 | 결과 |
| :--- | :--- |
| `…/법령/민법/제1019조` | ✅ **제1019조(승인, 포기의 기간)** 본문만 표시 — 조문 단위로 정확히 열린다 |
| `…/법령/장사법/제6조` | ✅ **약칭이 통한다.** "장사 등에 관한 법률(약칭: 장사법)" 제6조(매장 및 화장의 시기)가 열린다 |
| `…/법령/상속세 및 증여세법/제67조` | ✅ **공백이 들어가도 된다.** 인코딩만 하면 그대로 동작 |

🔵 **그래서 `legalBasis` 값을 거의 그대로 쓸 수 있다.** 변환은 두 가지뿐이다.
· `§` → `제N조` (예: `장사법 §6` → `장사법/제6조`)
· **항(`①②③`)은 떼고 조까지만** 건다 (예: `민법 §1019①` → `민법/제1019조`)
· 조문이 없으면 법령까지만 (예: `국민건강보험법` → `…/법령/국민건강보험법`)

🟡 **링크 라벨은 법령명과 조문을 그대로 보여준다** — `장사법 제6조 ↗`. *"법령정보센터"* 라는
사이트 이름을 라벨로 쓰지 않는다(사용자가 알고 싶은 건 **무슨 법 몇 조**다).

### 6.7 클래스 등재 — 그룹①에서 나온 것 (§9.3-2)

🔴 **새 화면은 여기 있는 것부터 쓴다.** 정본은 `eobomDev/frontend/src/styles/design-v2.css`이고,
이 표는 **"무엇이 이미 있는지"의 목록**이다. 없는 것만 새로 만들고, 만들었으면 **여기에 적는다.**
🔴 `index.css`(100KB)에 넣지 않는다.

| 쓰임 | 클래스 | 비고 |
| :--- | :--- | :--- |
| 읽기 폭 고정 | `.v2-content` | 좌측 목차 없는 화면도 764px(§5) |
| 훑는 목록 행 | `.v2-list-row` `.v2-list-main` `.v2-list-title` `.v2-list-meta` | 제목은 `min-width:0`+말줄임(모바일 2줄 방지) |
| 고르는 목록 카드 | `.v2-card-grid` `.v2-card` `.v2-card-title` `.v2-card-meta` `.v2-card-cta` | 2열, 767px 이하 1열 (규칙 1-1) |
| 버튼 | `.v2-btn-primary` `.v2-btn-outline` `.v2-btn-solid` | 전부 `inline-flex`(아이콘+글자 정렬) |
| 배지 | `.v2-badge-neutral` | 옛 `.hdr-mode-badge`를 이 이름으로 통합 |
| 빈 상태·오류 | `.v2-empty` `.v2-error-text` | |
| 안내·경고 띠 | `.v2-notice` `.v2-notice-warn` | |
| 진입 배너 | `.v2-banner` `.v2-banner-title` `.v2-banner-cta` | 중립색. 안쪽 여백 44px·요소 간 28px |
| 필터·칩 | `.v2-filter-row` `.v2-select` `.v2-chip-row` `.v2-chip` | 칩은 **모든 화면 노출**(모바일 전용 `.v2-mobile-tab`과 다름) |
| 2단 목록 | `.v2-two-col` | 768px 미만 1단 |
| 모달 버튼 그룹 | `.v2-modal-actions` | |
| 화면폭 분기 | `.v2-desktop-only` `.v2-mobile-only` | 🔵 **문구 자체가 바뀌는 자리는 JS 분기 대신 이것** — `useIsMobile` 훅을 새로 쓰지 않는다 |
| 목록 아래 CTA | `.v2-list-footer-btn` | 위 간격 16px |
| 폼 입력(§6.8) | `.v2-field` `.v2-input` `.v2-field-hint` | `.v2-field`가 라벨+입력+보조문구를 세로로 묶음. `.v2-input`은 필터·칩 행의 것과 같은 클래스(상자 스타일 공유, 배치만 자리마다 다름) |

🆕 **그룹②(폼)에서 나온 것** — 기준 화면 `obituary`, 2026-09-21 등재.
🔵 **폼 안에서만 쓰는 조정은 `.v2-form` 아래로 한정**한다(필터·계산기 같은 다른 자리의
`.v2-input`·`.v2-field`가 같이 바뀌면 안 된다).

| 쓰임 | 클래스 | 비고 |
| :--- | :--- | :--- |
| 2단 껍데기 | `.v2-form-shell` `.is-2col` `.v2-form-aside` `.v2-form-preview-inline` | **1048px**(§5-1). 767px 이하 1단 — `aside` 숨기고 `preview-inline` 노출 |
| 폼 본체 | `.v2-form` `.v2-form-section` `.is-plain` `.v2-form-row` `.is-wide-first` `.is-keep` | 구간 간격 **40px**(모바일 28px) · 구간 위 1px 선 · `row`는 한 줄 2칸(모바일 세로, `.is-keep`만 가로 유지) |
| 필수·선택 표시 | `.v2-req` `.v2-opt` | 12px 굵은 글자. 붉은색 / 흐린색 (규칙 16) |
| 체크 행 | `.v2-check` `.v2-check-sub` `.v2-check-error` | 최소 높이 44px · 상자 20px (규칙 18) |
| 접는 구간 | `.v2-more` `.v2-more-toggle` `.v2-more-body` | *선택 정보 더보기* — 구간 제목과 같은 크기의 행 하나 |
| 제출 | `.v2-form-submit` `.v2-icon-btn` | 버튼 폭 100%·높이 48px(모바일 52px) · `.v2-icon-btn`은 옆 입력 칸과 높이를 맞춘 44px(모바일 48px) |
| 카톡 카드 미리보기 | `.v2-kakao-stage` `.v2-kakao-card` `-image` `-body` `-title` `-desc` `-cta` `-foot` | 🔴 **우리 화면이 아니라 카톡 화면의 모사** (규칙 20) |
| 폼 모달 | `.v2-modal.is-form` `.v2-modal-actions.is-form-actions` | 560px·패딩 32px, 모바일 바텀시트 (규칙 21) |

### 6.8 폼·입력 — **그룹② 착수 전 선행 확정** (2026-09-18)

🔴 **아직 그룹②(폼) 시안은 없다.** 그런데 상속세 계산기 모달을 먼저 고치기로 해서, **거기서
나오는 입력 규칙이 그룹②의 씨앗**이 된다. 그래서 임시 인라인으로 줄이지 않고 **클래스로 만들어
여기 적는다** — `obituary`가 그대로 물려받는다(§9.2가 말한 *"§6에 폼 규칙이 없다"* 의 첫 칸).

15. 🔴 **입력 칸은 `.v2-field` / `.v2-input` / `.v2-field-hint`** 로 만든다. 옛 `.form-input`
    (`index.css:619`, 높이 52px)을 그대로 쓰지 않는다.

| 무엇 | 옛 `.form-input` | 🔴 확정값 | 근거 |
| :--- | :--- | :--- | :--- |
| 높이 | 52px | **44px**(데스크톱) · **48px**(모바일) | `00-38` §11 DoD #5 — 손으로 찍는 면의 하한이 44px |
| 글자 | 16px | 🔴 **16px 그대로 — 내리지 않는다** | iOS Safari는 **16px 미만 입력에 포커스하면 화면을 자동 확대**한다(돌아올 때 레이아웃이 틀어짐). `00-38` §3.2 본문 16px 표준과도 같다 |
| 좌우 패딩 | `0 1rem` | **0 14px** | |
| 라벨 | 16px | **13px** `--v2-fs-label` | |
| 보조문구(`* 약 3.00억원 …`) | 16px 본문색 | **13px** `--v2-fs-label` + `--v2-text-muted` | 🔵 **화면을 무겁게 만든 진짜 원인이 여기다** — 입력마다 본문 크기 문장이 한 줄씩 붙어 있었다 |
| 항목 간 세로 간격 | `1.1rem` 등 혼재 | **16px** 고정 | |

🔵 **"입력 칸을 줄여달라"를 높이만으로 읽지 않는다** — 높이는 44px가 하한이라 8px밖에 못 줄인다.
실제로 줄어드는 양은 **라벨·보조문구·간격**에서 나온다.

#### 6.8-1 🆕 **그룹② 대표 `obituary`에서 확정된 것** (2026-09-21 등재)

> 위 15번은 **계산기 모달에서 나온 씨앗**이었고, 여기부터는 **폼 대표 화면을 실제로 구현하면서**
> 나온 것이다. 🔵 `mypage` 등 남은 그룹② 화면은 **이걸 그대로 물려받는다** — 다시 정하지 않는다.

16. 🔴 **필수는 라벨 옆 12px 붉은 글자 `필수`** 로 표시한다. **별표(`*`)도 배지도 아니다** —
    긴급도를 배지로 만들지 않는 **규칙 5와 같은 결**이다. 강조가 필요한 선택 항목만 같은 자리에
    흐린 `선택`(`.v2-opt`)을 붙이고, **나머지 선택 항목에는 아무 표시도 하지 않는다**(표시가
    모든 줄에 붙으면 표시가 아니다).

17. 🔴 **오류는 폼 전체가 아니라 칸마다 붙인다.** 브라우저 기본 검증(`required` 속성)을 쓰지
    않는다 — 기본 말풍선은 **한 번에 한 칸만** 알려주고 우리 활자·색을 따르지 않는다.
    제출에 실패하면 **네 가지가 동시에** 일어난다.

    | | 무엇 | 값 |
    | :---: | :--- | :--- |
    | ① | 칸 아래 붉은 문장 | `.v2-error-text` — 보조문구 자리를 **대체**한다(같이 쌓지 않는다) |
    | ② | 칸 테두리 | `aria-invalid="true"` → **1.5px 붉은 테두리** |
    | ③ | 폼 위 요약 한 줄 | *"확인이 필요한 항목이 **N개** 있습니다. 붉게 표시된 곳을 확인해 주세요."* (`role="alert"`) |
    | ④ | 포커스 | **화면 위에서 첫 번째** 오류 칸으로 옮긴다 |

    🔴 **오류 문구는 "무엇을 하라"로 쓴다** — *"고인 성함을 입력해 주세요."* (`필수 항목입니다` ✗).
    🔵 **고친 칸은 즉시 푼다** — 값을 한 글자 고치면 **그 칸의 오류만** 사라진다(제출을 다시
    기다리지 않는다). 나머지 칸은 그대로 둔다.

18. 🔴 **체크는 한 가지 모양뿐이다**(`.v2-check`) — 동의·토글·선택을 서로 다르게 그리지 않는다.
    상자 **20px** · 행 최소 높이 **44px**(터치 하한, `00-38` §11 DoD #5) · 글자 16px ·
    부연은 아래 줄에 13px(`.v2-check-sub`).
    🔴 **폼의 체크는 라벨 전체가 클릭 영역이다**(`<label for>`) — **규칙 8의 예외**다.
    규칙 8이 상자만 누르게 한 이유는 훑는 목록에서 *"체크"* 와 *"안내 열기"* 가 **경쟁**하기
    때문인데(규칙 9), 폼에는 그 경쟁 동작이 없다. **규칙 8-1과 같은 논리**다.

19. 🔴 **저장 상태는 버튼이 말한다.** 스피너·토스트·별도 안내 줄을 만들지 않는다.
    누르는 순간 **버튼을 잠그고**(`disabled` + `aria-busy`) **글자를 바꾼다** —
    `부고장 만들기` → `처리 중…` / `수정 사항 저장` → `저장 중…`.
    🔵 **잠그는 것이 중복 제출 방지의 본체다** — 안내는 그 사실을 말할 뿐이다.

20. 🔴 **미리보기에는 설명문을 붙이지 않는다.** 웹은 오른쪽 열(`sticky`), 모바일은 폼 안
    확인 사항 **바로 앞**에 둔다(§5-1). *"입력하신 내용이 이렇게 보입니다"* 같은 줄은 두지
    않는다 — **보면 아는 것**이고, 규칙 12(줄글 최소)에 걸린다.
    🔵 **카톡 카드 미리보기는 이 문서의 색·그림자 규칙 밖이다** — 그건 **우리 화면의 면이
    아니라 카카오톡 화면을 그린 그림**이다(바깥 면 `#DCE3E8`, 카드에 그림자 있음). 🔴 그래서
    **여기 색을 우리 토큰으로 바꾸지 않는다** — 실제와 달라지면 미리보기가 거짓말이 된다.

21. 🔴 **수정은 새 화면이 아니라 모달**이다(`.v2-modal.is-form`, **560px** · 안쪽 여백 32px).
    **규칙 10(모달은 세 줄)은 안내 모달의 규칙**이고, 폼 모달은 거기 해당하지 않는다.
    - 모달 안에서는 **구간 제목과 구분선을 빼고** 필드만 **16px 간격**으로 나열한다(`.is-plain`).
    - 🔴 **닫기는 `X`가 아니라 하단 `[취소]` 버튼**이다 — 폼은 **쓰다 만 값이 사라지는** 곳이라
      닫기가 눈에 띄어야 한다.
    - 모바일은 **바텀시트**(규칙 6-5와 같음) · 버튼 높이 52px · `[취소] 1 : [저장] 2` 비율.

##### 🔵 15번 표의 보완 2건 — 시안 값을 그대로 쓰지 않은 곳

| 무엇 | 시안 | 🔴 확정값 | 이유 |
| :--- | :--- | :--- | :--- |
| 라벨 색 | `--v2-text-label` `#A29B90` | **`--v2-text-muted`** `#5C6773` | `#A29B90`은 흰 배경 대비 **약 2.6:1** — 13px 글자에 쓸 수 없다(`00-38` 접근성). 이 토큰은 **큰 장식 제목**(`.v2-page-title`) 자리에만 남긴다 |
| 입력 테두리 | 버튼 테두리와 공용 | **`--v2-input-border`** `#B9B3AA` | 입력 칸은 **누르는 면이 아니라 넣는 면**이라 버튼보다 테두리가 또렷해야 빈칸이 빈칸으로 보인다. 자리표시 글자는 `#737B85` |

##### 모바일에서 달라지는 값 (767px 이하)

| 무엇 | 웹 | 모바일 |
| :--- | :---: | :---: |
| 라벨 · 보조문구 · 체크 부연 | 13px | **12px** |
| 오류 문장 | 13px | **14px** |
| 구간 간격 | 40px | **28px** |
| 제출 버튼 높이 | 48px | **52px** |
| 행(`.v2-form-row`) | 가로 2칸 | **세로 1칸** (관계·성함처럼 짧은 쌍만 가로 유지) |

---

## ✅ 7. 웹과 모바일의 관계

🔴 **같은 화면이지만 밀도를 다르게 가져간다.** 기존 구현은 웹과 모바일이 **같은 카드 구조를
공유**해서 어느 쪽에도 맞지 않는 중간 형태가 됐다.

| | 웹 | 모바일 |
| :--- | :--- | :--- |
| 설명문 | 🔵 **둘 다 모달로 보낸다**(규칙 6-1) | ← 같음 |
| 상단 요약 | *가장 급한 것* 블록 있음 | **없음** |
| 기한 | 오른쪽 끝 열 | 제목 아래 한 줄 |
| 구간 이동 | 좌측 목차(세로) | 상단 탭(가로 스크롤) |
| 모달 | 가운데 540px · 근거에 부연 표시 | 바텀시트 · 근거는 조문까지만 |

🔵 **차이는 밀도가 아니라 배치다.** 규칙 6-1로 설명문이 양쪽에서 빠지면서, 웹과 모바일의 차이는
*"무엇을 보여주는가"* 가 아니라 *"어디에 놓는가"* 로 줄었다. 기존 구현이 **같은 카드 구조를 공유해**
어느 쪽에도 맞지 않던 것과 반대 방향이다.

---

## ❓ 8. 확정이 필요한 것

| # | 항목 | 상태 |
| :---: | :--- | :--- |
| **1** | ✅ **웹 목록 설명문 — 제거 확정(2026-09-18)** | **§6-1로 이관.** 목록은 웹·모바일 모두 **제목 + 기한**만 |
| **2** | ✅ **관련 기관 링크 — 확정(2026-09-18)** | **§6-14로 이관.** 기관이 특정되면 그 사이트, 아니면 **국가법령정보센터**로 보낸다 |
| **3** | 🟡 **`note`·`irreversibleNote` 필드를 지울 것인가** | #1이 확정되면서 **화면 어디에도 안 쓰인다**. 🔵 **권고: 남긴다.** ⓐ `07-02`(사망후 법정기한 체크리스트)가 검증한 **내용 자산**이고 ⓑ 나중에 상담·FAQ·전문가 연결 화면에서 쓸 수 있다 ⓒ 지워도 화면이 나아지지 않는다. 🔴 다만 **"화면에 쓰지 않는 필드"임을 `07-02`에 적어둬야** 다음 사람이 다시 목록에 꺼내지 않는다 |
| **4** | 🟡 **서체 실물** | 캔버스는 `Noto Serif KR`·`Noto Sans KR`로 그렸다(웹폰트 제약). 실제 구현은 `KoPub World Batang`·`Pretendard`로 돌아가며, **명조 제목의 인상이 캔버스와 같은지** 실기기 확인이 필요하다 |
| **5** | ✅ **`facility` — 현행 유지 확정(2026-09-18)** | 사람: *"현재 만족스러움. 고칠 필요 없음. 오히려 카드리스트를 쓰는 것의 대표 예시가 되어줬으면 해."* → **§6-1-1의 기준 화면**으로 등재. 🔴 **손대지 않는다**(그룹① 재구현 대상에서 제외) |
| **6** | ✅ **카드 표면 스타일 — 통일하지 않음(2026-09-18)** | 사람 선택. §6-1-1로 이관 — `facility`=그림자+호버 / `pickup`=테두리만, **둘 다 그대로 둔다.** 규칙은 *"카드를 써도 된다"* 까지만 적는다 |
| **7** | ✅ **상속세 계산기 모달 — A형 채택(2026-09-18)** | 사람: *"A형으로 하되 내부 입력 칸 등의 사이즈를 줄일 필요 있어 보임."* → 입력 수치는 **§6.8로 이관**(높이 44/48px·글자 16px 유지·라벨과 보조문구 13px) |

---

## 🚀 9. 진행 방식 — **그룹별** (2026-09-18 사람 확정)

🔴 **21개 화면을 하나씩 시안 잡지 않는다.** `/care-guide` 하나에 캔버스를 **아홉 번** 고쳤다.
그 과정은 **규칙을 만들기 위한 것**이었고, 규칙이 선 지금은 반복할 이유가 없다.

### 9.1 화면 그룹과 시안 대상

**그룹의 첫 화면만 시안**을 잡아 규칙을 확장하고, 같은 그룹의 나머지는 **규칙 적용 후 결과만
확인**한다. 어긋나면 그때 시안으로 돌아간다.

| 그룹 | 화면 | 시안 | 대표 |
| :---: | :--- | :---: | :--- |
| **①** 목록·체크리스트 | `care-guide` · `facility` · `counseling` · `pickup` · `my-obituaries` | ✅ **완료** | 훑는 목록 = `care-guide` · 🆕 **고르는 목록 = `facility`**(§6-1-1) |
| **②** 폼·입력 | `obituary` · `mypage` · `ending-note` · `farewell-messages` · `digital-estate` | 🔴 필요 | ✅ **`obituary`** |
| **③** 읽기(법정문서) | `privacy` · `terms` | 🟡 짧게 | `privacy` |
| **④** 랜딩(외부 유입) | `/o/:slug` · `/m/:slug` · `family-invite` | 🔴 필요 | ✅ **`/o/:slug`** |
| **⑤** 홈·진입 | `home` · `prep` · `bereaved` | 🔴 필요 | `home` |
| **⑥** 운영자 | `admin` · `biz` · `partner` | ⏸ 보류 | — |
| **⑦** 미정 | `memorial` | ⏸ 보류 | 추모관 재구현과 함께 |

🔵 **①은 시안 없이 바로 구현**할 수 있다 — `facility`·`counseling`·`pickup`·`my-obituaries` 4개.

✅ **①의 결과(2026-09-18)** — `care-guide`·`counseling`·`pickup`·`my-obituarylist` **4개 완료**
(커밋 `f61c5f4`까지, `my-obituarylist`는 사람 "통과" 확정). `facility`는 §8 #5로 **현행 유지**.
🔴 **남은 것은 상속세 계산기 모달 하나** — A형 채택(§8 #7), 구현은 `TaxSimulatorModal.tsx`.
🟡 뒤로 미룬 것 2건: `pickup` 거리순 정렬(vendors 목업에 **좌표가 없다**) · 전문가 `region` 필드
(가입 폼 + `PublicExpert` 타입 + 백엔드 스키마 3곳을 함께 고쳐야 하는 **별건**).

✅ **대표 확정(2026-09-18)** — ② `obituary`(인라인 **108개로 최다** = 폼 패턴이 가장 많이 들어 있고,
유족이 장례식장에서 실제로 쓰는 화면) · ④ `/o/:slug`(조문객 유입 **100% 모바일**).
🟡 ②의 대안이던 `mypage`는 로그인 직후 첫 착지라는 강점이 있으나, **폼 규칙을 뽑는 것이 목적**이라
복잡한 쪽을 골랐다. `mypage`는 같은 그룹이므로 `obituary`에서 나온 규칙을 적용만 한다.

### 9.2 그룹마다 시안이 따로 필요한 이유

| 그룹 | 목록형 규칙으로 안 되는 것 |
| :---: | :--- |
| **②** | **입력 중 오류·저장 상태·필수 표시**를 다뤄야 한다. §6에 그 규칙이 없다 |
| **③** | 조문 번호·들여쓰기·목차 등 **법정문서 특유의 구조**. 다만 본문 위주라 §4 타입과 `--lh-reader`로 거의 해결된다 |
| **④** | **조문객이 카톡으로 여는 화면**이라 로그인도 네비게이션도 없다. `00-38` §8.4가 *"회귀 비용이 가장 크다"* 고 한 곳이다 |
| **⑤** | `00-23`(메인화면 진입구조)이 레이아웃 정본이라 **규칙 6-3(헤더 드롭다운)과 부딪힐 수 있다** |

### 9.3 각 화면에서 하는 일

1. **구현** — 🔴 **인라인이 아니라 클래스로.** `index.css`(100KB)에 넣지 말고 분리한다.
2. **패턴 추가** — 새로 나온 공통 클래스를 이 문서 §6에 등재. 다음 화면이 그걸 쓴다.
3. **확인** — 실기동은 사람. 🔵 수치 확인이 필요하면 `00-38` §11.2의 360px iframe 방식을 쓴다
   (창 리사이즈는 Chrome 최소폭에 걸려 500px까지밖에 안 줄어든다).

🔴 **하지 않는 것**: 화면을 순회하며 **수치 위반 목록을 만드는 일**. 그게 §2의 반복이었다.

### 9.4 🟡 순서 — 권고

**① 나머지 4개**(시안 불필요) → **② 폼형**(가장 크고 규칙 확장이 많다) → **③ 읽기형**(가장 쉽다) →
**④ 랜딩형** → **⑤ 홈**.

🔵 다만 **④ 랜딩형을 앞당길 이유**가 있다 — 조문객은 실제 사용자 중 가장 많고 100% 모바일이다.
반대로 `00-38` §8.4는 *"이미 모바일 우선으로 잘 돼 있으니 거터만 맞추고 구조를 건드리지 않는다"*
고 했으므로 **변경 폭이 작을 수도** 있다. 사업 판단이므로 사람이 정한다.

---

## 🔗 10. 관련 문서

| 문서 | 관계 |
| :--- | :--- |
| `00-09` 디자인 시스템 | 🔴 **이 문서가 대체.** 색·타이포·여백·크래프트 규칙 전부 해제됨 |
| `00-29` 모바일 대응 전략 | 🔴 디자인 지침 해제. 🔵 실측 기록은 유효 |
| `00-38` 적응형 모바일 UX | 🔴 5대 원칙·DoD·페이지별 지침 해제. 🔵 §11.1·§11.2 실측 기록은 유효 |
| `00-23` 메인화면 진입구조 | 🟡 홈 레이아웃 정본. 규칙 3(헤더 드롭다운)이 홈에도 적용되는지는 **미확정** |
| `00-26` 모드별 네비게이션 | 🔴 규칙 3의 근거. `modeNav.ts`가 메뉴 구성 정본 |
| `07-02` 사망후 법정기한 체크리스트 | 기준 화면의 **내용 정본**. 이 문서는 표현만 정한다 |
