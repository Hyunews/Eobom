# 📝 2026-09-04 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현) / 사용자 (실기동 검증)

> **한 줄 요약**: 유족 메시지 보관함의 **음성 듣기·삭제부터 파기까지**(`06-05` Phase D-4·D-6~D-11)를
> 하루에 끝냈고, 마지막 어드민 파기 화면은 **더미 데이터로 실기동 검증까지 통과**했다.

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [Claude:Sonnet] D-4·D-6·D-6-1 — 음성 듣기·삭제 + 업로드 시점 이동 (wt121·wt122)

- 음성 재생을 **presigned URL이 아니라 서버 경유**로 구현했다(선암호화라 브라우저가 직접 재생하지
  못한다 — `06-05` §5.6-1). `GET /api/farewell-messages/:id/audio`는 **메시지 기준 소유권**만 보고,
  `mediaKey`를 받는 엔드포인트는 만들지 않았다.
- 삭제는 `mediaDeletedAt` 기록만 하고 **런타임에서 `DeleteObject`를 부르지 않는다**(§5.6-4).
- **업로드 시점을 "녹음 중지"에서 확인 모달의 "저장"으로 옮겼다**(§5.6-5) — 중지 즉시 업로드하면
  사용자가 취소해도 R2에 고아 객체가 남는다.
- 실기동 검증 통과(사용자).

### 2. [Claude:Opus] `06-05` §5.6-7·§5.6-8 신설 — 편지 하드 삭제로 R2 원본 참조가 끊기는 구멍

wt121 편차 검토 중에 나온 것. 편지 행을 **하드 삭제**하면 그 행이 들고 있던 `mediaKey`가 같이
사라져 **R2 원본을 가리킬 문자열이 없어진다** — 지울 수 없는 파일이 조용히 남는 구조였다.
→ 편지도 **소프트 삭제**(§5.6-7)로 바꾸고, 30일 경과분을 지우는 **파기 배치**(§5.6-8)를 신설했다.

### 3. [Claude:Sonnet] D-7·D-8 — 편지 소프트 삭제 + 파기 배치 (wt123)

`FarewellMessage.deletedAt` 추가, 목록·상세·음성 라우트 전부에 `deletedAt: null` 조건 추가,
`prisma/destroy-farewell-media.ts` 신설(🔴 `--confirm` 없이는 dry-run). 이 시점 dry-run 대상 0건.

### 4. [Claude:Opus] wt123 편차 재판단 — 원장을 **파일로 두면 안 된다** (`06-05` §5.6-8-1-1 신설)

wt124가 아카이브 파기 원장을 **파일**(`backups/archive-purge-pending.log`)로 만들었는데,
**운영 1단계는 Render에서 돌고 Render 파일시스템은 재배포 시 사라진다**. 원장이 사라지면
아카이브 버킷에 무엇이 남았는지 아무도 모른다 — 이미지 로컬디스크 건과 같은 사고다.
→ 판정 **🔄스펙갱신**: 원장을 **DB 테이블 `ArchivePurgeQueue`**(§6.4)로 바꾸고,
**실행 환경 판별**(§5.6-8-2)과 **어드민 파기 화면**(§5.6-8-3)을 D-10·D-11로 신설했다.
🔴 운영 자격증명을 로컬로 내려받아 돌리지 않기 위한 결정이기도 하다(`security.md` §1).

### 5. [Claude:Sonnet] D-9 재작업 + D-10 + D-11 (wt125)

- `ArchivePurgeQueue`(원장) · `FarewellPurgeAuditLog`(감사 로그) 두 테이블 신설.
  🔴 **DB 쓰기 게이트 통과** — `pg_dump` 백업 생성·**파일 확인** → 사람 CONFIRM → `migrate dev`.
- 파기 로직을 `farewellPurgeService.ts` **한 곳으로 모았다** — 스크립트와 어드민 화면이 같은 함수를
  부른다. 원장 생성 → R2 원본 삭제 → `mediaKey` 정리 **순서를 코드가 강제**한다.
- 버킷명이 `-dev`로 끝나면 개발 모드로 판별해 원장에 쓰지 않는다(D-10).
- 어드민 `SCR-010`에 **"유족메시지 파기" 탭** 추가 — 건별 선택만(일괄 버튼 없음),
  **대상 건수 직접 입력 + 비밀번호 재인증** 후 실행. 🔴 강제 삭제 API·`--force`는 만들지 않았다.

### 6. [사용자] 어드민 파기 화면 e2e 실기동 검증 — **통과** (wt126)

31일 전으로 백데이트한 더미 2건(①음성만 만료 ②편지 전체 만료)으로 목록 노출 → 선택 → 건수 확인
→ 재인증 → 실행까지 직접 수행. 결과가 스펙 그대로였다:

- ① 행은 `mediaKey`·`mediaMime`만 `null`이 되고 **행은 유지**(§5.6-4)
- ② 행은 **행 자체 파기**
- `FarewellPurgeAuditLog` 1건 생성(`count=2`, `targetIds`에 두 id 모두)
- `ArchivePurgeQueue` **0건 유지** — dev 버킷이라 원장에 안 쓰는 것이 정상(§5.6-8-2)

### 7. [Claude:Opus] 문서 반영 — 세션 마무리

- `06-05` **§6.5 신설** — `FarewellPurgeAuditLog`를 정본에 **소급 기재**했다.
  구현이 먼저였고(§8 #57을 만족시키려 wt125가 도입), 문서가 뒤따라간 건이라 그 사실을 절에 명시.
- `06-05` §8 — D-9·D-10·D-11에 완료 표시, #60(화면 대장 등재)을 ✅로 닫음.
- `00-06` §3 — 운영자 화면 탭 구성에 **"유족메시지 파기"** 등재. 🔵 **새 `SCR` 번호를 주지 않았다**
  — 새 화면이 아니라 `SCR-010`의 탭이다.

---

## 📌 남은 것 (다음 세션)

- 🔴 **`--confirm` 스크립트 경로 미실기동** · 운영 버킷에서 `ArchivePurgeQueue`가 실제로 쌓이는지,
  2단계(Cloudflare 대시보드 수동 삭제 → 완료 표시)도 미검증 — 둘 다 dev 환경에선 확인 불가.
- ▶ **다음 = D-5(반출)**. 🟡 후속으로 워커 필터(`00-11` §5.4-5-2-1-1).
- 🔵 검증 과정의 날것 기록(30일을 기다리지 않고 **31일 전으로 백데이트한 행을 새로 생성**해
  검증한 방법, 가짜 `mediaKey`를 쓴 이유, "건수 입력창의 회색 글씨는 `placeholder`" 건)은
  `claude_tasks.md` (92)에 있다.
