# 📝 2026-08-25 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [사용자] 모바일 재지적 — **08-21과 같은 문장**

- *"모바일 환경에서 편의성 가독성 너무 떨어짐. 모바일 환경에서 잘 볼 수 있도록(웹앱 등)
  기획부터 실천해야함."*
- 08-21에 같은 지적으로 `00-29`를 세우고 1차 작업(프리미티브 4종 + §5 1~3순위 이관,
  walkthrough (55))까지 들어간 **뒤에도** 체감이 그대로라는 뜻 → 추정 대신 **실측**부터.

### 2. [Claude:Opus] 배포본을 375px로 실계측 — 브라우저 실기동

- 08-21 1차는 **소스 `grep`만** 했고, walkthrough (55)·(56)이 *"브라우저 실기동은 전 섹션 공통으로
  안 함"* 이라고 스스로 남겨둔 공백이 있었다. 그 공백을 닫았다.
- `https://eobom.vercel.app`을 폭 375px 프레임에 띄우고 요소별 `getBoundingClientRect`·
  `getComputedStyle`로 8개 경로를 계측.

| 판정 기준(`00-29` §6.2) | 결과 |
| :--- | :--- |
| ① 375px에서 가로 스크롤 없음 | ✅ **통과** — 8개 경로 전부 `scrollWidth` 364(뷰포트 379) |
| ② 본문 15px 이상 | ❌ **전면 미달** — `/facility` 텍스트 **160/227곳(70%)**, `/care-guide` 26/54 |

- 🔴 **개발자의 재지적은 정확했다.** 1차 작업이 그리드·제목만 다뤘고 **본문 글자 크기를 다룬 적이
  없다.** "고쳤다"고 보고된 것과 "여전히 안 보인다"는 체감이 둘 다 사실이었다.

### 3. [Claude:Opus] 2차 원인 — `rem` 기준선이 디자인 시스템과 어긋나 있었음

- `index.css`가 `body { font-size: var(--base-font-size) }`(18px)만 걸고 **`html`은 브라우저
  기본 16px** 그대로다. **`rem`은 `html` 기준**이므로 인라인 `0.85rem`은 의도한 15.3px가 아니라
  **13.6px**(본문 18px의 76%)로 렌더돼 왔다.
- 🔴 **`00-09` §2.3이 *"협상 대상이 아님"* 으로 못박은 6070 시니어 본문 18px 기준선이,
  화면에서는 한 번도 적용된 적이 없다.**
- ✅ 대신 좋은 소식: 인라인 `fontSize` **377곳이 전부 `rem`, px 0곳** — **루트 한 줄로 전부
  움직인다.** 08-21의 *"982곳 이관"* 비용 추정은 폰트에 한해 과대평가였다.

### 4. [Claude:Opus] 조치 3개 확정 + 안전성 실측

| | 조치 | 규모 |
| :---: | :--- | :--- |
| A | `@media (max-width:768px){ html { font-size: var(--base-font-size) } }` | **1줄** |
| B | 인라인 `0.7`/`0.75`/`0.78`/`0.8rem` → `0.85rem` | **97곳** |
| C | `00-09` §2.2 기준선 규칙 등재 | 문서(완료) |

- **A+B → 본문 최소 15.3px**로 판정 기준 ②를 만족한다.
- 루트 ×1.0 / ×1.1 / ×1.15 / ×1.2 스윕 실측: **가로 오버플로 0, 텍스트 잘림 0** 전 구간
  (문서 높이만 +7~21%). ×1.15에서도 97곳이 남아 **B가 필요한 근거**가 나왔다.
- 🟡 데스크톱은 제외 — `rem` 여백 869곳이 같이 커져 2026-08-11 밀도 조정을 되엶.

### 5. [Claude:Opus] 편의성(스크롤 길이)·PWA 재답변

- `/facility` 1페이지가 375px에서 **19,698px = 약 23화면**, 카드 1개 640px(한 화면에 1.3개).
  → 🟡 모바일 페이지당 30 → 15개, 빈 이미지 플레이스홀더 ≤480px 숨김.
- **PWA**: 원인이 루트 기준선이었으므로 *"PWA는 글자 크기를 1픽셀도 안 고친다"*(08-21)가
  확증됐다. 🟡 다만 **조치 A·B 배포 후** 매니페스트만 얹는 반나절 작업으로는 권고 —
  근거는 가독성이 아니라 **위의 23화면 스크롤**이다. 서비스워커 캐시는 계속 미도입.

### 6. [Claude:Opus] 문서 갱신

- `00-29` — 2차 갱신 안내, §6.1 `.stat-row` 360px 자기모순 해소, §8 확정 4건 → **7건**,
  **§10~§15 신설**.
- `00-09` — §2.3 **`rem` 기준선 규칙 신설**, **§3.2 반응형 프리미티브 4종 등재**
  (walkthrough (55) 편차 [4]로 남아 있던 숙제), §3.1 *"하단 탭바"* 삭제(구현체 0건 실측).
- `00_DOCS_INDEX.md` 2개 행, `.harness/memory/context.md` ⓪-5·① 갱신.

### 7. [사용자] 06 엔딩노트 실구현 기획 요청 + **사장님 자료 3종**

- *"디지털 엔딩노트 및 유족 메시지 보관함 실구현을 위한 자료 수집 및 전략 필요함. 현재의 기능
  외에 어떠한 기능? 정보 수집? 들이 필요할 지 정리하고, 기획."*
- 사장님 제공: **3단계 생애주기별 연결 모델**(생전 초대 / 사후 승계 / 상중 긴급 온보딩) +
  **사용자 여정 화면 흐름** + **운영 정책 2건**(Zero-Knowledge 보안 분리 · 다중 유족 지정).

### 8. [Claude:Opus] `06-04` 엔딩노트·유족 메시지 보관함 실구현 기획서 작성

- **착수 지점 실측**: `EndingNotePage.tsx` **219줄 전부 목업**(저장이 `alert()`), 백엔드 모델·API
  **0건**. 반면 암호화 도구(`utils/crypto.ts`)는 이미 가동 중이라 **새로 만들 것이 없음**.
- ✅ **사장님 1단계는 이미 90% 구현돼 있습니다** — `00-27` Phase 1·2(가족 지정 + 초대 링크 +
  계정 연결)가 그대로 1단계입니다. 3단계(긴급 온보딩)도 부고장 개설 흐름으로 동작 중.
  **실체가 없는 것은 2단계(사후 승계)와 엔딩노트 본체뿐**입니다.
- 🔴 **화면이 이미 약속한 5건이 전부 뒷받침 없음** — 특히 *"국립연명의료관리기관 DB 호환"* 은
  연동 계획조차 없는 상태의 주장이라 `00-18` §2(*"KISA 최고 등급"*)와 같은 종류입니다.
  **모델보다 문구 정리(Phase 0)가 먼저**로 잡았습니다.
- 🔴🔴 **가장 시급한 발견 — 지금 화면이 비밀번호 원문 입력을 유도하고 있습니다.**
  placeholder 원문에 *"유품 정리 마스터 암호는…"* 이 예시로 들어 있어, 타사 약관의 계정 공유
  금지 위반을 **설계로 권하는 모양**이 됩니다 → **소재 안내형 문구로 교체** 권고.
- ⚠️ **사장님 안과의 충돌 3건 정정**: ①알림톡은 **ⓑ사망 통지에만**(ⓐ생전 지정은 08-21 확정대로
  본인 전달) ②정부24 연계는 **확인 전 단정 금지** ③Zero-Knowledge는 `06-03` §1.1이 이미 기각한
  개념이라 **「생전 봉인(Sealed)」** 으로 용어 통일(요구 자체는 채택).
- **신설 설계**: 모델 4개 · **수집 항목 2개 → 8개 섹션** · 열람 시점 3단 매트릭스 · 개봉 상태
  기계 · Phase 0~3. 🔴 확정 7건 + 외부 확인 6건.

### 9. [Claude:Opus] 사장님 의견에서 파생된 **판단 12건** 정리 — `00-14` §11 신설

- 개발자 지시: *"사장님 의견 관련하여, 판단해야 할 것들 정리해줘.(엔딩노트에 국한하지 않은
  내용이긴 해)"* — 접합 스펙은 `06-04`지만 **판단은 06을 넘어갑니다.**
- ✅ **먼저 6건을 회의 안건에서 걷어냈습니다** — 사장님 안의 1단계 초대·3단계 긴급 온보딩·
  1:N 매핑·생전 잠금·L3/L4·열람범위는 **이미 구현됐거나 설계 확정**입니다.
  *"정하실 것"* 이 아니라 *"보고드릴 것"* 입니다.
- **판단 12건** — 💰경영 5 / 🏢운영 2 / 🔧기술 3 / ⚖️법·문구 2.
- 🔑 **먼저 정할 3건**
  - **11-2 대표 발신번호 실번호** — 11-1(알림톡)이 여기에 종속되고, `1588-0000`이 아직
    화면에 떠 있습니다. **정하는 문제가 아니라 이미 한 약속을 되돌리는 문제.**
  - **11-3 사망 확인 처리 시간(SLA)** — 3단계 모델 전체가 *"확인 후 활성화"* 에 걸려 있는데
    **확인할 사람과 시간이 정해지지 않아 그 화살표가 비어 있습니다.**
  - **11-11 06의 수익 지점** — **비용만 나고 매출이 없는 유일한 도메인**입니다.
- 🔴 **뒤집히는 확정 1건**: *"이어봄은 발송 주체가 되지 않는다"*(08-21) ↔ 알림톡 발송
  → 트리거를 ⓐ생전/ⓑ사망으로 갈라 양립시키는 안을 §11.5에 명시.
- 🔍 **비용 0의 사실 확인 4건**(정부24·알림톡 요건·안심상속 원스톱·등록기관/상담창구)은
  회의 안건이 아니라 **담당자 배정 문제**로 분리했습니다.

### 10. [사용자] 🔴 범위 지적 — *"`06-04`와 사장님 지시사항이 겹쳐 문제가 생긴듯"*

- *"한 프롬프트에 각각을 지시했지만, `06-04`의 조사·기획과 사장님 지시사항은 별개의 내용이었음."*
- **맞는 지적입니다.** 한 프롬프트에 담긴 두 과제를 하나로 읽고 `06-04` 안에서 접합했습니다.
  그 결과 **06 구현 스펙과 플랫폼 연결 구조가 한 문서에 섞였고**, 06을 구현하는 사람이 알 필요
  없는 내용이 스펙에 들어가는 한편 **연결 구조는 06 문서 안에 갇혀** 04·07·`00-12`에서
  찾을 수 없게 됐습니다.

### 11. [Claude:Opus] 분리 — `00-30` 신설 + `06-04` 전면 재작성

- **`00-30` 생애주기 연결모델 검토서**(신규) — 사장님 자료 **원문 보존** + 단계별 대조 +
  연결 구조 + 충돌 3건 + UX 검토 + 운영정책 2건 + 도메인별 영향 + **선행조건**.
- **`06-04`** — 사장님 자료 의존 제거, *"엔딩노트를 어떻게 만들 것인가"* 만 남김.
- 이제 경계가 다시 1:1입니다 — **`06-04`=구현 스펙 / `00-30`=연결 구조 / `00-14` §11=사람의 판단.**
- 🔵 **분리하면서 새로 나온 것 3가지**
  - **"유족 계정"이 두 종류입니다** — 생전 지정(L3~L4)으로 들어온 유족과 긴급 온보딩(L1)으로
    들어온 상주는 권한이 다릅니다. 미리 말하지 않으면 *"가입했는데 왜 안 열리냐"* 가 CS가 됩니다.
  - **"보호 중" 배너는 장식이 아닙니다** — 수락한 유족은 수십 년간 올 이유가 없어 번호가
    바뀝니다. 배너가 **연 1회 재확인의 자연스러운 자리**입니다.
  - 🔴 **2단계 선행조건 5칸 중 서 있는 것이 하나도 없습니다.** 그중 *"확인할 사람과 시간"* 은
    **기술이 아니라 사람 문제라 개발로 메울 수 없습니다.**
- 🔴 **04의 구멍 발견** — 사장님이 *"디지털 유품 리스트"* 를 명시했는데 `04-01`은 **생전 정리까지만**
  설계돼 있고 **사후 유족 이관 경로가 없습니다.** 아직 어느 문서에도 등재되지 않은 항목입니다.

---

### 12. [Claude:Sonnet] `06-04`§6.4·§10 — Phase 0 문구정리 + STT 초안 Phase 1-a 구현

- 사용자가 `EndingNotePage.tsx` 한 곳을 지정해 Phase 0(화면 약속 6건 문구 정리) + Phase 1-a
  STT(Ⓑ 직접 녹음)를 한 사이클로 직접 지시.
- **Phase 0**: `:163` "국립연명의료관리기관 DB 호환" 성공 메시지 삭제, `:212` "열람 키 자동
  발송" → "사망 확인 후 지정 유족에게 열람 권한 부여", `:124~135` SMS 인증 대리인 필드
  전체 삭제(연동된 `:102~108` 배지·설명문도 함께), `:185` placeholder를 소재 안내형으로 교체,
  `:148` 저장 버튼을 비활성 "(준비 중)"으로. 부수적으로 `domainSlides.tsx`의 동일 "열람 키"
  문구도 같이 교정(grep 0건 기준이 `src/` 전체라서).
- **STT Phase 1-a**: Web Speech API(`SpeechRecognition`/`webkitSpeechRecognition`) 기반 직접
  녹음 + 타이핑 항상 병행 입력, 미지원 브라우저는 안내문으로 자연 폴백, 침묵 시 자동 재시작,
  저장 전 확인 게이트, 자필증서 4대 요건은 안내만(시스템이 값을 채우지 않음), 인쇄·복사·`.txt`
  다운로드만 제공(서버 저장·localStorage 저장 전부 없음).
- 🔴 **미검증**: 실브라우저 실기동(마이크 인식·Firefox 폴백) — `tsc`·`build`만 통과 확인.
  다음 세션에서 실기기/브라우저 확인 필요. 상세는 walkthrough(70).

## ⏸ 오늘도 닫지 못한 것

- **`MyPage`·`ObituaryPage`(관리)·`EndingNotePage`·로그인 상태 헤더** — 비로그인이면 안내
  화면만 렌더되고 실 OAuth라 자동 로그인이 불가능하다. **`00-29` §5의 1·3순위가 전부 여기 있다.**
- 🔴 **개발자가 폰으로 직접 로그인해 보는 것 외에 방법이 없다.** `00-26` §6 #9(375px 헤더)도
  그때 함께 닫는다.
