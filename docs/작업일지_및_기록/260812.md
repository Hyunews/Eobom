# 📝 2026-08-12 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [사용자] 08-11 구현 12건 브라우저 E2E 검증

- `/admin`·`/partner`·`/facility`·`/counseling` 대표가 직접 클릭 테스트. 대부분 정상 동작 확인, 3가지 개선 피드백 발견.

### 2. [Claude:Sonnet] E2E 피드백 3건 반영

- **대표자명 수정**: 관리자 대시보드 사업자 정보수정에서 `ownerName`(대표자명)도 고칠 수 있게 API·UI 추가.
- **전문가 사무실 주소 검색모달**: 자유 텍스트였던 `officeAddress`에 다음(Daum) 우편번호 서비스 기반 주소검색 모달(`AddressSearchModal.tsx`) 연결 — 가입폼·관리자 정보수정 양쪽. 별도 API 키·백엔드 불필요.
- **태그 필터 상시노출**: `/facility` 필터 박스 아래에 `TAG_CATALOG` 태그를 항상 보이는 작은 칩(`#사설` 형식)으로 노출, 카드까지 스크롤 안 해도 바로 클릭 필터 가능.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-12 (1) 항목.

### 3. [Claude:Sonnet] 반응형/사이드바 레이아웃 재점검

- 대표 지시: 창 크기를 줄이면 사이드바 확장 시 본문이 가려지거나 줄글이 어색하게 나오는 현상 재점검, 모바일 웹앱 사용도 고려.
- **사이드바 확장 시 본문 가림 버그 확인·수정**: `.main-wrapper` 여백이 72px로 고정돼 있어 사이드바가 240px로 확장돼도 본문이 안 밀려서 168px만큼 덮고 있었음 — 순수 CSS 형제 선택자(`.sidebar:hover + .main-wrapper`)로 동기화.
- **한글 줄바꿈 전역 적용**: `body`에 `word-break: keep-all` 추가(기존엔 없어서 좁은 폭에서 음절 단위로 끊겼음). 감사 중 실제론 어디서도 안 쓰이는 죽은 CSS(`.hero-*`)에만 이 설정이 있었던 걸 발견해 정리.
- **모바일 대응 선제 조치**: 풀페이지 스냅스크롤 섹션 높이에 `100dvh`(동적 뷰포트) 우선 적용 — 모바일 주소창 접힘/펼침에 따른 섹션 경계 어긋남 방지. 480px 이하에서 사이드바 폭 축소.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-12 (2) 항목.

### 4. [Claude:Sonnet] 파트너 리드 조회 화면 구현 — `01-05` §11 4단계

- 대표 지시: §6.1 API(`GET /api/partner/leads`·`/:leadNo`·`PATCH /:leadNo/status`)·§4.3 상태머신 그대로, §7.1(동의 없는 리드 비노출)·§7.3(응답 후 마스킹) 반드시 지킬 것.
- 지금까지 업체 문의가 `Lead`로 적재만 되고 사업자가 볼 화면이 없던 구멍을 메움. 파트너 허용 전이만 화이트리스트로 제한(INVALID는 운영자만, CONVERTED도 자기신고일 뿐 청구확정은 별도 운영자 단계).
- §7.1은 `thirdPartyConsentAt: {not:null}` 조회 조건 하나로 자동 달성(CALL 유형도 자동 제외). §7.3은 상태가 RESPONDED 이상이면 API 응답 시점에 이름·연락처를 즉시 마스킹.
- `BizDashboard.tsx`에 "받은 업체 문의" 화면 신설.
- **실제 브라우저로 로그인해서 전체 플로우 클릭 검증**(임시 스크립트로 테스트 데이터 생성 → 로그인 → NOTIFIED 자동전이 확인 → 응답완료 클릭 시 마스킹 확인 → 성사 처리로 종결 확인 → 종결상태 재변경 409, 없는 leadNo 404까지 확인 → 테스트 데이터 정리).
- 상세: `에이전트_기록/walkthrough.md` 2026-08-12 (3) 항목. **[Gemini] 같은 날 ✅통과 판정 + `01-05` 문서 §6.1·§11 상태 동기화까지 완료.**

### 5. [Claude:Sonnet] 파트너 화면 UX 개선 3건

- 대표 피드백: (1) "클레임"→"연동"으로 용어 통일, (2) 사진 업로드에 "저장" 버튼 추가(선택 즉시 업로드 안 되게), (3) 업체 문의 시 이름·연락처 자동입력(수정 가능, 문의사항만 새로 입력).
- 용어 통일은 UI 문구만 변경 — `FacilityClaim` 모델·API 경로 등 내부 이름은 그대로 둠.
- 사진 업로드는 `pendingImages` state로 선택 시 미리보기만 보여주고 "저장" 클릭 시에만 실제 업로드, "취소"로 폐기 가능.
- 문의 자동입력은 `User`에 연락처 필드가 없어 계정에서 못 끌어옴 — 마지막 제출값을 `localStorage`에 저장해 다음 문의에 채움. 로그인 상태면 이름은 계정 이름이 우선.
- 세 건 다 실제 브라우저 클릭으로 검증(파일 업로드 도구로 진짜 파일 선택까지). 검증 중 데모 로그인 계정으로는 `User` FK 액션이 500 에러가 나는 별개 버그를 발견 — 의도된 설계("DB 미저장" 주석)일 수 있어 고치지 않고 보고만 남김.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-12 (4) 항목.

### 6. [Claude:Sonnet] 대표 사진 선택 기능 + 데모 로그인 버그 실제 수정

- 대표 요청: 대표 사진 선택 기능 추가 / "데모 로그인 중복, 화면의 '빠른 데모 테스트용 선택'만 있으면 되는거 아니야?"
- **대표 사진 선택**: `PATCH /api/partner/facilities/:id/images/cover`로 지정한 사진을 배열 맨 앞으로 이동 — 소비자 화면이 이미 따르는 "images[0]=대표" 규칙을 그대로 이용해 마이그레이션 없이 구현. `BizDashboard.tsx` 썸네일에 별 아이콘(대표=금색, 나머지=클릭 가능한 빈 별) 추가.
- **데모 로그인 버그 실제 수정**: 08-12 (4)에서 "보고만 하고 미룬" 문제의 진짜 원인이 두 가지였음 — ① 백엔드 `demo-login`이 User 미저장 합성id로 토큰 발급(FK 위반 500) ② 화면의 데모 버튼은 그 API를 호출도 안 하고 이름만 프론트에서 표시(토큰 자체가 없어 모든 요청이 조용히 익명 처리). 대표가 본 "EB-260812-0002가 전달됐다"는 바로 이 ②번 경로였음(익명 제출). 백엔드는 User upsert로, 프론트 데모 버튼은 실제 API를 호출하도록 고쳐서 화면의 데모 버튼 하나로 통일 + 정상 동작하게 만듦.
- 실제 브라우저로 데모 로그인 → 토큰 저장 확인 → 업체 문의 API 201 성공 + 생성된 Lead의 userId가 실제 User 행을 가리키는지 확인. 대표 사진도 2장 업로드 → 별 클릭으로 순서 바뀌는 것 → 공개 API로 images[0] 변경 확인까지 검증.
- **확인 필요**: 진단 과정에서 만든 테스트 문의(`EB-260812-0002`·`0004`, 신청자명 "123")가 대표의 실제 연동 파트너 계정 큐에 남아있음 — 삭제 안 하고 보고만 남김.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-12 (5) 항목.

