# 📝 2026-08-10 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. 3에이전트 체제 전환 승인·반영

- 사용자가 08-07부터 대기 중이던 개발 Sonnet / 기획 Opus / 문서화·교차검증 Gemini Flash 전환안을 승인.
- `roles.md` §0 태그표(`[Claude:Opus]`/`[Claude:Sonnet]`/`[Gemini]`) 신설, `docs/` 쓰기 소유권 Gemini→Claude(Opus 모드) 이관, `harness-doctor.sh` 태그 검사 갱신. 상세는 `.harness/_meta/3에이전트_전환안.md`.
- `roles.md`가 예산(8KB) 한계에 닿아 기록 형식(구 §3)을 `.harness/record.md`로 분리.

### 2. [Claude:Opus] 장사시설 사업자 회원 + 리드 수수료 정산 기획 (docs 16 신설)

- 대표 지시 2건(사업자 별도 회원가입·관리 / 문의·견적요청 로그와 번호로 수수료 청구 근거 확보)을 `docs/01_장례_묘지_매칭/16_장사시설_사업자회원_및_리드_수수료_정산_명세서.md`로 확정.
- `Partner`(B2C User와 분리) · `FacilityClaim`(시설 소유권 클레임) · `Lead`(리드+견적요청번호 `EB-YYMMDD-NNNN`) · 정산 흐름 설계.
- 사용자 추가 요청으로 §12 「설정·정책 관리 방침」 신설: 수수료율은 `CommissionPolicy`(유효기간 있는 DB 레코드), 나머지 운영 정책은 `policy.ts` 한 곳, 시크릿은 `.env` — 3계층으로 하드코딩 제거.
- 대표 확정 필요 4건(수수료 기준 등)은 `pending-approvals.md`에 등록. **구현은 안 막힘** — 전부 값만 나중에 채우는 구조로 설계.

### 3. [Claude:Sonnet] 위 스펙 0~3단계 구현

- **0단계**: `backend/src/config/policy.ts` 신설. `FacilityPage.tsx`(위치 폴백 좌표)·`KakaoMapModal.tsx`(같은 좌표+5초 타임아웃) 하드코딩을 `frontend/src/config.ts` 상수로 회수.
- **1단계**: `pg_dump` 백업(`eobom/backend/backups/`, gitignore 처리) 후 `Partner`·`FacilityClaim`·`Lead`·`CommissionPolicy`·`Settlement`·`LeadNumberCounter` 마이그레이션. 데이터 유실 없음 확인(Facility 1,552건 유지).
- **2단계**: 사업자 signup/login/refresh/me. JWT `aud` 클레임으로 B2C·사업자 토큰 분리. `demoLogin`의 ADMIN 발급을 프로덕션에서 구조적으로 차단.
- **3단계**: `leadNo` 원자적 발번 + 제3자 제공 동의 저장. 견적요청/전화이벤트 API 신설, 답사예약(`BookingModal`)에 연락처·동의 체크박스 추가하고 `Lead`를 트랜잭션으로 함께 생성.
- 백엔드·프론트 빌드 통과 + 실서버 수동 시나리오 검증(가입→승인대기 거부→견적요청→동의거부→전화이벤트→실유저 예약) 완료, 테스트 데이터 정리.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-10 항목. 삽질 기록: `claude_tasks.md` 동일 날짜.

### 4. OAuth 로컬 HTTPS 콜백 장애 대응

- 증상: 카카오/네이버/구글 로그인이 `ERR_EMPTY_RESPONSE`로 실패. 원인은 mkcert HTTPS 전용 서빙(08-07 도입)과 `.env`의 http 콜백 URL 불일치 — 인증 후 리다이렉트가 TLS 포트에 평문으로 들어감.
- `.env` 3사 콜백 URL을 https로 수정. 사용자가 카카오 콘솔에서 KOE006(플랫폼 사이트 도메인 미등록)·리다이렉트 URI 미등록 2건을 추가로 발견해 함께 해결, 네이버·구글도 콘솔 등록 완료. 3사 모두 로그인 확인.
- 디버깅 중 백엔드 dev 서버가 `.env` 변경을 반영 못 하는 것(재시작 필요, `ts-node-dev`는 `.env`를 감시하지 않음)도 확인·설명함.

### 5. [Claude:Sonnet] 전문가(변호사·세무사·행정사·장례지도사) 계정 체계

- 대표 지시: "장사시설/전문가 회원가입 관리 따로. 법적 문제는 참고로만, 나중에 문서로 사장님 보고. 구현은 일단 진행."
- `Expert` 모델을 `Partner`와 완전 분리 신설(검증 기준·연결 도메인·수익 모델이 다름 — 특히 변호사법 제34조 때문에 `Lead`/`CommissionPolicy` 구조를 아예 안 붙임). `docs/17_전문가_계정_체계_구현_메모.md`에 근거·법적 리스크 기록(정식 스펙 아닌 구현 메모, 예외적으로 Sonnet이 직접 작성).
- 백엔드 인증(가입/로그인/refresh/me)은 `Partner`와 동일 패턴 재사용. 프론트 `PartnerPortalPage.tsx` 신설(장사시설/전문가 선택, 일반 이메일·비밀번호 폼, Footer 링크로만 노출).
- 백엔드 전체 플로우 curl로 검증(가입·검증오류·승인대기 거부·로그인·정산계좌 암복호화·refresh 회전·재사용 차단·교차토큰 차단 전부 확인). **프론트 UI는 브라우저 자동화 도구가 없어 미검증** — 사용자 확인 필요.

---

## 📌 커밋

- `b84cf6c` — 장사시설 사업자회원 + 리드 수수료 정산 인프라 0~3단계
- (3에이전트 전환·OAuth 콜백 수정·전문가 계정 체계는 이번 일지 작성 시점까지 미커밋 — 사용자가 직접 커밋)
