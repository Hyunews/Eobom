# 📝 2026-08-20 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [사용자] 모바일 부고장 카카오톡 전송 — 실제 개발 기획 요청

- *"카카오톡으로 부고장 전송하기 실제 개발에 기획 짜줘. 시스템 구현에 관한 기획."*

### 2. [Claude:Opus] `07-03` 모바일 부고장 카카오톡 전송 구현 기획서 신규 작성

- **착수 지점을 코드로 실측**: `ObituaryPage.tsx:20-30`이 `alert('[개발중]')` 상태, 백엔드
  부고장 API **0건**, `schema.prisma`에 `Obituary`·`Deceased` **없음**, 프론트 카카오 SDK는
  지도용(`dapi.kakao.com`) 하나뿐 — **공유 SDK 미도입**.
- 🔴 **"카톡 전송"이 세 가지 다른 것을 가리킨다는 점부터 갈랐다.**
  - ✅ **A. 카카오톡 공유(`Kakao.Share`)** — 유족이 자기 카톡으로 보냄. 무료, 연락처 수집 0건,
    도메인 등록만 하면 검수 없음. → **채택** (`00-13` §2.1이 이미 낸 답).
  - ❌ **B. 알림톡/친구톡** — 조문객 명단이 필요해 `00-13` §4.4 D안 기각 사유가 그대로 재현되고,
    ⚠️ **조문객은 이어봄과 거래관계가 없어 "정보성 메시지" 요건 충족이 불확실**하다(신규 논점).
  - 🟡 **C. 메시지 API** — "나에게 보내기"만 값이 있으나 1차 제외.
- ✅ **따라서 "전송"이 아니라 "공유"다** — 현행 문구는 *"이어봄이 대신 보내준다"* 는 오해를 만들고,
  유족이 창을 닫으면 **아무에게도 안 간다.** 문구 교체 권고.
- 🔵 **미리보기 카드 경로를 2개로 분리한 것이 이번 기획의 실익**:
  ①공유 버튼 = 코드로 카드 지정이라 **OG 불필요** / ②URL 재전파 = 카카오 스크랩이라 **OG 필수**.
  → **Phase 1이 백엔드 배포에 묶이지 않게** 됐다.
- ⚠️ **충돌 1건 발견·해소**: `feed` 템플릿은 이미지가 필수인데 `00-13` §8-8은 영정 제외 권고
  → 🟡 **공용 근조 이미지 고정**. 부수 효과로 **이미지 스토리지 전환(`00-11`) 대기 불필요**.
- 🔵 **`00-13` §7.2-2 원 권고를 배포 구조 근거로 수정**: 프론트는 Vercel·백엔드는 Render라
  부고 링크가 `onrender.com`으로 나가면 신뢰도가 무너진다 → 🟡 **Vercel 서버리스 함수**로 OG 셸.
- 스키마 정본화: `Deceased`(최소 범위) + `Obituary` + **`ObituaryMourner` 신설**(부고는 관계별
  다수 표기가 관행), **두 slug 파생 금지**, `Memorial.deceasedId` 마이그레이션(⚠️ `pg_dump` 선행).
- 🔴 **확정 필요 6건** — #1 조의금 계좌·#2 카톡 카드 내용은 **Phase 1에서 코드로 굳으므로 착수 전
  확정 필요**(둘 다 `00-13` §8에서 이미 대기 중). #3 상주 연락처 노출·#4 부고장 수명은 신규.
- 🔴 **선행 차단 2개**: `Deceased` 미확정 · **백엔드 미배포**(Phase 2·3 전체를 막음).

### 3. [사용자] *"대화방이 없는 개인에게는 친구목록에서 선택해서 발송하는 기능 가능한가"*

- ✅ **A안에 이미 포함돼 있다.** `Kakao.Share`가 띄우는 건 대화방 목록이 아니라 **카카오톡 공유
  대상 선택 화면**이고, **채팅 탭과 친구 탭이 함께** 있다. 친구 탭에서 고르면 새 1:1 방이 생긴다.
- 🔴 **반대로 친구 목록 API로 직접 만들면 부고 용도로는 작동하지 않는다** — 친구 목록·친구 메시지
  API는 **해당 앱에 카카오 로그인한 적 있는 친구만** 대상이라, **부고를 받아야 할 회사 동료·친척·
  동창(비회원)이 한 명도 안 뜬다.** 검수를 다 통과해도 **수신자를 이어봄 회원으로 제한하는
  열화판**이 된다.
- ⚠️ 공유창 친구 탭은 카카오톡 앱 UI라 버전 의존이므로 **문서로 단정하지 않고 Phase 1 실기기
  검증 항목**으로 넘겼다.
- → `07-03` **§2.4 전면 개정**(§2.4-1 친구 API 제약 / §2.4-2 폴백 매핑), **§9 Phase 1 완료 판정
  1개 → 4개로 확장.**

### 4. [사용자] 🔴 확정 필요 2건 확정 — *"조의금 계좌 노출 o / 카드에 영정을 넣으면 문제가 생겨?"*

- ✅ **조의금 계좌 — 도입 확정.** *"노출 o"* 를 **"기능 도입 + 기본 OFF 유족 토글"** 로 해석해 진행
  (조의금을 정중히 사양하는 유족이 실재하고, 계좌가 상시 박힌 페이지는 `00-13` §6.3이 경고한
  **가짜 부고 사기의 표적**이 된다). ⚠️ **"항상 노출" 의도였다면 정정 요청 대기.**
- ✅ **카드 영정 — 2단계 확정.** *"넣으면 문제가 생기냐"* 에 문제를 3층으로 갈라 답:
  - 🔴 **스토리지 미전환이 결정적** — 영정은 백엔드 로컬 디스크에 저장되고 재배포 시 소실된다
    (`systems.md` §5). **URL이 죽으면 이미 뿌려진 카톡 카드가 일제히 깨진다.** 넣고 말고가 아니라
    **아직 못 여는 문.**
  - 🟡 **카카오 캐시 잔존** — 부고장을 닫아도 캐시된 카드 이미지는 회수 불가.
  - ⚪ **법적 리스크는 낮다** — 개인정보보호법은 "살아 있는 개인"을 규율하므로 **고인 사진 자체는
    법상 개인정보가 아니다.** 유족이 직접 올려 직접 뿌리는 구조라 더 약하다.
  - → **1차 근조 이미지 고정 / `00-11` 스토리지 전환 후 `portraitShareEnabled` 토글**(계좌와 동일
    패턴, 기본 OFF, 켤 때 *"카카오 서버에 남으며 회수 불가"* 고지). **현재 일정에는 영향 없음.**
- 🔵 **`00-13` §8-8을 "영정 제외" → "조건부 허용"으로 재해석해 해소.** `00-13` 표에 아직 반영 안 됨
  → [Opus] 재정합 필요.
- → `07-03` **§3.3 전면 개정 + §3.3-1 신설**, §4.2 스키마에 `portraitShareEnabled` 추가,
  §8 #3-1 신설, §9에 **Phase 3+ (스토리지 전환 이후)** 블록 신설, §10 #1·#2 종결.

### 5. [사용자] 부고장 양식 확정 — *"고인 성함 / 상주 성함 / 빈소 위치 / 마음전하실곳(토글 혹은 체크?) / 연락처"*

- *"추후에 추가할 것이 생기면 그때 추가할게. 혹시나 부고장에 추가할 것이 있다면 알아서 판단하고
  기획해줘."* → **추가 판단을 위임받음.**
- 🟡 **발인 일시 추가 권고 (미승인 — 착수 전 확인 필요)**: 빈소만 있고 발인이 없으면 조문객이
  *"언제까지 가면 되는지"* 를 모른다. **카톡 링크는 며칠씩 떠돌기 때문에 이미 끝난 장례식에
  찾아가는 사람이 생긴다.** 부수적으로 **부고장 수명(§10 #4)을 판단할 유일한 기준**이다.
  별세 일시도 함께 권고(부고 첫 문장 관행 + 랜딩 표기, `Deceased.deathDate` 기존 필드).
- **호실·입관·장지·유족 추가는 스키마 유지 + 화면에서만 접음** — 지우면 나중에 마이그레이션이
  필요해진다.
- ✅ **"토글 혹은 체크?" → 토글. 단 근거를 바꿨다** — 토글의 진짜 기능은 켜고 끄기가 아니라
  **경고를 띄울 자리를 만드는 것**이다. 그래서:
  - **연락처 = 입력하면 노출**(별도 토글 없음. 입력 여부가 곧 토글)
  - **마음 전하실 곳 = 명시적 토글, 기본 OFF** + 켤 때 *"이 계좌번호는 부고장을 받은 분이 다시
    공유할 수 있습니다"* 경고. **입력=노출 방식엔 이 경고를 붙일 자리가 없다.**
- 🔴 **연락처 — "보안 연극" 경고를 명시**: `[전화 걸기]`(`tel:`)로 감싸도 **번호는 페이지 소스에
  그대로 남는다.** 표시상의 정리이지 보호가 아니다. **실효 완화는 부고장 종료 시 함께 404 하나뿐**
  이라 Phase 3 종료 버튼의 중요도를 올렸다.
- → `07-03` **§6.2 재작성 + §6.2-1~-3 신설**, §4.2에 `contactPhone` 추가(**암호화 안 함** — 입력=공개가
  전제라 암호화가 막는 위협이 없다), §5.3 화이트리스트에서 연락처를 **제외→포함으로 반전**,
  §6.3 랜딩 레이아웃에 연락처 줄 추가, **§10 #3 종결.**
- ✅ **이로써 Phase 1을 막는 확정 항목이 전부 해소**됐다. 남은 #4·#5·#6은 Phase 2~3 또는 별도 트랙.

### 6. [사용자] *"발인 일시 넣자..!"* — 필수 4개 확정

- ✅ **필수 = 고인 성함 · 상주 성함 · 빈소 위치 · 발인 일시.** `funeralAt`을 non-null로 바꾸고
  개설 검증에 추가. **별세 일시는 승인 대상이 아니었으므로 선택(접어둠)으로 내림** — 없어도
  `故 ○○○`로 성립한다.
- 🔵 **발인이 필수가 되면서 §10 #4(부고장 수명) 권고가 바뀌었다** → `07-03` **§6.2-4 신설**.
  - §6.2-3이 *"연락처·계좌 노출의 실효 완화는 부고장 종료 하나뿐"* 이라 했는데, **수동 종료만
    두면 유족이 잊었을 때 연락처와 계좌가 실린 페이지가 무기한 살아남는다.** 사별 직후에
    *"부고장을 닫아야지"* 를 기억할 유족은 많지 않다.
  - 이전엔 발인이 선택이라 자동 계산이 불가능했다. **이제 기준이 항상 존재한다.**
  - → **발인 + N일 자동 종료 + 유족의 수동 조기 종료.** 🔴 **N값은 미정**(발인 직후 끊으면 늦게
    소식을 접한 조문객이 정보를 못 본다).
  - ⚠️ **`00-13` §4.3의 phase 전환 트리거 문제를 다시 여는 것이 아니다** — 거기서 문제였던 건
    한 레코드가 성격을 바꾸는 사건이고 E안은 레코드를 둘로 나눠 그걸 없앴다. 여기 것은
    **부고장 레코드 하나의 만료**일 뿐이고 **추모관은 계속 산다.**
  - ⚠️ 구현은 **배치가 아니라 조회 시점 판정**(`funeralAt + N < now()`) — 스케줄러를 새로
    들이지 않는다.

### 7. [사용자] N=3일 확정 + *"노출이라는 게 한번 입력하고 남아있는 걸 의미하나"*

- ✅ **N = 3일 확정** — `발인일 + 3일 자정` 자동 종료. **§10 #4 종결.**
- ✅ **"노출"의 정의를 §5.4로 명문화** — 화면이 둘이고 "노출"은 항상 후자다.
  - **입력** = `/obituary` 작성 폼 (유족만 봄)
  - **노출** = `/o/{slug}` 랜딩 (카톡 링크를 받은 조문객이 봄)
  - 입력값은 **서버 DB `Obituary` 레코드**에 저장 — 브라우저 로컬이 아니라 기기를 바꿔도 남고,
    마이페이지에서 수정 가능하며, **slug가 그대로라 수정이 조문객 페이지에 즉시 반영**된다.
- 🔴 **이 질문이 못 보고 있던 결함 하나를 드러냈다 → §5.4-1 신설.**
  - **이미 뿌려진 카톡 카드는 공유 시점 스냅샷이라 바뀌지 않는다.** 빈소를 고쳐도 단톡방 카드엔
    옛 빈소가 남고, **카드만 보고 안 누른 사람은 틀린 곳으로 간다.**
  - 하필 §3.2가 카드 설명란에 넣기로 한 **빈소·발인이 가장 자주 바뀌는 정보**다.
  - 카드에서 빼는 안은 **§3에서 카드를 중요하게 다룬 이유가 사라져** 기각.
  - 🟡 **갱신 안내로 해결**: ①수정 시 *"이미 보낸 카드에는 반영되지 않습니다. 다시 공유해
    주세요"* + 그 자리에 공유 버튼 ②랜딩 상단 **최종 수정 시각** 표시.
  - ⚠️ **§3.4의 카카오 스크랩 캐시와는 다른 문제** — 그건 *URL 붙여넣기로 생긴 카드*이고
    이건 *이미 전송된 메시지*다. **캐시 초기화로 해결되지 않는다.**

### 8. [사용자] §5.4-1 대응 확정 — *"링크는 자동 수정 good, 카톡 메시지는 이용자가 다시 보내는 방식"*

- ✅ **책임 경계 확정** → `07-03` **§5.4-2 신설**.
  - **`/o/{slug}` 페이지 = 시스템 자동 반영** (`PATCH` 즉시, slug 불변)
  - **이미 보낸 카톡 카드 = 유족이 재공유**
- 🔵 **이건 선택이 아니라 구조적 강제다** — 이어봄이 정정 메시지를 대신 보내려면 B안(직접 발송)
  이어야 하는데, A안에서 **이미 나간 메시지는 유족의 카톡 안에 있다.** *"알아서"* 가 아니라
  **그 방법밖에 없다.**
- ✅ **다만 "알려주는 데까지"는 시스템 의무** — 유족이 수정 사실을 모르면 조치할 수 없다.
- 🔴 **안내 트리거 필드를 표로 못박음**(구현자가 판단하지 않는다):
  - **띄움** — 고인 성함(`title`) · 빈소 위치 · 호실 · 발인 일시(`description`)
  - **안 띄움** — 연락처 · 마음 전하실 곳 · 입관 · 장지 · 유족 추가 · 별세 일시
  - 근거: **연락처만 고쳤는데 *"다시 공유하세요"* 가 뜨면 유족은 뭐가 잘못됐나 싶어 불필요하게
    또 뿌린다.**
- §4.2에 **`cardFieldsUpdatedAt` 신설** — ⚠️ `updatedAt`으로 대체 불가(연락처만 고쳐도 바뀌어
  안내가 오작동).
- ✅ **재공유 카드 `·변경` 표기 확정** — 재공유하면 단톡방에 카드가 2장 쌓이는데 바쁜 방에서는
  어느 게 최신인지 헷갈린다. `[부고·변경] 故 ○○○ 님`. **카드 제목 함수 조건문 1개**이고
  §3.2·§6.2 미리보기와 포맷터를 공유하므로 세 곳이 자동 일치한다.

### 9. [Claude:Opus] 기획 종료 → `[Claude:Sonnet]` 핸드오프

- **`07-03` 확정 완료.** 확정 필요 6건 중 **#1~#4 종결**, #5(알림톡 법률검토)·#6(조문 로그인)은
  Phase 1 밖. **기획 쪽에서 Phase 1을 막는 항목 0건.**
- 🔴 **남은 차단은 Phase 0(전부 [사용자] 작업)**: ①카카오 디벨로퍼 Web 도메인에
  `https://eobom.vercel.app` 등록 ②JS 키를 `VITE_KAKAO_JS_KEY`로 추가 ③근조 카드 이미지 1장
  ④**백엔드 Render 배포**(Phase 2·3 전체를 막음).
- `context.md` ③ 갱신 — **07은 `07-03`으로 해제, 04·05는 `00-12` 대기 유지.**

### 9. [Claude:Opus] 문서 인덱스·기록 갱신

- `docs/00_DOCS_INDEX.md` 07 섹션에 `07-03` 행 추가.
- `walkthrough.md` (45) 항목 5개 필드 작성 (판정 대기).
- `reports/07_상중_행정_케어/` HTML 보고서 **미작성** → `[Gemini]` 요청 대상.
