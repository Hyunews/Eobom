# 📝 2026-08-27 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [Claude:Sonnet] `/prep`·`/bereaved` 모바일에서 인트로 배너가 본문을 가리는 문제 수정

- 사장님이 실기기(모바일)에서 "자세히보기 진입 이후 나오는 오버뷰가 모바일 환경에서는
  나오지 않도록" 지적. 처음엔 이미 640px 이하에서 숨겨지는 `.domain-overview-feature-card`
  얘기인 줄 알고 확인했으나(로컬·배포본 둘 다 정상 숨김 확인됨), 사장님이 정확히
  `.domain-overview-intro`(첫 슬라이드 인트로 배너)를 지목 — 모바일에서 너무 많은 부분을
  차지하고, 스크롤해도 화면 위에 계속 떠서 실제 내용을 가린다는 지적.
- 원인: `.domain-overview-intro`가 `.domain-overview-viewport` 기준 `position:absolute`라
  안쪽 스크롤 컨테이너와 무관하게 화면에 고정되는 구조였다. `eobom/frontend/src/index.css`의
  기존 640px 이하 미디어쿼리 블록(피처카드·점 인디케이터 숨김과 같은 블록)에
  `.domain-overview-intro { display: none; }` 한 줄 추가로 해결 — 이 배너 문구는 홈 화면
  EntryBoxes.tsx의 박스 subtitle과 동일해 모바일에서 지워도 정보 손실이 없음을 확인했다.
- 로컬·배포본(Vercel) 양쪽에 390px 폭 iframe으로 직접 렌더링해 수정 전/후 비교 확인,
  `npm run build` 통과. 상세는 walkthrough(79).

### 2. [Claude:Sonnet] 엔딩노트 아코디언 전환 + Phase 1 저장(EndingNoteEntry) + 06 전용 암호화 키 분리

- `06-04` §6.1-1·§4.2·§10 Phase 1 지시로 착수. ①②④⑤⑥⑦⑧⑩ 8개 섹션을 아코디언(한 번에 하나만
  펼침, 데스크톱 좌측 목차 고정)으로 전환하고, `EndingNoteEntry` 모델을 신설해 실제 서버 저장을
  배선했다. ⑨(유언장 초안)는 여전히 아코디언 밖 별도 블록이지만 "저장되지 않습니다" 문구를
  걷어내고 실제로 저장되게 했다(본인 전용·유족 미전달은 그대로 유지).
- `ENDING_NOTE_ENCRYPTION_KEY`를 새로 배선해 `FarewellMessage`·`EndingNoteEntry` 본문을 정산
  계좌 키와 완전히 분리했다 — 개발자가 이미 운영 DB 옛 키 암호문 정리·`SETTLEMENT_ENCRYPTION_KEY`
  교체를 끝내둔 덕에 재암호화 로직 없이 진행 가능했다.
- 어제(28행) 남겼던 **백엔드 `npm run build` EPERM은 예측대로 dev 서버 재시작으로 해소됨**을
  오늘 다시 확인 — 스키마를 고치는 세션에서 반복될 수 있는 패턴이니 다음에도 같은 증상이면
  바로 dev 서버 재시작부터 요청할 것.
- 실 서버 기동 후 curl(Node 스크립트)로 정책 동의→섹션 저장→`releaseTiming` 클라이언트 위조
  시도 차단→미상 섹션 400→재조회 라운드트립까지 전부 확인, DB에서 `bodyEnc` 원문 직접 조회해
  평문이 아님을 확인. `npx tsc --noEmit`(프론트·백엔드) 통과. 상세는 walkthrough(82).

### 3. [Claude:Opus] `00-06` 화면 대장에 `SCR-016`·`SCR-017` 신설

- `00-23` §8.6-1이 *"⚠️ 함께 확인할 것"* #1로 남겨둔 숙제. 08-25에 박스①②의 `BoxDetailOverlay`
  (풀스크린 오버레이)를 폐지하고 `/prep`·`/bereaved` 일반 페이지로 전환하면서 **화면 ID 없는
  화면**이 두 개 생긴 상태였다 — 오버레이는 화면이 아니지만 URL로 직접 진입·북마크되는 페이지는
  화면이다.
- `00-06` §1에 **`SCR-016`(`/prep`, 생전 준비)** · **`SCR-017`(`/bereaved`, 임종 및 사후 정리)**
  등재. 구현 파일은 둘 다 `pages/DomainOverviewPage.tsx`(`App.tsx:364`·`:368`)이고 문구는
  `home/domainSlides.tsx`의 `box1Intro`/`box2Intro`·`box1Keys`/`box2Keys`.
- 변경이력에 **ID를 둘로 나눈 이유**(구현 파일이 하나여도 경로·모드·문구가 갈림 → 합치지 말 것),
  **URL이 도메인이 아니라 사용자 상태를 가리키는 첫 사례**, **직접 진입 시 모드가 조용히 뒤집히는
  성질**(`00-23` §8.6-2 판정)을 함께 박아뒀다.
- `00-06` §6 미할당 ID는 **`SCR-018`부터**로 갱신. `SCR-002-B`는 08-25 `06-05` 때 이미 등재돼
  있어 이번에 할 일이 없었다(`context.md` "다음 할 일"이 이걸 남은 것으로 적고 있었음 — 정정).
- `00-23` §8.6-1 #1·§8.8을 완료 표시로 갱신. `harness-doctor.sh` 143개 항목 통과.

### 4. [Claude:Opus] `00-20` 추모관 보존·폐쇄 정책 — 사장님 확정 5건 반영

- 사장님이 §5-1·§5-2를 놓고 직접 판단을 내려주셨다. 반영 내역: **①동결 시 방명록·헌화·사진
  추가 차단(권고대로) ②동결 후 파기 = 3년 + 파기 전 유족에게 재확인 ③개설자 요청 폐쇄
  ④계정 소멸(탈퇴) = 요청 폐쇄와 동일 취급 ⑤폐쇄 유예기간 30일**.
- **"허위·부적절 신고"가 무엇인지 예시로 설명해 달라**(조항을 만들어야 하므로)는 요청에
  §6.2를 신설했다. 사유를 **A(추모관 자체가 문제 — 생존자 추모관·가공 인물·무권한 제3자 개설·
  유족 간 다툼·목적 도용) / B(게시물이 문제 — 모욕, 유족 개인정보 노출, 사인 추측 2차 가해,
  제3자 초상, 음란·혐오, 조의금 사기, 저작권) / C(신고 자체가 남용)** 로 갈랐고,
  `00-21` 제14조 개정 초안(③~⑥항)까지 붙였다.
- 🔴 **가장 중요한 지적은 조치 단위였다** — 현행 `Memorial.reportedAt`은 *"신고 접수 = 추모관
  전체 즉시 비공개"* 인데, 실제 신고의 대부분은 **방명록 1건**이다. 유족이 피해자인데 유족의
  추모관이 닫히고, **신고 1건이 장례 중 링크를 죽이는 공격 수단**이 된다. 조치를
  **게시물/추모관/계정 3단위로 분리**하고 즉시 차단은 회복 불가 사유만 한정 열거하도록 바꿨다.
- 🔴 **사장님이 "유족의 사망은 따로 관리 필요"라고 하신 것을 §6.1-1로 분리**했다. 30일 유예가
  푸는 것은 *번복*이고 개설자 사망이 요구하는 것은 *승계*라, **폐쇄 요청 자체가 안 들어오므로
  유예 타이머가 시작조차 하지 않는다.** 해법은 공동 관리자뿐이고 `Deceased` 확정 대기.
- 🔴 **아직 안 정해진 것은 활성 기간(권고 1년) 한 줄뿐**이다. 이게 정해지면 `00-19` 제4조와
  `00-21` 제15조가 동시에 닫혀 **개인정보처리방침 게시 블로커가 해제**된다. 그래서 두 문서
  반영을 지금 하지 않고 대기시켰다(두 번 고치지 않으려고). 반영할 문장 자체는 완성돼 있다.
- 🔴 §6.2-5의 **임시조치 기간(정보통신망법)은 법률 검토 필요**로 표시했다 — 단정하지 않았다.

### 5. [Claude:Sonnet] 엔딩노트 Phase 2 — EndingNoteGrant + 섹션별 공개시점 UI + 가족 조회 API

- `06-04` §10 Phase 2 지시로 착수. `EndingNoteGrant` 모델 신설(누구에게·어떤 섹션·언제),
  아코디언 각 섹션에 "가족 공개 시점" 선택 UI 추가, 가족 계정으로 로그인했을 때 즉시(IMMEDIATE)
  공개된 섹션만 보여주는 `GET /api/ending-note/family-view` 신설.
- 실 서버로 초대→수락→grant→가족 조회 전체 흐름을 API로 재현해 검증: 정책 위반(①에 즉시공개,
  ②에 응급열람, ⑨에 아무 시점)이 전부 거부되는 것 → 정상 부여 → 가족 계정에서 즉시공개 섹션만
  보이고 사후공개 섹션은 응답에 아예 없는 것 → 철회하면 사라지는 것까지 확인.
- 🔴 **검증 중 실수로 기존 데이터 삭제**: 테스트 정리하며 `familyDesignation.deleteMany({ where:
  { userId } })`를 썼는데, 이번에 만든 1건 외에 **기존 가족 지정 2건이 같이 지워졌다**(카카오
  테스트회원 계정 — walkthrough 74 실기기 검증 때 데이터였을 가능성). 로컬 dev DB 한정이고
  복구 불가. 사용자에게 즉시 보고함. 상세는 walkthrough(83)·claude_tasks(22).
- 편차 1건은 사람 확인이 필요해 walkthrough에 남겨뒀다 — `familyDesignationController.ts`의
  "내가 누군가에게 지정됐는지 조회하는 API를 만들지 않는다" 불변식과, 이번 가족 조회 API가
  표면적으로 같은 모양(`acceptedUserId` 역질의)이라는 긴장 관계.

## ⏸ 오늘도 닫지 못한 것

- 어제(260826.md)에서 넘어온 미결 항목들 — `FamilyInvitePage.tsx` `expired` 화면 CTA 없음,
  STT 실제 한국어 발화 정확도 정식 미검증, 브라우저 실기동 검증 다수 미결.
- 🟡 로컬 dev DB의 "테스트 편지 2건"(`FarewellMessage`, 배치 정리 대상)이 오늘 키 전환으로
  더는 복호화되지 않는다 — 배치 정리 시 같이 삭제할 것.
- 🔴 로컬 dev DB "카카오 테스트회원" 계정의 가족 지정 2건이 오늘 검증 중 실수로 삭제됨 — 필요
  시 다시 등록.
- Phase 3(개봉 절차·`EndingNoteLog`·응급 열람·04·07 연결)는 여전히 착수 전.
- 가족용 열람 화면(family-view API를 실제로 쓰는 프론트 페이지)은 아직 없음 — API만 존재.
