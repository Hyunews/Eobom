# 📝 2026-08-11 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [Claude:Sonnet] URL 라우팅 해시 → HistoryRouter(BrowserRouter) 전환

- 대표 지시: 해시 방식(`/#facility`) URL을 HistoryRouter 방식(`/facility`)으로 전환, 변경점을 md로 상세 기록(Gemini HTML 보고서화 예정).
- `App.tsx`를 `BrowserRouter`+`Routes`/`Route` 구조로 재작성. `setActiveTab`/`activeTab` 인터페이스를 유지해 Header/Sidebar/HomePage/MyPage 4개 컴포넌트는 무변경. OAuth 콜백은 pathname이 항상 `/`로 오고 해시는 페이지 라우팅과 무관한 신호라 백엔드도 무변경.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (1) 항목, `docs/00_핵심플랫폼/18`.

### 2. [Claude:Sonnet] 시설 태그(#해시태그) 클릭 필터 + TAG_CATALOG

- 대표 지시: 태그가 필요하면 구성 후 별도 관리 파일 또는 md 명세서로 관리.
- DB 감사 결과 분류 가능한 태그는 "공설"/"사설"뿐(나머지는 1회성 마케팅 문구)이라 화이트리스트 방식(`tagCatalog.ts`)으로 설계 — 등록된 값만 카드에서 클릭 가능한 필터로 동작, 나머지는 기존처럼 라벨만.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (2) 항목, `docs/01_장례_묘지_매칭/19`.

### 3. [Claude:Sonnet] DB 테이블 명세서(docs/05) 자동 생성 스크립트

- 대표 지시: docs/05 재생성, 각 컬럼 설명 필수.
- `.harness/tools/generate-db-doc.js` 신설 — `schema.prisma` 주석에서 문서를 자동 추출(마커 사이만 갱신, 그 아래 "보충 설명" 구간은 수동 유지). 물리 컬럼 146개 중 무주석이던 필드를 스키마에 직접 보강해 "설명 없음" 0건 달성.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (3) 항목.

### 4. [Claude:Opus] 도메인 번호 체계 재편

- 대표 지시: 웹 메뉴 순서대로 도메인 번호를 맞추고(01~04 사후/05 본인), 문서 번호도 도메인별로 다시 시작.
- 기획 판단으로 순서 확정: 01 장사시설 / 02 전문가 / 03 디지털유품·추모관 / 04 상중·행정 케어 / 05 엔딩노트·유언. 근거는 "01~04는 유족용 사후 기능, 05만 본인용 생전 기능"이라는 성격 구분 + 01·02가 유이한 거래(매칭)형이자 구현 성숙도 1·2위.
- 폴더 10개(docs/reports 각 5) + 파일 35개 `git mv`, 문서 ID를 `<도메인>-<순번>` 형식으로 재부여. 코드 주석 참조 23곳·하네스 문서 참조까지 일괄 갱신. 과거 기록은 소급 수정하지 않고 DOCS_INDEX에 구/신 대조표를 남김.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (4) 항목.

### 5. [Claude:Opus] Domain01 스펙 정리 + Domain02 기획 착수

- 대표·사장님 논의 결정 반영: **견적 비교 폐기**(수익은 업체 문의 리드 수수료 구조가 맞다는 결론), **답사 예약 폐기**(답사 갈 이유가 크지 않음 — 업체 사진 + "업체 자세히 보기"로 대체).
- `01-01` §2.3·§2.5를 `[유지]`→`[폐기]`로 정정(코드에선 08-10에 이미 삭제됐는데 스펙만 남아 문서가 거짓말하던 상태). `01-03`·`01-04`는 삭제 대신 폐기 배너로 판단 근거 보존. `01-01` §5에 "업체 자세히 보기" 대체 설계 신설.
- `pending-approvals`: 견적비교 A+C안 **기각 처리**, 대신 "제휴사 혜택·수익구조 재설계" 신규 등록(견적·답사·VR이 다 빠져 제휴 차별화가 비었음).
- Domain02(전문가 매칭) `02-03` 명세서 신설 — B2B 계정은 완성됐으나 B2C가 통째로 목업인 상태를 진단하고, 결제 없는 Stage 1(공개노출→상담신청→전문가 확인) 루프로 범위를 잘라 대표 승인 없이 착수 가능하게 설계.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (5) 항목.

### 6. [Claude:Sonnet] Domain01 폐기 코드 정리 + Domain02 Stage 1 구현

- 대표 지시: `VRViewerModal.tsx`·`FacilityBooking` 삭제, Domain02(전문가 매칭) Stage 1 착수(`02-03` §9 1~7단계).
- `Sidebar.tsx` 메뉴 순서를 도메인 재편에 맞춰 교체(`care-guide`↔`ending-note`).
- `FacilityBooking` 모델·API·`Lead.type`의 `BOOKING` 값 완전 제거(호출부·데이터 0건 확인 후). `VRViewerModal.tsx`·`mockData/experts.json` 삭제.
- `ConsultRequest`(과금 컬럼 없음, 변호사법 가드레일 §3.2 그대로 구현)·`ConsultNumberCounter`·`Expert.isPublished` 신규. 공개 API(`GET /api/experts`)는 select 화이트리스트로 정산계좌·자격증사본·연락처 등 민감 필드 전부 제외.
- `CounselingPage` 목업 제거하고 실연동, `ConsultRequestModal` 신설, `BizDashboard`에 "받은 상담 신청" 탭, `AdminPage`에 공개 노출 토글 추가.
- 17단계 curl 전수 검증 전부 통과(승인/공개 두 축 분리, 민감 필드 미노출, 동의 없음 400, 존재하지 않는 대상 404, EC- 발번, 상태변경 등). 테스트 계정 정리 완료. 백엔드·프론트 `tsc`/`build` 통과.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (6) 항목.

### 7. [Claude:Sonnet] 상속세 간이 시뮬레이터 실제 세법 구조로 재구현

- 대표 지시: 시뮬레이터를 상세하게 다시 만들고, 조건에 따라 값이 달라지는지 + 계산 기준을 참고사항으로 명시.
- 기존 "5억 고정공제 + 배우자 5억, 세율 20% 균일"이던 임의 수치를 실제 상속세및증여세법 구조(기초/일괄공제, 배우자공제, 금융재산공제, 5단계 누진세율, 신고세액공제)로 교체. 입력 7종으로 확장, 단계별 breakdown 노출, 반영/미반영 범위 + 면책 문구를 화면 하단에 명시.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (7) 항목.

### 8. [Claude:Sonnet] 상속세 시뮬레이터 모달로 분리

- 대표 지시: 상담페이지 내 시뮬레이터가 포션을 너무 많이 차지하니 버튼으로 모달을 띄워 사용하게 변경.
- `TaxSimulatorModal.tsx` 신설(로직 그대로 이동), `CounselingPage`는 배너 버튼만 남기고 전문가 목록을 전체 폭으로 확장.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (8) 항목.

### 9. [Claude:Sonnet] 전체 페이지 여백/밀도 축소 완료

- 대표 지시: "전체 다 진행해줘". 이전 세션에서 `index.css`의 공통 클래스 축소를 마친 뒤 이어서, Header/Sidebar/Footer + 나머지 9개 페이지 + 8개 모달의 인라인 여백을 같은 비율로 축소.
- Header/Sidebar/Footer는 직접 수정(72px 등 구조적 고정값은 보존), 나머지 페이지·모달은 파일 단위로 나눠 백그라운드 서브에이전트 4개에 병렬 위임 후 결과 검수.
- 접근성 토큰(폰트 18px, 터치 타겟 56px, line-height 1.7)은 전부 유지, `tsc --noEmit` 통과 확인. 브라우저 시각 확인은 불가해 코드 리뷰로만 판단.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (9) 항목.

### 10. [Claude:Sonnet] 프론트 버그 4건 수정 + 운영자 대시보드 인라인 정보수정

- 대표 리뷰 피드백: Footer의 파트너 링크가 옛 해시 URL 그대로 남음 / 파트너 가입 폼에 비밀번호 확인 없음 / 어제 로그인한 사업자·admin이 세션 만료 후에도 "로그인된 것처럼" 보이며 admin 승인 대시보드가 조용히 빈 화면으로 깨짐 / 운영자가 사업자·전문가 정보를 대시보드에서 수정할 수 없음.
- `Footer.tsx`를 `<Link to="/partner">`로, 파트너 가입 폼에 비밀번호 확인 필드 추가. `AdminPage.tsx`·`BizDashboard.tsx`에 401 감지 시 자동 로그아웃하는 `authFetch` 래퍼 추가(액세스 토큰 2h 만료 후 방치되던 버그 수정). 관리자 대시보드에 담당자명/연락처(사업자)·연락처/소개(전문가) 인라인 수정 기능 신규 추가 — 사업자등록번호·자격증번호 등 검증 필드는 의도적으로 제외.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (10) 항목.

### 11. [Claude:Sonnet] 03/05 관리자 열람범위 설계 메모 + 운영자 대시보드 회원검색·전체시설 조회

- 대표 지시: "03 05 관련해서 md파일 만들어둬줘" + "가능한 부분 한에서 대시보드 업글".
- `docs/03_.../03-01`(회원연동 원칙 메모), `docs/05_.../05-01`(관리자 열람범위 A/B안 쟁점 정리, 미확정) 신설. 둘 다 백엔드가 아직 없어 지금 구현할 대상은 없고, 향후 해당 도메인 실구현 시 지킬 원칙만 남김.
- 부수 발견: `CareGuidePage`의 D-Day 체크리스트 1·2번이 `checked:true`로 하드코딩돼 항상 체크된 채 보이던 버그 수정.
- 대시보드: 사업자/전문가 탭에 "전체" 상태필터 + 검색창 추가, 기존 공개 시설 검색 API를 재사용한 "전체 시설(1,552건)" 조회 탭 신규 — 새 백엔드 없이 구현.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (11) 항목.

### 12. [Claude:Sonnet] 운영자 대시보드 시각 균형 개선 + 전문가 사무실 주소 필드

- 대표 피드백: 대시보드 카드 안 글자·버튼 크기 밸런스가 안 맞음 / 전체시설 탭이 한 줄에 하나씩이라 비효율적 / 전문가 가입 시 사무실 주소를 받고 수정도 가능해야 함.
- 원인은 카드 안 버튼들이 사이트 전역 `.btn`(56px, 1.1rem — 시니어 접근성용)을 그대로 쓰고 있던 것 — `AdminPage.tsx`에만 로컬로 작은 버튼/입력 스타일을 만들어 적용, 카드 제목 font-size도 명시. 전체시설 탭은 세로 목록 → 반응형 그리드(2~3열)로 교체.
- `Expert.officeAddress` 신규 컬럼(선택, 4개 직역 공용) — 가입폼·운영자 정보수정·전문가 본인 대시보드 3곳에 연결. 마이그레이션 적용, 백엔드·프론트 `tsc` 통과.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (12) 항목.

### 13. [Claude:Opus] 오늘 구현분 반영 — docs 6건 스펙 정합화

- 대표 지시: "walkthrough 참고해서 오늘 작업한 내용에 따라 md파일 정리".
- `01-05`: 답사예약(`BOOKING`) 폐기를 7곳에 반영, API 표에 구현상태 열 신설, **운영자 회원정보 수정 범위 원칙 신설**(신원 검증 필드는 수정 대신 반려). 추가로 **미기록 편차 발견·정정** — 운영자 인증이 초안의 `User.role='ADMIN'`이 아니라 독립 `Admin` 모델로 구현돼 있었음.
- `02-03`: `officeAddress` 반영(4대 직역 공용으로 둔 이유 명시), 운영자 API 확장, Stage 1 전 단계 완료 표시.
- `00-09`: **시니어 접근성 토큰(18px/56px/1.7) 정본을 처음으로 문서화** — 지금까지 어디에도 없어 밀도 조정 때마다 재판단이 필요했음. 여백 스케일·운영자 화면 밀도 예외도 함께 규정.
- `00-06`·`00-04`: 둘 다 심하게 낡아 전면 재작성. 화면 3종(파트너 포털·사업자 대시보드·운영자 대시보드)과 API 30여 개가 통째로 누락돼 있었고 삭제된 API가 잔존 중이었음.
- 상세: `에이전트_기록/walkthrough.md` 2026-08-11 (13) 항목.

---

## 📌 커밋

- 미커밋 상태 — 사용자가 직접 커밋 예정.
