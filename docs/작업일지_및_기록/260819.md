# 📝 2026-08-19 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [사용자] 타 IDE(Antigravity)에서 Opus 사용 가능 여부 문의

- *"AntigravityIDE의 gemini쪽에서 Opus 모델을 지원하는데, 그것을 이용해도 너처럼 바로 작동하는거니?"*
- 🔴 **답: 모델과 하네스는 별개다.** 실측 결과 **`AGENTS.md`·`CLAUDE.md`·`GEMINI.md`가 전부
  `.harness/` 안에만 있고 저장소 루트에는 없다.** 대부분의 에이전트 도구는 루트의 `AGENTS.md`를
  자동 로드 대상으로 찾으므로, 다른 도구는 하네스 존재를 모르고 시작할 가능성이 크다.
- ⚠️ **이건 Antigravity만의 문제가 아니다** — 지금 세션이 하네스를 아는 것은 저장소 구조 덕이
  아니라 **Claude 계정 메모리에 이어봄 하네스 구조가 저장돼 있어서**다. 즉 새 Claude Code 세션도
  저장소만 보고는 못 찾을 수 있는 **구조적 구멍**.
- 🟡 **권고: 저장소 루트에 `AGENTS.md` 포인터 파일 신설**(실제 규칙은 `.harness/AGENTS.md`임을
  안내하는 1장). 도구 무관하게 잡히고 현행 세션에도 안전망이 된다. → 사용자 승인 대기.

### 2. [사용자] `reports/` 디자인 산출물 기반 화면 개선 요청 → 🔴 **전제 반박**

- 요청: *"gemini가 디자인 관련해서 작업한 html, md가 reports에 있을 것. 이를 바탕으로 화면을
  세련되게 바꾸고자 함."*
- 🔴 **실측 결과 방향이 거꾸로였다. `reports/style.css`(2026-08-05)는 앱보다 뒤처져 있어
  그대로 적용하면 퇴행이다.**

| 토큰 | `reports/style.css` | `00-09` 정본 = `index.css` |
| :--- | :--- | :--- |
| Point | `#4E7055` ❌ | `#5B7065` |
| Background | `#F7F4EF` ❌ | `#FBF9F5` |
| Text | `#2F3E46` ❌ | `#1A2B4C` |
| Accent Gold | **없음** ❌ | `#D4A359` |
| 본문 서체 | Noto Sans KR ❌ | Pretendard |
| 제목 명조 | **없음** ❌ | KoPubWorld 명조 |
| Card hover | `-5px` ❌ | `-4px` |
| 긴급 FAB | `.floating-call` 잔존 ❌ | 제거됨 |

- **원인**: `reports/`는 `docs/` 마크다운을 HTML로 시각화한 **문서 뷰어**이지 제품 시안이 아니다
  (`generate_all_reports.js` 생성). 목적이 다르니 자연히 갈라지는데 **어느 쪽이 정본인지 문서에
  적혀 있지 않아 혼선이 났다.**

### 3. [Claude:Opus] 🔴 `00-09` 정정 — **문서가 없는 것을 있다고 말하고 있었다**

2번을 대조하다 발견. **`00-09`가 두 번의 결정을 못 따라온 상태**였다.

| 위치 | 종전 서술 | 실측 |
| :--- | :--- | :--- |
| §2.1 Alert 행 | `#EF4444`/`#DC2626`, *"24시간 긴급 콜 FAB"* | 🔴 `#EF4444` **저장소 0건** / FAB **삭제됨** |
| §3.1 구조 | *"Bottom-Right Floating FAB: 🚨 24시간 긴급 콜"* | 🔴 존재하지 않음 |
| §4-3 | `50% { opacity: 0.5 }`, 이름 *"Emergency"* | 🔴 실제 `0.6` + `scale(1.15)`, 유일 사용처는 **지도 핀** |
| §3.1 사이드바 | 6개 메뉴(메인 + 5대 도메인) | ✅ 정확 — 수정 안 함 |

- **경위는 2단계**: ①`00-14` §2-4가 *"24시간 긴급콜"* 을 **받을 사람이 없는 약속**으로 판정해
  *"장례식장 바로 찾기"* 로 정정(08-14) → ②그 대체 버튼마저 **08-18 개발자 지시로 완전 제거**
  (walkthrough (32), `FloatingEmergency.tsx` 삭제). **`00-09`만 두 번 다 놓쳤다.**
- 🔴 **`00-14` §2가 여덟 번 잡은 결함과 같은 종류**이고, 방향은 §2-9처럼 **문서가 사실이 아닌 것을
  현재 시제로 말하는** 쪽이다. 정본 표라서 방치하면 다음 구현자가 그대로 만든다.
- `#DC2626`은 살아 있으나 **용도가 다르다** — `TaxSimulatorModal`(상속세 결과) ·
  `KakaoMapModal`(핀) · 오류 메시지 3곳. 실측대로 다시 적었다.
- ⚠️ `index.css:127` 주석이 아직 *"Emergency Hotline Pulsing Badge"* 인데, **코드 주석 정정은
  `[Claude:Sonnet]` 몫**이라 문서에는 사실만 기록했다.

### 4. [Claude:Opus] `00-09` §5 신설 — **정본 경계**

2번 혼선의 근본 원인이 *"어느 쪽이 정본인지 문서에 없음"* 이었으므로 규칙을 세웠다.

- ✅ **정본**: 색·서체·여백·접근성 토큰 → `00-09` / 구현 → `index.css`
- ❌ **정본 아님**: `reports/style.css`, `reports/**/*.html` 인라인 스타일
- 🔵 `reports/`는 **`[Gemini]` 소유이며 Claude에게 읽기 전용**(`CLAUDE.md`) — 드리프트가 보이면
  고치지 말고 `context.md`에 `[Gemini]` 요청으로 남긴다.
- 🟡 **`style.css` 처분은 `[Gemini]` 판단**: ①`00-09`에 맞춰 갱신 ②보고서 전용임을 상단 주석에
  명시 ③삭제 중 택. **목적은 "제품 시안으로 오해될 여지 제거"** 이고, 문서 가독성은 보고서의
  요구라 무조건 `index.css`와 같아질 필요는 없다.

### 5. [Claude:Opus] 🔴 `00-23` §8.5 위반 발견 → `[Sonnet]` 프롬프트 작성

- *"세련되게"* 의 실체를 찾다가 **어제 구현이 스펙을 위반한 상태**임을 확인.
  > `00-23` §8.5: *"Desktop ≥1024px — 상단 2열(① ②, **큰 카드**) + 하단 2열(③ ④, **높이 약 절반**)"*
- 현재는 4개가 `1fr 1fr` 한 그리드에 **전부 동일 위계**다. 게다가 어제 ①②에서 항목 목록을
  걷어내면서 **주가 되어야 할 ①②가 비고, 부가 되어야 할 ③④(입력창·버튼)가 더 꽉 차 위계가
  역전**됐다. 🔵 **카드가 휑해 보이는 건 취향 문제가 아니라 스펙 위반의 증상이다.**
- 다음 사이클 프롬프트를 작성해 전달(근거 `00-23` §8.5 + `00-09`, **`reports/style.css` 참조 금지**
  명시, 접근성 토큰·앰버 배지·가짜 목록 금지 재확인).

### 7. [Gemini] `reports/style.css` 처분 완료 & `00-09` 재생성 & walkthrough (33) 판정

- **`reports/style.css` 처분 (A안 + B안 통합 적용)**:
  - 파일 최상단에 **"⚠️ 보고서 뷰어 전용 — 제품 디자인 정본 아님(정본: docs/00-09 및 eobom/frontend/src/index.css)"** 대형 경고 주석 배치 (B안 충족).
  - `00-09` §2.1·§2.2·§2.4·§4 정본 토큰(딥 네이비 `#1A2B4C`, 웜 그린 `#5B7065`, 웜 골드 `#D4A359`, 소프트 웜 크림 `#FBF9F5`, Pretendard, KoPub World Batang, hover `-4px`, card padding `1.5rem`) 동기화 (A안 충족).
  - 실물이 삭제된 `.floating-call` 클래스 완전 제거 확인.
- **`00-09` 재생성 및 전체 HTML 보고서 47건 일괄 빌드**:
  - `00-09` 디자인 시스템 (§2.1 Alert 정정, §3.1 긴급 FAB 제거 사실 반영, §4 pulse 정정, §5 정본 경계 신설) 갱신 반영.
  - `node .harness/tools/generate_all_reports.js` 실행으로 전체 47개 보고서 동기화 완료.
- **Walkthrough (33) 게이트 검증 판정**:
  - `(33) [Opus] 00-09 정정(긴급 FAB·pulse 실측) + §5 정본 경계 신설` ➔ **✅통과** 판정 기록.
- **검증**: `bash .harness/tools/harness-doctor.sh` → **✅ 129개 항목 통과.**

---

### 8. [Claude:Sonnet] 메인화면 시각 정제 — 4박스 위계 복원 + 히어로·오버레이 마감

- (5)의 `[Sonnet]` 프롬프트를 이어받아 착수. `00-23` §8.5(4박스 레이아웃) 위반을 고쳐 상단①②/하단③④
  위계를 두 그리드 블록으로 분리하고, 박스①②의 빈 공간을 아이콘 확대+클릭 불가 요약 칩+CTA 강조로
  채움. 히어로·`BoxDetailOverlay` 슬라이드는 `alignItems: center→start`로 정렬 리듬을 통일.
- 🟡 (5)가 남긴 `index.css:127` 주석(*"Emergency Hotline Pulsing Badge"*)도 이번에 정정 —
  `grep`으로 유일 사용처가 `KakaoMapModal.tsx`(지도 핀)임을 재확인 후 사실대로 고침.
- 데스크톱(1440px)·태블릿(834px, iframe 프로브)·모바일(375px, iframe 프로브) 3폭 실화면 확인,
  진입 4곳·🔒로그인모달·준비중 무반응·오버레이 휠 스크롤 회귀 없음 확인. `tsc --noEmit` 프론트·백엔드
  에러 0. 상세는 walkthrough (34).
- 🔴 **새로 발견한 미해결**: App 레벨 `<Header>`가 375px 모바일 폭에서 가로 오버플로(사용자 프로필
  칩이 안 줄바꿈). 이번 사이클 파일 범위 밖이라 손대지 않고 다음 에이전트 몫으로 남김.

## ⏳ 남은 것

- 🔴 **`00-23` §8 재정합이 아직 남았다** — walkthrough (32)가 §8.1(01~05 섹션 유지 전제)·
  §8.2(항목 목록 노출)를 뒤집었다. §8.5(레이아웃)는 (34)로 실물과 다시 맞춰졌으니, `[Opus]`가
  재정합할 때 §8.5는 (34) 기준으로 반영하면 된다.
- 🟡 **루트 `AGENTS.md` 포인터 신설** — 사용자 승인 대기(1번).
- ✅ **`[Gemini]` 2건 완료**: `reports/style.css` 처분 완료 · `00-09` 재생성(§2.1·§3.1·§4·§5 반영) 완료.
- ✅ `index.css:127` 주석(*"Emergency Hotline"*) 정정 완료(8번).
- 🔴 **새 미해결**: App 레벨 `<Header>` 375px 가로 오버플로(8번) — 담당·범위 미정.
- ⏳ walkthrough (34) `[Gemini]` 판정 대기.

