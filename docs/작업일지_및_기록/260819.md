# 📝 2026-08-19 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [사용자] 타 IDE(Antigravity)에서 Opus 사용 가능 여부 문의

- *"AntigravityIDE의 gemini쪽에서 Opus 모델을 지원하는데, 그것을 이용해도 너처럼 바로 작동하는거니?"*
- 🔴 **답: 모델과 하네스는 별개다.** 실측 결과 **`AGENTS.md`·`CLAUDE.md`·`GEMINI.md`가 전부
  `.harness/` 안에만 있고 저장소 루트에는 없다.** 대부분의 에이전트 도구는 루트의 `AGENTS.md`를
  자동 로드 대상으로 찾으므로, 다른 도구는 하네스 존재를 모르고 시작할 가능성이 크다.
- ⚠️ **이건 Antigravity만의 문제가 아니다** — 지금 세션이 하네스를 아는 것은 저장소 구조 덕이
  아니라 **Claude 계정 메모리에 이어봄 하네스 구조가 저장돼 있어서**다. 즉 새 Claude Code 세션도
  저장소만 보고는 못 찾을 수 있는 **구조적 구멍**.
- 🟡 **권고: 저장소 루트에 `AGENTS.md` 포인터 파일 신설**(실제 규칙은 `.harness/AGENTS.md`임을
  안내하는 1장). 도구 무관하게 잡히고 현행 세션에도 안전망이 된다. → 사용자 승인 대기.

### 2. [사용자] `reports/` 디자인 산출물 기반 화면 개선 요청 → 🔴 **전제 반박**

- 요청: *"gemini가 디자인 관련해서 작업한 html, md가 reports에 있을 것. 이를 바탕으로 화면을
  세련되게 바꾸고자 함."*
- 🔴 **실측 결과 방향이 거꾸로였다. `reports/style.css`(2026-08-05)는 앱보다 뒤처져 있어
  그대로 적용하면 퇴행이다.**

| 토큰 | `reports/style.css` | `00-09` 정본 = `index.css` |
| :--- | :--- | :--- |
| Point | `#4E7055` ❌ | `#5B7065` |
| Background | `#F7F4EF` ❌ | `#FBF9F5` |
| Text | `#2F3E46` ❌ | `#1A2B4C` |
| Accent Gold | **없음** ❌ | `#D4A359` |
| 본문 서체 | Noto Sans KR ❌ | Pretendard |
| 제목 명조 | **없음** ❌ | KoPubWorld 명조 |
| Card hover | `-5px` ❌ | `-4px` |
| 긴급 FAB | `.floating-call` 잔존 ❌ | 제거됨 |

- **원인**: `reports/`는 `docs/` 마크다운을 HTML로 시각화한 **문서 뷰어**이지 제품 시안이 아니다
  (`generate_all_reports.js` 생성). 목적이 다르니 자연히 갈라지는데 **어느 쪽이 정본인지 문서에
  적혀 있지 않아 혼선이 났다.**

### 3. [Claude:Opus] 🔴 `00-09` 정정 — **문서가 없는 것을 있다고 말하고 있었다**

2번을 대조하다 발견. **`00-09`가 두 번의 결정을 못 따라온 상태**였다.

| 위치 | 종전 서술 | 실측 |
| :--- | :--- | :--- |
| §2.1 Alert 행 | `#EF4444`/`#DC2626`, *"24시간 긴급 콜 FAB"* | 🔴 `#EF4444` **저장소 0건** / FAB **삭제됨** |
| §3.1 구조 | *"Bottom-Right Floating FAB: 🚨 24시간 긴급 콜"* | 🔴 존재하지 않음 |
| §4-3 | `50% { opacity: 0.5 }`, 이름 *"Emergency"* | 🔴 실제 `0.6` + `scale(1.15)`, 유일 사용처는 **지도 핀** |
| §3.1 사이드바 | 6개 메뉴(메인 + 5대 도메인) | ✅ 정확 — 수정 안 함 |

- **경위는 2단계**: ①`00-14` §2-4가 *"24시간 긴급콜"* 을 **받을 사람이 없는 약속**으로 판정해
  *"장례식장 바로 찾기"* 로 정정(08-14) → ②그 대체 버튼마저 **08-18 개발자 지시로 완전 제거**
  (walkthrough (32), `FloatingEmergency.tsx` 삭제). **`00-09`만 두 번 다 놓쳤다.**
- 🔴 **`00-14` §2가 여덟 번 잡은 결함과 같은 종류**이고, 방향은 §2-9처럼 **문서가 사실이 아닌 것을
  현재 시제로 말하는** 쪽이다. 정본 표라서 방치하면 다음 구현자가 그대로 만든다.
- `#DC2626`은 살아 있으나 **용도가 다르다** — `TaxSimulatorModal`(상속세 결과) ·
  `KakaoMapModal`(핀) · 오류 메시지 3곳. 실측대로 다시 적었다.
- ⚠️ `index.css:127` 주석이 아직 *"Emergency Hotline Pulsing Badge"* 인데, **코드 주석 정정은
  `[Claude:Sonnet]` 몫**이라 문서에는 사실만 기록했다.

### 4. [Claude:Opus] `00-09` §5 신설 — **정본 경계**

2번 혼선의 근본 원인이 *"어느 쪽이 정본인지 문서에 없음"* 이었으므로 규칙을 세웠다.

- ✅ **정본**: 색·서체·여백·접근성 토큰 → `00-09` / 구현 → `index.css`
- ❌ **정본 아님**: `reports/style.css`, `reports/**/*.html` 인라인 스타일
- 🔵 `reports/`는 **`[Gemini]` 소유이며 Claude에게 읽기 전용**(`CLAUDE.md`) — 드리프트가 보이면
  고치지 말고 `context.md`에 `[Gemini]` 요청으로 남긴다.
- 🟡 **`style.css` 처분은 `[Gemini]` 판단**: ①`00-09`에 맞춰 갱신 ②보고서 전용임을 상단 주석에
  명시 ③삭제 중 택. **목적은 "제품 시안으로 오해될 여지 제거"** 이고, 문서 가독성은 보고서의
  요구라 무조건 `index.css`와 같아질 필요는 없다.

### 5. [Claude:Opus] 🔴 `00-23` §8.5 위반 발견 → `[Sonnet]` 프롬프트 작성

- *"세련되게"* 의 실체를 찾다가 **어제 구현이 스펙을 위반한 상태**임을 확인.
  > `00-23` §8.5: *"Desktop ≥1024px — 상단 2열(① ②, **큰 카드**) + 하단 2열(③ ④, **높이 약 절반**)"*
- 현재는 4개가 `1fr 1fr` 한 그리드에 **전부 동일 위계**다. 게다가 어제 ①②에서 항목 목록을
  걷어내면서 **주가 되어야 할 ①②가 비고, 부가 되어야 할 ③④(입력창·버튼)가 더 꽉 차 위계가
  역전**됐다. 🔵 **카드가 휑해 보이는 건 취향 문제가 아니라 스펙 위반의 증상이다.**
- 다음 사이클 프롬프트를 작성해 전달(근거 `00-23` §8.5 + `00-09`, **`reports/style.css` 참조 금지**
  명시, 접근성 토큰·앰버 배지·가짜 목록 금지 재확인).

### 7. [Gemini] `reports/style.css` 처분 완료 & `00-09` 재생성 & walkthrough (33) 판정

- **`reports/style.css` 처분 (A안 + B안 통합 적용)**:
  - 파일 최상단에 **"⚠️ 보고서 뷰어 전용 — 제품 디자인 정본 아님(정본: docs/00-09 및 eobom/frontend/src/index.css)"** 대형 경고 주석 배치 (B안 충족).
  - `00-09` §2.1·§2.2·§2.4·§4 정본 토큰(딥 네이비 `#1A2B4C`, 웜 그린 `#5B7065`, 웜 골드 `#D4A359`, 소프트 웜 크림 `#FBF9F5`, Pretendard, KoPub World Batang, hover `-4px`, card padding `1.5rem`) 동기화 (A안 충족).
  - 실물이 삭제된 `.floating-call` 클래스 완전 제거 확인.
- **`00-09` 재생성 및 전체 HTML 보고서 47건 일괄 빌드**:
  - `00-09` 디자인 시스템 (§2.1 Alert 정정, §3.1 긴급 FAB 제거 사실 반영, §4 pulse 정정, §5 정본 경계 신설) 갱신 반영.
  - `node .harness/tools/generate_all_reports.js` 실행으로 전체 47개 보고서 동기화 완료.
- **Walkthrough (33) 게이트 검증 판정**:
  - `(33) [Opus] 00-09 정정(긴급 FAB·pulse 실측) + §5 정본 경계 신설` ➔ **✅통과** 판정 기록.
- **검증**: `bash .harness/tools/harness-doctor.sh` → **✅ 129개 항목 통과.**

---

### 8. [Claude:Sonnet] 메인화면 시각 정제 — 4박스 위계 복원 + 히어로·오버레이 마감

- (5)의 `[Sonnet]` 프롬프트를 이어받아 착수. `00-23` §8.5(4박스 레이아웃) 위반을 고쳐 상단①②/하단③④
  위계를 두 그리드 블록으로 분리하고, 박스①②의 빈 공간을 아이콘 확대+클릭 불가 요약 칩+CTA 강조로
  채움. 히어로·`BoxDetailOverlay` 슬라이드는 `alignItems: center→start`로 정렬 리듬을 통일.
- 🟡 (5)가 남긴 `index.css:127` 주석(*"Emergency Hotline Pulsing Badge"*)도 이번에 정정 —
  `grep`으로 유일 사용처가 `KakaoMapModal.tsx`(지도 핀)임을 재확인 후 사실대로 고침.
- 데스크톱(1440px)·태블릿(834px, iframe 프로브)·모바일(375px, iframe 프로브) 3폭 실화면 확인,
  진입 4곳·🔒로그인모달·준비중 무반응·오버레이 휠 스크롤 회귀 없음 확인. `tsc --noEmit` 프론트·백엔드
  에러 0. 상세는 walkthrough (34).
- 🔴 **새로 발견한 미해결**: App 레벨 `<Header>`가 375px 모바일 폭에서 가로 오버플로(사용자 프로필
  칩이 안 줄바꿈). 이번 사이클 파일 범위 밖이라 손대지 않고 다음 에이전트 몫으로 남김.

### 9. [사용자] 03/04/05 도메인 분리 문의 → 🔴 **바로 착수 불가 판정**

- *"디지털 유품 및 현물 정리 서비스 메뉴에 담긴 ①디지털 자산/계정 정산 ②현물 유품 수거/정리
  ③디지털 추모관&앨범을 각각의 도메인&메뉴로 쓰고자 함. sonnet에게 바로 명령해도 될까?"*
- ✅ **방향은 맞다** — `00_DOCS_INDEX` 도메인 번호 체계가 이미 03/04/05로 갈라놨고, 화면만 못
  따라오고 있었다. `DigitalEstatePage.tsx`(=`SCR-003`)가 서브탭 3개로 뭉쳐 있는 건 구조적 부채.
- 🔴 **그러나 바로 주면 깨지는 것 5가지**: ①03·05는 문서가 *"열지 말라"* 고 못 박음(`03-02` §7.2
  업체 0건 / `05-01` §2.6) ②**어제 만든 4박스와 정면 모순**(4박스는 준비 중 비활성인데 사이드바는
  상설 진입) ③`SCR-013`~ **화면 ID 미발급 — 발급은 `[Opus]` 몫** ④`00-09` §3.1(사이드바 6개)과 충돌
  ⑤`03-02` §5 `CleanupRequest` 미구현이라 03 페이지에 담을 내용이 없음.
- → 이 건은 **10번(모드별 네비게이션)에 흡수**됐다. 유가족 모드 메뉴에서 03/04/05를 각각 노출하면
  별도 사이클이 필요 없다.

### 10. [Claude:Opus] `00-26` 신설 — 모드별 네비게이션 개편 검토서

- 개발자 제안: *"홈 첫 진입에서 **메뉴 없애기**, 4박스 각각 **로그인**, 진입 후 **맞춤형 메뉴**"*
- ✅ **이것이 `00-12` §6.2가 2026-08-12부터 확정 대기로 묶어둔 "모드 전환 UX"의 답이다.** §6.2가
  B안(수동 전환)을 기각한 **유일한 사유**가 *"전환 존재를 모르면 무용"* 인데, **4박스가 첫 화면이면
  전환 존재를 모를 수가 없다.** 화면 구조가 기각 사유를 제거한다.
- 🔵 **가장 큰 실익은 `Deceased` 의존을 끊는 것** — C안 권고의 자동 판정은 `Deceased` 등록이
  신호인데 **그것이 지금 04·05·07을 전부 묶은 블로커**다. 즉 C안은 *"블로커가 풀려야 만들 수 있는"*
  안이었고, 개발자 방식은 지금 가능하다. **C안 폐기가 아니라 선후 배치**로 정리했다.
- ✅ **맞춤 메뉴는 새로 정할 게 없었다** — `00-02` §3.1이 모드별 도메인을 이미 확정(생전 02·04·06 /
  유가족 01·02·03·04·05·07). `00-23` §3 실측 상태만 배지로 붙였다. 🔴 유가족 6개 중 **실제 열리는
  것은 4개.**
- 🔴 **핵심 지적: "각각 로그인"은 4개 중 1.5개만 맞다.** ②유가족은 사별 직후 유족에게 로그인 벽
  (`07-02` 원칙 붕괴), ③Guest는 **대상이 비회원 조문객**이라 `00-23` §2.2가 발굴한 분기가 다시 막힌다.
  → 🟡 **대안: 진입 조건을 "로그인"이 아니라 "모드 선택"으로.** 원하는 결과는 전부 얻으면서
  §4.1이 안 깨지고, **차이는 로그인을 묻는 시점뿐.**
- 🔴 **새 발견: 사이드바 라벨이 5개 중 3개 어긋나 있다**(`00-23` #6은 1건으로 봤음) —
  *"장례·묘지 매칭"*→**장사시설 매칭**(01은 봉안당·수목장 포함 1,552건인데 범위를 좁게 오해시킴) ·
  *"상속·법률 케어"*→**전문가 매칭** · *"디지털 유품 정리"*→**디지털 자산·계정 정산**.
- 🔵 **④파트너가 이미 `isPortalRoute`로 크롬을 분리해 동작 중**(`00-06` §7.4) — 모드별 사이드바는
  새 구조가 아니라 **기존 패턴의 확장**이다.

### 11. 🔴 [Claude:Opus] 문서번호 충돌 발견 — `00-24`는 이미 점유돼 있었다

- 검토서를 `00-24`로 만들었다가 인덱스에 넣으려는데 **`00-24` 행이 없고 `00-25`가 이미 존재**해
  이상해서 `git log -S "00-24"`로 확인했다.
- 🔴 **커밋 `e7855a5`에서 `[Gemini]`가 `reports/00_핵심플랫폼/00-24_이어봄_프론트엔드_UIUX_디자인_
  개선_보고서.html`을 만들어 번호를 점유**하고 있었다(파일 내부에 *"문서 번호: 00-24"* 명시).
  → 다음 빈 번호인 **`00-26`으로 파일명 변경.**
- ⚠️ **그 과정에서 구조 문제를 발견했다** — `00-24`는 **`reports/`에만 있고 `docs/` 정본이 없다.**
  `roles.md` §1-2의 순서(`[Opus]` `docs/` → `[Gemini]` `reports/`)와 **역방향**이라 정본 없는
  보고서가 됐고, 그래서 인덱스에서도 안 보였다. `reports/`는 Claude 읽기 전용이므로 고치지 않고
  **인덱스에 "정본 없음" 경고 행을 신설**해 드러냈다. 🔵 존치/정본화/폐기 판단 필요.

### 12. [Claude:Sonnet] 모드별 네비게이션 개편 — 홈 메뉴 제거 + 진입 모드별 맞춤 사이드바

- `00-26` §7(10번 신설 문서)의 구현 스펙대로 착수. 홈(`/`)에서 사이드바를 숨기고(Header는 유지),
  4박스 클릭 시 모드(`prep`/`bereaved`)를 `localStorage`에 확정한 뒤 도메인 페이지 사이드바를
  모드별 맞춤 메뉴(3개/6개)로 바꿈. §4.2-1 확정대로 ①생전준비만 로그인 게이트, ②유가족·③Guest는
  로그인 없이 즉시 진입(07 열람·조문 보호). §5 라벨 3건("장례·묘지 매칭"→"장사시설 매칭" 등)도
  같이 정정.
- 🔴 **8번(App `<Header>` 375px 오버플로)을 해소하면서 헤더 모드 칩(§4.4 A안)을 신설했는데,
  실기동 중 로그인 상태+좁은 창 폭에서 칩과 사용자정보가 겹치는 걸 직접 발견해 고쳤다** —
  절대중앙배치 대신 로고/칩/사용자정보 3분할 실제 flex 흐름으로 재설계해 구조적으로 겹침이
  불가능하게 만듦. Header 자체의 오버플로는 375px 전 경로에서 완전히 해소 확인.
- 🔴 **새로 발견**: `CounselingPage`·`CareGuidePage` 등 개별 도메인 페이지 자체가 375px에서
  별도 오버플로를 갖고 있다(Header와 무관, 페이지 콘텐츠 문제) — 이번 사이클 파일 범위 밖이라
  손대지 않고 다음 에이전트 몫으로 남김.
- 데스크톱·태블릿폭·모바일(iframe 프로브) 3폭 확인, 4박스 4경로(①로그인모달 ②로그인없이+07
  즉시열람 ③Guest 무반응 ④/partner) + 모드 전환 칩 + 홈 복귀 시 4박스 노출 전부 확인. `tsc
  --noEmit` 프론트·백엔드 에러 0. 상세는 walkthrough (36).

### 13. [Claude:Opus] `00-26` 재정합 — 게이트 판정(38·39·43 🔄스펙갱신) 반영

- 12번 이후 개발자 즉석 지시가 연달아 들어가면서(walkthrough (37)~(43)) **`00-26`이 하루 만에
  5곳에서 실물과 어긋났다.** Gemini 게이트가 (38)·(39)·(43)을 🔄스펙갱신으로 판정해 `[Opus]`가
  세 건을 한 번에 재정합했다. **코드를 정본으로 읽고 문서를 맞추는 순서**로 진행.
- **고친 문서 6개**: `00-26`(§1·§3.1·§3.2·§4.2-1·§4.4·§5.1·§6·§7.1·§7.2·§7.3·§8) ·
  `00-23`(§3.1 신설·§8.2 이관 고지) · `00-06`(`SCR-013`~`015` 등재·§6 미할당 갱신) ·
  `00-02`(§3.1 모드 라벨/04 제외·§3.2 화면 열) · `00_DOCS_INDEX`(도메인표·00-06·00-23·00-26 행).
- 🔴 **뒤집힌 5건**: ①§3.1 생전 준비 **3개→2개**(04 제거) ②§3.2 유가족 **6개→7개**(모바일 부고장
  신설·유족 시간 순 재정렬·03·05 `준비중`→`미리보기`) ③§4.2-1 **로그인 전면 반전** — *"①④만 로그인"*
  폐기, **게이트가 박스에서 항목으로** 이동하고 범위는 **`care-guide` 하나만 빼고 전부**
  ④§4.4 모드 **칩→드롭다운**(로고 옆·3항목 즉시 전환·홈에서도 노출) ⑤§7.1 *"새 화면 ID 없음"* 위반.
- 🔴 **가장 무거운 것은 ⑤가 드러낸 사실** — `00-23` §3과 `00-26`이 근거로 삼은 *"03·05는 실체가
  없다"* 는 **전제 자체가 틀렸다.** 둘 다 `DigitalEstatePage` 서브탭에 **예시 데이터로 이미
  구현돼 있었고**, 라우트만 보고 서브탭 안을 열어보지 않아 실측이 놓쳤다. **없던 것을 만든 게
  아니라 있던 것을 찾은 것**이다. → `00-23` §3.1에 **놓친 이유까지** 적어 재발을 막았다.
- ⚠️ **`SCR-015`(`/memorial`, 추모관 화면 예시)와 `SCR-011`(`/memorial/:slug`, 고인별 공개
  추모관)은 다른 화면이다.** 이름·경로가 비슷해 섞이면 **05 블로커(`Deceased`·사진 오픈 금지)가
  풀린 것처럼 보인다** — `00-06`·`00-23`·`00-26` 세 곳에 각각 못박아 뒀다.
- 🔵 **미결 #1~#5 전부 닫힘.** 신규 3건: **#7**(04를 생전 준비로 되돌릴 시점 — `04-02` 사전지정
  UI 구현 시) · **#8**(`03-02` §7.2 *"업체 0건 동안 준비 중"* 개정 — `/pickup` 노출과 충돌) ·
  **#9**(375px 헤더 오버플로, (34)부터 3회째 이월).

### 14. [사장님·개발자] `03-02` 미결 3건 확정 → `[Claude:Opus]` 반영

- 13번이 올린 후속 미결에 대한 답 3건. **`03-02` §6.1 → §6.2 → §7.2 순서로 대응**한다.

| # | 확정 내용 | 반영 |
| :---: | :--- | :--- |
| **§6.1** | 🔵 *"폐기물 처리 서비스·재활용센터·청소 전문업체 이름으로 사업이 활성화된 것으로 확인. **우선 우리가 업체를 심사하지는 않을 것**이므로 패스."* | **미결 #2 보류** — 허가·신고증 제출, 행정사·지자체 확인 모두 시행 안 함 |
| **§6.2** | 🔵 *"**추후 다룰 유언 및 상속 개발과 연동**될 것이라 우선 넘김."* | **미결 #7 이관** — 유언·상속 도메인 설계 사이클로 |
| **§7.2** | 🔵 *"**더미 회사로 우선 진행**. 아직 서비스 단계가 아니기에 **목업을 꼼꼼히** 만드는 데 집중."* | **§7.2 개정** — *"준비 중으로 둔다"* → **"고지 3조건을 지키면 노출"** |

- 🔴 **§6.1의 파생 효과 하나** — **심사를 하지 않으면 화면에 *"검증·추천·엄선"* 을 쓸 수 없다.**
  실측 결과 **`PickupPage.tsx:38`에 *"…전문 업체 추천"* 이 이미 있다**(→ `[Sonnet]` 수정 대상).
  같은 페이지의 *"매칭"*·*"연결해 드립니다"* 는 **우리가 실제로 하는 일**이라 문제없고,
  `domainSlides.tsx:78`의 *"검증된 변호사·세무사"* 는 **02이고 02엔 실제 승인 심사가 있으므로
  건드리면 안 된다.** 일괄 치환 금지.
- 🔴 **§7.2 개정에서 별점과 상호를 갈랐다** — 배너(*"업체·평점은 실존하지 않으며…"*)는
  **이용자에겐 고지가 되지만 제3자에겐 안 된다.** 그래서 **창작 별점은 허용**(피해가 이용자에게
  가고 배너가 고지함, 목업 목적상 필요)하되 **실존 상호·번호·주소는 절대 금지**(피해가 그 업체에
  가는데 그 사람은 배너를 볼 일이 없음). ✅ 현재 더미 10건은 **지역명+일반명사 조합에 전화번호
  필드 자체가 없어** 조건을 충족한다. ⚠️ **전화번호를 추가하는 순간 걸린다.**
- 🔵 **참고로 딸려온 유언·상속 방향** — *"이용자에게 관련 가족 등의 **이름과 연락처**를 받고 이를
  통해 진행"*(사장님 논의). **이건 03보다 `00-12` §7.3(유족 "발견" 순환)에 훨씬 크게 작용**해서
  그쪽에 등재했다. ✅ §7.3 권고(지정 즉시 통지)와 **같은 해법**이고 §2.3의 *"지정당한 사람의
  수락"* 구멍도 함께 메운다. 🔴 **다만 수집 대상이 제3자(가족)라 이용자가 대신 동의할 수 없다** —
  **통지=고지 / 수락=동의 / 순환 차단이 같은 화면 하나**로 풀리므로 착수 시 통합 설계 권고,
  `00-18`·`00-19` 처리방침 개정 동반(`00-12` §8 #7 신설). ⚠️ **기획 예정이며 착수 지시 아님.**
- ✅ **`00-26` §6 #8이 이 개정으로 닫혔다.**

## ⏳ 남은 것

> 🔄 **13번(재정합) 반영 — 아래 항목 중 `00-23` §8·#1-1은 갱신됐습니다.**
> - ✅ **`00-23` §8 재정합 완료**(13번) — §8.2 항목 목록은 `00-26` §3.1·§3.2로 이관, §8.3·§8.5는
>   계속 정본. §3 실측표 오류도 §3.1로 정정.
> - 🟡 **`00-26` §6 #1-1은 "해소"가 아니라 "절반 해소"로 정리됐습니다** — 오버레이 소개글은
>   열리지만 **02 전문가 목록 조회는 여전히 로그인 뒤**입니다(§4.2-1).
> - 🔴 **신규 `[Opus]` 후속**: `03-02` §7.2 개정(`00-26` §6 #8) · `00-09` §3.1 *"사이드바 6개 메뉴"*
>   갱신(모드별 2개/7개).
> - 🔴 **신규 `[Gemini]` 후속**: `reports/` 재생성 5건 — `00-26`·`00-23`·`00-06`·`00-02`·인덱스.

- 🔴 **`00-23` §8 재정합이 아직 남았다** — walkthrough (32)가 §8.1(01~05 섹션 유지 전제)·
  §8.2(항목 목록 노출)를 뒤집었다. §8.5(레이아웃)는 (34)로 실물과 다시 맞춰졌으니, `[Opus]`가
  재정합할 때 §8.5는 (34) 기준으로 반영하면 된다.
- 🟡 **루트 `AGENTS.md` 포인터 신설** — 사용자 승인 대기(1번).
- ✅ **`[Gemini]` 2건 완료**: `reports/style.css` 처분 완료 · `00-09` 재생성(§2.1·§3.1·§4·§5 반영) 완료.
- ✅ `index.css:127` 주석(*"Emergency Hotline"*) 정정 완료(8번) · ✅ **App `<Header>` 375px
  오버플로도 해소 완료(12번, 모드 칩 겹침 버그 포함)**.
- 🔴 **새 미해결**: `CounselingPage`·`CareGuidePage` 등 개별 도메인 페이지의 375px 오버플로(12번)
  — 담당·범위 미정.
- 🔵 **`00-26` §6 #1-1(①의 02 조회 차단 완화)·#2(모드 기억 방식)는 목업이 이미 나왔다** — 사장님이
  보고 "다안(완화 없음) 유지" 여부만 정하면 됨(12번 walkthrough 편차 참고).
- 🔴 **`00-24` 문서번호 정본 없음 문제**(11번) — 존치/정본화/폐기 판단 필요.
- ⏳ walkthrough (35)·(36) `[Gemini]` 판정 대기.

