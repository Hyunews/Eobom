# 📝 2026-08-18 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [사용자] 금요일(08-14) 작업 이어서 진행 지시

- `00-14` 2차 잔여 4건(5-4 · 7-4 · 6-3 · 6-2)이 대상. 08-14 세션에서 4건분 사전조사를
  `memory/project/00-14-2차-잔여-착수준비.md`에 남겨뒀으므로 **웹 재조사 없이 문서화부터 시작**.

### 2. [Claude:Opus] `00-17` 신설 — 개인정보 인증(ISMS-P) · 국외이전 대응안 (§5-4)

- **ISMS-P 의무대상 아님.** 현행 기준으로도, 진행 중인 3단계 개편안(강화/표준/간편)으로도
  문턱에 닿지 않음. 다만 시행령·고시가 **2027년까지 미확정**이라 재확인 트리거 T1~T4를 걸었다.
  *"오픈 전 확인"* 은 시점이 우리 사정에 묶여 헛돌기 때문.
- **CSAP는 이어봄 과제가 아님** — 클라우드 사업자가 받는 인증. `00-11` §5.2가 ISMS-P와 한 문장에
  묶어둔 것을 갈라 내렸다.
- 🔴 **`00-11` §5.2 국외이전 서술이 부정확해서 정정.** *"국외 이전에는 별도 고지·동의 등 추가
  의무가 따른다"* → **개인정보보호법 §28조의8의 이전 근거는 5가지**이고, 클라우드 호스팅은
  **"계약 이행을 위한 처리위탁·보관"이라 처리방침 공개로 갈음**된다. **해외 리전이 위법인 게 아니다.**
  국내 리전 권고는 유지하되 근거가 *"위법 회피"* → **"논점 미생성"** 으로 바뀜.
- **현행 스택 국외 요소 점검** — `00-11` §5.1이 **Vercel 유지**를 권고했고 `render.yaml`은
  **Render `plan: free`**. 국외이전의 실질 해소는 국내 리전 선택이 아니라 **Render 이탈**이며,
  **Vercel은 남으므로 공개 항목이 0이 되지 않는다.**
- **의무대상이 아니어도 안전성 확보조치는 규모 무관 적용** — 새 규칙을 만들지 않고
  이미 어디에 있는지 8행으로 매핑(`security.md`·`00-11` §7.2·`00-04`·`00-12` §4 등).

### 3. [Claude:Opus] 🔴 `00-14` §2.4 신규 — **2-6. 처리방침·약관이 화면에만 있음**

- `00-17`을 쓰다 발견. **`Footer.tsx:77`·`Footer.tsx:80`에 "서비스 이용약관"·"개인정보 처리방침"이
  `<li>` 텍스트로 노출 중인데 링크도 페이지도 문서도 없다.**
- `00-14` §2가 잡은 *"화면이 약속했는데 뒷받침이 없음"* 과 같은 종류. §2 전수조사는
  `HomePage`·`EndingNotePage` 계열만, §2.2 추가조사는 목업 JSON만 봐서 **둘 다 `Footer`를 놓쳤다.**
- 게다가 **처리방침은 국외이전을 동의 없이 하는 법적 수단 그 자체**라 §2보다 급하다.

### 4. [Claude:Opus] `00-16` 신설 — L3 사망확인 서류 검토서 (§7-4)

- **권고(화면 확인 후 미저장)는 유지, 근거가 승격.** 종전은 *"유출되면 회수 못 한다"* 는 운영 판단
  이었는데, **개인정보보호법 §24조의2**는 법령 근거 없는 **주민등록번호 처리를 금지**하고
  **이 조문은 동의로도 풀리지 않는다.** 서류에는 주민등록번호가 인쇄돼 있다.
  → `00-12` §8-2·`04-01` §10-2 #2를 **확정 대기 목록에서 내렸다.**
- 🔴 **개발자가 말한 "정보 부족"을 채우다 설계 결함 2건 발견**:
  - **시간축** — 사망신고 처리 전에는 기본증명서에 사망이 안 뜬다. **L3를 기본증명서로 설계하면
    D+0~수일의 가장 급한 구간에서 작동하지 않는다.** 1차 서류는 사망진단서·시체검안서.
  - **한 서류로 둘을 못 한다** — *"사망했다"*(`DeathVerification`)와 *"신청인이 유족이다"*
    (`BereavedRelation`)는 다른 서류. 해법은 **생전 지정(`DESIGNATED`)을 기본 경로로** 두는 것이며
    `04-02` §4·`06-02` §3과 같은 형태의 답.

### 5. [Claude:Opus] `00-15` 신설 — 회귀 테스트 도입 검토서 (§6-3)

- 지시가 *"우선 장단점 문서화 먼저"* 였으므로 **결정서가 아니라 검토서**. §4에 **도입 반대 근거를
  이득과 같은 비중으로** 적었다.
- **실측 재확인**: 백엔드·프론트 둘 다 테스트 러너 없음, `eobom/backend/tests/` 없음, 회귀 테스트 0건.
  규모는 컨트롤러 14 · 라우트 10 · Prisma 모델 20 · 토큰 4종.
- **게이트는 자동 테스트의 대체재가 아니라는 것이 이미 실증됨** — walkthrough (19)·(22)에서
  두 주체 산출물이 한 커밋에 섞여 **§2-4(미선언 변경 대조)가 diff로 귀속 불가**가 되어 우회됐다.
- **반대 근거도 실제로 무겁다** — `Deceased` 미확정 단계라 지금 쓴 테스트 상당수가 폐기 대상이고,
  **방치된 빨간 테스트는 없는 것보다 나쁘다.**
- **권고: 전면 도입 아님.** ①비용 0의 0단계(스크립트 보존 규칙) ②"안 바뀌는 것부터"
  (인증·권한 경계 → 발번 규칙 → 정책 계산).

### 6. [Claude:Opus] 연계 문서 갱신 · 검증

- `00-11`(§5.2·§8-5·§9) · `00-12`(§3·§4.2·§8-2·§9) · `04-01`(§10-2) · `05-02`(§6.4·§11-6) ·
  `00-14`(§1·§2.4·§5-4·§6-3·§7-4·§8) · `00_DOCS_INDEX`(신규 3건).
- `bash .harness/tools/harness-doctor.sh` → **✅ 119개 항목 통과.**
- 인덱스의 `reports/` 칸은 **백틱 경로 대신 "⏳ `[Gemini]` 생성 대기"** 로 적었다 —
  doctor §5가 인덱스의 백틱 `reports/` 경로 실존을 검사하기 때문.

### 7. [사용자] 6-2 착수 승인 + 커밋 지시

- *"6-2 착수해도 돼. 그리고 커밋해줘"* → `pending-approvals.md`의 06 항목을 **해제됨으로 이동**.
  ⚠️ **해제된 것은 "착수 금지"이지 A/B 결론이 아니다** — 확정은 다시 사람 몫이라는 단서를 함께 남김.
- 5-4·7-4·6-3 사이클 커밋 (`6ea5d57`, 15 files / +900). **금요일 게이트 (22) 판정도 함께 들어감.**

### 8. [Claude:Opus] `06-03` 신설 — 엔딩노트 열람통제·예외열람 정책 (§6-2)

- 🔴 **질문의 전제부터 정정.** 개발자는 *"열람 정책"* 을 물었지만 **서버가 복호화 키를 갖는 한
  운영자는 언제든 볼 수 있고, "안 본다"는 규칙일 뿐**이다. 그래서 뼈대를 *"특수 상황 목록"* 이
  아니라 **"약속을 무엇으로 뒷받침하나"(4층)** 로 세웠다. 목록만 늘리면 §2가 지적한
  *"약속만 화면에 걸려 있다"* 의 재발이다.
- **A안(메타데이터만) 채택 권고.** B안 기각 근거는 *"위험해서"* 가 아니라 **"필요가 없어서"** —
  B안의 유일한 명분이던 고객지원 문의가 메타데이터로 전부 답이 되고, 본문이 필요한 유일한
  경우조차 **본인 열람으로 해결**된다.
- **특수 상황은 2개뿐** — 법원 영장(거부 불가라 **약관 명시 필수**), 본인의 명시적 요청.
  **L3·L4 유족 전달은 "열람"이 아니라 "전달"** 이라 용어·로그·화면을 분리했다.
  🔴 **자살 징후 감지는 기각** — 감지하려면 내용을 봐야 하는 순환 논리. 대신 **상담 창구 상시 노출**.
- **예외 열람 통제 4종**: 2인 승인 · 사유 필수 기록 · 불변 감사 로그 · **사후 통지**.
- **§6에서 선 긋기** — 키 분할·봉투 암호화는 `00-11` 사업자 확정에 걸리므로 지금은
  ①평문 저장 금지 ②키를 코드·이미지에 굽지 않기 **둘만** 확정.
- **§7 — 06은 `03-01` §2 조인 원칙의 유일한 예외.** `include` 통짜 조인 시 본문이 딸려온다.

### 9. [Gemini] `reports/` HTML 4건 생성 · 인덱스 경로 교체 · walkthrough 판정 3건 처리
- **HTML 4건 생성**: `00-15`·`00-16`·`00-17`·`06-03` 마크다운 정본 기반 HTML 시각화 보고서 4건 생성 완료(`generate_all_reports.js` 매핑 추가 및 전체 40건 정상 생성).
- **인덱스 갱신**: `docs/00_DOCS_INDEX.md`의 "⏳ `[Gemini]` 생성 대기" 4곳을 실제 백틱 경로(`reports/...`)로 교체 완료.
- **검증 게이트 3건 판정**:
  - **(21) [Sonnet] 07-02 체크리스트 1단계**: `careGuideTasks.json` 23개 항목 및 `CareGuidePage.tsx` 상속포기 배너·severity 그룹핑·미검증 배지·링크 분리 등 스펙 일치 확인 / `tsc && vite build` 정상 통과 → **✅통과**
  - **(23) [Opus] 00-14 2차 2마감**: `00-15`·`00-16`·`00-17` 신설 및 연계 문서 정합성·harness-doctor 통과 확인 → **✅통과**
  - **(24) [Opus] 06-03**: `06-03` 엔딩노트 열람통제 정책 및 4층 구조·통제 4종·`03-01` 조인 예외 확인 → **✅통과**
- **하네스 무결성**: `harness-doctor.sh` 122개 항목 전부 통과 (부팅 합계 10KB ≤ 11KB, reports 40개 링크 전부 실존).

### 10. [Gemini] walkthrough (25) 게이트 검증 판정 완료
- **(25) [Sonnet] 03 착수 0단계**: `schema.prisma`의 `Lead.facilityId` nullable(`String?`) 전환, `Partner.partnerType` 신설(`@default("FUNERAL_FACILITY")`), `migration.sql` 외래키 `ON DELETE SET NULL` 생성 확인 / `leadService.ts` 앱 레벨 01 필수 제약 충족 확인 / 백엔드·프론트 `npx tsc --noEmit` 에러 0건 통과 재현 / harness-doctor 122개 항목 검사 통과 → **✅통과**

### 11. [Claude:Opus] `03-02` §4.1 해소 갱신 — walkthrough (25) 반영

- (25)의 **편차 필드가 요청한 건**이다. Sonnet이 한 번 §4.1을 고쳤다가 *"구현 중 `docs/`를
  고치지 않는다"*(`CLAUDE.md`) 규칙을 확인하고 `git checkout`으로 되돌린 뒤 넘긴 항목.
- §4.1 제목 🔴 → ✅ + 🔵 해소 블록 신설. **원문 진단은 지우지 않고 남겼다** — 아래 "앱 레벨
  강제" 권고가 **아직 절반만 섰기 때문**이다.
- 🔴 **절반만 섰다는 것이 이번 갱신의 핵심**: `FUNERAL_FACILITY` 필수 강제는 `createLead`가
  이미 하고 있지만(코드 추가 없이), **`ESTATE_CLEANUP` null 허용 강제는 03 리드 생성 경로 자체가
  없어 미착수**다. → §5 `CleanupRequest` 구현 시 분기 규칙 신설 필요.
  **`00-15` §3이 회귀 테스트 근거로 든 "DB 제약을 앱 로직으로 옮긴 변경"의 실물이 이것이다.**
- 함께 갱신: §4 재사용표 `Lead` 행 · §7.1 단계 0(완료)·단계 1(`partnerType`은 0에서 이미 나감) ·
  §8 미결 #6(닫힘).

### 12. [Claude:Opus] `00-18` 신설 — 개인정보처리방침·이용약관 초안 설계서 (§2.4 2-6)

- `00-17` §4.2 ①②③④를 실제로 이행한 문서. **처리방침의 뼈대인 "무엇을 수집하나"를
  문서가 아니라 `schema.prisma` 20개 모델 전수 실측으로 채웠다** — 처리방침이 가장 자주
  틀리는 지점이 *"실제로 받는 것과 적어둔 것이 다른"* 것이기 때문.
- 🔴 **가장 큰 결과는 문서가 아니라 발견이다.** 2-6이 *곁눈으로* 나온 건이라 `Footer`의 나머지
  문구를 아무도 안 봤다는 것을 깨닫고 **`Footer.tsx` 전문 155줄을 처음부터 끝까지 읽었다.**
  **4개 문구 중 3개가 뒷받침 없는 약속이었다.**
  - **2-7 — *"KISA 기준 최고 등급 보안 암호화"*(`Footer.tsx:74`)**: 2-1~2-6과 **종류가 다르다.**
    앞의 것들은 *"아직 없는 것을 있다고"* 였지만 이건 ***"존재하지 않는 것을"*** 말한다 —
    KISA가 운영하는 건 ISMS·ISMS-P **인증**(합격/불합격)이고 *"최고 등급 암호화"* 라는 **등급
    체계가 없다.** 게다가 `00-17` §2가 확인한 **ISMS-P 미인증** 상태에서 *"KISA 기준"* 은
    **인증 사칭으로 읽힌다.** 실제 암호화도 부분적이다(비밀번호·토큰만 확인, 정산 계좌는 규칙뿐,
    엔딩노트 AES-256은 계획).
  - **2-8 — *"1588-0000"*(`Footer.tsx:116`)**: §2-4에서 *"24h 긴급콜"* 은 지웠는데 **번호가
    남았다.** 같은 파일 85행 주석이 08-14 정정 경위를 적어둔 바로 그 자리다.
    ⚠️ **처리방침에 직결** — **보호책임자 연락처가 필수 기재사항**인데 화면의 유일한 연락처가 가짜.
  - **검색으로 확인**: 두 문자열 다 **저장소 전체에서 `Footer.tsx` 한 곳에만** 존재.
    `docs/`·`reports/`·`.harness/` 어디에도 근거가 없다.
- **처리방침 쪽 결과**: 정보주체 4종 분리(🔴 **비회원 조문객이 로그인 없이 개인정보를 생성**),
  함정 3가지(`visitorHash`는 IP 원문 미저장이지만 자동수집 기재 대상 / **고인은 법상 정보주체가
  아니지만 동일 취급 권고** / `consentSnapshot`), **국외이전 공개표를 실제로 채움**(Vercel·Google
  잔존, Render는 이탈 예정이나 **이탈 전 오픈 시 등재 필수**), 위탁 ↔ 제3자 제공 구분.
- 🔴 **보유·파기 표 8행 중 4행이 빈칸**이라는 것이 정직한 결과다. 다만 **회원 탈퇴 규칙은
  스키마가 이미 절반을 답하고 있다** — `Lead.userId`·`ConsultRequest.userId`가 `SetNull`,
  `SocialAccount`가 `Cascade`. **설계는 섰는데 문장이 없었을 뿐**이라 그대로 적으면 된다.
  ⚠️ 반면 **소프트 삭제 3종(`unlinkedAt`·`deletedByOwnerAt`·`hiddenAt`)은 파기 시점이 전무** —
  파기 규칙 없는 소프트 삭제는 *"안 지우는 것"* 과 같다.
- **약관 쪽 결과**: 표준 목차는 특기사항 없음. **이어봄 고유 조항 9개**를 배치했는데 **새로 쓴
  문장이 없다** — 전부 다른 문서가 이미 만들어둔 것을 그릇에 담았을 뿐이다(영장 ← `06-03`,
  재전파 ← `00-13`, 허위 개설 ← `05-01`, 무효력 ← `06-02`, 중개 지위 ← `03-02`).
  🔴 **⑦ 사망 후 계정 처리만은 베낄 원본이 없다** — 일반 약관은 사망을 다루지 않는다.
  **이어봄의 사업 자체가 거기이므로 외부 검토에서 가장 시간을 써야 할 곳도 여기다.**
  ⚠️ **⑥은 `Memorial.falseReportAgreedAt`으로 이미 동의를 받고 있는데 대응 조항이 없어
  "동의만 있고 근거가 없는" 상태였다.**
- **게시 게이트 5개**를 세우고 **순서를 못 박았다** — 🔴 **문구 수정(2-7·2-8)이 페이지 신설보다
  먼저**다. 반대로 하면 페이지를 만드는 동안 거짓 문구가 계속 노출된다. 2-7·2-8은 **커밋 한 번**.
- 연계 갱신: `00-14`(§1 표·2차 3마감 블록·**§2.5·§2.6 신설**·§8 대장 2행) ·
  `00-17`(§4.2 이관 블록·§7) · `06-03`(§8 #4·§9) · `00-13`(§8 #6 블록) · `00_DOCS_INDEX`.
- **검증**: `bash .harness/tools/harness-doctor.sh` → **✅ 122개 항목 통과.**
  인덱스 `reports/` 칸은 (23)·(24)와 같은 이유로 "⏳ `[Gemini]` 생성 대기".

### 13. [Gemini] `reports/` HTML 41건 일괄 생성 및 walkthrough (26) 판정 완료
- **HTML 41건 일괄 생성**: `generate_all_reports.js`에 `00-18` 매핑 추가 및 전체 41건 HTML 보고서 재생성 완료 (`reports/00_핵심플랫폼/00-18_이어봄_개인정보처리방침_및_이용약관_기획서.html` 신규 생성, `03-02`·`00-14`·`00-17`·`00-13`·`06-03` 최신 정본 반영).
- **인덱스 갱신**: `docs/00_DOCS_INDEX.md`의 `00-18` 대응 보고서 열을 백틱 실존 경로로 교체 완료.
- **검증 게이트 판정**:
  - **(26) [Opus] 00-18 신설 + 03-02 §4.1 해소**: `Footer.tsx:74·77·80·116` 실측 및 KISA/1588 문자열 단독 존재 재현 확인 / `schema.prisma` 20개 모델 전수 인벤토리 일치 / `03-02` §4.1 해소 블록 및 미착수 03 강제 명시 확인 / `00-14`·`00-17` 연계 갱신 확인 → **✅통과**
- **하네스 무결성**: `harness-doctor.sh` 검사 전건 통과 확인.

### 14. [사용자] Footer 확정 2건 + 처리방침 초안 착수 지시

- **2-7 ✅ *"KISA 문구 아예 제거. (추후 관련 보안 내용 있다면 추가가능)"*** — `00-18` §2.3의
  대체 문구 권고보다 **한 단계 보수적**이고 이쪽이 낫다. **대체 문구도 "지금 사실인 것"이라
  범위가 바뀌면 다시 대조해야 하는 부채인데, 빈칸은 부채가 되지 않는다.**
  *"추후 추가 가능"* 은 **이 프로젝트에서 처음으로 "실체가 먼저, 화면이 나중"이 된 사례**다.
- **2-8 🔵 *"가짜 번호 그대로 두기, 번호 확정되면 직접 수정"*** — 현행 유지 확정.
  🟡 다만 **화면과 처리방침 제12조는 다른 요구**다. 화면은 그대로 둬도 되지만
  **보호책임자 연락처는 법정 필수 기재사항이라 실연락처가 반드시 필요**하다 → `00-19` 제12조에 명시.
- **2 *"초안 작성 ㄱㄱ"*** → `00-19` 착수.

### 15. [Claude:Opus] `00-18` §11 신설 — *"표준 양식이 있는가"* 에 대한 답

- **조사 전 내 답과 조사 후 답이 달랐다.** 처음엔 *"둘 다 표준이 있으니 베끼면 된다"* 로 생각했는데,
  **처리방침은 기재사항이 법으로 열거돼 있고(법 §30① + 시행령 §31) 약관은 그런 게 아예 없다.**
  **규제 방향이 반대다** — 하나는 *"이걸 써라"*(적극), 다른 하나는 약관규제법의
  *"이건 쓰면 무효다"*(소극). **안 찾아봤으면 "표준약관 받아서 고치면 된다"고 잘못 답할 뻔했다.**
- ✅ **처리방침 = 베낄 수 있다**(개인정보위「개인정보 처리방침 작성지침」2025.4에 항목별 작성례).
  ❌ **약관 = 못 베낀다.** 공정위 표준약관은 ①소비자 과금 ②2자 구조 ③**본인이 쓰다 해지**를
  전제하는데 이어봄은 ①무과금 ②3자 ③**본인이 죽고 유족이 이어받는다.**
  🔴 **표준약관은 이용자가 죽는 상황을 상정하지 않는데 이어봄의 사업 자체가 거기다.**
  → §7.2 ⑦의 *"베낄 원본이 없다"* 가 감이 아니라 근거로 확정됐다. **순서: 처리방침이 먼저.**
- 🟡 **§6의 "빈칸 4개"를 §11.3에서 다시 뜯어 무게를 갈랐다** — 4개가 같은 무게가 아니었다.
  탈퇴 규칙은 스키마가 답을 갖고 있고, 2개는 권고값 제시 가능, 🔴 **진짜 사장님 판단은
  추모관 보존기간 1개뿐**이다. 🔴 **보호책임자는 2-8과 한 뿌리.**

### 16. [Claude:Opus] `00-19` 신설 — 개인정보처리방침 **v0.9 초안**

- **이 저장소에서 성격이 다른 첫 문서다.** 다른 `docs/`는 *"AI가 읽고 고치는 기획 본문"* 인데
  **`00-19`는 본문이 그대로 `/privacy` 화면에 나가는 게시 예정 정본**이다.
  문서 머리에 성격 구분(설계 근거는 `00-18`, 게시할 문장은 `00-19`)과 🔴 게시 금지를 박았다.
  법 §30① 기준 **전 14조**.
- 🔵 **§0 편집자 주가 실용적 핵심** — 회신 필요 6건을 표 하나로 모으고, **④⑤⑥은 권고값을
  이미 채워 *"이대로 갑시다"* 만 하면 되게** 했다. 🔴 **실질 블로커는 ①보호책임자
  ②추모관 보유기간 ③사업자 정보 3건이고, 그중 판단이 필요한 건 ② 하나.**
  **사장님이 14조를 다 읽을 리 없다는 걸 전제로 만들었다.**
- ✅ **코드 실측 결과가 예상보다 훨씬 유리했다**:
  - **쿠키 미사용** — 백엔드 전체에 `cookie`·`httpOnly`·`sameSite` **0건**. 토큰은 프론트
    `localStorage`에만 있고 서버가 별도 수집하지 않는다.
  - **제3자 분석·광고 도구 0건** — `gtag|analytics|facebook|hotjar|...` 프론트 전수 검색에서
    유일한 히트가 목업 JSON의 **"페이스북"이라는 서비스명 문자열**. 도구가 아니다.
  - **비회원 IP 원문 미저장** — `memorialController.ts:32` `sha256(visitorToken:ip)` 즉시 해시.
  → **일반 처리방침에서 가장 길고 지저분한 쿠키·행태정보 절이 전부 *"사용하지 않습니다"* 로
  끝난다.** 사별·유언 서비스에서 *"추적하지 않는다"* 는 마케팅이 아니라 사실이라 §0.1에 뽑았다.
  🔴 **단 "지금 사실"이므로 도구 하나만 붙여도 처리방침을 먼저 고쳐야 한다**는 단서를 달았다.
- **제3-5조「수집하지 않는 정보」는 표준 목차에 없는 조인데 넣었다** — 주민번호 미수집(`00-16`)·
  서류 미저장·무과금이 **전부 강점인데 표준 목차대로만 쓰면 어디에도 안 적힌다.**
- 🔴 **제9-2조(만 14세 미만)에서 새 결함 후보를 발견했다.** 표준 문구를 넣으려다 확인해보니
  **연령 확인 절차가 없다** — 소셜 로그인만으로 가입된다. 그대로 썼으면 **처리방침 자체가
  `00-14` §2류 결함**(화면이 약속했는데 뒷받침 없음)이 될 뻔했다. 🔴로 남기고 개발자 판단으로 넘김.
- **제10조(안전조치)에는 "실제로 하고 있는 것"만 적었다** — 저장 암호화·백업은 규칙은 있으나
  가동 전이라 뺐다. **방금 2-7에서 그 이유로 문구를 지웠는데 처리방침에서 같은 짓을 하면
  앞뒤가 안 맞는다.** 조 안에 이 원칙을 명시했다.
- **검증**: `bash .harness/tools/harness-doctor.sh` → **✅ 123개 항목 통과.**
  `00-18` 인덱스 칸은 Gemini가 이미 실경로로 교체해둬서 **건드리지 않고 설명만 보강**했다.

---

### 17. [사용자] `00-19` 검토 회신 6건

- ✅ **총칙**: (주)삼일정보시스템 / 이권형 / 전남광주통합특별시 광산구 하남산단8번로 177, 6층
- ✅ **보호책임자**: 윤현우 / 개인정보 담당자 / 070-8856-2725 / yhw@samil25.co.kr
- ✅ **④⑤⑥ 권고값대로 확정** · 🔵 **⑥·⑦ 클라우드·국외이전 보류**(사업자 미정)
- 🔵 **검토 지시 2건**: ②*"장사시설 도메인 위치정보가 수집에 해당하지 않는가"*
  ③*"추모관 폐쇄 기준을 만들어야 할 것 같다 — 3개월? 사용자에게 결정하게 할지?"*

### 18. [Claude:Opus] 🔴 위치정보 — **사장님 지적이 맞았다. `00-14` §2.7(2-9) 신설**

- **처음엔 "확인만 하면 되겠지"였다.** `security.md` §4에 트랙 A/B가 정리돼 있어 *"아직 개발
  단계니 수집 안 한다고 써도 되겠지"* 로 넘어갈 뻔했는데, §4의 *"현재 A/B가 공존 중"* 이
  걸려서 **코드를 직접 봤다.**
- 🔴 **실측 결과 — 저장만 안 할 뿐 수집하고 있었다.**
  - `FacilityPage.tsx:102` 화면 진입 즉시 `getCurrentPosition()` — **정확한 GPS 취득**
  - `FacilityPage.tsx:115` 🔴 **좌표를 이어봄 서버로 전송**(`/api/geo/reverse`)
  - `geoController.ts:68` 🔴 **서버가 카카오 API로 재전송**
  - `geoController.ts:80` ✅ DB 저장 없음
  → **"저장하지 않는다"는 맞지만 "수집하지 않는다"는 틀리다.** 위치정보법의 규율 대상은
  **취득·이용·제공**이고 저장은 그중 하나일 뿐이다. **그대로 갔으면 처리방침이 허위 기재가 됐다.**
- 🔴 **2-9는 앞의 8건과 방향이 반대라 더 위험하다.** 2-1~2-8은 *화면이 약속했는데 실체가 없음*
  (과장)이고, **2-9는 실체가 있는데 고지가 없음**(위반 소지)이다. **전자는 이용자가 기대했다
  실망하는 것이고, 후자는 이용자가 모르는 채로 이미 일어난 것이다.**
  ⚠️ **전수조사를 8번 하면서 매번 "화면 문구"만 봤고 실제 동작은 안 봤다** — 2-9는 문구가
  아예 없어서 못 봤다. **"없는 것"을 찾는 방법이 전수조사에 없다.**
- ✅ **해결책이 이미 구현돼 있다는 것이 결정적이다.** 트랙 B(`/api/geo/regions` — 실제 보유 시설
  주소에서 시/도→시/군/구 추출)가 **이미 있고** 화면 선택 UI와 GPS 실패 폴백도 동작 중이다.
  🟡 **A안(트랙 A 제거) 권고 — 근거는 "위법이라서"가 아니라 "얻는 것보다 드는 것이 커서"** 다.
  자동 감지가 아껴주는 건 *"지역 한 번 고르기"* 인데, 대가는 **사업자 신고 2~4주 + 동의 UI +
  처리방침 절 + 카카오 전달 고지**다. **`01-03`(견적비교) 때와 같은 판단 구조.**

### 19. [Claude:Opus] `00-20` 신설 — 추모관 보존기간·폐쇄 정책 검토서

- 🔴 **전제부터 정정: "하나의 숫자로는 못 정한다."** 질문은 *"3개월이냐 장기냐"* 한 축인데
  **세 축이 겹쳐 있다.**
  - ①**부고(07) ↔ 추모(05)는 수명이 정반대** — `00-13` §2가 링크에 대해 이미 낸 진단이
    보유기간에 그대로 적용된다. **사장님의 "3개월"은 부고장 쪽으로는 정확**하고,
    **틀린 것은 숫자가 아니라 둘을 같은 것으로 본 것**이다.
  - ②**텍스트 ↔ 사진은 비용이 두 자릿수 차이** — **"스토리지 비용"의 실체는 사진 하나**이고
    텍스트는 사실상 영구 보존해도 된다.
  - ③🔴 **유족 ↔ 조문객은 권리 주체가 다르다** — 방명록 `authorName`은 **비회원 개인정보**이고,
    **유족이 "영구 유지"를 골랐다고 조문객 정보를 영구 보관할 권한이 생기지 않는다.**
- ❌ **3개월 기각 근거 = 한국 상례의 리듬.** 49재(7주)·백일은 통과하지만 **첫 기일(1년) 직전에
  닫힌다.** 🔴 **유족이 1년 만에 왔는데 사라져 있는 것**은 이 도메인에서 *"고인을 두 번 잃는
  것"* 이다. **1년은 임의의 숫자가 아니라 첫 기일을 지나는 최소 기간.**
- 🟡 **"사용자 결정" — 찬성하되 시점이 전부다.** **개설 시점에 물으면 안 된다**: 장례 이틀째에
  *"3개월 / 1년 / 영구"* 라디오 버튼은 기능이 아니라 **고인 값매기기**다. **11개월 뒤에 물으면
  세 문제가 다 사라진다** — 판단 가능하고, *"계속 두시겠습니까"* 는 더하는 질문이다.
- 🟡 **핵심 권고: 만료 시 삭제가 아니라 "동결".** `00-13` §7.2-3의 한 줄 아이디어를 승격했다.
  **비용은 "보관"이 아니라 "살아 있음"에서 나온다** — 쓰기 0·모더레이션 0인데
  유족에게는 *"남아 있다"* 가 지켜진다. 🟡 **동결 후 파기까지 N년은 지금 정하지 않기를 권고**
  (최초 파기가 오픈 후 최소 1+N년 뒤라 급하지 않고, 지금 정하면 근거 없는 숫자).
- 🔴 **"폐쇄 기준"은 기간이 아니라 목록** — 4종 중 2종만 구현돼 있다. 특히
  **④개설자 계정 소멸(탈퇴·사망) 대비가 전무**하다. **추모관 개설자도 언젠가 죽는다 —
  이어봄에서 이건 가정법이 아니라 사업 그 자체다.** `createdByUserId`는 단일 FK이고
  `Lead.userId`와 달리 `SetNull`도 아니다. 🟡 공동 관리자 권고하되 `Deceased` 확정에 걸려 등재만.
- **구현은 작다** — `Memorial.expiresAt`·`frozenAt` **필드 2개** + 쓰기 가드 + 배치.
  ⚠️ **가드를 방명록·헌화·사진 3경로에 전부 걸어야 하고 한 곳만 빠져도 동결이 아니다.**
- **검증**: `harness-doctor.sh` → **✅ 124개 항목 통과.**

### 20. [사용자] 위치정보 B안 가정 지시 + 체크리스트 요청 → `00-21`·`00-22` 신설

- *"방통위에 사업자 신고를 했다고 **가정**하고 진행해보자. 일단은 **빠르게 틀을** 만드는게 우선"*
  → **`00-21` 이용약관 초안(전 24조)** 신설. 🔴 **가정이 사실로 둔갑하지 않도록 §0.2 잠금 규칙**
  을 박았다 — 신고 확인 전까지 **제6장 미렌더링 + GPS 중단, 조문과 기능은 한 쌍.**
- *"법적인 것들의 세부 요구 사항들을 체크할 수 있도록"* → **`00-22` 체크리스트** 신설.
  8개 문서에 흩어진 법적 미결을 **30건·5그룹**으로 모았다.

### 21. [Claude:Sonnet] `/terms`·`/privacy` 구현 (E-1·E-2·E-3) — walkthrough (29)

- **E-1** `Footer.tsx:74` KISA 문구 삭제(`1588-0000`은 존치) · **E-2** `LOCATION_BASED_SERVICE_REGISTERED`
  (`config.ts`, 현재 `false`) **하나로 3곳 동시 게이트**(GPS·약관 제6장·처리방침 제3-6조) ·
  **E-3** 라우트·페이지 신설.
- **검증**: 브라우저 실기동 — `/api/geo/reverse`가 **`GEOLOCATION_FALLBACK` 고정값만 전송**함을
  네트워크 로그로 재현, 약관 전문에서 **제18조 다음 바로 제22조**로 이어짐을 확인. tsc 에러 0.

### 22. [Claude:Opus] E-4~E-6 재분류 — **Sonnet 지적을 절반만 받았다**

- (29)가 *"E-4~E-6 방치하면 §2류 결함"* 이라고 지적했는데, **기준을 세워 갈랐다**(`00-18` §8.3):
  **문장이 "지금 사실"인가, "이행기가 안 온 약속"인가.**
  - **E-4**(*"가입을 받지 않습니다"*) → 🔴 **현재 시제 사실 주장인데 연령 코드 0건이라 지금 거짓.**
  - **E-5·E-6** → ❌ **거짓 아님.** **이전 버전이 존재하지 않고 개정도 없었다.**
    *"약관에 탈퇴 처리를 적어두고 탈퇴자가 아직 없는 걸 결함이라 하지 않는 것과 같다."*
- 🔴 **다만 위험은 "지금 거짓"이 아니라 "그때 잊는 것"** — §2가 여덟 번 당한 패턴이라
  **트리거 표**(`00-18` §8.4)를 만들고 `00-22`에도 **같은 표를 복제**했다(한 곳에만 있으면 못 본다).
- 🔵 **(29) ③(마크다운↔TSX 이중 관리)이 예상 밖의 소득** — E-5를 **(a)본문 데이터화 /
  (b)이전 버전 열람**으로 쪼개니 **(a)가 그 이중 관리 해결과 같은 작업**이었다.
  → **(a)는 "약속해서"가 아니라 "지금이 제일 싸서" v1.0 전 권고**(조문 38개인 지금이 최저 비용).
- **E-4 스펙 확정(`00-19` §9-2-1)** — 🔴 **자기신고, 생년월일·연령대 미수집.**
  *"만 14세 미만을 막으려고 전 이용자의 생년월일을 걷는"* 설계는 최소수집과 충돌하고,
  소셜 연령대는 **3사 중 일부만 + 거부 가능**이라 정책이 아니다. `00-16`·`05-01` §4.4와 같은 태도 **3회차.**
  ⚠️ **데모 로그인 버튼에도 동일 게이트** — 우회 경로가 남으면 게이트가 아니다.

### 23. [Claude:Opus] `00-23` 신설 — 메인화면 4박스 개편 검토

- 개발자 구상(*"메인에 생전/임종/게스트/파트너 박스, 메뉴바는 그대로"*)에 대한 검토.
- ✅ **방향 채택** — 새 아이디어가 아니라 **`00-02` §3.1이 08-12에 확정한 2모드 분리를 화면으로
  처음 실현하는 것**이고, **`00-12` §6.2(모드 전환 UX 미결)의 답**이다.
- 🔵 **Guest 분기는 개발자가 찾은 진짜 공백** — `00-13`이 링크 모델을 5안까지 비교했으나
  **전부 "링크를 누르고 온 사람"만 가정**했다. **링크를 잃은 조문객은 갈 곳이 없다.**
- 🔴 **핵심 지적: 9개 항목 중 지금 열 수 있는 것은 5개**(03 업체 0건 / 05 미구현 / 04·06 목업).
  그대로 걸면 §2 재발. ✅ **해법은 이미 확정**(§2.2 *"준비 중 명시"*) → **3단계 표시**, 그 표가 로드맵.
- **수정 4건**: ①로그인은 **박스가 아니라 항목**에(*"막을 것은 쓰기이지 보기가 아니다"*)
  ②유족 박스 6개 평면 나열이 **`07-02` "판단 순" 원칙과 충돌** — 07은 상위 층
  ③**04가 생전 준비에서 누락**(승계 있는 도메인) ④**Guest 입력창은 brute-force 표면** →
  🟡 **링크 전체 붙여넣기** + 실패 메시지 통일(`00-13` §6.1).
- ⚠️ **§5.3은 미결이 아니라 전제 조건** — 사이드바 *"디지털 유품 정리"* ↔ `00-02` *"디지털
  자산·계정 정산"* 로 **라벨이 이미 어긋나 있다.**
- **검증**: `harness-doctor.sh` → **✅ 125개 항목 통과.**

---

### 24. [Claude:Sonnet] 메인화면 4박스 진입 목업 구현 — walkthrough (31)

- `00-23` §8 스펙대로 `EntryBoxes.tsx` 신설, `HomePage.tsx` 히어로-섹션1 사이에 신규 스냅 섹션으로 삽입.
  ①생전 준비 ②임종 및 사후 정리(1층 상중케어 단독강조·2층 5항목) ③Guest(비활성) ④파트너, §8.3
  3단계 배지(진입/🟡미리보기/⬜준비중, 앰버 미사용) 구현.
- **검증**: `tsc --noEmit`(frontend·backend) 에러 0. 브라우저 실기동으로 진입 4곳(전문가상담·상중케어·
  파트너로그인·엔딩노트) 및 준비중 2곳(디지털추모관·현물유품수거) 전부 클릭 확인 — 엔딩노트는
  `onOpenLogin()` 모달만 뜨고 `/ending-note`로 넘어가지 않음, 준비중은 `alert()` 없이 무반응.
  데스크톱·태블릿·모바일 3폭 확인(태블릿·모바일은 `resize_window`가 실제 창에 반영되지 않아
  동일 origin iframe 프로브로 대체 확인 — walkthrough (31) 편차 참고).
- 🔵 **§6 미결 #3(박스 명칭)·#4(Guest 입력 방식)·#8(모바일 순서)은 이 목업을 사장님이 보고 결정.**

### 25. [Claude:Sonnet] 메인화면 2차 개편 — walkthrough (32)

- 사장님 채팅 지시 5건: ①히어로 체크리스트·버튼 제거 + 박스·이미지 크기 축소(좌우 여백 확보)
  ②4박스는 간단 설명만 남기고 클릭 시 01~05 소개 내용을 "같은 방식(풀스크린 휠)"으로 노출
  ③01~05 소개 섹션 전부 제거(각 도메인은 사용자 페이지에서 접근) ④에필로그의 두 CTA 버튼 제거
  ⑤전 페이지의 "장례식장 바로 찾기" 긴급 버튼 제거.
- 착수 전 `AskUserQuestion`으로 ②의 실행 방식(박스별 미니 풀스크린 오버레이로 확정)·03 섹션 분할
  방식(항목별로 쪼갬)·Guest/파트너 처리(기존 방식 유지)를 먼저 확인.
- **구현**: `EntryBoxes.tsx` 전면 재작성(박스①②는 헤더만 남기고 카드 전체 클릭 → 오버레이 오픈),
  `domainSlides.tsx`·`BoxDetailOverlay.tsx` 신설(03·05는 실체 없어 신규 저작 — 가짜 숫자 없이
  "준비 중" 사실만), `HomePage.tsx`에서 옛 섹션1~5 삭제 + 히어로 축소 + 에필로그 버튼 제거,
  `FloatingEmergency.tsx` 삭제 + `App.tsx` 참조 제거.
- **검증**: `tsc --noEmit`(frontend·backend) 에러 0. 브라우저 실기동 — 박스①②오버레이 휠스크롤·
  🔒로그인모달·준비중 슬라이드(가짜 목록 없음)·X닫기 전부 확인, `/counseling`에서 긴급버튼 미노출
  확인, 콘솔 에러 0.
- 🔴 **이번 지시는 `00-23` §8을 정면으로 뒤집는다** — §8을 이 walkthrough 기준으로 재정합하는 건
  `[Opus]` 다음 사이클 몫(스펙 문서는 이번 범위에서 손대지 않음).

## ⏳ 남은 것

- 🔴 **`06-03`은 권고까지** — A/B 최종 확정과 **"2인 승인의 2인이 실재하는지"**(`00-14` §6-1)는 사람 답이 필요하다.
- 🔴 **2-9(위치정보)가 지금 가장 급하다** — 다른 미결과 달리 **이미 일어나고 있는 일**이고,
  🟡 A안(제거)은 **커밋 1회**다. 🔵 사장님·개발자 확정 필요.
- 🔴 **`00-19` 게시 블로커는 이제 2건뿐** — **추모관 보유기간**(`00-20` 확정 대기) · **위치정보**(2-9).
  나머지는 다 찼거나 **보류가 확정**됐다.
- 🔵 **`00-20`에서 사장님께 필요한 결정은 2개** — *"활성 1년"* 과 *"만료 시 동결(삭제 아님)"*.
- ✅ **2-7·2-8 확정 완료** → `[Sonnet]`이 `Footer.tsx:74` 항목 제거(2-8은 현행 유지).
### 26. [Gemini] HTML 보고서 46건 일괄 빌드 & walkthrough (27)~(32) 판정 완료

- `generate_all_reports.js`에 `00-21`, `00-22`, `00-23` 매핑 추가 후 전체 46개 HTML 보고서 일괄 빌드 완료.
- `docs/00_DOCS_INDEX.md`의 `00-21`~`00-23` 대기 상태를 실존 백틱 경로로 동기화 완료.
- `walkthrough.md` 게이트 판정 (27)~(32) 전건 ✅통과 기록:
  - (27) `00-18` §11 작성방법 및 `00-19` v0.9 초안 신설 ✅
  - (28) `00-19` 사장님 검토 반영·위치정보 결함(2-9)·`00-20` 보존정책 신설 ✅
  - (29) 법적 문서 화면 구현(`/terms`·`/privacy`·Footer KISA 삭제·GPS 중단) ✅
  - (30) E-4~E-6 재분류·`00-23` 메인화면 진입구조 개편 검토서 신설 ✅
  - (31) 메인 4박스 목업 구현(`EntryBoxes.tsx`) ✅
  - (32) 메인화면 2차 개편(히어로 축소·`BoxDetailOverlay`·슬라이드 7종·`FloatingEmergency` 삭제) ✅
- `harness-doctor.sh` 128개 항목 전건 통과 확인.

## ⏳ 남은 것

- 🔴 **`06-03`은 권고까지** — A/B 최종 확정과 **"2인 승인의 2인이 실재하는지"**(`00-14` §6-1)는 사람 답이 필요하다.
- 🔴 **2-9(위치정보)가 지금 가장 급하다** — 다른 미결과 달리 **이미 일어나고 있는 일**이고,
  🟡 A안(제거)은 **커밋 1회**다. 🔵 사장님·개발자 확정 필요.
- 🔴 **`00-19` 게시 블로커는 이제 2건뿐** — **추모관 보유기간**(`00-20` 확정 대기) · **위치정보**(2-9).
  나머지는 다 찼거나 **보류가 확정**됐다.
- 🔵 **`00-20`에서 사장님께 필요한 결정은 2개** — *"활성 1년"* 과 *"만료 시 동결(삭제 아님)"*.
- ✅ **2-7·2-8 확정 완료** → `[Sonnet]`이 `Footer.tsx:74` 항목 제거(2-8은 현행 유지).
- ✅ **약관 초안(단계 3)은 지금 착수 가능** — `00-18` §7.2에 9개 조항이 이미 배치돼 있다.
  🔴 단 ⑦(사망 후 계정)만은 베낄 원본이 없어 시간이 걸린다.
- 🔴 **`00-19` 제9-2조 만 14세 미만은 문구가 확정됐으므로 `[Sonnet]` 구현 의무가 생겼다** —
  가입 화면 고지가 없으면 **처리방침이 거짓말이 된다.**
- 🔴 **`00-20` §6.1(개설자 계정 소멸)** — `Deceased` 사이클에서 반드시 함께 볼 것(`05-02` §11).
- ✅ **`03-02` §4.1 해소 갱신 완료.** 🔴 **`ESTATE_CLEANUP` 앱 레벨 강제는 미착수** → §5 `CleanupRequest` 구현 시 신설.
- ✅ **`reports/` 46건 전건 생성 완료** — **walkthrough (27)~(32) 판정 전건 통과.**
- 사전조사 파일 `memory/project/00-14-2차-잔여-착수준비.md`는 **4건 전부 소비 → 삭제 대기**(사람 확인).
