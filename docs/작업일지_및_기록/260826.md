# 📝 2026-08-26 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [Claude:Sonnet] 발신번호 · 가족초대 카카오공유+성함대조 · 초대CTA · `06-05` Phase A

- 사용자가 5개 작업을 한 사이클로 직접 지시: ①발신번호 실번호 ②가족 초대 카카오톡 공유 도입
  (`00-27` §9.1-4, 사장님 08-26 확정) ③수락 시 성함 대조 가드레일(§9.1-4-3) ④초대 완료 화면
  CTA(§9.1-4-2) ⑤`06-05` Phase A(유족 메시지 보관함 골격).
- **발신번호**: `Footer.tsx` `1588-0000` → `070-8856-2725`(`00-14` §11-2 닫힘).
- **카카오 공유**: `MyPageFamilyDesignation.tsx`의 초대 공유를 `navigator.share` 기반에서
  `utils/kakaoShare.ts`(기존 부고장 유틸 재사용) 기반으로 전면 교체 — 데스크톱에서
  `navigator.share`가 폴백을 죽이던 문제(08-26 사장님 실기기 확인)를 근본적으로 제거.
  링크 복사 버튼을 카카오 성공 여부와 무관하게 항상 노출. 알리기 전 상시 경고 배너 추가.
- **성함 대조**: `FamilyDesignation.acceptAttempts` 컬럼 신설(로컬 dev DB에만 마이그레이션 —
  운영 DB는 배포 시 자동 적용), 수락 화면에 이름 입력란 추가, 서버가 공백 제거 후 대조,
  5회 오답 시 토큰 잠금.
- **초대 CTA**: 수락/거절/오류 3개 화면에 "이어봄 홈으로" 등 진입 버튼 추가 — 예전엔 헤더도
  없는 막다른 화면이었다.
- **`06-05` Phase A**: `FarewellMessagePage.tsx` 신설(`/farewell-messages`) — 가족 지정 실데이터로
  수신자 카드 목록 + 편지 작성 UI(저장은 Phase B, 아직 없음). 메인 도메인 슬라이드를 2개로
  분리(사장님 확정)하고 기존 `ending-note` 슬라이드 문구를 엔딩노트 전용으로 정리, `EndingNotePage.tsx`의
  자유 텍스트 ③(메시지·비상자산 텍스트에어리어)을 제거하고 보관함으로 크로스링크.
- 검증 중 발견: `07-03` §5.3-2 부고장 소유권 검증(어제 세션)과 마찬가지로, §9.1-1의 초대 링크
  필수 3종(1회용·만료·재발급시 폐기)이 **이미 정상 구현**돼 있어 코드 변경 없이 확인만 함.
- 🔴 **미검증**: 카카오 공유 실제 왕복·성함 대조 흐름·CTA 라우팅 전부 브라우저 실기동 안 함
  (dev 서버는 사용자 쪽에서 기동하는 운용 방식). 상세는 walkthrough(74).

### 2. [Claude:Sonnet] 홈 섹션2 박스③④(추모관·파트너) 높이·헤더 크기를 박스①②와 통일

- 사용자 지시: "추모관 파트너의 마우스오버 하지 않았을 때의 높이가 작음. 1,2 박스처럼 300px로
  수정해줘. 또한 그에 맞게 박스 안의 내용 또한 앞의 박스와 통일되어야 할거야."
- `.entry-box-secondary`(200px) CSS 클래스를 폐지하고 `.entry-box-primary`(300px)로 흡수,
  `EntryBoxes.tsx`의 박스③④가 이 클래스를 쓰도록 교체. `BoxHeader`의 `size="sm"`(아이콘
  48px·제목 1.3rem)을 제거해 기본값 `"lg"`(64px·1.5rem)로, 아이콘 glyph도 24→32로 박스①②와
  맞춤. 기능적 내용(링크 입력·파트너 로그인 행)은 그대로 유지 — 칩 목록은 억지로 만들지 않음.
- 🔴 **미검증**: 로컬 dev 서버가 꺼져 있어 실제 렌더를 확인하지 못함. 상세는 walkthrough(75).

### 3. [Claude:Sonnet] STT Ⓐ 파일 업로드 실구현 — NCP CLOVA Speech

- 사용자 지시(스펙 `06-04` §6.4-9·§6.4-11) — provider = NCP CLOVA Speech, Ⓑ(직접 녹음)는 이미
  완료돼 있어 이번은 파일 업로드 경로를 얹는 작업.
- `SttProvider` 인터페이스 + `ClovaSpeechProvider` 구현, `multer.memoryStorage()`(기존
  diskStorage 재사용 안 함), ffmpeg-static으로 m4a→mp3 stdin/stdout 스트림 변환(임시 파일 없음).
  `EndingNotePage.tsx` 기존 ⑨ 섹션에 업로드 UI 추가(새 화면 없음) — 매번 받는 동의 체크박스
  (지정된 문구 그대로, 불리한 문장 먼저) + 대체수단 안내, 업로드/변환 중 구분 표시(XHR), 결과는
  기존 초안 편집 영역(`draftText`)으로 합류.
- 🔴 **실측 확인 3건**(사용자가 개발자 본인 계정에 실제 Invoke URL·Secret을 이미 `.env`에
  넣어둔 상태라 라이브 API로 직접 검증) — ① Object Storage에 결과가 안 떨어짐을
  `resultToObs:false` 응답 echo로 확인 ② m4a→mp3 변환 경로를 실제 CLOVA 호출로 재현 성공
  ③ ①의 결과로 KMS 권한 문제는 애초에 발생 여지가 없음(moot).
- 🔴 **실측 중 문서에 없던 버그 2건 발견·수정**: (a) `params`를 JSON Blob으로 감싸면 CLOVA가
  무조건 400(`Invalid params: params`) — 일반 문자열 필드로 보내야 함. (b) 콘솔의 "화자 인식
  끔" 설정이 API 기본값에 반영되지 않아, 요청에 `diarization:{enable:false}`를 명시하지
  않으면 매번 400(`speaker detect is off`). 커밋될 실제 코드(`ClovaSpeechProvider`)를
  `ts-node`로 직접 호출해 수정 후 재검증까지 완료.
- 🔴 **플래그(`CLOVA_STT_ENABLED`)는 그대로 false로 둠** — 배선만 완료, 실사용자 공개는
  `00-19` 처리방침 반영과 `User.sttDataAgreedAt` 추가(1-b와 함께) 이후 사람이 판단할 일.
- ⚠️ **확인 필요**: `.harness/memory/backlog.md`에 "STT가 `FarewellMessagePage`로 옮겨간다"는
  메모가 있었는데, 이번 지시는 명시적으로 `EndingNotePage.tsx` ⑨ 섹션에 붙이라고 했고
  스펙 §6.4-9-6·§6.4-2 원문과도 일치해 그대로 구현했다. 이 이관 메모가 유효한 결정이면 다음에
  실제로 옮겨야 한다.
- 상세는 walkthrough(76).

### 4. [Claude:Sonnet] `06-05` Phase B — 유족 메시지 보관함 저장 + STT를 보관함으로 이관

- 사용자가 `[Claude:Opus]` 핸드오프 형식으로 지시 — 위 3번 일지의 "확인 필요" 메모가 이번에
  실제로 정정됐다: 말로 남기기(음성 입력 전체)가 엔딩노트 ⑨에서 **유족 메시지 보관함으로
  이관**됐다(`06-05` §4.2 정정, 08-26). 엔딩노트 ⑨는 타이핑 전용 필사 도구로 축소.
- **DB**: 선행 조건대로 `pg_dump`로 운영 Supabase 백업(`eobom_pre_farewellmessage_20260826_162332.dump`,
  458KB) 먼저 뜨고 나서 `EndingNote`·`FarewellMessage` 모델 신설 + 로컬 마이그레이션 적용.
  `releaseTiming`·`status` 컬럼은 스펙대로 두지 않음(개봉 상태는 `EndingNote` 하나가 소유).
- **API**: `farewellMessageController`(list 미리보기/get 전문/create/update, 전부 본인 것만) +
  라우트 신설. 수신자(`recipientId`)가 본인의 `FamilyDesignation`인지 검증하되, 없는 id와 남의
  id를 같은 메시지로 응답해 정보 노출을 막음.
- **프론트**: `EndingNotePage.tsx`에 인라인으로 있던 Ⓐ(파일 업로드)·Ⓑ(직접 녹음) STT UI를
  `VoiceToTextInput` 컴포넌트로 그대로 추출해 `FarewellMessageCard`(신설, 수신자별 편지 목록+
  편집기)로 이식. 저장 전 확인 단계(06-04 §6.4-5)를 편집기에도 그대로 유지. 엔딩노트 ⑨ 고지에
  "마음을 전하고 싶으시면 유족 메시지 보관함을 이용해 주세요." 한 줄 추가.
- **검증**: 로컬 실행 중이던 백엔드에 실 유저 JWT로 작성→목록→단건조회→수정 전 구간 실측,
  `psql`로 `bodyEnc` 암호문 확인, Claude in Chrome으로 두 화면 실제 렌더까지 확인.
- 🟡 로컬 dev DB에 테스트로 만든 편지 2건이 남아있음 — 배치 정리 때 같이 삭제 예정(사용자 지시).
- 상세는 walkthrough(77).

### 5. [Claude:Sonnet] Render 배포본 CLOVA STT 환경변수 누락 진단·해소

- 사용자가 "유족메시지 보관함 > 업로드는 구현 안됨"이라고 보고 — 로컬에선 플래그가 꺼져 있어
  UI가 정상적으로 숨는 것뿐(버그 아님)이라고 설명했으나, 사용자가 `.env`를 고치고 재시작해도
  여전히 안 됨 → 그 프로세스가 `.env` 저장보다 먼저 뜬 것을 `wmic` 시각 비교로 진단.
- 이어서 사용자가 실은 **배포본(Render)**을 보고 있었다고 정정 — `render.yaml`의 `envVars`에
  `CLOVA_*` 3개가 **애초에 선언된 적이 없다**는 게 근본 원인이었다(로컬 개발 중 배포 반영을
  빠뜨림). 사용자가 Render 대시보드에 직접 등록 → `/api/stt/status` 재확인으로 `true` 전환
  확인 → **사장님이 배포본에서 실제 m4a 업로드→텍스트 변환까지 실기기 확인 완료.**
- `render.yaml`에 `CLOVA_STT_ENABLED`·`CLOVA_SPEECH_INVOKE_URL`·`CLOVA_SPEECH_SECRET`을
  `sync: false`로 문서화(값은 안 들어감) — 다음 재배포 때 또 빠뜨리지 않도록.
- 상세는 walkthrough(78).

## ⏸ 오늘도 닫지 못한 것

- `FamilyInvitePage.tsx`의 `expired`(만료) 화면은 여전히 CTA가 없음 — 이번 지시 범위(accepted·
  declined·에러 3개) 밖이라 손대지 않음.
- STT 실제 한국어 발화 정확도는 여전히 정식 미검증(마이크 없는 개발 환경) — 다만 사장님이 배포본
  업로드 경로는 실기기로 확인 완료.
- 백엔드 `npm run build`가 세션 종료 시점에 EPERM(dev 서버가 잡고 있는 prisma 엔진 dll 잠금)으로
  실패할 수 있음 — dev 서버 재시작하면 사라지는 일시적 문제, 코드 결함 아님.
- 브라우저 실기동 검증 다수 미결 — walkthrough(74·75) "다음 에이전트가 알아야 할 것" 참고.
