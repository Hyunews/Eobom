# 📝 2026-08-31 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [Claude:Sonnet] `04-01` §8 0단계·0-b단계 — 비밀번호 placeholder 확인 + 계정 찾기 정적 화면

- 사용자 직접 지시(`04-01` §0·§8·§8.1 근거). 0단계는 `EndingNotePage.tsx:185`의 비밀번호
  원문 입력 유도 placeholder를 소재 안내형으로 교체하라는 지시였으나, 확인 결과 **이미
  세션 이전 커밋(`eb1db97`, "엔딩노트 아코디언 전환 + Phase 1 저장")에서 해당 블록이 통째로
  삭제돼 있었다** — `git log -p`로 삭제 시점 확인 후 코드 변경 없이 그대로 보고.
- 0-b단계는 `DigitalEstatePage.tsx`에 "계정 찾기" 정적 섹션 신설. STEP 0 고지 3줄(KISO
  정책규정 §28①·§28② 인용, 항상 노출) + STEP 1 카드 정확히 2개(안심상속 원스톱·상속인
  금융거래조회, 1-C는 미노출) — 사망일 입력칸·D+N 표기 없이 §0.6 구간 라벨만("사망하신 달의
  말일부터 1년 안에" 등). 외부 URL 2개는 `WebSearch`로 공식 링크 사전 검증 후 삽입.
  기존 레거시 카탈로그("정산" 문구 그대로)는 손대지 않고 새 섹션 위에 얹었다 — 전면 개편은
  `04-01` §8의 Step 5로 이미 분리돼 있어 이번 스코프 밖.
- `npx tsc --noEmit` 통과. claude-in-chrome으로 `/digital-estate` 실제 접속 — STEP 0·1
  스크린샷 확인, 외부 링크 `href` 2개를 원본 URL과 직접 문자열 대조, 콘솔 에러 0건.
  상세는 walkthrough(89), 시행착오는 claude_tasks(27).

### 2. [Claude:Sonnet] `04-01` §8 0-b 보정 — STEP 0 4번째 줄 + 1-C 삭제 주석 정리

- `04-03` §2.2-1 확정(정보주체 권리행사 유족 대행 ❌ 불가)에 따라 `DigitalEstatePage.tsx`
  주석 교체 + STEP 0 안내박스에 4번째 줄("개인정보 포털은 본인만 이용 가능") 추가.
  `DISCOVERY_PATHS`는 그대로 2개 유지. `tsc --noEmit` 통과, 브라우저로 4줄 렌더 확인.
  상세는 walkthrough(90).

### 3. [Claude:Sonnet] `04-01` §8 1단계 A — `DigitalCleanupItem` 스키마 확장

- `schema.prisma`의 `DigitalCleanupItem`에 `deceasedId`(FK → `Deceased.id`)·`origin`
  컬럼 추가, 인덱스를 `[userId, status]` → `[userId, deceasedId, status]`로 교체.
  `Deceased`에 역참조 `cleanupItems` 추가. B단계(`sourceSettingId` 등)는
  `PreDeathPlatformSetting` 미신설로 스코프 밖.
- 🔴 **DB 쓰기** — 로컬 Docker DB(`eobom-postgres`, 5433)를 `docker exec pg_dump`로 직접
  백업(`local-20260831-152954.dump`, 178KB) 후 사용자 CONFIRM 받고
  `prisma migrate dev --name digital_cleanup_item_deceased` 실행. `generate-db-doc.js`
  재실행으로 `00-05` 갱신. `tsc --noEmit`(backend) 통과. 시행착오는 claude_tasks(29).

### 4. [Claude:Sonnet] `CareGuidePage` 카테고리 박스 가로 배치 + 펼치기 화살표 전용화

- 사용자 직접 지시(문서 스펙 없음). `07-02` 상중 행정 체크리스트 카테고리 박스(신고·조회 ·
  상속 승인·포기 등)가 가로 폭 여유가 있어도 세로로만 쌓이던 문제 — 원인은 CSS 다단
  (`columns`)의 "위→아래 우선" 채움 순서. `index.css`를 flex-wrap으로 교체해 해결.
- 펼치기(`ChevronDown`)를 카드 전체 클릭이 아닌 화살표 버튼 전용으로 변경 —
  `toggleExpand` 시그니처를 `(id, e)`에서 `(id)`로 단순화하고, 카드 자식 요소들에 남아있던
  `stopPropagation()`(카드 클릭 확산 방지용, 이제 불필요) 전부 제거.
- `npx tsc --noEmit`(frontend) 통과. claude-in-chrome으로 `/care-guide` 실접속 — "3개월
  안에" 구간 3개 카테고리 가로 배치 확인, 카드 본문 클릭 무반응·화살표 클릭 정상 펼침·
  체크박스 클릭 시 펼침 상태 불변 확인. 상세는 walkthrough(93), 시행착오는 claude_tasks(30).

### 5. [Claude:Sonnet] `EndingNotePage` 유언장 초안 인쇄 — 최하단 성명·날인 칸 추가

- 사용자 직접 지시(문서 스펙 없음). `draftText` 기본값(개발자 직접 수정분, 26-08-31)은
  범위 밖으로 그대로 두고, `handlePrintDraft`의 인쇄 출력 HTML 최하단에 "성명
  ______________ (인)" 구조의 날인 칸만 추가 — 화면의 자필증서 4대 요건 안내(주소·연월일·
  성명·날인)와 같은 맥락.
- `npx tsc --noEmit`(frontend) 통과. `window.print()`는 OS 대화상자를 띄워 자동화가
  멈출 위험이 있어 실클릭 대신 동일 HTML을 `document.write`로 별도 탭에 렌더링해 스크린샷
  확인 — 구분선 아래 성명 밑줄 + `(인)` 박스 정상 표시. 상세는 walkthrough(94).

## ⏸ 오늘도 닫지 못한 것

- `DigitalEstatePage.tsx`의 레거시 "정산" 카탈로그 블록 — Step 5 전면 개편 대기 중.
- `04-01` §8 1단계 B(`sourceSettingId`·`inheritedIntent`·`inheritedNote`) — 생전 축
  모델(`PreDeathPlatformSetting`) 신설 이후 착수 가능.
