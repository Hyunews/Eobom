# 📝 2026-08-14 작업일지

> **프로젝트:** 이어봄 (Eobom) 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼 (`eobom`)
> **담당:** Claude:Opus (기획) / Claude:Sonnet (구현)

---

## 🛠️ 오늘 진행한 주요 실무 작업

### 1. [개발자] 회원 유형 진입 분기 제안

- 사장님 부재로 **결정이 필요 없는 작업**을 우선 진행하기로. 제안: *"메인 페이지 혹은 로그인 모달에서 어떤 회원인지 나누는 게 필요하지 않나. 세부 구현이 안 됐더라도 일단 나눠놓고 생각하는 게…"*

### 2. [Claude:Opus] 현황 실측 → 진단 정정

- 역할 4종(`User` / `Partner` / `Expert` / `Admin`)은 **이미 DB·인증체계·포털까지 완전 분리**돼 있음을 확인. 즉 "나누는" 작업이 아니라 **"닿게 하는"** 작업.
- 진입로 실측 결과 결함 2건:
  - **유일한 진입로가 13px** — `Footer.tsx:136`의 `/partner` 링크가 카피라이트 줄에 `·`로 이어붙은 `0.82rem`/`#6B7280`. `00-09` §2.3 본문 `18px` 기준의 72% 미달. 홈에선 스냅 7섹션 중 마지막까지 내려야 나옴.
  - **잘못된 문이 포털 안까지 역류** — `App.tsx:178-187`에서 `Header`·`Sidebar`가 `<Routes>` 바깥에 있어 `/partner`·`/admin`에서도 B2C 크롬이 렌더됨. 사업자가 파트너 포털에서 소비자 `로그인`을 누르면 소셜 로그인 모달이 열리는 순환.
- `LoginModal.tsx:11` 주석의 경로 표기(`/#partner`)가 `00-10` HistoryRouter 전환 이후 미갱신 상태인 것도 발견.

### 3. [Claude:Opus] `00-06` §7 신설 — 회원 유형 진입 동선

- **핵심 원칙 확정: 역할은 나누고, 모드는 나누지 않는다.**
  - 역할(누구인가) → 인증 체계가 물리적으로 달라 **진입 시점에 분기**.
  - 모드(생전 준비/유족) → `00-12` §6.2가 A안(계정 유형 분리 가입)을 이미 기각(*한 사람이 둘 다 필요*). 권고는 C안(`Deceased` 자동 판정)이고 §6 자체가 🟡 미확정 + `Deceased` 미구현 → **지금 만들면 반드시 뒤집힘**. 이번 범위에서 제외.
- 진입 지점 3곳 스펙: ① `LoginModal` 하단 분기(신설, 데모 버튼 **위**) ② `Footer` 카피라이트 줄에서 분리·`0.88rem` 승격 ③ `Header`는 **하지 않음**(B2C 다수가 매 화면에서 무관한 항목을 보게 됨) ④ `/admin` 링크 없음 유지.
- `/partner`·`/admin`의 B2C 크롬(`Header`·`Sidebar`·`FloatingEmergency`) 제거 + 로고·홈 링크만 있는 최소 상단 바로 대체.
- `00-06` §2에 "접근 경로는 §7에서 개편 확정, 표는 구현 전 현황" 단서 추가.
- 신규 SCR ID 발생 없음 — 기존 SCR-001·SCR-008 사이의 **연결**만 다룸.

### 4. [Claude:Sonnet] `00-06` §7 구현 — 진입 지점 ①② + 포털 크롬 분리 + 주석 정정

- ① `LoginModal.tsx`: 소셜 로그인 3종과 데모 테스트 블록 사이에 파트너 진입 버튼 신설. 클릭 시 모달 닫고 `/partner`로 이동.
- ② `Footer.tsx`: 파트너 링크를 카피라이트 줄에서 분리, `0.82rem` → `0.88rem`으로 확대.
- `App.tsx`: `isPortalRoute` 판정 추가 — `/partner`·`/admin`에서 `Header`·`Sidebar`·`FloatingEmergency`·`Footer`를 렌더하지 않고 로고+홈 링크만 있는 최소 상단 바로 대체. 사이드바 전용 CSS(`.main-wrapper`의 고정 `margin-left: 72px`)를 포털 경로에 적용하지 않도록 분기.
- `LoginModal.tsx:11` 주석의 `/#partner`·`/#admin` 표기를 실제 경로(`/partner`·`/admin`)로 정정.
- `tsc --noEmit`·`vite build` 통과. Chrome 브라우저로 dev 서버 직접 구동해 홈→로그인모달→파트너 진입→최소 크롬→홈 복귀, `/admin` 직접 접속, `/counseling` Footer 링크까지 전부 눈으로 확인.
- `AdminPage.tsx:8`에도 같은 `#admin` 주석 잔재 발견했으나 이번 스펙 범위(§7.6은 LoginModal만 지목) 밖이라 남겨둠 — 상세: `에이전트_기록/walkthrough.md` 2026-08-14 (18) 항목.

### 5. [Claude:Opus] 다음 작업 선정 — `00-14` §2 즉시처리 4건이 코드 0건임을 발견

- 08-13에 **4건 전부 방향 확정 + 후속 기획서 3건 작성 완료**인데 **실제 화면은 하나도 안 고쳐져 있었음.** 전부 지금 사용자에게 노출 중인 법적 리스크라 우선순위 최상으로 판단.
  - **2-2** `EndingNotePage.tsx:169·173` "유언 편지" / `HomePage.tsx:508` "소중한 유언을 보관하세요" 그대로, 면책 문구 없음. 정작 상속세 계산기엔 면책이 붙어 있음(`TaxSimulatorModal.tsx:198`).
  - **2-3** `HomePage.tsx:354` "공증 법무사 매칭" 광고 중인데 `Expert.category`에 법무사 값 없음.
  - **2-4** `FloatingEmergency.tsx:36` "24시간 긴급상담", `Footer.tsx:117` "24시간 긴급 장례 지도사 즉시 파견" 그대로.
  - **2-1** `CareGuidePage`에 상속포기·한정승인 0건.
- **4건이 균질하지 않다는 점을 확인하고 착수 범위를 갈랐음** — Sonnet이 뭉뚱그려 시작하면 커밋이 뒤엉키고 사장님 결정 영역을 침범할 위험이 있었음.
  - 2-2·2-3은 프론트 문구·상수 수준이라 **즉시 가능**(`02-05` §3.4가 "마이그레이션 필요 없습니다" 명시 — `category`가 문자열).
  - **2-4는 부분 차단** — `07-02` §7 미결 #4가 "상담 채널 개설 주체·응답 시간 → 🔴 사장님 확정". 다만 현재 문구는 **받을 사람이 없는 거짓 약속**이므로 제거는 사실 정정이라 지금 하고, §5.3이 권고한 "②상담 문의" 갈래는 **보낼 채널이 없어** 만들지 않기로 결정.
  - **2-1은 별건으로 분리** — `07-02` §6이 쓰라는 `AdminTask` 모델이 스키마에 없음(`Admin`만 존재). 모델 신설 + 마이그레이션 + 22항목 시드 + `CareGuidePage` 재작성 규모.
  - `02-05` §2.2 상황기반 진입(🟡)도 `CounselingPage` 개편이라 별건.

### 6. [Claude:Opus] 하네스 예산 정리 — 호칭·결정권 규칙을 `roles.md` §4로 이관

- ⑥ 항목 추가로 부팅 합계가 11KB 예산을 넘김. `AGENTS.md` §9("숫자를 올리기 전에 이 내용이 이 파일에 있을 게 맞나부터 본다")에 따라 예산을 올리지 않고 내용을 옮김.
- **호칭·결정권 규칙(개발자 vs 사장님)은 "지금 상태"가 아니라 항구적 규칙**이라 `context.md`가 아닌 `roles.md` §4(에스컬레이션) 앞머리가 정본 자리로 판단 — 이관 후 `context.md`엔 포인터 1줄만 남김.
- 이관 직후 `roles.md`가 8KB 예산을 넘겨(8270B) 표를 산문으로 압축. 부수로 `context.md`의 스테일 항목 정정("07 상중행정 문서0건" → 문서 2건 존재하므로 "문서2·구현0").
- `harness-doctor` 113개 항목 통과.

### 7. [Claude:Sonnet] `00-14` §2 즉시처리 3건(2-2·2-3·2-4) 구현 — 거짓 약속·법적 오해 소지 문구 제거

- **2-2**: `EndingNotePage.tsx`·`HomePage.tsx`의 "유언 편지/유언 메시지"→"유족에게 남기는 메시지", "사전 연명의료 의향서"→"연명의료 의향 메모"로 통일. 연명의료 카드·재산 카드 양쪽에 법적 효력 면책 배너 추가, 재산 카드엔 `/counseling`(Domain 02)로 보내는 "유언장 안내 보기 →" 링크 신설.
- **2-3**: `JUDICIAL_SCRIVENER`(상속 법무사)를 `schema.prisma`·`CounselingPage.tsx`·`PartnerPortalPage.tsx`·`HomePage.tsx`에 추가. **스펙에 없던 발견**: 백엔드 `expertController.ts`의 카테고리 화이트리스트도 안 고치면 가입 API가 400을 냈을 것이라 같이 수정. "디지털 유품 행정사"→"행정사"도 §3.4 지시대로 정정.
- **2-4**: `FloatingEmergency`가 알림창 대신 `/facility`로 이동하도록 교체("장례식장 바로 찾기"). `Footer`·`CareGuidePage`에서 "24h 긴급콜/즉시 파견" 표현 제거. **스펙에 없던 발견**: `HomePage.tsx` 히어로·07섹션에 "15분 내 운구차 즉시 출동" 등 더 구체적인 거짓 약속이 남아있어 같이 정정.
- `tsc --noEmit`(frontend·backend)·`vite build` 통과. Chrome dev 서버로 홈 전 섹션·엔딩노트(데모 로그인)·상담 카테고리 탭·파트너 가입폼까지 직접 확인.
- **편차**: `HomePage.tsx`의 도메인 배지 번호가 08-12 재편 이후 갱신 안 된 걸 발견했으나(예: "03. 디지털 유품 정리" 배지가 실제로는 04~05 내용) 이번 작업 범위보다 훨씬 큰 전수 정합성 문제라 손대지 않고 다음 항목으로 남김. 상세: `에이전트_기록/walkthrough.md` 2026-08-14 (19) 항목.

### 8. [Gemini] 검증 게이트 판정 — `walkthrough.md` (18)·(19)번 항목

- **(18)번 항목 판정**: ✅통과 (`00-06` §7.3~7.6 스펙 대조 완료 — LoginModal 하단 파트너 로그인 분기, Footer 파트너 링크 행 분리·승격, 포털 경로(/partner, /admin) B2C 크롬 분리 및 최소 상단 바 대체, 라우트 주석 정정 정상 구현 및 tsc/빌드 검증 완료)
- **(19)번 항목 판정**: ✅통과 (`06-02` §6.1/§3.1/§4.1, `02-05` §3.3~3.4, `07-02` §5.3 스펙 대조 완료 — 엔딩노트 법적 효력 면책 및 용어 정정, 상속 법무사 5직역 추가 및 백엔드 화이트리스트 동반 반영, 24h 긴급콜 문구 정정 및 시설검색 연계 정상 확인, tsc/빌드 검증 완료)

### 9. [Claude:Opus] `00-14` §2-5 신규 등재 — 목업 데이터가 실제 정보처럼 노출 중

- ⑦번(`HomePage` 배지 번호)을 확인하러 갔다가 **`00-14` §2에 잡히지 않은 5번째 건**을 발견. `mockData/digitalEstate.json`:
  - **가짜 제휴업체 10곳** — 실명처럼 보이는 업체명 + **★4.7~5.0 평점** + *"제휴 전문 업체 리스트입니다"* 단정 + `무료 방문 견적 신청` 버튼(alert만). 03(현물수거)은 `03-01` 보류 메모 1건뿐이라 **사업 자체가 없음**. 업체 태그에 §2-4에서 방금 제거한 *"24시 긴급 출동"* 이 그대로 남아 있었음.
  - **가짜 조문 방명록 10건** — 창작 조문 메시지 + 실제처럼 보이는 작성자·날짜(2026-07-28~29). 05(추모관)는 SCR-011 미구현 + `05-01` §2.6 오픈 금지.
  - **Unsplash 추모 사진 10장** — 타인의 사진을 고인 추모 앨범으로 전시 + 프로덕션이 외부 이미지 호스트에 의존.
- **2-3(없는 직역 광고)과 같은 종류인데 별점·업체명까지 창작했다는 점에서 한 단계 더 나감.** §2 초안이 4건만 잡은 것은 `HomePage`·`EndingNotePage` 계열만 훑고 목업 JSON은 안 봤기 때문.
- **확정: 지우지 않고 "예시 데이터"임을 화면에 명시.** 지우면 `00-14` 회의에서 보여줄 화면이 없어지고, 두면 거짓말. 명시하면 둘 다 해결되며 ③(04·05 프론트 보류)과도 충돌 없음.
- **⑦번(배지 번호)은 고치지 않기로 결론.** `00-12` §7이 *"번호는 사용자 여정 순서로 하고 메뉴 순서와의 1:1 대응은 포기한다"* 고 이미 확정했으므로, 배지에 신 도메인 번호를 다시 매기면 방금 폐기한 규칙을 되살리는 셈. 배지 `01~05`는 "5대 핵심 서비스 중 n번째" 서수로 읽히면 됨.
- **대신 진짜 구조 문제를 §2.3으로 등재** — `DigitalEstatePage` 한 페이지의 서브탭 3개가 재편 후 서로 다른 도메인(정산 04 / 현물수거 03 / 추모관 05). 문서는 08-12에 갈렸는데 화면은 아직 구 03 통합 구조. 분할은 ③이 풀린 뒤 별도 사이클.
- `00-14` §1 요약표 §2 건수 4→5 갱신. `harness-doctor` 113개 항목 통과.

### 10. [Claude:Sonnet] `00-14` §2-5 구현 — `/digital-estate` 예시 데이터 명시

- 세 서브탭의 문제 성격이 달라 처방을 갈랐음. 현물수거(가짜 신원+평점)는 카드별 "예시" 배지, 추모관(가짜 조문·사진)은 방명록·갤러리 각각에 안내 문구, 계정정산은 애초에 가짜 신원 문제가 아니라 **"완료 단정"** 문제(로컬 state만 바뀌는데 "성공적으로 업로드/접수되었습니다"라고 말함)라 알림 문구를 준비중 표현으로 정정.
- 페이지 공통 배너 신설, "Naver OCR 즉시 검증" 문구 완화, 현물수거 "제휴 전문 업체" 단정 제거 + 예시 배지 10개 + CTA "(예시)" 표기, 추모관 방명록·사진 섹션에 예시 안내문 추가.
- Unsplash 사진은 지우지 않고 "제공처 명시"로 처리 — 스톡 사진이라 특정 인물 사칭이 아니고, 지우면 갤러리 UI 시연이 안 됨.
- `tsc --noEmit`·`vite build` 통과. Chrome dev 서버로 세 서브탭 전부 확인.
- **편차**: 헌화 카운터·계정정산 목록(네이버/카카오 등) 자체는 그대로 둠 — 사용자 상호작용이거나 가짜 신원 문제가 아니라서. 상세: `에이전트_기록/walkthrough.md` 2026-08-14 (20) 항목.

### 11. [Gemini] 검증 게이트 판정 — `walkthrough.md` (20)번 항목

- **(20)번 항목 판정**: ✅통과 (`00-14` §2.2 (2-5) 스펙 대조 완료 — `/digital-estate` 공통 예시 데이터 배너 신설, 계정정산 OCR 과장 문구 및 처리 완료 단정 알림 정정, 현물수거 가짜 제휴업체 예시 라벨링·버튼·알림 정정, 추모관 방명록·사진 예시 안내문 명시 정상 구현 및 tsc 통과 확인)

### 12. [개발자→Claude:Opus] 검증 게이트 개정 — Gemini가 한 번도 문제를 못 잡던 원인 제거

- 개발자 지적: *"Gemini가 검증·판정을 해주는데 한번도 의심이나 문제를 집어낸 적이 없다."* 실측 결과 **판정 누적 중 ❌반려 0건 · 🔄스펙갱신 0건** — 3가지 판정 중 2개가 미사용.
- **원인은 태도가 아니라 구조.** 구 `GEMINI.md`가 *"walkthrough 요약과 스펙 대조까지만, 코드는 읽지 않는다"* 로 못박아, Gemini는 **Claude가 쓴 스펙**과 **Claude가 쓴 자기보고서**를 맞춰보고 있었음. 보고서는 스펙을 보고 쓰이므로 구조상 어긋날 수 없어 **실패시킬 정보가 없었음.**
- **조치**: `GEMINI.md` 검증 범위 전면 개정(리뷰 금지 유지 / **검증 의무 신설**, 4단계 절차·반려 체크리스트 8항목·스펙갱신 신호 4항목), `roles.md` §2, `record.md` §1-1(`결과`를 **반증 가능하게**), `done.md` §4-1, `harness-doctor.sh` **§8 판정 분포 자동 감시** 신설.
- 경위·한계는 `.harness/_meta/검증게이트_개정_260814.md`.

### 13. [Gemini] 개정 1차 검증 — (19)·(20) 재판정

- 개발자 제안으로 **이미 통과된 항목을 새 절차로 재판정**시켜 즉시 시험. (19)는 정답을 아는 문제였음(`편차` 3건이 "스펙에 없던 발견"이라 §4 신호 2개 해당 → 정답은 🔄스펙갱신).
- **결과: 3개 채점 기준 전부 통과.** 검색 19건 + `tsc`·`vite build` 3회 + `git status` 실제 실행, (19) 판정이 ✅→**🔄스펙갱신**으로 전환, (20)은 `file:line` 실측 근거 제시. 제거 문자열 7종을 Opus가 독립 재검색해 전부 0건 확인.
- **같은 모델이 규칙만 바꿔서 판정을 뒤집었다** — 앞선 전원 통과가 Gemini의 성향이 아니라 규칙의 산물이었음을 직접 입증.
- **시험이 되레 스펙 결함 2건을 드러냄**: ① §2-4(미선언 변경 대조)는 3개 사이클 미커밋 혼재로 **실행 불가능**한데 Gemini가 "대조 불가" 대신 숫자를 맞춰 답함 → `GEMINI.md` §2-4에 단서 추가. ② §8 카운터가 **`🔄`(4바이트 이모지)를 grep이 못 잡아** 스펙갱신을 영원히 0으로 세고 있었음 → 한글 단어 기반으로 계수 방식 교체 후, 기준선을 낮춰 🔴이 실제로 뜨는지까지 확인.

### 14. [Claude:Opus] 🔄스펙갱신 반영 — 기획서 3곳 보강

- **`02-05` §3.4**: 반영 대상에 `expertController.ts` `EXPERT_CATEGORIES` 추가. *"왜 빠졌나"* 도 기록 — `category`가 `enum`이 아니라 `String`이라 스키마 주석만 고치면 될 것처럼 보였으나, **DB가 느슨한 만큼 코드가 조이고 있었음**. 같은 구조인 `Facility.type`·`DigitalPlatform.category`·`Partner.status` 주의 명시.
- **`06-02` §6.1**: **교체 대상을 "줄"이 아니라 "어휘"로 재정의.** 표에 적힌 3곳만 고치면 한 화면에 신·구 표현이 공존하는 자기모순이 생김. 실제로 있었던 위치(배지·부제·미로그인 안내문·성공 토스트 등)를 전수 명시. 면책이 **2곳**(재산·연명의료, 근거 법이 달라 합치면 안 됨)이라는 점도 추가.
- **`07-02` §5.3**: 대상 파일에 `Footer.tsx`·`HomePage` 히어로·`HomePage` 07섹션 3곳 추가. *"대상을 파일이 아니라 표현으로 지목하라"* 는 지침과 검색어 목록(`24시간`·`즉시 파견`·`디스패치`·`15분 내` 등) 명시 — **마케팅 문구는 기능 화면보다 홈·푸터에 더 과장되게 적혀 있다.**
- `walkthrough.md` (19) 판정을 재판정 결과로 교체(구 판정은 취소선 보존). `harness-doctor` 113개 항목 통과.
