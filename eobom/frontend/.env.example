# Kakao Maps JS API Key
VITE_KAKAO_MAP_KEY=your_kakao_javascript_api_key_here

# 백엔드 API 주소 override (선택). 비워두면 로컬은 자동으로
# "접속한 호스트 + 5000번 포트"를 씀 — LAN IP로 접속해도 동작함.
# 프로덕션처럼 프론트/백엔드가 다른 도메인일 때만 명시적으로 채운다.
# VITE_BACKEND_URL=https://api.eobom.example.com
