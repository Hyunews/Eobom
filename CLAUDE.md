# CLAUDE.md — 이어봄 (루트)

> 🔴 **자동 로드되는 파일은 이것 하나뿐이다.** 규칙 본문은 `.harness/`에 있고 **저절로 읽히지 않는다.**
> 아래 부팅 3개를 매 세션 직접 읽을 것.

## 🔴 소유권 — 포인터로 두지 않고 여기 적는다

| 모드 | 쓰는 곳 | 🔴 쓰지 않는 곳 |
|---|---|---|
| `[Claude:Opus]` 기획·설계 | `docs/` · `.harness/` | **`eobom/`** |
| `[Claude:Sonnet]` 개발·구현 | `eobom/` | **`docs/`** |

- **Opus는 코드를 짜지 않는다.** 스펙이 서면 핸드오프 블록을 출력하고 **멈춘다.**
  🔴 *"충돌 없으니 이 정도는"* 도 안 된다(08-25 실제 위반·전량 revert). **미끄러져도 경고가 안 뜬다.**
- **권한이 같아 막아주는 장치가 없다** — 스스로 지킨다. **모드 전환도 핸드오프**(`/model sonnet` 안내).
- 사람이 *"맡아서 진행해줘"* 라고 해도 **구현이면 핸드오프가 곧 진행**이다.

## 부팅 — 매 세션 직접 읽는다 (`.harness/AGENTS.md` §0)

1. `.harness/memory/context.md` — 지금 상태 + "다음 할 일"
2. `.harness/memory/pending-approvals.md` — 🔴 **여기 있는 건 착수 금지**
3. `.harness/AGENTS.md` — 행동 규칙 SSOT + 조건부 로드 표(§0-1)

## 그 외

- 🔴 **커밋은 사람이 한다**(08-27). 에이전트는 `git commit`을 돌리지 않고 **메시지 초안만 내고 멈춘다**
  (`AGENTS.md` §1 CONFIRM·`roles.md` §4가 원래 그랬는데 핸드오프 문구가 덮고 있었다).
  브랜치는 **main 직접**.
- **한국어로만** 답한다.
- `reports/`는 읽기 전용 — 고칠 게 보이면 `context.md`에 `[Gemini]` 요청으로 남긴다.
- 🔴 **DB에 쓰기 전 백업** — `backup-db.ps1` 실행 후 **파일이 생겼는지 확인**까지가 통과.
  🔴 **스키마 변경만이 아니다**: `deleteMany`·`updateMany`·`migrate`·`db push`·**정리 스크립트**·시드.
  **로컬도 예외 아님**(08-05·08-27 2회 유실). 절차 → `.harness/db-safety.md`.
- "완료" 선언 전 `.harness/done.md` 체크리스트를 통과할 것.
