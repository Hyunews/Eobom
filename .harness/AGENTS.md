# AGENTS.md — 이어봄 하네스 행동 규칙 SSOT

> 이 파일이 유일한 행동 규칙 본문이다. `.harness/CLAUDE.md`(**Opus 기획 + Sonnet 구현**, 두 절을 한 파일에) / `.harness/GEMINI.md`(**문서화·교차검증**)는 각자의 역할만 덧붙이는 얇은 파일이며, 규칙이 충돌하면 **이 문서가 이긴다**.
> 🔴 **`CLAUDE.md`를 Opus용·Sonnet용으로 쪼개지 않는다**(08-27 기각) — 이름을 바꾸면 **자동 로드를 잃고**, 어느 파일을 열지는 에이전트가 정하므로 *모드 오인* 사고에 무력하다. 근거 → `_meta/CLAUDE_md_분리_검토.md`
>
> 대상: **1인 + 3주체** — `[Claude:Opus]` 기획·설계 / `[Claude:Sonnet]` 개발·구현 / `[Gemini]` Flash, 기계적 문서화·교차검증. 사람이 `/model`과 창을 직접 전환하며 쓴다. 에이전트가 다른 에이전트를 자동 호출하지 않는다. (태그·전환 방법 → `roles.md` §0)

---

## 0. 부팅 — 매 세션 반드시 읽는 것 (0번은 자동, 1~3은 직접 연다)

| 순서 | 파일 | 무엇을 얻나 |
|---|---|---|
| 0 | **루트 `CLAUDE.md`** | 🔴 **유일하게 자동 로드되는 파일.** 1~3은 CLI가 밀어 넣지 않으므로 읽으라고 시키는 것과 **소유권 규칙 본문**이 거기 있다(2026-08-25 신설 — 그전엔 규칙이 세션에 도달한 적이 없다). |
| 1 | `.harness/memory/context.md` | **"다음 할 일" 한 줄** + 지금 상태. 맨 위 3줄만 봐도 바로 작업 시작 가능해야 한다. |
| 2 | `.harness/memory/pending-approvals.md` | **사람 승인 대기 목록. 여기 있는 건 착수 금지.** `context.md`를 통째로 새로 써도 이 파일은 별도라 안 지워진다 — 그게 존재 이유. |
| 3 | `.harness/AGENTS.md` (이 파일) | 행동 규칙 + 아래 조건부 로드 표 |

**부팅 파일 합계 16KB를 넘기지 않는다**(2026-08-28 상향 — 근거는 `harness-doctor.sh` §1 주석). 넘으면 doctor가 실패시킨다 — 부팅이 무거워지면 매 세션 비용이 되기 때문.
🔴 **예산을 올리기 전에 §9 순서를 지킨다** — 내용을 먼저 쳐내고, 그래도 안 되면 *무엇을 어디로 옮겼는지*를 doctor 주석에 남긴 뒤에 올린다.
`pending-approvals.md`는 사람만 항목을 지운다(승인/반려 의사를 밝혔을 때). 에이전트가 스스로 판단해서 지우지 않는다.

## 0-1. 조건부 로드 — 해당 작업일 때만 읽는다

| 작업 유형 | 읽을 파일 |
|---|---|
| 🔴 **내 소유 영역 밖 파일을 쓰기 직전**(Opus가 `eobom/`, Sonnet이 `docs/`) · 에이전트 전환 | `.harness/roles.md` |
| 개인정보·시크릿·외부 발행(배포/공개) 건드릴 때 | `.harness/security.md` |
| 🔴 **DB에 쓰는 명령을 돌리기 직전** — `deleteMany`·`updateMany`·`migrate`·`db push`·정리 스크립트·시드 | `.harness/db-safety.md` |
| 작업을 "끝났다"고 선언하기 직전 | `.harness/done.md` |
| `walkthrough.md`·일지에 기록을 남길 때 | `.harness/record.md` |
| 외부 연동(OAuth·지도·공공데이터·DB·배포) 건드릴 때 | `.harness/systems.md` |
| 반복 작업(2회차 이상)일 때 | `.harness/skills/README.md` |
| `context.md`의 할 일을 집었는데 **배경·근거·미검증 항목**이 필요할 때 | `.harness/memory/backlog.md` |
| 메모리를 남기거나 과거 결정 이유가 궁금할 때 | `.harness/memory/MEMORY.md` → 해당 메모리 |
| 문서↔코드 링크를 걸 때 | `.harness/memory/g-brain-map.md` |

> 위 표에 없으면 읽지 않는다. "혹시 몰라서 다 읽기"가 컨텍스트를 태우는 주범이다.

---

## 1. 작업 순서

```
PLAN → CONFIRM → CODE/WRITE → SAVE → UPDATE
```

- **CONFIRM**: 되돌리기 어려운 작업(파일 삭제·대량 이동·커밋·배포·외부 발행)은 실행 전 사용자 확인. 그 외 일반 작업은 확인 없이 진행한다.
- 🔴 **DB 쓰기는 CONFIRM 대상이다**(2026-08-28 신설, 유실 2회). **기존 행을 지우거나 덮어쓰는 것**
  — `deleteMany`·`updateMany`·`migrate`·`db push`·정리 스크립트·시드 재적재 — 은 **실행 전에
  사람에게 묻는다.** *"백업했으니 괜찮다"* 도 묻고 한다. 절차는 `db-safety.md`.
  ⚠️ **조회(SELECT)와 단건 생성은 묻지 않는다** — 다 물으면 아무도 안 읽는 확인창이 된다.
  🔴 **이 조항은 코드 가드를 대신하지 않는다.** 규칙은 이미 두 번 실패했다(→ `db-safety.md` §7).
- **UPDATE**: `memory/context.md`의 "다음 할 일" 갱신. 반복 작업이었으면 `skills/`에 절차 남기기.
- **핸드오프**: 상대 에이전트 차례로 넘어가면 응답 맨 끝에 **복사용 프롬프트 블록**을 출력한다(→ `roles.md` §6). 사용자가 무슨 말을 해야 할지 고민하게 두지 않는다.

## 2. 소유권 — 한 파일은 한 에이전트만 쓴다

**중복 검증을 없애는 핵심 규칙.** 상대가 확정한 것은 다시 검토하지 않고 신뢰한다.

| 영역 | 🔴 쓰기 (이 주체만) | 읽기 |
|---|---|---|
| `docs/` (기획 SSOT) | **`[Claude:Opus]`** | Sonnet · Gemini |
| `eobom/` (소스코드) | **`[Claude:Sonnet]`** | Opus · Gemini |
| `reports/` (시각화 HTML) | Gemini | Claude |
| ↳ 🔴 **git 커밋 제외**(2026-08-20, 로컬 전용) — 근거·주의는 `roles.md` §1-1 | | |
| `.harness/` | Claude(Opus·Sonnet 공용) | Gemini |
| `assets/` | 사람 | 둘 다 |
| `docs/작업일지_및_기록/` | **파일별로 나뉨** → `roles.md` §1-2 | |

🔴 **`docs/`와 `eobom/`은 같은 `Claude`가 아니다** — 본문은 루트 `CLAUDE.md`(자동 로드) · `roles.md` §1-1.

검증 게이트는 **`walkthrough.md` 단 한 곳**이며 결과는 통과 / 반려 / 스펙갱신 3가지다.
구현이 기획과 틀어지는 **편차 처리 절차는 `roles.md` §2-1** — 조용히 다르게 구현하거나 `docs/`를 직접 고치지 않는다.

## 6. 검색·정보 품질

- 라이브러리 버전/API 스펙처럼 빨리 바뀌는 정보는 "확인 필요" 또는 컷오프 기준을 명시하고 단정하지 않는다.
- 공식 문서 우선, 커뮤니티 글은 교차검증 후 보조로만.

## 7. 산출물 저장 위치 (Dual Document Policy)

**같은 내용을 두 형식으로 둔다** — `docs/`(**AI가 읽고 고치는 정본**, Markdown, 도메인별 한글
디렉토리) ↔ `reports/`(**사람이 브라우저로 보는 것**, HTML/PDF, 커밋 제외·로컬 전용).
나머지는 §2 소유권 표와 같다 — `assets/`(원천 데이터·로고) · `eobom/`(소스) · `.harness/`(규칙·메모리·툴).

- **같은 문서를 두 곳에 복사하지 않는다.** 한 곳이 정본이고 나머지는 링크로 참조한다.
- 코드·기획·하네스가 **한 레포에 공존**한다(옛 외장 두뇌 전제는 2026-08-07 폐기 → `_meta/`).

## 9. 자기 점검

- 구조가 깨졌는지는 사람이 기억해서 확인하지 않는다. `tools/harness-doctor.sh` 하나가 전부 검사한다.
- **검사 대상 0건 = 성공이 아니라 실패.** 조용한 통과가 제일 위험하다(2026-08-07 사고 경위 → `_meta/`).
- **예산 초과 시 숫자를 올리기 전에 "이 내용이 이 파일에 있을 게 맞나"부터 본다.** 중복·위임 가능분을
  먼저 걷어낸다(선례: `roles.md`→`record.md` 08-10 / §3·4·5·8 삭제 08-12 — 전부 §0-1 정본에 있던 중복).
- **섹션 번호는 재사용하지 않는다.** 외부 문서가 `§7`처럼 참조한다 — 지운 번호는 비워 둔다.
