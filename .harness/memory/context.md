# context.md — 지금 상태

> 맨 위 "다음 할 일" 한 줄만 읽고 바로 작업을 시작할 수 있어야 한다. 3KB 초과 금지 —
> 길어지면 `systems.md`(연동)나 `walkthrough.md`(이력)로 옮긴다.

---

🔒 작업 전 pending-approvals.md 확인

## ▶ 다음 할 일 — 2026-08-12 이후

**① [대표님] `00-12` 검토 + 미결 6건(§8)** — 공통기반 기획서 신설(walkthrough (17)). 03~07 전체의
전제라 이게 먼저다. 특히 **도메인 번호 재편안(§7)**·모드 분리(§6)·운영자 대행 법률검토.
`03-03` §11 미결 9건 중 개설게이트는 `00-12` §4 사다리로 해소됨(추모관=L1, PUBLIC=L2).

**② [Claude:Sonnet] Domain 03 구현 전면 보류** — 백엔드 1~7단계 완료(walkthrough (10)~(15))했으나
`00-12`의 `Deceased` 모델 도입 시 재작업 필요. **①확정 전 프론트 착수 금지.**

**② [사용자] 재확인 2건** — (1) 대표자명 수정·태그 칩 클릭. (2) 반응형/사이드바 —
`resize_window` 무동작이라 좁은 화면 검증 못 함, 창을 줄여서 확인 필요.

**③ [대표님] 인프라 전략 확정** — `00-11` 결정서 참조(권고: 관리형 클라우드 국내 리전). 확정 4건 §8.

**④ [Claude:Opus]** `01-05` §11·§6.1 진행표 동기화.

**미해결 3건(08-12 (6), 판단 필요)**: REJECTED 클레임 재승인 안 막힘 / 정지 후 기존 access token 유효 /
AdminPage에 정지 버튼 없음(API로만 가능).

**테스트 쓰레기는 일괄 정리 보류(대표 결정)** — 하나씩 지우지 말 것.

**착수 금지**: 승인 대기 5건 → `pending-approvals.md`. **시한 있는 항목 없음.**

---

## 지금 상태

```text
프로젝트  : 이어봄(Eobom) — 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼
협업 체계 : Opus(기획 docs/) → Sonnet(구현 eobom/) → Gemini(reports/·검증)
            사람이 /model·창 전환. 소유권은 roles.md §0·§1
도메인    : 01 장사시설 / 02 전문가(Stage1) / 03 백엔드7단계+기획서(프론트 보류) / 04 목업 / 05 목업
마지막    : 2026-08-12 [Opus] 00-12 공통기반 기획서 신설 (walkthrough (17)) — 커밋 전
게이트    : (1)~(7) [Gemini] ✅통과. (8)~(17) 판정 대기
재편예정  : 03→현물수거/04자산정산/05추모관/06엔딩노트/07상중행정 (00-12 §7, 미확정)
```

- **기획 SSOT** `docs/`(목차 `00_DOCS_INDEX.md`) / **보고서** `reports/` / **이력** `docs/작업일지_및_기록/`
- **소스** `eobom/frontend`(React18+Vite5+TS), `eobom/backend`(Express+Passport+Prisma+JWT)

## 지금 막고 있는 것

- **백엔드 미배포**: 소셜 로그인 실서비스 불가 + 이미지 로컬 디스크 저장(→ `systems.md` §5).
  **추모관 사진도 여기 묶임** — 스토리지 교체 전 오픈 금지(`03-02` §4.6).
- **승인 대기 5건** → `pending-approvals.md`.
- **미커밋**: `00-11` 신규, 하네스 예산정리, `03-02` 스키마 1단계(신규 마이그레이션 포함).
