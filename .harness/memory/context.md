# context.md — 지금 상태

> 맨 위 한 줄만 보고 바로 시작할 수 있어야 한다. **3KB 초과 금지**(harness-doctor가 잰다).
> 항목이 길어지면 `backlog.md`로, 끝난 항목은 `walkthrough.md`로 옮긴다.

🔒 `pending-approvals.md` 확인 · 상세 `backlog.md`

---

## ▶ 다음 할 일

**1. [사용자] 남은 확정** — 🔴 **위치정보법 신고**(`00-22` B-1) = `00-19` **유일한 게시
블로커**. · `00-14` §11-3·§11-11 · 법률자문(`00-22` A-5). 🟡 카톡 점심·공휴일 표기(비차단).
⏸ 11-1 알림톡은 발신번호 **사전등록**이 별건.
🆕 `00-27` §6 **`PRIMARY` 복수 허용** — 04 #7이 따라감(비차단).
🆕 **기술부채** `_meta/…260831.md` — C-1→`00-34` · C-3→`00-35`.
🆕 🔴 **`00-35` 2.5단계 승인**(§10.3) — `SectionTimingControl` 8곳 순회 승격(−48줄, 859→811).
**3단계 기각·4단계 불채택**(§10.2·10.4) — 이게 마지막. 소급 기록 끝나 착수 가능.

**2. [Sonnet] 🔴 다음 = 클립보드 복사 문구가 배포에서 안 뜸**(wt111, dev는 정상 — 사람이 직접
요청 예정). 🔵 **실기동 검증은 이제 사람이 한다**(09-03 지시, 크레딧 절약) — `tsc`·`build`까지
하고 "실기동 검증 대기"로 넘기고 멈춘다. **dev 서버 띄우지 않는다.**
✅ **wt108·110·111 사람 실검증 통과**(wt113 기록) — `/m/:slug` 헌화·방명록, 목록·공유·헤더 이동.
⚠️ wt108은 **배포(vercel) 기준만** — 로컬 `/m/:slug`는 404(원인 미확정, wt113 `편차`).
wt112(삭제)는 아직 실기동 대기.
Opus는 `00-06` 레지스트리만 갱신.
✅ `07-04` §5.2 완결 · wt102~104 — 체크 8번 재검증 대기.
`07-04` F(체크상태)는 `00-19` 제4조(§3.4) = **Opus 먼저**.
🔵 04 B단계는 `PreDeathPlatformSetting` 신설 이후(0-b·A = wt90·92 완료).
🆕 **`docs/트러블슈팅/`**(TS-001·002) — 증상 색인. 문제 발견 시 한 건씩. 🔴 Opus만 씀.

**3. [Gemini] 게이트 대기 12건** — wt102~113(1174ee9 소급기록 wt107 완료).
🆕 **reports 재생성**(`00-31`·`00-32`·`04-03` 포함). 🟡 옛 스펙갱신 3건 → `backlog.md` ⑭.

🟡 dev DB 삭제분·검증분 7건 — 배치정리 필요(🔴 DB 쓰기 = CONFIRM).

## 지금 상태

```text
프로젝트: 이어봄(Eobom) — 디지털엔딩&웰다잉 토탈케어 플랫폼
마지막  : 2026-09-03 — 사람 실기동 검증 결과 기록(wt113) · 삭제 기능(wt112)
```

- 소스 `eobom/frontend`(React18+Vite5+TS) · `eobom/backend`(Express+Prisma+JWT)
- 목차 `docs/00_DOCS_INDEX.md` / 저장위치 `AGENTS.md` §7
- 🔴 **20KB↑ 통독 차단**(훅, 09-02 하향) — `grep -n`→`sed -n` 또는 `limit≤400`.
  🔴 로그 3종(`walkthrough`·`claude_tasks`·`gemini_tasks`)은 **크기 무관**. `AGENTS.md` §10.

## 지금 막고 있는 것

- 🔴 **06 백엔드 — `Entry`+`Grant`까지 배선됨**(08-27). `Log`만 0건(Phase 3). 가족용 열람
  화면(프론트)은 아직 없음 — API만.
- 🔴 **이미지·음성 로컬디스크**: 재배포 시 소실 → 추모관사진 오픈금지(`systems.md` §5).
- ✅ **`/m/:slug` 목업 해소**(wt108·109) — **배포 실검증까지 통과**(wt113). 로컬만 404(미확정).
- **`Deceased` 미확정**: 04·05 묶임(→ `backlog.md` ③).
