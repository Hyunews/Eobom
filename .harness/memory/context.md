# context.md — 지금 상태

> 맨 위 "다음 할 일" 한 줄만 읽고 바로 작업을 시작할 수 있어야 한다. 3KB 초과 금지 —
> 길어지면 `systems.md`(연동)나 `walkthrough.md`(이력)로 옮긴다.

---

🔒 작업 전 pending-approvals.md 확인

## ▶ 다음 할 일 — 2026-08-18 이후

**① [Claude:Opus] `00-14` 2차 잔여 **6-2 한 건**(엔딩노트 열람범위).**
08-18에 5-4·7-4·6-3 완료(→ `00-15`·`00-16`·`00-17` 신설).
✅ **2026-08-18 사용자 착수 승인 — pending-approvals 해제됨**(단 **A/B 최종 확정은 여전히 사람 몫**).
사전조사는 `memory/project/00-14-2차-잔여-착수준비.md` §4. **문서 나가면 이 파일 삭제.**

**② [Claude:Opus] 🔴 신규 `00-14` §2.4(2-6)** — **처리방침·약관이 `Footer.tsx`에 노출 중인데
문서가 없다.** 빈칸 목록은 `00-17` §4.2. 별도 문서(`00-18` 후보) + 🔴 외부 검토.

**③ [Claude:Sonnet] 04·05 프론트 보류 유지** — 해제 조건 `Deceased` 확정.
E안으로 **07(부고장)도 같은 조건**. 셋 + ⑤ + `00-16` §6③을 한 사이클로.

**④ [Claude:Sonnet] 03 착수 0단계(별건·지금 가능)** — `Lead.facilityId`가 **필수**라 03 리드를
넣을 수 없다(`03-02` §4.1). nullable 전환 + `partnerType`별 앱 레벨 강제.

**⑤ [Claude:Sonnet] `00-14` §2-1 2단계** — `AdminTask` 모델+마이그레이션+진행상태 영속화.

**⑥ [Gemini] `reports/` HTML 3건**(`00-15`·`00-16`·`00-17`) 생성 → 인덱스 경로 교체.
**walkthrough (21)·(23) 판정 대기.**

**⑦ [사용자] 재확인** — 대표자명·태그 칩 / 반응형 / **`00-15` §6 물음 2개**(0단계는 비용 0).

**미해결 3건(08-12 (6))**: REJECTED 클레임 재승인 / 정지 후 access token 유효 / AdminPage 정지 버튼.
**테스트 쓰레기 정리 보류(사장님).** **착수 금지 5건** → `pending-approvals.md`.

---

## 지금 상태

```text
프로젝트  : 이어봄(Eobom) — 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼
협업 체계 : Opus(docs/) → Sonnet(eobom/) → Gemini(reports/·검증) · 소유권 roles.md §1
도메인    : 01 장사시설 / 02 전문가(Stage1) / 03 현물수거(기획확정·구현0) / 04·05(백엔드7단계,
            프론트 보류) / 06 엔딩노트 목업 / 07 상중행정(문서2·구현1) / 08 비즈분석
마지막    : 2026-08-18 [Opus] 00-14 2차 2마감 — 00-15·00-16·00-17 신설 (미커밋)
게이트    : (1)~(7)·(18)~(20)·(22) ✅통과. (8)~(17)·(21)·(23) 판정 대기
```

- **소스** `eobom/frontend`(React18+Vite5+TS), `eobom/backend`(Express+Passport+Prisma+JWT)
- 기획 목차는 `docs/00_DOCS_INDEX.md`. 저장 위치 규칙은 `AGENTS.md` §7.

## 지금 막고 있는 것

- **백엔드 미배포**: 소셜 로그인 실서비스 불가 + 이미지 로컬 디스크 저장(→ `systems.md` §5).
  **추모관 사진도 여기 묶임** — 스토리지 교체 전 오픈 금지(`05-01` §2.6). Render 이탈은
  국외이전 논점의 실질 해소이기도 하다(`00-17` §3.4).
- **`Deceased` 미확정**: 04·05뿐 아니라 **07까지** 묶임(`00-13` §4.5-3).
- **문서 없음**: 개인정보처리방침·이용약관 — 화면은 이미 있다고 말하는 중(②).
