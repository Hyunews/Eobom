# context.md — 지금 상태

> 맨 위 "다음 할 일" 한 줄만 읽고 바로 작업을 시작할 수 있어야 한다. 3KB 초과 금지 —
> 길어지면 `systems.md`(연동)나 `walkthrough.md`(이력)로 옮긴다.

---

🔒 작업 전 pending-approvals.md 확인

## ▶ 다음 할 일 — 2026-08-12 계획 (순서대로)

**① [사용자] 08-12 재확인 2건**
- (1) E2E 피드백 3건(대표자명 수정·전문가 사무실주소 검색모달·태그 필터 상시노출) — 주소검색모달만 대표 확인 완료, 나머지 2건 미확인.
- (2) 반응형/사이드바 재점검(사이드바 확장 시 본문 가림 버그 수정 + 한글 줄바꿈 전역 적용 + 모바일 뷰포트 대응) — **브라우저 자동화의 `resize_window`가 이 환경에서 무동작이라 좁은 화면 스크린샷 검증을 못 함**, 코드 분석 + 1920px 폭에서의 호버 동작만 확인. 대표가 실제 창을 줄여서 재확인 필요.

**②(완료) [Claude:Sonnet] 파트너 리드 조회 화면** — `01-05` §11 **4단계 완료, `[Gemini]` ✅통과 판정 + 문서
동기화까지 완료**(walkthrough 08-12 (3), `01-05` §6.1·§11 ⬜→✅ 갱신됨). 이어서 08-12 (4) 파트너 화면 UX
3건 + 08-12 (5) 대표 사진 선택 기능·데모 로그인 실제 수정까지 전부 완료, 브라우저 클릭 검증됨 — Gemini
판정 대기.
**테스트 쓰레기 정리는 보류(대표 결정, 2026-08-12)**: `EB-260812-0002`·`0004`(신청자명 "123", 목업로그인
버그 진단용) 등 데모 테스트 중 쌓이는 리드/리뷰 등은 **지금 하나씩 안 지우고, 데모 테스트를 충분히 한
뒤 한 번에 일괄 정리**하기로 함. 에이전트는 임의로 먼저 지우지 말 것 — 정리 요청이 오면 그때 진행.

**③(완료) [Claude:Sonnet] 잔여 정리 2건** — 클레임 반려(REJECTED)·사업자/전문가 정지(SUSPENDED) 경로 실제
API 검증 완료(walkthrough 08-12 (6)) / 폐기 컬럼 `vrImages`·`detailedPrices`·`priceValue` 제거(schema+마이그레이션+seed
3종+seed-data) + `00-05` 재생성 완료. **Gemini 판정 대기.** 발견한 이슈 2건(REJECTED 클레임 재승인 안 막힘,
정지 후 기존 access token 즉시 무효화 안 됨)은 안 고치고 walkthrough에 기록만 해둠 — 판단 필요.

**④ [Claude:Opus] `03` 정식 명세서** — 현물 수거업체는 기획 보류라 **제외**, 디지털 계정 삭제·추모관만.
`03-01`의 `userId` FK 원칙 반영. `01-05` §11·§6.1 진행표 동기화도 같이 처리 가능.

**⑤ [Gemini]** ③~④ 게이트 + `reports/` 재생성 (②는 08-12 (3)으로 게이트 큐에 이미 있음, ③은 08-12 (6)으로 새로 추가됨).

**착수 금지**: 승인 대기 5건 → `pending-approvals.md`. **Postgres 만료 ~09-07, 결정 서둘 것.**

---

## 지금 상태

```text
프로젝트  : 이어봄(Eobom) — 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼
협업 체계 : Opus(기획 docs/) → Sonnet(구현 eobom/·.harness/) → Gemini(reports/·검증)
            사람이 /model 과 창을 전환. 소유권 상세는 roles.md §0·§1
도메인    : 01 장사시설 / 02 전문가(Stage1 완료) / 03 디지털유품·추모관 / 04 상중행정케어 / 05 엔딩노트
마지막    : 2026-08-12 [Claude:Sonnet] 잔여 정리 2건 — 클레임 반려·정지 검증 + 폐기 컬럼 제거 (walkthrough (6)) — 커밋 전.
            (3)은 [Gemini] ✅통과 + 01-05 문서 동기화 이미 완료.
```

- **기획 SSOT** `docs/`(목차 `00_DOCS_INDEX.md`) / **보고서** `reports/` / **이력** `docs/작업일지_및_기록/`
- **소스** `eobom/frontend`(React18+Vite5+TS), `eobom/backend`(Express+Passport+Prisma+JWT)

## 지금 막고 있는 것

- **백엔드 미배포**: 소셜 로그인 실서비스 불가 + 이미지 로컬 디스크 저장(→ `systems.md` §5).
- **승인 대기 5건** → `pending-approvals.md`.
- **`[Gemini]` 게이트**: 08-10~11 19건 완료(18 ✅ / 1 🔄). 08-12 (3) ✅통과 완료. (1)·(2)·(4)·(5)·(6) 판정 대기.
- **커밋** `4c86bc6`(08-11 전체) 이후 08-12 (1)~(6) 전부 미커밋 상태(빌드·tsc 통과, 사용자 커밋 지시 대기). `eobom/backend/prisma/migrations/20260812024232_drop_facility_deprecated_columns/`도 포함.

