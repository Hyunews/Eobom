# context.md — 지금 상태

> 맨 위 "다음 할 일" 한 줄만 읽고 바로 작업을 시작할 수 있어야 한다. 3KB 초과 금지 —
> 길어지면 `systems.md`(연동)나 `walkthrough.md`(이력)로 옮긴다.

---

🔒 작업 전 pending-approvals.md 확인

## ▶ 다음 할 일 — 2026-08-12

**① [Gemini] 게이트 5건 + `reports/` 재생성** — walkthrough 08-12 (1)·(2)·(4)·(5)·(6)·(7) 판정 대기.
**`03-02` HTML 보고서는 신규 생성** 필요.

**② [사용자] 재확인 2건** — (1) 대표자명 수정·태그 칩 클릭(주소검색모달만 확인됨).
(2) 반응형/사이드바 — `resize_window`가 무동작이라 좁은 화면 검증 못 함, 실제 창을 줄여서 확인 필요.

**③ [Claude:Sonnet] `03-02` 구현 착수 가능** — 스펙만 있고 구현 0. `03-02` §9 10단계, 스키마 6개 모델부터.
Stage 1은 결제 없어 승인 불필요.

**④ [Claude:Opus] 남은 문서 작업** — `01-05` §11·§6.1 진행표 동기화(08-12 (7)에서 안 함).

**미해결 2건(08-12 (6) 발견, 판단 필요)**: REJECTED 클레임 재승인이 안 막힘 / 정지 후 기존 access token 유효.

**테스트 쓰레기 일괄 정리 보류(대표 결정)**: 데모 중 쌓이는 리드·리뷰를 하나씩 지우지 말 것.

**착수 금지**: 승인 대기 5건 → `pending-approvals.md`. **Postgres 만료 ~09-07, 결정 서둘 것.**

---

## 지금 상태

```text
프로젝트  : 이어봄(Eobom) — 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼
협업 체계 : Opus(기획 docs/) → Sonnet(구현 eobom/·.harness/) → Gemini(reports/·검증)
            사람이 /model 과 창을 전환. 소유권 상세는 roles.md §0·§1
도메인    : 01 장사시설 / 02 전문가(Stage1 완료) / 03 스펙완료·구현0 / 04 목업 / 05 목업
마지막    : 2026-08-12 [Claude:Opus] 03-02 정식 명세서 신규 (walkthrough (7)) — 커밋 전
```

- **기획 SSOT** `docs/`(목차 `00_DOCS_INDEX.md`) / **보고서** `reports/` / **이력** `docs/작업일지_및_기록/`
- **소스** `eobom/frontend`(React18+Vite5+TS), `eobom/backend`(Express+Passport+Prisma+JWT)

## 지금 막고 있는 것

- **백엔드 미배포**: 소셜 로그인 실서비스 불가 + 이미지 로컬 디스크 저장(→ `systems.md` §5).
  **추모관 사진도 여기 묶임** — 스토리지 교체 전 오픈 금지(`03-02` §4.6).
- **승인 대기 5건** → `pending-approvals.md`.
- **커밋** `4c86bc6` 이후 08-12 (1)~(7) 전부 미커밋(빌드·tsc 통과, 커밋 지시 대기).
  신규 마이그레이션 `20260812024232_drop_facility_deprecated_columns/` 포함.
