# context.md — 지금 상태

> 맨 위 "다음 할 일" 한 줄만 읽고 바로 작업을 시작할 수 있어야 한다. 3KB 초과 금지 —
> 길어지면 `systems.md`(연동)나 `walkthrough.md`(이력)로 옮긴다.

---

🔒 작업 전 pending-approvals.md 확인

## ▶ 다음 할 일 — 2026-08-12 이후

**⓪ [Gemini] `reports/` 8도메인 재편 반영** — `docs/`는 완료, `reports/`는 구 구조.
`generate_all_reports.js` 매니페스트가 **이미 신 경로**라 그대로 돌리면 된다. 매핑은 `00_DOCS_INDEX` 부록 A.
생성 후 인덱스 HTML 열 5건(`미작성`·`미갱신`) 채울 것.

**① [대표님] `00-12` 미결(§8)** — 도메인 번호 재편(§7)은 확정·실행 완료. 남은 건 모드 분리(§6)·
운영자 대행 법률검토·**§7.3 연결의 "발견" 단계(대표 상의 대기)**.

**①-2 [대표님] `00-13` 링크 모델 6건(§8)** — 권고 C안(단계 분리). **#2 확정 전 #3(조문 로그인)
판단 불가**, `05-01` 프론트 착수 보류.

**② [Claude:Sonnet] 구 Domain 03(→ 신 04·05) 구현 보류** — 백엔드 1~7단계 완료(walkthrough (10)~(15)).
`Deceased` 도입 시 재작업. **①확정 전 프론트 금지.**

**② [사용자] 재확인 2건** — 대표자명 수정·태그 칩 / 반응형(`resize_window` 무동작, 창 줄여 확인).

**③ [대표님] 인프라 전략** — `00-11` §8 확정 4건(권고: 관리형 클라우드 국내 리전).

**④ [Claude:Opus]** `01-05` §11·§6.1 진행표 동기화. **⑤** `00-14` 유스케이스 지도(①-2 확정 후).

**미해결 3건(08-12 (6))**: REJECTED 클레임 재승인 / 정지 후 access token 유효 / AdminPage 정지 버튼 없음.

**테스트 쓰레기 일괄 정리 보류(대표 결정)** — 하나씩 지우지 말 것.

**착수 금지**: 승인 대기 5건 → `pending-approvals.md`. **시한 있는 항목 없음.**

---

## 지금 상태

```text
프로젝트  : 이어봄(Eobom) — 디지털 엔딩 & 웰다잉 토탈 케어 플랫폼
협업 체계 : Opus(기획 docs/) → Sonnet(구현 eobom/) → Gemini(reports/·검증)
            사람이 /model·창 전환. 소유권은 roles.md §0·§1
도메인    : 01 장사시설 / 02 전문가(Stage1) / 03 현물수거(기획보류) / 04 정산·05 추모관(백엔드7단계,
            프론트 보류) / 06 엔딩노트 목업 / 07 상중행정 문서0건 / 08 비즈분석
마지막    : 2026-08-13 [Opus] 8도메인 재편 실행 + 00-13 링크 결정서 — 커밋 전
게이트    : (1)~(7) [Gemini] ✅통과. (8)~(17) 판정 대기
```

- **기획 SSOT** `docs/`(목차 `00_DOCS_INDEX.md`) / **보고서** `reports/` / **이력** `docs/작업일지_및_기록/`
- **소스** `eobom/frontend`(React18+Vite5+TS), `eobom/backend`(Express+Passport+Prisma+JWT)

## 지금 막고 있는 것

- **백엔드 미배포**: 소셜 로그인 실서비스 불가 + 이미지 로컬 디스크 저장(→ `systems.md` §5).
  **추모관 사진도 여기 묶임** — 스토리지 교체 전 오픈 금지(`05-01` §2.6).
- **승인 대기 5건** → `pending-approvals.md`.
- **미커밋**: `00-11` 신규, 하네스 예산정리, 구 `03-02` 스키마 1단계(신규 마이그레이션 포함).
