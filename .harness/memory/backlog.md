# backlog.md — 진행 중 작업 상세

> **언제 읽나**: `context.md`의 할 일을 집었는데 **배경·근거·미검증 항목**이 필요할 때. 부팅에선 안 읽는다.
> **완료 항목의 상세는 `walkthrough.md`다.** 여기엔 안 끝난 것만 남긴다.

---

## ⓪ 2026-08-21 개발자 웹 순회 지적 7건

1. **도메인 번호 통일** → 🔴 확정필요(범위 미정: 문서ID? 메뉴순서?). `00-12` §7=1:1대응 포기 확정. 🟡 재번호 비권고.
2. 로그인 후 개인정보란 → ✅ 구현됨(`d680681`). 🔴 필드 추가는 `00-19` 제9-2조(생년월일 미수집)와 충돌 검토.
3. **마이페이지 카운터·75%** → 🔴 하드코딩(`MyPage.tsx:76·82`). 실데이터로 바꾸거나 제거.
4. **체크리스트 접기** → `CareGuidePage.tsx` 제목+체크만, 상세는 펼침(`07-02` §3 표시원칙 유지).
5. **모바일 본문 15px** → ❌ 미착수(`/facility` 160/227곳). 원인=`rem`이 `html` 16px 기준이라
   `00-09` §2.3 **18px 토큰이 무효**. **[Sonnet] 조치 A**(`index.css`에
   `@media (max-width:768px){html{font-size:var(--base-font-size)}}` 1줄) **+ B**(`0.8rem`이하 97곳→`0.85rem`).
   ✅ 가로스크롤·오버플로·잘림은 실측 0건. 🔴 확정 7건(`00-29` §8).
6. 로그인 속도 → **Render 무료 슬립** + **oregon↔서울 왕복 ~1.3s×3회**(`authController:197·208·217`).
   → 승인 대기(인프라) 항목에 종속.
7. ✅ 닫힘(08-25) — 헤더 평면메뉴 4개→3개. 정본 `00-26` §3.3-1.

**⓪-8 ✅ 모바일 풀페이지 스냅 폐지** 구현완료(08-25) — 게이트 대기. 정본 `00-23` §5.4-1.
🔴 미검증: 실기기 주소창 접힘·섹션2→3.

**⓪-9 ✅ 박스①② 오버레이 폐지→페이지화**(`/prep`·`/bereaved`) 반영완료. 정본 `00-23` §8.6-1·2.
🔴 남은 것: **`00-06` 화면ID 신설(SCR-016·017) = [Opus]** · 게이트 실기기 미검증.

## ① 375px 헤더
✅ 비로그인 해소(85px). 🔴 **로그인 상태 미검증**(실OAuth라 자동계측 불가) —
**[사용자]가 폰으로** `MyPage`·`ObituaryPage`와 함께 봐야 닫힘(`00-29` §15).

## ② 게시 블로커
- **[사장님] 결정 2건** — ⓐ `00-20`(활성1년/동결) ⓑ 위치정보법 §2 신고·면제확인(비용0, `00-14` §2.10).
- `00-18`·`00-19`·`00-21` **묶어 1회 개정**(`00-27`·`00-28`·`06-04`·`06-05` 수집항목 반영).
- ②-0 ✅ E-4 만14세 게이트 구현완료(walkthrough 56). 다음은 E-5(a) 본문화.

## ③ 도메인 04·05 보류
해제조건 = `Deceased` 확정(`00-13` §4.5-3). ✅ 07은 `07-03` §4.1이 최소범위 선도입해 해제.
🔵 03은 진행(더미회사 목업) — 🔴 실존 상호·번호·주소 금지.
🔴 **오픈 블로커**: `/m/:slug`가 열렸는데 `MemorialPage`는 slug를 안 읽는 **목업**(아무 slug나 같은 화면).
🔵 추모관 개발과 동시진행(개발자 판단) but **실사용자 유입 전 반드시 차단**.

## ④ 가족지정·프로필 (`00-27`·`00-28`)
✅ Phase1(`d680681`)·Phase2(`fa865a4`) 완료, 게이트 통과(walkthrough 55·56).
🔴 **초대화면 브라우저 실기동 미검증**(실OAuth라 로컬 불가) — 배포본에서 링크를 열어 확인할 것.
🟡 §9.1-1(1명전용·재전파금지) 문구 미반영 — `MyPageFamilyDesignation.tsx:233`이 *"대화방에
붙여넣어"* 라 단톡방 유도. *"1:1로 전달"* 로 교정(`:218` shareText도). 🔵 카톡SDK는 안 넣음.

## ⑤ 운영자 (`00-14` §2-1 2단계)
**[Sonnet]** `AdminTask` 모델+마이그레이션+진행상태 영속화.
**[사용자] 재확인 3건** — REJECTED 재승인 / 정지 후 토큰 / AdminPage 정지.

## ⑧ 카톡 부고장 (`07-03`)
✅ Phase0·카드→랜딩 도달(원인=카카오 도메인 2군데). Phase1 §9 ①②⑤⑥ ✅ / 남은 것 ③친구탭개인·④폴백.
🔴 **[Sonnet] Phase3 #9 종료기능** — 소유권을 `localStorage`로 판정해 **남의 관리화면이 뜸**
(서버 권한은 정상 방어). 정본 §5.3-2(`isOwner`·`obituaryId` 서버 판정).
⬜ 9-1 공유집계 · 9-2 마이페이지목록. Phase2(Vercel OG) 후순위.

## ⑨ 06 엔딩노트 (`06-04`) — 🔴 백엔드 0건
✅ **Phase 0**(문구 정리, `grep` 0건) · ✅ **STT Phase 1-a**(Ⓑ직접녹음+타이핑 동등, 저장 0) — walkthrough 70.
🔴 게이트 대기 · 브라우저 실기동 미검증.
⬜ **Phase 1 = 모델 4개 + 8섹션**. 🔴 `pg_dump` 선행. 🔴 확정 7건(`06-04` §13).
⏸ Ⓐ mp3 업로드 = Phase 2 — 서버STT라 **이어봄이 위탁자**. 업체선정·계약·`00-19` 수탁자 등재 선행.
🔴 Ⓐ만의 위험: 고인 육성 업로드(§6.4-2-3) · m4a 지원 · 변환 후 즉시파기.

**⑨-0 3단계 생애주기 → `00-30`** — ✅1·3단계 구현됨 / 🔴2단계만 없고 **사망확인 하나에 전부 걸림**.
§9 선행 5칸 중 서 있는 것 0개. 충돌 3: 알림톡·정부24·ZK.

**⑨-1 🔴[사장님] 판단 12건 → `00-14` §11.** 먼저 정할 3건 =
**11-2 발신번호 실번호**(`1588-0000` 가짜, 11-1 알림톡이 종속) ·
**11-3 사망확인 처리SLA**(3단계 전체가 걸림, 11-4 2인승인과 한뿌리) ·
**11-11 06 수익지점**(비용만 나는 유일 도메인). 🔍 확인만 하면 되는 것 4건(비용0)은 §11.4.

## ⑪ 06 유족 메시지 보관함 (`06-05`) — 🆕 08-25 도메인 분리
✅ 기획 완료 — `06-04`에서 §6.1 ③ 이관, `SCR-002-B` 신설, `FarewellMessage` 모델 설계.
⬜ **Phase A(골격)** — `FarewellMessagePage.tsx` + 라우트 + 사이드바 + `EndingNotePage`의 ③ 제거 + 크로스링크.
⬜ **Phase B(저장)** — 모델 3개 + `farewellMessageController`. 🔴 `pg_dump` 선행.
🔴 **음성은 Phase D로 밀림** — 업로드 파일이 재배포 시 초기화되는 로컬 디스크에 저장되고
(`systems.md` §5), 유족 메시지는 **소실 시점에 다시 올릴 사람이 없다.** 오브젝트 스토리지 선행.
🔴 확정 5건(`06-05` §10) — **#1 메인 도메인 슬라이드 1개 vs 2개가 Phase A를 막는다.**
