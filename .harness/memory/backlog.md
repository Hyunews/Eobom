# backlog.md — 진행 중 작업 상세

> **언제 읽나**: `context.md`의 할 일을 집었는데 **배경·근거·미검증 항목**이 필요할 때. 부팅에선 안 읽는다.
> **완료 항목의 상세는 `walkthrough.md`다.** 여기엔 안 끝난 것만 남긴다.

---
## ⓪ 2026-08-21 개발자 웹 순회 지적 — 남은 것 (5·7·8·9 닫힘, wt 72·74~)

1. **도메인 번호 통일** → 🔴 확정필요(범위 미정). 🟡 재번호 비권고.
2. 로그인 후 개인정보란 → 🔴 필드 추가는 `00-19` 제9-2조와 충돌 검토.

6. 로그인 속도 → **Render 무료 슬립** + **oregon↔서울 ~1.3s×3회**(`authController:197·208·217`).
   → 승인 대기(인프라)에 종속.

🔴 **모바일 네이티브 스크롤 실기기 미검증**(주소창 접힘·섹션2→3) — ⓪-8 스냅 폐지(`00-23` §5.4-1)의 남은 확인.

## ② 게시 블로커
- **[사장님] 결정 2건** — ⓐ `00-20`(활성1년/동결) ⓑ 위치정보법 §2 신고·면제확인(비용0, `00-14` §2.10).
- `00-18`·`00-19`·`00-21` **묶어 1회 개정**(`00-27`·`00-28`·`06-04`·`06-05` + CLOVA 위탁·7일보관).
- ②-0 ✅ E-4 만14세 게이트 완료. 다음은 E-5(a) 본문화.

## ③ 도메인 04·05 보류
해제조건 = `Deceased` 확정(`00-13` §4.5-3). ✅ 07은 `07-03` §4.1이 최소범위 선도입해 해제.
🔵 03은 진행(더미회사 목업) — 🔴 실존 상호·번호·주소 금지.
✅ **오픈 블로커 해소(09-07 코드확인)** — `/m/:slug`는 이제 `MemorialLandingPage`가 받고
`useParams`로 slug를 읽어 `/api/memorials/:slug`(+guestbook·tributes·report)를 실제로 부른다.
`05-01` §6.1-1 신설 완료. **남은 것은 없다.**
🆕 **보존 잔여**(context에서 이관) — 동결·`purgeAt`·통지 2종(`00-20` §8.1-1). ⓐ 결정이 먼저.

## ⑤ 운영자 (`00-14` §2-1 2단계)
**[Sonnet]** `AdminTask` 모델+마이그레이션+영속화. **[사용자] 재확인 3건** — REJECTED 재승인 /
정지 후 토큰 / AdminPage 정지.

## ⑧ 카톡 부고장 (`07-03`)
✅ Phase0·Phase1 §9 ①②⑤⑥ · Phase3 #9 소유권(walkthrough 73, 이미 서버판정이라 코드변경 없음).
⬜ 남은 것: ③친구탭개인 · ④폴백(실기기·타인 필요) · **9-1 공유집계**(🔴 유일하게 서버작업만으로
착수 가능 — `shareCount` 컬럼은 `schema.prisma:605`에 있는데 **코드 참조 0건**, 09-07 grep 확인).
✅ 9-2 마이페이지목록 완료(`GET /api/me/obituaries` + `MyObituaryListPage.tsx`).
Phase2(Vercel OG) 후순위. 실기동 미검증.

## ⑨·⑪ 도메인 06 (엔딩노트 `06-04` · 유족 메시지 `06-05`)

🔵 경위·미결 근거는 [`backlog_06_엔딩노트.md`](backlog_06_엔딩노트.md)로 분리했다(09-17). 요지만: 🔴 ⑨ `Log` 0건·가족열람 화면 없음 · 🔴 ⑪ 음성은 Phase D(오브젝트 스토리지 선행) · 🔴 확정 대기 = `00-14` §11 12건(먼저 3건: 11-2 발신번호·11-3 사망확인 SLA·11-11 06 수익지점).

## ⑫ 인프라 리전 — 실측 근거 (08-28 이관 · 09-17 아카이브)

🔵 실측 수치·경위는 [`backlog_아카이브_2609.md`](backlog_아카이브_2609.md) ⑫로 옮겼다. 결정은 `pending-approvals.md`(인프라 전략 확정)에 있다.

## ⑬ 리프레시 토큰이 저장만 되고 안 쓰인다 (09-01 `context.md`에서 이관)

> **본체는 `00-34` §2.3이다. 여기엔 착수 조건만 둔다.**

백엔드에 `/refresh` 엔드포인트가 **3개** 있는데 프론트에서 **호출하는 곳이 0곳**이다.
지금은 액세스 토큰이 만료되면 `00-34` 2단계로 붙인 **401 → 자동 로그아웃**으로 처리된다.
즉 **깨져 있지 않고**, 사용자에게 보이는 증상은 *"가끔 다시 로그인"* 뿐이다.

🔵 **별건이며 급하지 않다.** 착수하려면 먼저 정해야 한다 — 만료 시간 · 재발급 실패 시 동작 ·
동시 요청 중복 재발급 방지. 🔴 **셋을 안 정하고 배선하면 401 → 재발급 실패 → 재시도 루프가 난다.**

## ⑭ 옛 🔄스펙갱신 3건이 반영됐는지 모른다 (09-02 발견)

wt(38)(39)(43) 2026-08-19 판정이 전부 *"재정합 필요"* 로 끝나 있다 — `00-26` §3.1·§3.2·§4.2-1·
§4.4·§7.1·§7.3 + `00-02` §3.2 + `00-23` §3. **doctor #8을 고치다 지나가다 본 것이라 확인은 안 했다.**
🔵 셋 다 `00-26`을 가리키니 **한 번 열어보면 같이 판가름 난다.** 반영돼 있으면 이 항목을 지운다.

🔴 **새어 있었다면 구조 문제다.** `done.md` §4는 스펙갱신이 나오면 *"Opus가 `docs/` 수정"* 이라고만
정하고 **반영했는지 되짚는 자리가 없다.** 확인 결과를 보고 재제출 표시를 강제할지 같이 정할 것.

## ⑯ 브라우저 실기동 검증 대기 목록 (09-07 정리)

사람이 실기기로 확인해야 하는 것들(09-03 방침). 코드·`tsc`·`build`는 전부 통과 상태.
🔵 360px 계측으로 걸러지는 항목은 `00-38` §11.1이 대신 잡는다 — 여기는 **계측으로 안 되는 것**만.

- **`--confirm` 플래그**(`06-05` 파기 스크립트) · **운영 버킷 2단계**(R2 dev/운영 분리) — wt121~127, dev 환경 한계로 미확인.
- **공유 집계**(`shareCount` +1) — wt140. 카톡/Web Share/링크복사 **세 경로 각각** 실제로 오르는지.
- **LAN IP 공유 링크**(`captureFrontendOrigin`) — wt141. `192.168.0.111:5173`로 접속해 만든 부고장 링크가 그 주소로 나오는지.
- **전체반출 버튼 배치**(`06-05`) — wt130. 수신자 2명 이상이면 "전체 반출(zip)"이 카드 수만큼 반복 노출(동작은 동일, 시각적 중복). 의도한 배치가 맞는지 **recipient 복수 계정**으로.

판정 나면 `walkthrough.md`의 해당 wt 항목에 결과를 追記하고 이 줄을 지운다.
## ⑱ 🔴 CRLF 줄바꿈 오염 — 사람 판단 필요 (09-17 발견 · 09-17 Opus 재측정)

🔵 측정 상세는 [`backlog_아카이브_2609.md`](backlog_아카이브_2609.md) ⑱. 여기는 결론과 요청만.

**사실**(`git cat-file`로 HEAD 블롭 직접 측정):
- 🔴 **추적 파일 285개 중 177개(62%)가 CRLF 블롭**이다. `.gitattributes`는 **없다**.
- 🔴 **09-16·09-17 커밋이 만든 문제가 아니다** — `index.css`는 `b6e2f70`(09-11)부터, `README.md`는
  `ba93691`부터 이미 CRLF였다. 그 커밋들은 **원래 CRLF이던 파일을 그대로 유지**했을 뿐이다.
- 🔴 `core.autocrlf`가 **세션마다 뒤집혔다** — 09-16에 repo-local `true`(WSL 88건→28건 해소),
  09-17에 누군가 `false`로 되돌림. **`false`가 오염을 부르는 방향**이다(작업 트리 CRLF가 그대로 커밋).
- ✅ **런타임 영향 없음** — 깨지는 것은 `git diff`/`blame` 가독성뿐.

**사람이 골라야 할 것 — 순서가 있다**:
1. 🔴 **먼저 `.gitattributes`**(없으면 또 뒤집힌다): `* text=auto eol=lf` + `*.ps1 text eol=crlf`. 🟡 `.sh`는 반드시 LF.
2. 그다음 **전면 재정규화 1회**(`git add --renormalize .` — 177개가 바뀌는 **대형 커밋**, 순수 줄바꿈임을 메시지에 명시) **또는 현상 유지**. 🔵 *"5개 파일만"* 은 부분 처방이라 권하지
   않는다 — 62%가 CRLF인데 5개만 LF로 바꾸면 혼재가 심해진다.

## ⑲ 마이페이지 후속 9건 — Opus 기획 필요 (09-21 사용자 지시 · Sonnet 사실조사)

**구현 끝(Sonnet)**: ① 카톡 부고 알리기 버튼 아이콘 = `MessageCircle`(푸터 `Footer.tsx`·`FooterMobile.tsx`와 같은 말풍선) ② mypage 시안 아이콘 — 상담=편지비행기(`Send`)·문의=말풍선(`MessageCircle`), 캔버스 `https://claude.ai/artifact/FmacNUuzKECyG5bQfzxx8q` v3 ⑦ "디지털 자산 정리" → "디지털 정산"(`MyPage.tsx` 라벨·시안). 🟡 `MyPage.tsx`의 통계·행 아이콘은 **시안 확정 뒤 구현**하며 지금은 그대로(상담=`MessageCircle`·문의=`Send`).

**스펙·결정이 필요한 6건** — 코드 사실은 아래, 결정은 Opus:

| # | 요청 | 코드 사실 (Sonnet 조사) | 결정할 것 |
| :-: | :--- | :--- | :--- |
| 3 | 카톡 문의를 어떻게 세고 어디에 연결하나 | 카톡 채널은 외부 링크뿐(`pf.kakao.com/_LVxdxaX/chat`, `Footer.tsx:157`·`FooterMobile.tsx:32`) — **우리 서버는 누가 채널에 말 걸었는지 알 수 없다.** `GET /api/me/summary`의 `leadCount`는 **업체 상담(Lead)**이고 09-10 사람 정정으로 화면은 `상담 = consultCount + leadCount`, `문의 = 0 고정`(자리만). `00-36` §6 #1·M-2는 **문의 = Lead**로 정의 → 사용자 정의(**문의 = 카톡**)와 **충돌** | ⓐ 문의를 무엇으로 셀지(카톡 상담톡 API·웹훅이 가능한지 — 불가하면 숫자 없이 "카톡 채널 열기" 행으로 강등) ⓑ 통계 칸 유지 여부 ⓒ `00-36` M-2의 문의 정의 정정 |
| 4 | 상담 내역 페이지·저장사항 | 사용자용 목록 API **0건** — `GET /api/me/leads` 미구현(`00-36` M-2 #6·#7). `ConsultRequest`도 사용자 조회 라우트 없음(`expertRoutes` `/consult-requests`=전문가, `adminRoutes`=운영자, `expertPublicRoutes`=제출 POST). mypage "상담 신청 내역" 행은 내역이 아니라 `counseling`(신청 화면)으로 간다 | 목록 필드·상태 표기·`userId` 일치 조건(`Lead`는 비회원 문의도 담는 테이블) — 이후 Sonnet이 API 2개 + 화면 구현 |
| 5 | 회원 탈퇴(최하단) | `00-36` §3.3·§4.3·M-3 + `06-05` §5.4-2(30일 유예)로 **스펙은 확정**, 구현 **0건**(`authRoutes.ts`엔 `/unlink-provider`뿐). 🔴 M-3은 **스키마 변경 + 파기 배치 + `db-safety.md` 백업·CONFIRM 선행** | M-3 착수 승인(`pending-approvals` 대상인지) · 화면 위치는 사용자 지시대로 C구역 맨 아래 |
| 6 | 개인정보 수집 동의 — 역할별 on/off | 마케팅 동의는 `User.marketingAgreedAt`(`schema.prisma:46`) + "내 정보" 모달(`MyPageProfile.tsx:293`)에 **이미 있다.** 동의 항목은 코드상 **marketing 하나뿐**(가입 필수 2건은 끌 수 없음=탈퇴). Opus 주의사항: 같은 값을 두 곳에서 고치지 않게 새 화면이면 기존 체크박스를 **이동** | "역할별"이 뜻하는 항목(USER·전문가·사업자별 동의가 실제로 무엇인지) · 화면 위치(내 정보 모달 vs 별도 구간) |
| 8 | 나에게 공유된 엔딩노트 항목 | 백엔드 `GET /api/ending-note/family-view`(`endingNoteRoutes.ts:26`)가 **이미 있다**(ACCEPTED 지정 + IMMEDIATE 부여 섹션만). 프론트는 호출하는 곳 **0건** | 화면 위치("내가 받은 것" 구간 신설?)·지정자 표기·열람 화면 |
| 9 | 내가 수락한 가족 지정(받는 입장) | 🔴 `familyDesignationController.ts` **불변식 3**("내가 지정됐는지 조회하는 API는 없다")과 충돌. `endingNoteController.ts:303`이 이미 `acceptedUserId` 역질의로 **편차를 냈고** Opus 확인 요청이 walkthrough 편차에 있다(판정 미기록) | 수락 후 노출 범위(지정자 성함·관계·scope만, 연락처 금지) · 수락 철회 허용 여부 · 불변식 3 문구 정정 |

✅ **2026-09-21 Opus 결정 완료 — 6건 전부 문서에 등재됨.** 여기는 요약만, 본문은 등재처를 본다.

| # | 결정 | 등재처 |
| :-: | :--- | :--- |
| 3 | 문의 = **카카오톡 채널**(집계 불가·외부 링크). 통계 **2칸**으로 축소 · *"카카오톡으로 문의하기"* 링크 행 · `Lead`는 **상담**에 속함 | `00-36` §4.1·§6 #5·#6 |
| 4 | `Lead` + `ConsultRequest`를 **한 목록**(`SCR-019`) · 상태 **4단계로 접기** · 정산 필드 화이트리스트 | `00-36` §4.4 |
| 5 | **착수 승인**(`pending-approvals` 비대상). 🔴 migrate **직전** 백업+CONFIRM은 별도 | `00-36` M-3 |
| 6 | "역할별" → **성격별 3층**(계정필수·선택·건별). 마케팅 체크박스는 **이동**(복제 금지) | `00-36` §4.5 |
| 8 | C구역 **"나에게 공유된 것"** 신설 + `SCR-020`. API(`family-view`)는 이미 있음 | `00-36` §4.6 |
| 9 | 불변식 3은 **수락 전까지** — 3조건 하 허용(편차 ✅통과). 수락 철회는 **`DECLINED` 재사용**(스키마 변경 0) | `00-27` §3-1·§9.2 |

🔴 **2026-09-21 M-2 이후 Opus 추가 판정 3건** — M-3 전에 처리한다.

| 무엇 | 결정 | 등재처 |
| :--- | :--- | :--- |
| `CALL`이 *"접수됨"* 으로 뜬다 | 🔴 **목록에서 제외.** ①`createCallEvent`가 `userId`를 붙이지 않는다(문서는 원래 *익명*) ②`GET /api/me/leads`에 `type:{not:'CALL'}` ③기존 행은 **지우지 않는다**(DB 쓰기 회피) | `00-36` §4.4-1 · `01-05` §4.1 |
| 카톡 상담도 남길까 | 🟡 **지금은 안 남긴다** — 우리가 아는 건 *클릭*뿐이라 `CALL`과 같은 문제다. `Lead`(청구 증거)에 얹지 않는다. 제대로 세려면 §6 #6(상담톡 API) | `00-36` §4.4-1 |
| 방명록 API만 있고 화면 없음 | 🔴 **`SCR-021` 화면 + B구역 행을 만든다**(사람 지시). 삭제는 **소프트 삭제 `deletedByAuthorAt`** 신설이라 **M-3 마이그레이션과 함께** — 1차는 읽기 전용 | `00-36` §4.7·§4.7-1 · `00-06` |
| `SCR-019` 구현자 재량 3건 | ✅ **추인**(필드명 `no`·100건 상한·페이지네이션 없음). 🔴 **단 100건 초과 시 잘렸다는 한 줄**을 목록 끝에 단다 | `00-36` §6 #9 |

🔵 **시안(캔버스 v4) 요청** — 구조 정본은 **`00-36` §4.1 구조도**다. 거기 4구역 그림대로:
①통계 3칸 → **2칸** ②B구역에 *"내가 남긴 방명록"* ③**C구역 "나에게 공유된 것"** 신설
④D구역에 *"카카오톡으로 문의하기"*·*"개인정보·동의"*·**회원 탈퇴**·**로그아웃**
⑤*"디지털 자산 정리"* → *"디지털 정산"*. 🔴 아이콘은 **상담=`Send`·문의=`MessageCircle`**.

🔄 **09-21 진행** — Opus가 6건을 `00-36` §4.4~§4.6·§5에 결정 등재했다. **M-1.5(프론트만: 통계 2칸·카톡 문의 행·나에게 공유된 것 SCR-020·로그아웃 행·아이콘 정정)는 구현 완료**(walkthrough 09-21 마지막 항목). 남은 것: M-2(`/api/me/leads`·`/api/me/consult-requests`·방명록)·M-3(회원 탈퇴, 🔴 스키마+백업+CONFIRM)·§9.2(수락 철회 API).
