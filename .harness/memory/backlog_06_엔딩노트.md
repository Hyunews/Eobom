# backlog — 도메인 06 (엔딩노트 · 유족 메시지 보관함)

> 🔵 acklog.md에서 **도메인 06 근거**만 떼어낸 파일이다(2026-09-17 분리 — doctor가 세 번째로
> 분리를 요구했고, 06은 Phase 1이 남아 가장 오래 끌 항목이라 주제 분리 대상으로 골랐다).
> 정본 스펙은 `docs/06_엔딩노트_유언/`(`06-04`·`06-05`)이고 여기는 **경위·미결 근거**만 둔다.
> 상한 없음(AGENTS.md §9 — 배수구는 상한 없는 곳).

---

## ⑨ 06 엔딩노트 (`06-04`) — `Entry`·`Grant` 배선됨(08-27) · 🔴 `Log` 0건·가족열람 화면 없음
✅ Phase 0 · STT Phase 1-a(walkthrough 70). ⬜ **Phase 1 = 모델 4개 + 8섹션**. 🔴 `pg_dump` 선행 · 확정 7건(§13).
🔵 **08-26 — STT는 `06-05` 보관함으로 이관**(§6.4-2). 초판 *"⑨ 유족 미전달"* 은 **마음+재산이 한 녹음에 섞인다**는 현실과 안 맞았다 → **음성=보관함 / ⑨=타이핑 필사도구**.

### ⑨-A STT(CLOVA Speech) ✅ 완료 — 🔴 **정본 = `06-04` §6.4-11**(배선·실측 상세는 거기)
🔵 **위수탁계약 안 맺음** — 약관 7조② 택일에서 **제3자 제공 동의** 선택 → `00-19`에 「제3자 제공」으로 기재(제공받는 자·목적·항목(음성)·7일).
✅ **플래그 해제 조건 = ①동의 체크박스 동작 확인 ②`00-19` 반영. 둘뿐.** ⏸ 한국어 정확도 미검증(Free 월 15분). ⚠️ 로컬은 `CLOVA_STT_ENABLED=true`(플래그 목적 = **배포 차단**).

### ⑨-D 화면 구성 — 🔵 **탭 아니라 아코디언**(08-27) · 정본 `06-04` §6.1-1
탭이 안 되는 이유 3개(안 열어본 탭 = *"빠짐없이"* 와 반대 · 6070이 숨은 내용을 인지해야 함 · 모바일 가로스크롤)는 §6.1-1에 있다.
🔴 **⑨ 유언장 초안은 아코디언에 넣지 않음**(폼이 아니라 글쓰기·인쇄). ⏸ **완료 표시는 Phase 1 이후** — 저장이 없는데 뱃지를 달면 *"저장됐다"* 오해(§2.2).

**⑨-0 3단계 생애주기 → `00-30`** — ✅1·3단계 구현 / 🔴2단계만 없고 **사망확인 하나에 전부 걸림**(§9 선행 5칸 중 0개 · 충돌 3: 알림톡·정부24·ZK).

**⑨-1 🔴[사장님] 판단 12건 → `00-14` §11.** 먼저 정할 3건 =
**11-2 발신번호 실번호**(`1588-0000` 가짜, 11-1 알림톡이 종속) ·
**11-3 사망확인 처리SLA**(3단계 전체가 걸림, 11-4 2인승인과 한뿌리) ·
**11-11 06 수익지점**(비용만 나는 유일 도메인). 🔍 확인만 하면 되는 것 4건(비용0)은 §11.4.

---

## ⑪ 06 유족 메시지 보관함 (`06-05`) — 🆕 08-25 도메인 분리
✅ 기획 완료 — `06-04` §6.1 ③ 이관, `SCR-002-B` 신설.
✅ **Phase A·B 완료(09-17 확인)** — `FarewellMessage` 모델·`farewellMessageController`·라우트·페이지 전부 실존, wt174 모바일 적응형까지.
🔴 **음성은 Phase D로 밀림** — 로컬 디스크 저장이라 재배포 시 소실(`systems.md` §5). 유족 메시지는 **소실 시점에 다시 올릴 사람이 없다.** 오브젝트 스토리지 선행.
🔴 확정 5건(`06-05` §10) — **#1 메인 도메인 슬라이드 1개 vs 2개가 Phase A를 막는다.**

🆕 **D-5 반출(09-07, wt128) 후속** — §5.4-4(사망 시 반출)는 아직 실동작 없음. `RELEASED` 상태
전이도, 유족(가족)이 로그인해 들어오는 인증 경로도 코드에 전무하다(`FamilyDesignation`은 본인이
만드는 레코드일 뿐 가족용 세션이 아님) — Phase C(개봉, §3.3)에서 그 인증을 먼저 만들고
`farewellMessageExport.ts`의 `streamFarewellMessageExportZip`을 note.status==='RELEASED' 가드와
함께 재사용한다. 🟡 후속 워커필터(§5.4-5-2-1-1)도 미착수.